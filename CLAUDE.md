# Навигатор ПП РФ №719 — Проектная база для Claude Code

## Контекст проекта
Сервис для Курской торгово-промышленной палаты (ТПП).
Помогает предприятиям разобраться в требованиях **Постановления Правительства РФ №719**
о подтверждении производства промышленной продукции на территории РФ (критерии локализации,
балльная система, документальное подтверждение).

**Заказчик**: Курская ТПП  
**Цель**: снизить нагрузку на экспертов ТПП, ускорить прохождение процедуры для предприятий  
**Ограничение**: ИИ — черновик, финальный вердикт всегда за экспертом ТПП

---

## Стек (только RF-доступные инструменты)

| Слой | Технология | Причина выбора |
|---|---|---|
| LLM | DeepSeek V3 (`deepseek-chat`) | API доступен из РФ, дёшево, качественно |
| Embeddings | `sentence-transformers` (локально) | Без внешних API, бесплатно |
| Vector DB | Qdrant (self-hosted Docker) | Open source, РФ-доступен, production-ready |
| RAG | **рукописный** (прямо на `qdrant_client` + `sentence-transformers`) | Полный контроль, минимум зависимостей при рваной сети; LlamaIndex НЕ используется (кандидат на стадию 4 — parent/child, см. ниже) |
| Backend | FastAPI (Python 3.11+) | Стандарт, async, хорошая документация |
| База данных | SQLite (dev) → PostgreSQL (prod) | Простой старт, масштабируется |
| Frontend 1.0 | Лёгкий веб-фронт (HTML/JS) на FastAPI | Мультиюзер-чат + роли `user`/`expert`/`admin`, без React-сборки |
| Frontend (ранний MVP) | Streamlit (форма) | Навигатор-MVP; в 1.0 вытесняется чатом |
| Frontend Prod | React + Vite (2.0+) | Только если подтвердит ценность |
| Хостинг | Selectel / Timeweb / Yandex Cloud | РФ-юрисдикция, ФЗ-152 |
| Telegram UI | aiogram 3 (опционально) | Быстрый доступ для экспертов ТПП |

> **RAG реализован вручную, без фреймворка.** Гибрид-поиск собран напрямую: dense — `e5-large`
> (`sentence-transformers`, локально), sparse — **локальный BM25** (`app/rag/sparse.py`, mmh3 +
> Snowball, без скачивания моделей), слияние — нативный `FusionQuery(RRF)` Qdrant + доменный
> ОКПД2-буст. Это осознанный выбор под условия проекта (RF-first, рваная сеть DPI/zapret, контроль
> над анти-галлюцинациями), а не недоработка. **LlamaIndex не подключён**; его рассматриваем точечно
> только на стадии 4 (parent/child auto-merging), если упрёмся в усечение операций. Полный разбор
> архитектуры и план улучшений — [docs/RAG_EXPLAINED.md](docs/RAG_EXPLAINED.md) и раздел RAG в
> [docs/IDEAS.md](docs/IDEAS.md).

### Запуск Python
```bash
py -3 -m venv .venv
# Windows:
.venv\Scripts\pip install -r requirements.txt
.venv\Scripts\python main.py
# Linux/Mac:
source .venv/bin/activate && pip install -r requirements.txt && python main.py
```

---

## Секреты (никогда не коммитить)
Файл `.env` в `.gitignore`. Копировать из `.env.example`.
- `DEEPSEEK_API_KEY` — от platform.deepseek.com
- `DATABASE_URL` — строка подключения к БД
- `QDRANT_URL` — адрес Qdrant (локально: http://localhost:6333)
- `SESSION_SECRET` — подпись сессионных кук; в проде приложение НЕ стартует с дефолтным

> Полный список настроек с пояснениями — в `.env.example`; он сверен с `app/core/config.Settings`
> (расхождение ловит тест `TestEnvExampleMatchesSettings`). Telegram-интерфейс из планов не ушёл,
> но кода под него нет — переменной в конфиге тоже.

> **Модель эмбеддингов (provisioning).** По умолчанию e5-large качается с HuggingFace при
> первом `load_kb`. На нестабильной сети / из РФ CDN рвётся — решения: `hf_xet` (уже в
> requirements, чанковая докачка), зеркало `HF_ENDPOINT=https://hf-mirror.com`, либо ручная
> папка `models/` + абсолютный путь в `EMBEDDING_MODEL`. Подробно — `SETUP.md` (Шаг 5),
> бэклог по теме — `docs/IDEAS.md` (раздел Инфра). Сама модель и `models/` — в `.gitignore`.

## Персональные данные (152-ФЗ) — никогда в git

Выгрузки с боевого сервера (`navigator-export/`, дампы `*.db`, `dump_tables.json`, таблицы выданных
доступов) содержат **ПДн реальных пользователей региональных ТПП**: диалоги, ФИО, регион, Telegram.
Они в `.gitignore` — и это не «на всякий случай»: git-история необратима, приватность репозитория
её не отменяет.

**Правило:** выгрузки хранить **вне рабочего дерева репозитория** (напр. `D:\navigator-export\`),
в репо — только агрегаты и обезличенный анализ (как `docs/REPORT_JULY_2026.md`). Перед любым
`git add -A` проверять `git status`. История проверена 2026-08-10 — вхождений нет.

---

## Структура репозитория

```
719-post/
├── CLAUDE.md              ← ты здесь (главный контекст проекта)
├── ROADMAP.md             ← фазы разработки и статус
├── .env.example           ← шаблон переменных окружения
├── .gitignore
├── .github/workflows/     ← CI: юнит-тесты на push и PR
├── requirements.txt       ← зависимости сервиса (рантайм)
├── requirements-dev.txt   ← только для разработчика (streamlit-MVP); тестам не нужен
├── main.py                ← точка запуска FastAPI (веб-чат 1.0)
│
├── app/                   ← дерево ниже сверено с файлами 2026-08-10
│   ├── core/
│   │   ├── config.py      ← настройки из .env (pydantic-settings)
│   │   ├── prompts.py     ← ВСЕ промпты: парсер базы, навигатор, процедурный + дисклеймер
│   │   ├── costs.py       ← оценка стоимости DeepSeek в ₽ (для админки)
│   │   └── regions.py     ← справочник регионов + префиксы логинов
│   ├── rag/
│   │   ├── retriever.py   ← гибрид-поиск (dense+sparse RRF) + ОКПД2-буст; search_cases/search_rules
│   │   ├── pipeline.py    ← ядро: запрос → гейты → контекст → DeepSeek → ответ (+стриминг)
│   │   ├── embeddings.py  ← обёртка над e5-large (локально)
│   │   ├── sparse.py      ← локальный BM25 (mmh3 + Snowball), без скачивания моделей
│   │   ├── reranker.py    ← LLM-реранкер top-k (только при отсутствии совпадения по коду)
│   │   ├── meta.py        ← детерминированные ответы: приветствие/что умеешь/спасибо
│   │   ├── procedural.py  ← детектор процедурного интента → маршрут на корпус Правил
│   │   ├── translate.py   ← детерминированный перевод ТН ВЭД↔ОКПД2 по прямому запросу
│   │   ├── okpd2_ref.py   ← справочник ОКПД2 + переходные ключи (лукапы, без сети)
│   │   ├── documents_ref.py ← K15: ЗАКРЫТЫЙ перечень подтверждающих документов + гард
│   │   ├── thresholds.py  ← пороги баллов из примечаний приложения
│   │   ├── inheritance.py ← требования группы для позиций с пустой ячейкой (R6)
│   │   └── fragments.py   ← пометка позиций с расколотой общей ячейкой (R29)
│   ├── tools/
│   │   └── navigator.py   ← извлечение ОКПД2 + вызов пайплайна + базовый чек-лист
│   │                        (validator.py / act_gen.py — ПЛАН 2.0/3.0, файлов нет)
│   ├── api/
│   │   ├── routes.py      ← POST /navigate
│   │   ├── chat.py        ← /api/chat, /api/chat/stream (SSE), беседы, экспорт
│   │   ├── web.py         ← страницы (login/chat/profile/admin) + /api/feedback
│   │   ├── auth.py        ← bcrypt, сессии, RBAC, «запомнить меня», гейт профиля
│   │   ├── admin_stats.py ← сборка данных админ-панели (скоркард, регионы, триаж)
│   │   └── schemas.py     ← Pydantic-схемы /navigate
│   ├── db/
│   │   ├── models.py      ← SQLAlchemy: users / messages / feedback
│   │   ├── engine.py      ← синхронный SQLite + лёгкие миграции колонок
│   │   └── queries.py     ← CRUD
│   └── web/               ← Jinja2-шаблоны + статика (chat.js, style.css)
│
├── knowledge_base/
│   ├── manifest.yaml      ← K8: СОСТАВ КОРПУСА (паспорт документа: сила, статус, срок,
│   │                        редакция). Загрузчик читает его, а не хардкод путей
│   ├── pp719/             ← PDF и TXT файлы ПП №719 + приложения
│   ├── cases/             ← верифицированные кейсы от эксперта ТПП
│   └── templates/         ← шаблоны актов экспертизы ТПП
│
├── frontend/              ← Streamlit UI (MVP)
│   └── app.py
│
├── scripts/
│   ├── load_kb.py         ← скрипт: загрузить knowledge_base в Qdrant
│   ├── seed_cases.py      ← скрипт: загрузить кейсы экспертов
│   ├── convert_metodrek.py ← PDF Методрекомендаций ТПП → текст корпуса (K15)
│   ├── eval_*.py          ← замеры по EVAL_GUIDE (в т.ч. eval_cases_influence — влияние петли кейсов)
│   └── deploy/            ← ЭТАЛОННАЯ ВЫКАТКА: deploy.sh + releases/<тег>.env + checks/common
│
├── tests/
│   ├── test_rag.py
│   └── test_tools.py
│
└── docs/                  ← сверено с файлами 2026-08-12
    ├── BACKLOG.md         ← ЗАДАЧНИК: все открытые задачи в порядке исполнения, с фактурой
    │                        (дублируется issues + доска github.com/users/pan3v1n/projects/1)
    ├── PRODUCT_SPEC.md    ← ПОЛНАЯ СПЕКА РАБОТАЮЩЕЙ ВЕРСИИ (v0.5.0-test8 на VM): принципы,
    │                        ЦА, архитектура, данные, маршрут запроса, анти-галлюцинационный
    │                        контур, метрики, эксплуатация. «Как есть», не «как задумано»
    ├── CONCEPT.md         ← КОНЦЕПЦИЯ + функциональные требования по версиям (FR-0.5…FR-4.0)
    ├── AGENTS_PLAN.md     ← ИИ-агенты в RAG: агенты СБОРКИ данных (A1–A6) — да,
    │                        запросный агент — только на ветке СТ-1 (B1); чего не делать
    ├── REVIEW_TZ.md       ← ГЛАВНЫЙ рабочий план: задачи R1–R30 + чек-лист редеплоя §1а
    ├── KB_EXPANSION_TZ.md ← ТЗ расширения базы знаний: K1–K17 (+ architecture.html — схема)
    ├── WAVE3_PROTOCOL.md  ← ПРОТОКОЛ третьей волны: окно дат замера, бриф экспертам,
    │                        списки позиций на сверку
    ├── EXPERT_LETTER_CHECKLIST.md ← ГОТОВОЕ ПИСЬМО эксперту + что приложено (лист сверки
    │                        блоки А–Д и приложение Д). Владельцу — вписать контакт и срок
    ├── NEXT_SESSION.md    ← оперативный «где остановились и что дальше»
    ├── POST_TEST1_TZ.md   ← задачи T1–T18 по фидбеку теста №1
    ├── REPORT_JULY_2026.md← отчёт заказчику: результаты теста 17 региональных ТПП
    ├── REPORT_AUGUST_2026.md ← отчёт заказчику за август 2026: хронология, корпуса, качество,
    │                        выкатки до `test22`, инцидент 28.08
    ├── REVIEW_2026-08-10.md ← аудит-снимок на дату (НЕ редактируется)
    ├── RAG_EXPLAINED.md   ← разбор архитектуры RAG + границы faithfulness-гарда
    ├── LAUNCH.md · DEPLOY.md · BRANCHING.md · BUDGET.md · SETUP-соседи
    ├── HARDWARE_ONPREM.md ← сборка сервера из железа кладовой ТПП (спеки, отбор, приёмка)
    ├── TESTING.md         ← протокол приёмки с экспертом ТПП
    ├── USER_GUIDE.md      ← инструкция для пользователя/эксперта ТПП
    ├── IDEAS.md           ← бэклог идей/гипотез по доработкам
    ├── eval_baseline/     ← БАЗА СРАВНЕНИЯ: правило обновления (только релизный тег) + baseline.json
    ├── eval_runs/         ← отчёты прогонов по датам
    ├── eval_*.md          ← отчёты замеров (retrieval, answers, coverage, determinism …)
    └── test_cases.md/.csv ← готовые приёмочные кейсы с реальным выводом

(SETUP.md и WORK_ON_OTHER_PC.md — в корне репозитория.)
```

---

## Граф знаний проекта (graphify)

**При любом вопросе по архитектуре, коду или связям внутри проекта — сначала обращайся к графу,
а не грепай файлы вслепую.** Граф построен инструментом [graphify](https://github.com/safishamsi/graphify)
и лежит в `graphify-out/`.

Что внутри графа (полная пересборка **2026-08-10**): **1586 узлов, 3311 рёбер, 108 сообществ**.
- **код** — tree-sitter AST по 115 файлам (`.py` + `chat.js`): 794 узла;
- **документация проекта** — 47 файлов (`docs/`, `README`, `ROADMAP`, `CLAUDE.md`, `V1_SPEC` и др.),
  извлечение LLM: 532 узла + 260 узлов-обоснований;
- **мосты doc→code — 625 рёбер (19 % всех связей)**: концепт из документа → конкретная функция или
  класс. Ради них граф и строится: «где реализовано то, что описано в ТЗ».

⚠️ **`knowledge_base/` из графа исключён намеренно** (40 файлов, 10.9 МБ). Текста самого ПП №719 в
графе нет — он живёт в Qdrant для RAG-поиска; граф про устройство *системы*, а не регламента.
Индексация корпуса залила бы граф сущностями постановления и стоила бы в ~15 раз дороже.

Как пользоваться (в порядке предпочтения):
1. Прочитать `graphify-out/GRAPH_REPORT.md` — god-узлы (ядро архитектуры), сообщества,
   surprising connections, готовые вопросы.
2. `graphify query "<вопрос>"` — обход графа по вопросу (нужен установленный пакет `graphifyy`).
3. Открыть `graphify-out/graph.html` в браузере — визуально (мосты doc↔code видно глазами).

⚠️ **Граф локальный — в репозитории его НЕТ** (`graphify-out/` в `.gitignore`). На свежем клоне
его нужно пересобрать:
```bash
pip install graphifyy        # один раз, в Python с которым работает агент
# затем в Claude Code:
/graphify .
```
На **Windows** перед ручными вызовами graphify ставь `PYTHONUTF8=1 PYTHONIOENCODING=utf-8`
(в корпусе кириллица — иначе `UnicodeDecodeError`) и используй реальный Python, а не Store-заглушку `python3`.

### ⚠️ Windows-ловушка `.graphify_root` — из-за неё автообновление молчало 8 недель

Хук читает корень проекта из `graphify-out/.graphify_root`. Скилл записывает туда результат
`cd <path> && pwd`, а в Git Bash это **POSIX-путь** `/d/navigator-719`. Windows-Python понимает его
как `D:\d\navigator-719` — несуществующую папку (хук её же и создавал пустой). Итог: на каждом
коммите в лог падало «No code files found - nothing to rebuild», и граф не обновлялся с 17.06,
хотя `CLAUDE.md` обещал автообновление.

**Правильное содержимое файла — Windows-путь с прямыми слэшами:**
```
D:/navigator-719
```
Проверить, что автообновление живо:
```bash
cat graphify-out/.graphify_root                    # должен быть D:/navigator-719
tail -3 ~/.cache/graphify-rebuild.log              # после коммита — "Rebuilt: N nodes"
```
Если после `/graphify .` файл снова стал POSIX-путём — перезаписать вручную, скилл делает это
каждый раз.

### Как пересобирать полностью (со слоем документации)

Кодовый слой пересобирается сам на каждом коммите (post-commit hook), LLM не нужен.
Слой документации требует извлечения субагентами — прогон 2026-08-10 занял 8 параллельных
агентов на 47 файлов (754 КБ). Ключевые приёмы, без которых получается мусор:
1. **Исключить `knowledge_base/`** — иначе в граф уедет текст ПП №719 (10.9 МБ).
2. **Дать агентам индекс кодовых узлов** — выгрузить AST-узлы в `.graphify_code_index.tsv`
   (`id<TAB>label<TAB>файл`) и велеть искать в нём по grep. Без этого мосты doc→code
   получаются висячими: агент выдумывает идентификаторы, которых в графе нет.
   Результат прогона: 625 мостов, **0 висячих рёбер**.
3. **Делить порции по объёму, а не по числу файлов** — размеры отличаются в сто раз.

Результаты извлечения кэшируются (`graphify-out/cache/`, 47 файлов) — следующая пересборка
документации будет бесплатной, пока файлы не изменились.

---

## Ключевые принципы разработки

1. **AI — черновик, эксперт — вердикт.** Ответ системы — предварительный анализ, требующий
   проверки уполномоченным экспертом ТПП. **Где именно стоит пометка** (уточнено 2026-08-10,
   задача R3): в интерфейсе — постоянной строкой под полем ввода (`chat.html`), а не в теле
   каждого ответа (это осознанно убрано, правило 6 промпта навигатора: внутренний инструмент,
   повтор дисклеймера в каждом сообщении — шум); в **любой выгрузке** (`/api/export`,
   `/api/conversations/{id}/export`, `/api/admin/export`) — шапкой документа и подписью каждой
   реплики ассистента. Экспорт маркируется обязательно: именно он уносит ответ за пределы
   сервиса — заявителю, в переписку, в приложение к заявке.

2. **Петля обучения.** Ошибки, которые находит эксперт, фиксируются в `knowledge_base/cases/`
   и переиндексируются — точность растёт без дообучения модели.

3. **RF-first стек.** Никаких зависимостей от сервисов, заблокированных или недоступных в РФ.
   Все внешние API — только DeepSeek (работает из РФ напрямую).

4. **Portable dev environment.** Весь код в GitHub. CLAUDE.md содержит полный контекст.
   Любой Claude Code на любом ПК с доступом к репо может продолжить работу.

---

## Текущий статус

> **Актуально на 2026-09-07.** Оперативный «где мы остановились и что дальше» —
> **[docs/NEXT_SESSION.md](docs/NEXT_SESSION.md)**; задачник — **[docs/BACKLOG.md](docs/BACKLOG.md)**;
> **как продукт работает сейчас — [docs/PRODUCT_SPEC.md](docs/PRODUCT_SPEC.md)**; протокол замеров
> — **[EVAL_GUIDE.md](EVAL_GUIDE.md)**, последний прогон —
> [docs/eval_runs/2026-08-25_test17_full.md](docs/eval_runs/2026-08-25_test17_full.md);
> план доработок со статусами — [docs/REVIEW_TZ.md](docs/REVIEW_TZ.md); аудит —
> [docs/REVIEW_2026-08-10.md](docs/REVIEW_2026-08-10.md).

✅ **08.09.2026 — `K16` #48 ЗАКРЫТА, РЕЛИЗ `v0.5.0-test24` СОБРАН, НА БОЮ НЕ ВЫКАЧЕН.**
Бой не тронут: `prod` = `main` = `f0c19d8` = `v0.5.0-test23`. `dev` = **`503ad96`**, батарея
**1284 OK**, коллекции локально **1377 / 752 / 12**, ось `K9` **24/25 · 25/25 · 5/5** не
сдвинулась, свип маршрута 237 вопросов — **0 расхождений**.

⚠⚠⚠ **РАУНД 11: 15 находок, 1 HIGH — СНОВА РЕГРЕССИЯ ПРОВЕРЯЕМОГО ПАКЕТА.** Гейт длины из
раунда 10 поставили на ОБЫЧНЫЙ путь, где матч никем не опровергнут; посылка «ключ недопустимой
длины не обозначает НИЧЕГО» **ложна на лукапе** — `conditions_for` матчит по ПРЕФИКСУ, и ключ
длиннее записи доезжает до своей строки. Замер класса «код Перечня + лишний знак»: **147 потерь
вердикта из 308, приобретений 0**; после правки — 0 расхождений с состоянием до раунда 10.
⚠⚠⚠ **РЕКОМЕНДАЦИЯ РЕВЬЮ ЗАМЕРЕНА И ОТВЕРГНУТА** (третий такой случай в проекте): отмена «окна
разрыва» теряет **453 верных вердикта из 906**, не убирая ни одного ложного — 24 к 1 против.
⚠⚠⚠ **ПРИЗНАК ФОРМЫ ЗАМЕНЁН ФАКТОМ.** Раунды 7–11 разводили «подъём к родителю» ПЯТЬЮ признаками
формы (слово-счётчик, длина основы, размер хвоста, длина целого, ширина окна) — каждый закрывал
свой случай и открывал соседний, потому что классы **неразличимы лексически**. `_climb_resolves`
спрашивает Перечень: есть запись у родителя — поднимаемся, нет — молчим. Итог: **38 ложных
«НЕ включён» убрано, потерь 0**, все 453 верных вердикта сохранены. Ряд HIGH: 2·2·3·2·2·0·2·6·1·1·1.

✅ **`K16` #48: корпус `pp719_rules` 681 → 752.** Положение об экспертизе происхождения
(приложение к приказу ТПП РФ **№49** от 14.07.2023) — 62 записи; FAQ ГИСП — 9 записей,
`legal_force: 5`, `topic: ГИСП`.
⚠⚠⚠ **ПЕРВОИСТОЧНИК ПУБЛИКУЕТ УСТАРЕВШИЙ ФАЙЛ:** на tpprf.ru лежит PDF редакции 2023 г., а
документ действует **в ред. приказа №59 от 16.07.2026** — того же, которым изменён Приказ №14.
Сверка: похожесть тела 0.983, **+371 знак**, ОДИН абзац в п. 6.13; он дописывается конвертером
дословно, с контролем на единственность якоря И на отсутствие дубля.
⚠⚠ **ПЕРЕСКАЗ FAQ БРАТЬ НЕЛЬЗЯ:** сторонние изложения называют шагом «заключение ТПП,
подтверждающее производство» — фантом, под который `K15` завела перечень и гард. В корпусе такой
текст **заземлил бы** выдумку, и гард пропустил бы её. Настоящая страница чиста (проверено),
gisp.gov.ru закрыт JS-проверкой — файл сохраняет владелец из браузера.
⚠ Квота окна: у Положения своя строка в `_RULES_TOPIC`, у №52 сужен ствол «экспертиз» (его
экспертиза подтверждает ПРОИЗВОДСТВО, а не определяет СТРАНУ). Радиус на 21 вопросе четырёх
наборов: **одно** целевое изменение, у №52 7/7 своих на месте.
⚠ FAQ строки квоты НЕ получил намеренно: он слабейший источник корпуса, и практика во главе окна
над нормой перевернула бы иерархию `K13`. Критерий #48 требует «источник и пометку», а не первого
места; в окне документ присутствует.

⚠⚠⚠ **ЧЕГО Я НЕ СДЕЛАЛ И ПОЧЕМУ — РЕШЕНИЕ ВЛАДЕЛЬЦА** (подробно в
[docs/NEXT_SESSION.md](docs/NEXT_SESSION.md)):
**раунд 12 был назначен, правило записано до прогона (`a8a0362`), ревью запущено — и прервано
лимитом сессии: находок ноль, вердикта нет.** PR `dev` → `main` без объявленного ревью не
открывался: правило пишется заранее ровно чтобы его не обходили. Выкатка на VM — за владельцем
(окно недоступности ~15–35 мин; переиндексация обязана быть ПОЛНОЙ — загрузчик не делает `--doc` для нового документа. ⚠ Обоснование «`avgdl` 95.8 → 82.3» ОТОЗВАНО раундом 12: пересчёт даёт 81.73 → 82.35, сдвиг +0.76 %).
Платный прогон уровней 2–3 на новом корпусе не снят.

---

✅ **07.09.2026, блок 1 — РАУНД 10 ОТРАБОТАН: 17 находок, 1 HIGH. Правило остановки НЕ выполнено.**
`dev` = **`fa74b85`**, батарея **1217 → 1246, OK**, CI зелёный, открытых issues 29. Бой не тронут:
`prod` = `main` = `f0c19d8` = `v0.5.0-test23`.
⚠⚠⚠ **HIGH ОКАЗАЛАСЬ РЕГРЕССИЕЙ САМОЙ ПРОВЕРЯЕМОЙ ПРАВКИ.** MED-6 усекала НАСТОЯЩИЕ девятизначные
коды записи 4+2+2+1 («0710 40 00 0» → «0710 40 00»): признак «десятый знак приписывается к
девятизначной ОСНОВЕ» молча предполагал ровно одну форму записи. Ключ усекался у **59 из 59**
девятизначных кодов Перечня, вердикт «включён» терялся у **53 из 59** (шесть уцелели совпадением
по ПРЕФИКСУ — величины разные, и это записано). Класс HIGH первого раунда, который комментарий
той же правки объявлял здесь невозможным.
⚠⚠ **Признак ЗАМЕНЁН, а не подкручен:** раунды 7, 8 и 9 отвечали на один вопрос («принадлежит ли
последняя группа коду») тремя разными признаками — длиной ОСНОВЫ, размером ХВОСТА, словом-
счётчиком; каждый закрывал свой случай и открывал соседний. Новый взят из первоисточника и
проверяем тестом ПРОТИВ таблицы (`_TNVED_KEY_LENGTHS`). Радиус: против `6d41582` починено 236,
сломано 0; против `f0c19d8` разбор совпадает **побайтно на 1895 запросах из 1895**.
⚠ Ещё по коду: `gap` считался от УСЕЧЁННОГО ключа, хотя это расстояние в ТЕКСТЕ — код внутри окна
выпадал, блок СТ-1 молча исчезал (9600 запросов: 240 «было None → стал код», потерь 0) · **девять
ЖИВЫХ коллизий `by_key`** у Приказа №52 и сносок, к которым сторож MED-4 слеп по построению;
неоднозначный родитель больше не прикрепляется вовсе (`index_text` всех 681 записи побайтно тот
же, sha256 совпал — **переиндексации НЕ требует**).
⚠⚠ **Восемь находок из семнадцати — про ЛОЖНЫЕ ИЛИ ПЕРЕЖИВШИЕ СВОЙ ФАКТ ОБОСНОВАНИЯ**, тот же
класс, что чинила MED-1 в самом проверяемом коммите: «инертна» сказано о МАРШРУТЕ, а прочитано
как о поведении (мутация в исходнике: тема меняется на 5 пробниках из 5, маршрут на 0 из 19) ·
комментарий MED-2 называл таблицу `_DOC_HINTS`, которой в проекте НЕТ, и один общий фрагмент из
шести · довод отказа от `head_buf` ссылался на MED-4, которую тот же коммит и снял.
⚠⚠ **МУТАЦИОННАЯ ПРОВЕРКА НАШЛА ДЕФЕКТ В МОЕЙ ЖЕ ПРАВКЕ, И ОН ИЗ КЛАССА НАХОДКИ 13.** Сторож
находки 4 во второй редакции считал `topics.classify` по ТЕЛУ функции — и прожил час: мой же
комментарий к находке 12 назвал это имя в теле, сторож покраснел **на верном коде**. Считать
ТЕКСТ функции нельзя вообще; переписан на `co_names` код-объекта, куда проза не попадает по
построению. Девять сторожей проверены мутацией: каждая правка отменена, названный тест краснеет.
**Оси перемерены ПОСЛЕ правок** (правило требовало перепроверить заявление «`K9` и маршрут не
трогались» — подтвердилось): `K9` **24/25 · 25/25 · 5/5** не сдвинулась за десять раундов · свип
маршрута 237 вопросов — вне сферы 53 · процедурных 0 · ложных 1 · пропусков 15, побайтно то же ·
коллекции **1377 / 681 / 12**. Ряд HIGH по десяти раундам: **2·2·3·2·2·0·2·6·1·1**.
⚠⚠ **ДАЛЬШЕ — РЕШЕНИЕ ВЛАДЕЛЬЦА (правило `7b7b3c3`, пункт 2): раунд 11 ИЛИ PR `dev` → `main`.**
Автоматического следующего раунда нет. *За:* HIGH десятого раунда сидела в правке ВОСЬМОГО,
найденной ревью, — пакет правок по ревью снова оказался объектом ревью; правки этого раунда никем
не проверены. *Против:* ряд HIGH не убывает ничем, а `K16` #48 — основная работа — не начата.
Затем **`K16` #48** своим окном полной переиндексации (⚠ документов на диске НЕТ: Положение об
экспертизе страны происхождения от 14.07.2023 — tpprf.ru, FAQ ГИСП — gisp.gov.ru; добыть) →
выкатка `test24` → `B1` #61. Письмо эксперту владелец отправляет сам.

✅ **07.09.2026, блок 0 сессии — гигиена.** Бой не тронут: `prod` = `main` = `f0c19d8` =
`v0.5.0-test23`. `dev` впереди `main` на остаток раунда 8 (`6d41582`: семь находок, HIGH 0,
**НЕ ревьюён**), на слитую ветку железа и на этот пакет документации. **1217 тестов, OK**
документированной командой. Локальный Qdrant поднят, коллекции **1377 / 681 / 12** сходятся с
профилем выкатки. Открытых issues **29**, PR нет, CI зелёный.
⚠ **Документация отставала на одну выкатку:** выкатка `test23`, раунд 9, откат гейта и остаток
раунда 8 были записаны только в `docs/eval_runs/` и в сообщениях коммитов. Исправлено.
⚠ **Слита `worktree-docs-hardware-onprem`** (13.08, три коммита, только документация):
`docs/HARDWARE_ONPREM.md` — сборка on-prem сервера из железа кладовой ТПП. Ветка три недели жила
несли́той и нигде не была записана; worktree и ветка удалены.
⚠ **`AGENTS.md`** (неотслеживаемая копия `CLAUDE.md` для Codex, семь строк отличий) — ждёт решения
владельца: коммитить копию или заменить указателем на `CLAUDE.md`. Не тронут.
**Дальше по состоянию на тот момент:** раунд 10 ревью → `K16` #48 → выкатка `test24`.
_(раунд 10 отработан, см. блок выше)_

✅ **03.09.2026: ВЫКАЧЕНА `v0.5.0-test23` — `K14` #35 ЗАКРЫТА.** `prod` = `main` = `f0c19d8` = тег,
вердикт скрипта «все проверки прошли», **провалов 0**. Выкатка **С ПОЛНОЙ ПЕРЕИНДЕКСАЦИЕЙ**
`pp719_rules` 328 → **681**, окно недоступности процедурной ветки **~34 минуты**; бэкап снят до
всего (28 пользователей, 1262 сообщения, 297 оценок). Маркеры пакета **8 + 1 анти**. Релизная
проверка СТ-1 на бою: Соглашение СНГ первым на «условия достаточной переработки» и «кумулятивный
принцип», Приказ ТПП РФ №14 первым на «как получить СТ-1». **Раунд 9 ревью** (правило записано ДО
прогона, `cf69311`): 8 находок, **1 HIGH** — правка HIGH-5 раунда 8 вернула «уверенный неверный
код» («1500 единиц» → 1500) и отменена: формы неразделимы лексически, выбор сделан направлением
ошибки (неверный код дороже отсутствующего). Правило «нет HIGH → выкатываем» не выполнено, раунд 10
по правилу — решение владельца; раунд 10 не проводился, выкачено после закрытия HIGH. Ряд девяти
раундов: находок 7·5·7·5·6·8·5·15·8, HIGH 2·2·3·2·2·0·2·6·1; ось `K9` не сдвинулась ни разу
(24/25 · 25/25 · 5/5). **Остаток раунда 8** (MED-1, MED-2, MED-4, MED-6, LOW-7…9) закрыт ПОСЛЕ
выкатки в `dev` (`6d41582`), батарея 1215 → 1217.

⚠⚠ **ТРИ ЗАХОДА ВЫКАТКИ ОСТАНОВЛЕНЫ ПРЕДОХРАНИТЕЛЯМИ, ВСЕ ТРИ — ОШИБКИ СБОРКИ ПАКЕТА:** ① нет
`SHA256SUMS.txt` (стоп ДО бэкапа) · ② нет тарбола `navigator-719-<тег>.tar.gz` (распакованного
дерева мало; стоп ПОСЛЕ бэкапа, рабочая копия не тронута) · ③ соединение не установилось.
**Скрипт ждёт в каталоге пакета манифест сумм И тарбол релиза** — читать `deploy.sh` ДО сборки,
а не после каждого падения. ⚠ Своя сверка рядом с предохранителем его не заменяет: агрегат хешей
сходился, а скрипт требует манифест В ПАКЕТЕ. ⚠ Windows-`sha256sum` пишет `hash *./path`, Linux —
`hash  ./path`: агрегат расходится из-за ФОРМАТА, сравнивать хеш отсортированных хешей файлов.
⚠⚠ **Канал режет по ЧАСТОТЕ подключений, и блокировка НАРАСТАЕТ** — после серии попыток кулдаун
7–8 минут; подключения планировать заранее и объединять. ⚠ На VM нет `gh`: гейт CI в `deploy.sh`
там честно говорит «НЕ ПРОВЕРЕНО» — проверять со своей стороны.

⚠⚠⚠ **ГЕЙТ ВНЕ-СФЕРЫ ПРОЦЕДУРНОЙ ВЕТКИ ЗАВЕДЁН И ОТКАЧЕН ТЕМ ЖЕ ДНЁМ (`208b379`) — ЗАМЕР ОПРОВЕРГ
ОТЧЁТ РАУНДА 8.** Развилку раунда 8 владелец закрыл вариантом (а) — структурный гейт вместо
словаря. Замер трёх конструкций на 164 вопросах пяти популяций (`scripts/eval_rules_relevance.py`):
выигрыш **2 пункта из 36** (шум) ценой **девяти отказов на флагманских вопросах**, включая «что
такое кумулятивный принцип» (термин самого проиндексированного Соглашения) и «кто выдаёт
сертификат происхождения в Курской области» — вопрос про самого ЗАКАЗЧИКА. Порога, спасающего оба
класса, НЕ существует: полосы перекрываются по существу (целевые 0.8316–0.8836, чужие
0.8297–0.8622) — таможенные вопросы ДЕЙСТВИТЕЛЬНО про происхождение, и корпус про него же.
Против ОБЩЕЙ вне-сферы гейт работал (сходство 0.73–0.80, полосы расходятся). Нашёл ЖИВОЙ прогон,
а не четыре офлайн-замера: 33 → 25 выглядело улучшением, пока семь вопросов не показали СОСТАВ
потерь. Отрицательный результат закреплён исполняемо (тесты утверждают ОТСУТСТВИЕ порога) +
[eval_runs/2026-09-03_rules_relevance_gate.md](docs/eval_runs/2026-09-03_rules_relevance_gate.md).
**Не заводить заново без нового замера.**
⚠⚠ **За 03.09 отменены ТРИ предписания отчёта раунда 8** — гейт (замером), фикс MED-3 через
`head_buf` (на первоисточнике «потерянный» текст оказался продолжением ЗАГОЛОВКА; `head_buf` завёл
бы 10 обрывков класса `D12` и поднял бы MED-4 на 10 разделах из 11), HIGH-5 (разделение списка
вернуло «уверенный неверный код»). **Рекомендация ревью требует той же проверки, что правка кода.**

---

_(история)_ ⏳ **31.08.2026: `K14` #35 ГОТОВА И ЗАМЕРЕНА — НА БОЮ НЕ ВЫКАЧЕНА.** Бой не тронут:
`prod` = `main` = `722ac8e` = `v0.5.0-test22`. `dev` = **`2912f9d`**, впереди на **30 коммитов**,
PR #137, CI зелёный. **1173 теста** — зелено и с живым Qdrant, и с недоступным.
Корпус `pp719_rules` **328 → 681** (Соглашение СНГ 66 + Приказ ТПП РФ №14 287), таблица условий
Перечня **245** записей. Приёмка: условие Перечня **1.00 ×4**, faithfulness **1.00 ×4** — критерий
ТЗ выполнен. Профиль `v0.5.0-test23` готов (маркеры 7 + 1 анти), релизная проверка **0 провалов**.

⚠⚠ **ВЫКАТКА `v0.5.0-test23` — С ПОЛНОЙ ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`**, не частичной
(`REINDEX_FULL` в профиле): по `K11` `avgdl` запекается в sparse-вектор каждой точки, и новый
документ уводит в чужой масштаб BM25 точки СОСЕДЕЙ. Будет окно недоступности процедурной ветки.
Решение владельца: `K16` #48 — **отдельным окном**.

⚠⚠⚠ **СЕМЬ РАУНДОВ РЕВЬЮ PR #137: 43 находки, 13 HIGH, СХОДИМОСТИ НЕТ** (находок 7·5·7·5·6·8·5,
HIGH 2·2·3·2·2·**0**·2). Батарея была зелёной на всех сорока трёх — дефекты живут в зазорах между
компонентами, у каждого из которых свои зелёные тесты. **Ось `K9` не сдвинулась ни за один раунд**
(атрибуция@1 24/25 · представленность 25/25 · нужный пункт 5/5) — это и есть свидетельство, что
пакет не разъехался с прежним поведением корпуса.

⚠⚠ **ВЫВОД РАУНДА 6 О СХОДИМОСТИ ОКАЗАЛСЯ НЕВЕРНЫМ И ИСПРАВЛЕН ТАМ ЖЕ, ГДЕ ЗАПИСАН.** «Тяжесть
падает» было объявлено по ОДНОЙ точке ряда, дисперсию которого никто не мерил. Правки самого ревью
давали HIGH в раундах 2, 5 и 7 — три из семи. **Правило трёх прогонов относится и к «признакам
сходимости» цикла ревью.**

⚠⚠ **Раунд 7 нашёл, что таможенный вопрос получал ответ, СИНТЕЗИРОВАННЫЙ из Правил СНГ:** слова-
спутники ТН ВЭД были общей таможенной лексикой («условия ввоза», «сертификат соответствия»), а у
процедурной ветки нет гейта вне-сферы. ⚠⚠ **Моя первая правка была замерена на наборе, который я
под неё же и подобрал** — выглядела чистой, а батарея уронила ШЕСТЬ тестов прежних раундов (две
формулировки закреплены как контракт, одна дословно экспертная). Решено ЗАМЕРОМ трёх состояний:
стволы 11/1 · только фразы 0/3 · **стволы + ЗАКРЫТЫЙ СПИСОК дисквалификаторов 0/1** — третий
доминирует оба. Форма правила сменилась с «список разрешённого» на «список ИСКЛЮЧЕНИЙ».

⚠⚠ **Правка находки о числе-счётчике дала СВОЮ регрессию**, пойманную проверкой краёв, а не ревью:
отсечение усекало НАСТОЯЩИЕ полные коды («8544 70 000 0 позиции» → «8544 70 000») — класс HIGH
первого раунда. Разрешено только у четырёхзначной основы: «8403 10» → «8403» это подъём к
РОДИТЕЛЮ, а срез хвоста у десятизначного — переход к ЧУЖОЙ позиции.

⚠⚠ **Класс «таможня + ТН ВЭД» заведён в свип ОТДЕЛЬНЫМ файлом** `scripts/eval_offdomain_tnved.json`,
а не в `eval_golden_negative`: тот кормит ПОРОГОВУЮ метрику отказа, и 12 легко проходящих кейсов
подняли бы её **0.87 → 0.89 без единой правки кода**. Разные отказы — разные популяции.

**Дальше:** решение владельца — раунд 8 ТОЛЬКО по диффу раунда 7 (с правилом остановки «нет HIGH →
выкатываем») **или** выкатка `v0.5.0-test23` сейчас. Затем `K16` #48 своим окном, затем `B1` #61.

---

✅ **30.08.2026: ВЫКАЧЕНА `v0.5.0-test22`.** `prod` = `main` = `722ac8e` = тег, код возврата
скрипта **0**, «все проверки прошли». Выкатка **рантаймовая** — Qdrant не перезапускался
(коллекции 1377 / 328 / 12 целы). **967 тестов**, открытых issues **31**.
Слит PR #135 (19 коммитов) — с `main` снята регрессия гарда `K15`; закрыт `#136` (7 находок
раунда 6 ревью). Живая проверка ×5: **здоровых 5 из 5, источников 6**, `K15` в журнале — **0**.

⚠⚠ **ПРОБНИК ЗДОРОВЬЯ ОБЪЯВИЛ НЕЗДОРОВЫМ ИСПРАВНЫЙ БОЙ И ОСТАНОВИЛ ВЫКАТКУ.** Временная учётка
заводилась ролью `user`, а `user` — региональный тестировщик, обязанный заполнить профиль до
чата (`require_user_profiled`): все пробы возвращали **403 `profile_required`**. Тот же класс,
что чинился в `#136` в ту же сессию — инструмент упёрся в СВОЁ состояние и обвинил в этом сервис.
⚠ Дефект был ложно ОТРИЦАТЕЛЬНЫМ (остановил выкатку, а не пропустил плохую) — ровно поэтому
предохранитель обязан стоять НА ПУТИ действия. Лечится ролью `expert` для внутренних проб плюс
третьим исходом у пробника: 401/403/429 → **«НЕ ИЗМЕРЕНО»**, о сервисе не сказано ничего.

⚠⚠ **`#136`: общий корень четырёх находок — распознаватель, позванный В ОДНОМ месте из трёх.**
`rate_limited()` завели раундом 5 и спросили только в опросе; в `judge` 429 доходил до вердикта
**«ПРОЙДЕН»** — инструмент не наблюдал поломку вовсе, а отчитывался, что проверил фолбэк.
Заведён четвёртый исход «НЕ ИЗМЕРЕНО». Пол опроса теперь ВЫВОДИТСЯ из бюджета окна (7.0 → 10.1 с),
а оракул теста заменён на независимую симуляцию: прежний повторял модель кода и потому её ошибку
не видел. **Каждый из 12 новых тестов проверен мутацией.**

---

✅ **26.08.2026, итог дня: ВЫКАЧЕНА `v0.5.0-test19` — ревью захода 3 отработано.**
`prod` = `main` = `452057d` = тег, вердикт «все проверки прошли», маркеры 7 + 0 анти, выкатка
рантаймовая. **851 тест**, открытых issues 27.

⚠⚠ **ЗАПИСЬ «`/code-review` — КОМАНДА ПОЛЬЗОВАТЕЛЬСКАЯ» ОКАЗАЛАСЬ НЕВЕРНОЙ И ДЕРЖАЛА ДОЛГ МЕСЯЦ.**
Агент запускает ревью сам, форком в фоне; пользовательская и платная только `ultra`. Из-за той
записи три пакета на бою числились непроверяемыми не «руки не дошли», а «нельзя». Прошлые круги
обрывались «по лимиту сессии» потому, что шли В ТОЙ ЖЕ сессии, где работа.

**Ревью захода 3: 7 находок, 6 исправлено, 1 отклонена.**

⚠⚠ **Главная была НА БОЮ, в числах для эксперта:** «Контакторы и пускатели электромагнитные
низковольтные» получали главной строкой график ВАКУУМНЫХ контакторов со ступенью 51 балл, к ним
не относящейся. Обе позиции возвращали ПОБАЙТНО одну строку — `blocks[0]` захардкожен, ответ не
зависел от вопроса. Чинить в разборе нельзя по построению: там ещё не известно, кто спросит.

⚠⚠ **Порог, не доезжавший до эксперта:** «Мочеприемники», прим. 81, строка названа ДОСЛОВНО так
же, порог 135 баллов — рантайм отдавал None. Тай-брейк требовал пересечения ≥2 слов, однословное
имя даёт максимум 1: НЕПРОХОДИМО ПО ПОСТРОЕНИЮ. Прятался месяц, потому что классификатор его тоже
не видел, а `attachment_defects` гейтится порогом 0 — гейт показывал ноль при живом дефекте.

⚠ **Замер радиуса нашёл дефект в моей же правке** (токен «6300» вытаскивал более узкий диапазон;
числа исключены — идентичность несут слова). Радиус итоговый: 1374 позиции, **+1 порог,
потеряно 0**, покрытие 50.8 % → 50.7 %.

⚠ **Одна находка ОТКЛОНЕНА:** `case_fits_scope` — поведение при пустом окне закреплено тестом с
обоснованием; правка написана и откачена. Менять осознанное решение по находке ревью нельзя.

⚠⚠ **Сухой прогон поймал две ошибки в моём же профиле выкатки**, и вторая важнее: анти-маркер
наведён на файл, где проверяемой строки НИКОГДА не было — проходил только потому, что не мог
сработать. Навести на верный файл тоже нельзя: там строка теперь в комментарии, объясняющем
правку. **Нет анти-маркера лучше, чем ложный или пустой.**

---

✅ **26.08.2026: ВЫКАЧЕНА `v0.5.0-test18`.** `prod` = `main` = `a8d6c21` = тег, вердикт
скрипта «все проверки прошли», провалов 0. Выкатка **рантаймовая** (коллекции 1377 / 328 / 12),
окна недоступности не было. **828 тестов**, открытых issues 27. За день закрыты `EV21` #119,
`K15-1` #120, `P3` #121 и четыре issue, лежавшие сделанными; заведена `K12-1` #122.

⚠⚠ **Живая проверка на бою — ПОВТОРНАЯ, 5 прогонов.** «Нужно ли заключение ТПП для внесения
продукции в реестр»: фантомных документов **0/5**, незаземлённых чисел **0/5**, каждый ответ прямо
говорит, что такого документа не существует. **До правки тот же вопрос УТВЕРЖДАЛ несуществующий
документ 3 из 3.** Контроли ×2: загранпаспорт отказан, выписка идёт процедурной веткой,
этикетировщики остались товарными.

⚠ **Скрипт выкатки сам назвал пробел** — релизной проверки не было. Заведена
`checks/v0.5.0-test18/`: пять переехавших вопросов + четыре контрольных + гард. Офлайновая,
прогнана на бою, код возврата 0. ⚠ **Первый запуск остановил предохранитель:** в пакете не было
`SHA256SUMS.txt`. Сумму считать ЛОКАЛЬНО и передавать отдельно — иначе проверка сертифицирует
сама себя. ⚠ Канал рвался ЧЕТЫРЕ раза, каждый раз проходил со второй попытки (пауза ~200 с).

---

✅ **26.08.2026, _раньше в тот же день_: `EV21` #119 ЗАКРЫТА — приёмочный набор впервые покрывает
кластер жалоб №1.** _(Числа ниже — на момент этого шага; итог дня см. выше.)_
Кейсы **46 → 50**, четыре документных, покрывают ОБЕ ветки (чистый вопрос идёт процедурной,
смешанный — товарной). Оракул вынесен в общий модуль `scripts/eval_documents.py`, у него
положительный контроль против закрытого перечня и против ярлыков манифеста. **794 теста.**
На бою по-прежнему `v0.5.0-test17`; правки замера НЕ выкачены. Открытых issues **27**.

⚠ **Оракул НЕ зовёт рантайм-гард** `documents_ref.unverified_documents`: позови он предохранитель —
снятие предохранителя сделало бы метрику зелёной (класс `O3` #104). Независимость проверяется
мутацией, а не обещанием.

✅ **`K15-1` #120 СДЕЛАНА в тот же день.** Чинилась **асимметрия**, а не число: три соседние строки
списка STRONG-маркеров задавали РАЗНЫЕ правила разрыва — у отглагольного «внесение … в реестр»
разрыв запрещён вовсе, у глагола «внести его в реестр» разрешён до четырёх слов. ⚠⚠ Ровно этот
случай **уже был починен в `topics.py`** и не переехал через модуль. Вторая половина: **гейт БЛОКА
справочника разошёлся с гейтом его СОДЕРЖИМОГО** — разъяснение зависело от ВОПРОСА, а сам
справочник от ТЕМЫ. Согласованы.
Радиус замерен новым свипом `scripts/eval_routing.py` (188 вопросов, офлайн; до сих пор маршрут не
мерил никто): **+3 / −0 / ложных срабатываний 0**, два из трёх — из приёмочного набора `K9`.
Живая проверка ×3: «несуществующий документ не утверждён» и «разъяснение по условию»
**0.75 ×3 → 1.00 ×3**; контекст кейса #50 сменился с 12 817 символов требований к охранной
сигнализации на 6304 символа норм плюс справочник. **807 тестов.**

✅ **`P3` #121 — свип вскрыл дефект крупнее, и он ЗАКРЫТ в тот же день.** 13 из 25 вопросов
приёмочного набора `K9` не доходили до процедурной ветки и отвечались по ТОВАРНОМУ корпусу:
«как получить выписку из реестра» → регистраторы данных рейса. **Не поймала ни одна метрика** —
`eval_rules` дёргает `search_rules` напрямую, минуя гейт, то есть «атрибуция@1 0.96» была
утверждением о поиске В КОРПУСЕ, а не о попадании в него; дописано в шапку `eval_rules.py`.
**Решение выбрано ЗАМЕРОМ трёх вариантов:** победил сигнал ТЕМЫ с 719-якорем (расширение словаря
гейта не дало ничего, «якорь без товарного сигнала» дал лишнее ложное срабатывание). Пропуски
17 → **11**, вне-сферы **0**, документные маршруты целы. **816 тестов.**

⚠⚠ **Две регрессии чуть не уехали, и обе прятал МОЙ ЖЕ СВИП.** Смешанный вопрос уходил с товарной
ветки (а он обязан там остаться — ради него `K12`); вне-719 «порядок получения загранпаспорта»
уходил на процедурную (`topics` калиброван на вопросах, которые гейт УЖЕ пропустил). Свип считал
первых «классом, где маршрут не важен», а вторых не считал вовсе. **Инструмент, которым мерят
правку, — сам объект проверки: «ноль» означал не «чисто», а «не считаю».**

⚠ Попутно: **ТПП не было в списке 719-якорей** — список называл ГИСП и Минпромторг и пропускал
организацию, которая выдаёт оба подтверждающих документа.
⚠ **Критерий «ноль пропусков» из постановки оказался недостижим выбранным классом средств** —
остаток 11 вопросов утвердительные, без глагола и без темы. Структурный ответ: добор процедурной
ветки при НИЗКОЙ релевантности товарной выдачи. Отдельной задачей.
⚠ Заведена **`K12-1` #122** (P2): фрагмент промпта темы `registry_entry` уже самой темы.

⚠⚠ **Запрет «заключения ТПП» три месяца считался там, где термин не мог появиться** — метрика §2.2
бежала по 42 ТОВАРНЫМ вопросам, и «1.00 ×3» означало не «не выдумывается», а «неоткуда взять».
Сама проверка была голой регуляркой: первый же документный кейс покрасил бы ею ВЕРНЫЙ ответ
с отрицанием — тот же класс, что чинили в рантайм-гарде 25.08 (`ddd3e16`).

⚠⚠ **Четыре дефекта нашлись в САМИХ замерах, и все — живым прогоном при 794 зелёных тестах:**
порог ссылок `[N]` считался по `hits`, пустому у процедурного ответа (источники там в
`rule_sources`) · распознаватель источника искал «методические рекомендации», а контекст
подписывает сокращением манифеста «**Методрекомендации**» — пункты в окне БЫЛИ, замер отчитался
«источник не назван» · `TypeError` на `expected_section: null` · «0.00» там, где случаев нет.

**Числа документных кейсов ПОСЛЕ правок дня (3 прогона):** перечень доехал **1.00 ×3** ·
faithfulness **1.00 ×3** · разъяснение по условию **1.00 ×3** · несуществующий документ не
утверждён **1.00 ×3** · источник назван 0.67 / 0.67 / 1.00 — плавает, класс `EV20`.
_(до правок две средние метрики были 0.75 ×3 — падал кейс #50, он же #120.)_
Товарная популяция не сдвинулась: `--kind product` воспроизводит базу **точно** (4934 / 3852,
хвост 0.33). Отчёты — [eval_runs/2026-08-26_ev21.md](docs/eval_runs/2026-08-26_ev21.md) и
[eval_runs/2026-08-26_routing.md](docs/eval_runs/2026-08-26_routing.md).

**Порядок дальше:** **один** платный прогон уровней 1–2 на 50 кейсах ×3 → заход 6 `K14`.
⚠ Прогон идёт ПОСЛЕ правок намеренно: `EV21` поведения не меняет (только код замера плюс no-op
выделение `plan_procedural`), значит поведенческие правки в пакете — #120 и #121, и одна оплата
покрывает всё. Открытых issues **27**.

---

✅ **25.08.2026, вечер: ВЫКАЧЕНА `v0.5.0-test16` — заход 5.5 на бою.**
`prod` = `main` = `848911e` = тег. Вердикт скрипта «все проверки прошли», провалов 0.
Выкатка **рантаймовая**: коллекции те же (1377 / 328 / 12), окна недоступности не было.
**751 тест.**

**Живая проверка на бою — ПОВТОРНАЯ, по правилу, купленному предыдущей выкаткой:** 5 прогонов
документного вопроса, лишних упоминаний **0**, гард чист, незаземлённых чисел нет; контроль
(спросили про заключение) — объясняет, что документа нет. Прежде тот же вопрос давал термин
**2 раза из 5**.

⚠⚠ **`O5` #113 подтверждён на бою:** сухой прогон сказал «сверяю маркеры с ПАКЕТОМ поверх рабочей
копии» и дал **6 + 1 анти**. Накануне на том же месте было «13 маркеров не совпало» — по верному
профилю.

⚠ **Что этот релиз НЕ мерил:** уровни 2–3 после правок не гонялись. Уровень 0 и уровень 1
процедурной оси совпали с базой; товарный свип пропущен обоснованно (проверено `git diff`: 
`search()` и коллекция `pp719` не тронуты). Каждая правка замерена адресно. Полный прогон —
перед заходом 6.

---
✅ **25.08.2026: ЗАХОД 5 СДЕЛАН И ЗАМЕРЕН — `K13` #46 и `K15` #36 закрыты, решение
ВЫКАТЫВАТЬ `v0.5.0-test15`. 726 тестов.** Замер уровней 0–3 пройден целиком, ни одна метрика
с порогом не просела ([eval_runs/2026-08-25_zahod5.md](docs/eval_runs/2026-08-25_zahod5.md)).
На VM по-прежнему `v0.5.0-test14`; правки НЕ выкачены. `K13` рантаймовая (значение
берётся из МАНИФЕСТА по `doc_type`, а не из payload — у товарного корпуса и кейсов паспорта в
payload нет). `K15` закрыта обеими половинами: закрытый справочник документов ДАННЫМИ + рантайм-гард
на выдуманный документ, и Методрекомендации ТПП вер. 1.18.3 в корпусе — `pp719_rules` **311 → 328**.

⚠⚠ **СЛЕДУЮЩАЯ ВЫКАТКА — С ПОЛНОЙ ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`, НЕ ЧАСТИЧНОЙ.** Это пробел
`K11`, найденный на её первом настоящем ДОБАВЛЕНИИ документа: `avgdl` считается по всему
корпусу и **запекается в sparse-вектор каждой точки**, поэтому новый документ уводит в чужой
масштаб BM25 не свои точки, а точки **соседей**, загруженных раньше (замерено **95.84 → 96.72**).
Предохранитель стоит на пути действия: `--doc` для отсутствующего в коллекции документа
останавливается, осознанный обход — `--allow-new-doc`.

⚠⚠ **Методрекомендации помечены действующими, но описывают ОТМЕНЁННЫЙ шаг:** вводная и схема
процесса ведут за «заключением Минпромторга», а «Правила выдачи заключения» утратили силу
**29.06.2024** (ПП РФ N 894). ⚠ `legal_force` тут не помогает — сила решает КОНФЛИКТ НОРМ, а не
УСТАРЕВАНИЕ. Решение владельца: индексировать, исключив устаревшее (932 символа из 19 759)
через `exclude_fragments` манифеста, **с положительным контролем** — несовпавшее правило
останавливает загрузку.

⚠ **Слепой запрет слова «заключение» был бы дефектом:** «экспертное заключение» — ДЕЙСТВУЮЩИЙ
документ (итог выездной проверки ТПП РФ, основание ИСКЛЮЧИТЬ реестровую запись, п. 36–40 Правил).
Не существует ровно одного: заключения ТПП, ПОДТВЕРЖДАЮЩЕГО ПРОИЗВОДСТВО.

---

✅ **24.08.2026: ВЫКАЧЕНА `v0.5.0-test14`** — на VM `d119e58`, `prod` = `main` = тег, вердикт
скрипта «все проверки прошли». ⚠⚠ Переиндексация впервые шла средствами **`K11` по одному
документу ПРЯМО НА БОЮ**: коллекция держалась 311 точек, процедурная ветка не пустела ни
секунды; снапшот (1.08 ГБ) не понадобился. ⚠⚠ Канал до VM режет ПО ЧАСТОТЕ ПОДКЛЮЧЕНИЙ, а не
по объёму — одно соединение на файл + пауза 60–90 с; мелочь надёжнее идёт base64 в аргументе.

✅ **CI ПОЧИНЕН, ПИСЬМО ЭКСПЕРТУ СОБРАНО, `K8` #39 ЗАКРЫТ, РЕВЬЮ ПРОЙДЕНО.
Тестов 664, зелено в ДОКУМЕНТИРОВАННОЙ среде. ЗАХОД 4 ЗАКРЫТ ЦЕЛИКОМ.**

✅ **`K11` #41 и `#106` — вторая половина захода 4.** Манифест описывает все три корпуса
(`pp719_rules` 311 · `pp719` 1377 из 29 файлов · `verified_cases` 12 с исключением шаблонов),
читалка вынесена в `app/core/manifest.py`. Переиндексация стала частичной: `--doc <doc_type>`
грузит один документ, не трогая остальные. **Приёмка на живом Qdrant:** у 244 чужих точек
идентификаторы совпадают и payload не тронут. ⚠⚠ Главная тонкость оказалась не в удалении,
а в `avgdl`: он ЗАПЕКАЕТСЯ в sparse-вектор, и своя средняя длина документа сместила бы его
BM25 относительно соседей (корпусная 95.8 против 84.7 у Приказа №52 и 112.2 у сносок) —
ранжирование поехало бы у всех при правдоподобных числах.

⚠⚠ **Ревью захода 4 нашло 7 дефектов, и первый объясняет, почему остальных я не видел:**
батарея у меня зеленела только потому, что я запускал её с `PYTHONUTF8=1`, а документирован
запуск БЕЗ него — там те же 638 тестов давали 2 падения и 5 ошибок. Скрипты печатают
`⚠ ✅ → ⏭`, которых нет в cp1251, и падают на Windows в момент печати; CI на ubuntu этот
класс не видит вовсе. Заведён `app/core/console.enable_utf8()`, защищены 4 скрипта,
остальные 37 — зарегистрированный долг **#107** (список может только сокращаться).
⚠ Правка по ревью внесла свой дефект (гейт ломался там, где тега нет) — поймал CI,
четвёртое подтверждение правила «пакет правок по ревью — сам объект ревью».

⚠⚠ **`O3` #104 — новый для проекта класс дефекта: предохранитель ГОВОРИЛ, и его не слушали.**
CI стоял красным **шесть прогонов подряд** — с `35a7682` (21.08), и в этой красноте PR #103 уехал
в `main`, а `v0.5.0-test13` — на бой. Падал один тест: `TestNamedCodeBeatsScope` звал `embed_query`
и ЖИВУЮ коллекцию Qdrant, то есть перестал быть офлайновым. `tests.yml` предупреждает ровно об
этом («тест, которому понадобятся Qdrant или ключ, упадёт — и это правильный сигнал»). Боевой код
цел: `by_code` обходит и порог, и фильтр области структурно. ⚠ Сообщение падения ВРАЛО о причине,
а прежний тест имел только положительную половину — остался бы зелёным и при полностью удалённом
`case_fits_scope`. Сделано: тест на заглушках (5.5 с → 0.001 с) + **отрицательный контроль**
(мутационная проверка: каждое утверждение ловит снятие своего предохранителя) + **гейт CI в
`deploy.sh --dry-run`** (красная батарея останавливает выкатку; «проверить не смог» — предупреждение,
не провал) + 4 теста на сам гейт на подставном `gh`.
⏳ Отложено 24.08: путь «красный CI → `main`» остаётся открытым. ⚠ Branch protection на этом
аккаунте НЕДОСТУПЕН — и rulesets, и classic rules на приватном репо требуют организационного
Team/Enterprise («Not enforced» в интерфейсе). Бесплатные замены (issue при красном `main`;
`pre-push` хук при локальном merge) отложены решением владельца.

✅ **Письмо эксперту собрано** (`R29-4` #53 P0 по риску + `D13` #95): `docs/EXPERT_LETTER_CHECKLIST.md`
— готовое к отправке, владельцу остаётся вписать контакт и срок. Лист сверки получил **блок Д**:
300 позиций без порога в баллах, два класса (215 «улик нет вовсе» + 85 «величина есть, но не в
баллах»), распределение по 18 разделам, показательные позиции с уликой, полный перечень — отдельным
приложением. Отвечать поштучно не нужно, вопрос методический. Приоритет изменён: сначала блок **В**
(4 решения) — там требование приписано не той продукции, — потом А/XXI (26 позиций одним решением);
прежний порядок считал объём, но не цену ошибки. Классификатор зовётся **импортом**, а не копией
правил: вторая копия разбора порогов уехала бы наружу, эксперту.

✅ **`K8` #39 ЗАКРЫТ — состав корпуса перестал жить в коде.** `knowledge_base/manifest.yaml`:
паспорт (`authority`, `legal_force`, `doc_kind`, `topic`, `key_type`, `status`, `valid_from/to`,
`supersedes`) у **всех 311 пунктов**, словари допустимых значений проверяются на загрузке.
Ради чего задача и заводилась: утратившие силу «Правила выдачи заключения» исключались решением,
не записанным нигде, — теперь оно записано с датой и проверяется тестом. Два контура: загрузчик
не индексирует, `retriever.alive_only` отсекает на каждом запросе. ⚠ Фильтр `must_not`, а не
`must status=действует`: у точек прежней сборки поля нет, и `must` выключил бы процедурную ветку
целиком при выкатке без переиндексации. `A3` #58 закрыт как ненужный (паспорт относится к
ДОКУМЕНТУ, документов пять), остаток — #106 (товарный корпус и кейсы).

⚠⚠ **Сборка манифеста сразу нашла дефект привязки (#105): 13 пунктов раздела IV Правил были
подписаны разделом III.** Нарезка сложила два раздела в один файл, а раздел брался из ИМЕНИ файла.
Якорь печатается пользователю как источник, заголовок раздела уезжает в вектор; метрика атрибуции
`K9` меряет документ, не раздел, и поймать это не могла. Радиус по всем 311 записям: 13 сменили
раздел, потеряно 0.

⚠ **Следующая выкатка — С ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`** (`K8` и #105 меняют payload).

---

✅ **21.08.2026, заход 3: `A1` #56 и `K2` #47 ЗАКРЫТЫ. Тестов 587.**

`A1`: полоса «дефектов разбора порогов» **16.6–31.6 % → 0.0 %**. Классификация стала кодом, а не
цитатой: `scripts/classify_missing_thresholds.py` даёт каждой непокрытой позиции класс и УЛИКУ
(номер примечания, строка, дословная цитата). ⚠ Первая редакция классификатора была завышена
вдвое — разбор всех находок поимённо показал, что в 18 случаях примечание написано про ДРУГУЮ
продукцию под тем же кодом («Турбины на водяном паре» ловили порог строки «Главная
энергетическая установка» из прим. 17 про судостроение), а в 3 — это порог ШАССИ по разделу II.

`K2`: разбор знал ДВЕ формы примечания из шести. Разобраны остальные четыре — коды во вводной со
ступенями ниже (прим. 10, 18, 19, 26(2), 26(3), 71, 72), имя вместо кода (прим. 59, 60, 67–69),
«классифицируемой кодом … из CODE "ИМЯ" - N баллов» с подблоками «для <условие>» (прим. 27),
подзаголовок + таблица по узлам (прим. 37, 74). **Радиус: +28 позиций получили порог, у 3 он
сменился с группового на точный, 35 получили ВЕРНУЮ ссылку на подпункт (были «[прим. 26]» вместо
«[прим. 26.1]»), потеряно 0.** Покрытие `points`+`mixed` **53.1 % → 50.8 %**.

⚠⚠ **Четыре дефекта в СОБСТВЕННЫХ правках найдены замером радиуса, а не рассуждением:** график
выключателей уходил 11 чужим позициям («Реле управления», «Зажимы наборные», «Предохранители
плавкие») · прим. 72.1 «одномодовые волокна» отдавало порог многомодовым и преформам (три
подпункта делят один код) · порог насосов был бы неполон вдвое («Насосные агрегаты» 1300 против
«Насосы» 750) · дата отрывалась от своего числа. ⚠ Две промежуточные редакции правила БИЛИ ПО
ВЕРНОМУ КОДУ — снесли 18 законных групповых порогов и откатили память 26.20.22 на групповой порог.

✅ **`K6` #43 (бюджет контекста) СДЕЛАНА.** Хвост нецелевых кандидатов свёрнут в ОДИН блок:
после `EV7` у кандидата осталось лишь имя, код, якорь и ЗАПРЕТ — и запрет повторялся семь раз
дословно, занимая половину контекста. Замер: блоки кандидатов 3 454 символа из 6 890, медиана
хвоста (3 515) БОЛЬШЕ медианы целевого блока (1 965). Стало: контекст **6 890 → 5 034 (−27 %)**,
медиана −35 %, доля хвоста 50 % → 31 %, целевые блоки не тронуты, целевой хит всегда в начале.
Приёмка целиком: faithfulness 1.00 (427 чисел, 0 выдуманных) · отказ вне сферы 4/4 · порог 19/19 ·
баллы 134/134 · все восемь запретов §2.2 = 1.00 · детерминизм 0.80 (база 0.60) · гейт «без чужих
чисел» 1.00 (0 утечек из 50 при 210 доступных).

⚠⚠ **Побочно найден дефект МЕТРИКИ полноты, объясняющий загадку 19.08.** «Назвал источник
требований» падало на УТОЧНЯЮЩЕМ ответе (правило 1г: кода нет → перечисли кандидатов и попроси
код). Такой ответ требований не показывает вовсе, а метрика требовала назвать их источник — то
есть штрафовала за предписанное поведение и меряла РЕЖИМ ответа, а не атрибуцию. Отсюда и
«0.67, назавтра 1.00 на неизменном коде». Исправлено, стало 2/2.

❌ **`K3` #42 (контекстный заголовок чанка) ИЗМЕРЕНА И ОТВЕРГНУТА.** Три варианта, ни один не
прошёл критерий «`eval_coverage` и `eval_paraphrase` не хуже базы». Предпосылка не воспроизвелась:
на реалистичном классе «требование + наименование продукции» ретрив даёт 11/12 top-1. Имя группы
в векторе СДВИГАЕТ идентичность — кросс-секционные коллизии 9 → 6, но перефраз 0.565 → 0.551
(сломалось 7, починилось 2); требования группы в sparse делают сиблингов копиями родителя
(recall@1 0.943 → 0.936). ⚠⚠ Правка индекса бьёт шире списка изменённых записей. Отчёт —
[docs/eval_runs/2026-08-21_K3_contextual_header.md](docs/eval_runs/2026-08-21_K3_contextual_header.md).

**Критерий `K2` назначен и выполнен: дефектов привязки 0** (`--max-defect-share 0` + тест).
Эксперту (#95) вопрос сузился до 215 позиций «порога нет в первоисточнике» и 85 «модель не
балльная», каждая с уликой.

✅ **21.08.2026 закрыта ГИГИЕНА ЗАМЕРА — три долга фазы 0, тестов 533 → 551.**
`EV14` #98: заведён `docs/eval_baseline/` — гайд требовал его с первого прогона, а каталога не
существовало, поэтому «дельта ловит тихую деградацию» не работало вовсе. База обновляется
**только на релизном теге**: иначе каждый прогон сравнивается с предыдущим, и спуск на 0.02 за
раз шесть раз подряд читается как «без изменений».
`EV15` #99: предусловие «`verified_cases` выключены» не исполнялось ни разу — **решено замером,
а не спором**. `scripts/eval_cases_influence.py` (0 ₽): петля не срабатывает **ни разу** на
уровнях 0–2 и наборах гейта отказа (0 из 102) → предусловие исполняем, платного перезамера базы
не нужно; на детерминизме 2 из 10, эти числа станут несопоставимы (приемлемо — их и так
перестраивает `EV10`). У скрипта обязательный **положительный контроль**: 12 из 12 кейсов
находятся по своим вопросам, иначе он падает и чисел не печатает.
`O2` #100: эталонный скрипт выкатки переехал в репозиторий — `scripts/deploy/deploy.sh` + профиль
релиза + `checks/common/` (проверки, идущие на КАЖДОМ релизе: `D12` проверялась на `test11` и
исчезла в `test12` вместе со скриптом) + 18 тестов на предохранители + сухой прогон профиля.

⚠⚠ **Заведена `EV16` #102 — побочная находка замера.** Кейс эксперта о ЧУЖОЙ продукции проходит
порог релевантности: «металлорежущие станки с ЧПУ» → кейс по ОКПД2 26.20.13 «Машины
вычислительные электронные цифровые», dense **0.851** против законных 0.843. Сдвигом порога не
лечится — полоса калибровки перестала разделять. Число кейса при этом ЗАЗЕМЛЕНО для faithfulness
(`grounding = ctx + cases_ctx`), но ЧУЖОЕ для гейта детерминизма: две метрики трактуют одно
число по-разному.

⚠⚠ **Выкаченный тег `v0.5.0-test12` замера уровней 2–3 не имеет:** отчёт 20.08 снят на `71a03fb`,
после которого `thresholds.py` изменился на 162 строки правками `EV13` и уехал на бой. Оговорка
внесена в базу сравнения; закрывается прогоном захода 3.

**Версия `0.5.0`. На VM `130.193.36.174` развёрнут тег `v0.5.0-test13`** (21.08.2026, коммит
`2045193`): `prod` = `main` = тег, коллекции **`pp719` 1377** · `pp719_rules` **311** ·
`verified_cases` **12** (выкатка РАНТАЙМОВАЯ — корпус и индексатор не тронуты). **611 тестов.**

⚠ **Выкатка впервые прошла ЭТАЛОННЫМ СКРИПТОМ ИЗ РЕПОЗИТОРИЯ** (`scripts/deploy/deploy.sh`,
`O2` #100): бэкап снят до всего (4.8 МБ, 28 пользователей, 1262 сообщения, 297 оценок), маркеры
8+1 сверены, шесть общих проверок рантайма пройдены, коллекции не изменились.
⚠ И скрипт сразу нашёл дефект в себе: проверка страниц требовала ровно `200`, а `/` законно
отдаёт `302` на `/login` — предохранитель ударил по ВЕРНОМУ коду и объявил провал. Исправлен,
выкатка перезапущена и завершилась ✅ с нулём провалов.

**Проверено на боевой машине после выкатки:** лифты 28.22.16.111 → 120/135 [прим. 18] · счётчики
26.51.63.130 → прим. 19 · светодиоды `.216` → 39 баллов [прим. 71], `.211` → 35 [прим. 72] ·
магниты 25.99.29.110 → 70/270/900 [прим. 10] · контекст бульдозеров 12 275 → **9 958** символов,
хвост кандидатов ОДИН блок с ОДНИМ запретом · кейс о вычислительных машинах на «станки с ЧПУ»
отсечён (без окна 1, с окном 0).

⚠⚠ **`EV13` (#97): полное ревью дельты нашло 15 дефектов, четыре — в РАЗБОРЕ ПОРОГОВ, то есть
в числах, которые видит эксперт.** Прежний прогон ревью оборвался по лимиту сессии, отчитавшись
одним углом из десяти, и код уехал на бой непроверенным. Что чинилось: сборщик ступеней брал
любую строку со словами «не менее», и у прим. 17 в «Порог:» уезжал абзац прозы на 1402 символа
(там «не менее 5 ЛЕТ» — срок хранения документации) — «Танкеры морские» отдавали 1469 символов
текста требований, «газовозы» не получали порога вовсе · граница закрывающей кавычки не покрывала
« и » / « (» · перенос ячейки значения давал «Порог: … — от [прим. 39]» без величины · пустые
ячейки сдвигали колонки, и закупочный порог уезжал на год назад. **Радиус: +1 порог, потеряно 0,
изменено 8 — каждое сверено с первоисточником.**

⚠⚠ **Числа `K2` отозваны ВТОРОЙ раз за сутки: метрика, заведённая взамен непроверяемых «18.4 %»,
имела дефект того же класса** — засчитывала порогом любую непустую строку (проценты, счётные
величины, абзацы требований); 116 из 529 «покрытых» записей не содержали слова «балл». Честно:
points+mixed **58.8 % → 53.1 %** (−5.7 п.п.), полоса разделения **16.6 – 31.6 %**.
Отозваны 39.9 · 46.3 · 26.2 · 15.1 · 25.6 %.

_Прежняя выкатка:_ `v0.5.0-test11` (20.08.2026, `4851134`) — заходы 1 и 2 одним пакетом.
_Прежняя выкатка:_ `v0.5.0-test10` (18.08.2026, `5984839`) — `EV6`, `EV7` и пакет ревью PR #87.

**Проверено в рантайме на боевой машине (20.08):** `14.12.30.131` → «Спецодежда» (`14.12`,
раздел XVII) по иерархии, заглушек-обрывков в выдаче нет (`D12`) · ни одна позиция не получает
закупочный порог как общий (`K2`) · прим. 17: общий график 2025 → **3400**, оговорка про квотные
инвестпроекты вынесена отдельной строкой со своим условием · живой ответ по рыболовным судам
печатает оба графика раздельно, незаземлённых чисел 0 · `/ping`, `/help`, `/terms`, `/privacy`
отвечают.

**Замер перед выкаткой** ([docs/eval_runs/2026-08-20_zahod2.md](docs/eval_runs/2026-08-20_zahod2.md)):
recall@1 **0.944 ± 0.000** · невидимок 0 · кросс-секционных коллизий **9** (было 11) · code-hit
1366/1366 · faithfulness **1.00 ×3** · отказ вне сферы 4/4 ×3 · порог доехал 1.00 ×3 · **назвал
источник требований 1.00 ×3** (метрика, просевшая 19.08 до 0.67 и удалённая тогда из сводок) ·
все семь запретов §2.2 = 1.00 ×3 · гейт «без чужих чисел» **1.00** (0 утечек из 50).
Детерминизм 0.60–0.90 — долг `EV10` #91.

⚠ **Находка выкатки: шаг «бэкап боевой БД ДО всего» в прежних скриптах был фикцией.**
`backup_db.py --verify` требует ИМЯ ФАЙЛА; без него argparse печатал usage и копия НЕ создавалась.
Данные всё это время спасал только суточный cron (`O1`). Исправлено: скрипт зовёт
`backup_db.py --dir /data/backups --keep 14` и **прерывает выкатку**, если копия не снялась
(в этот раз снялась: 4.7 МБ, целостность ok, 28 пользователей, 1256 сообщений, 297 оценок).

⚠ **Две проверки-предохранителя в скрипте выкатки ложно сработали на ВЕРНОМ коде:** `grep` по имени
удалённой константы поймал её упоминание в комментарии, объясняющем удаление, а `\d` — двойку в
слове «ОКПД2». Первая остановила выкатку, вторая напугала assert'ом. Предохранитель, бьющий по
верному коду, дороже отсутствующего: он заставляет усомниться в правке. Обе уточнены.

**Что вошло в `v0.5.0-test10`.** Требования НЕЦЕЛЕВЫХ кандидатов больше не попадают в контекст
(остаются имя, код, якорь и строка-запрет): их баллы утекали в ответ о чужой продукции, а правило
промпта на боевом окне 8 не держало.

⚠⚠ **Ревью перед merge нашло 15 дефектов, и главный обесценивал сам отчёт `EV7`: метрика, которой
правка отчитывалась, после этой же правки перестала что-либо измерять.** Эталон «чужих чисел»
строился рендером `format_context`, а `EV7` из этого рендера требования кандидатов и убрал —
разность пуста ПО ПОСТРОЕНИЮ. Оракул переписан на ДАННЫЕ записи; в отчёт добавлен размер пула
(«сколько чисел было доступно к утечке»), без которого «1.00» не интерпретируется.

✅ **ПЕРЕЗАМЕР ЧЕСТНЫМИ ИНСТРУМЕНТАМИ (18.08) — правка подтвердилась:** гейт «без чужих чисел»
**1.00 ×4, ноль утечек из 200 прогонов при 209 доступных**; faithfulness **1.00** (422 числа, ноль
выдуманных); полнота 1.00 (порог 16/16, баллы 128/128, все запреты §2.2); отказ вне сферы 4/4;
контекст меньше на **53 %** (побочно закрыта половина `K6`). Детерминизм чисел 0.90 ± 0.08 —
остаётся долгом (мега-продукты). Отчёт — [docs/eval_runs/2026-08-18_review_PR87.md](docs/eval_runs/2026-08-18_review_PR87.md).

⚠ Прочее из ревью, закрытое там же: частичный код (`28.13` подходит 52 записям) обходил `EV7`
целиком — контекст выходил 15 799 символов против 6 194 сейчас; счётчик «(в базе N операц.)»
заземлял выдуманные числа; `EV7` стирал требования сиблингов СОБСТВЕННОЙ расколотой ячейки цели
(−7 операций на белых светодиодах); «кто целевой» решалось в ЧЕТЫРЁХ местах; псевдозапись раздела
могла стать целевой и оставить ответ без требований. Обоснование цены `EV7` («просьба сравнить
пришла с кодом, обе записи целевые») **опровергнуто** — `extract_okpd2` берёт только первый код
(issue #88). **455 тестов.**

**Суть `EV6` (#84):** целевой позицией ответа больше не становится строка-КВАЛИФИКАТОР расколотой
ячейки («Светодиоды (в части светодиодов белого диапазона)») при живом сиблинге той же группы.
⚠ Диагноз, с которым задача заводилась («пустая запись-дубль»), опровергнут сверкой с
первоисточником: это ОДНА ячейка требований, разъехавшаяся по трём строкам кода (`R29`), и данные
там верны настолько, насколько восстановимы; состав группы подтверждает эксперт (`R29-4`).
⚠ Числа гейта, которыми `EV6` и `EV7` отчитывались 17.08 (0.80 → 0.90 → 1.00), получены метрикой,
которую ревью признало пустой, — действительны только результаты перезамера 18.08 выше.

**Прогон `EVAL_GUIDE` (16.08) и пакет доработок по его итогам.** Уровни 0, 1, 2, 4 пройдены;
faithfulness **1.00 в трёх прогонах подряд** (1264 числа, ноль незаземлённых), отказ вне сферы
36/36, recall@1 0.940 ± 0.001, latency p50 8.3 с, ответ 0.34 ₽. Исправлено: ничья RRF решалась
случаем (тай-брейк в `retriever._order_key` — это и есть причина «неустранимого» разброса `M1`);
гард считал выдумкой числа из вопроса; добавлен второй сигнал out-of-scope по классу ОКПД2
(`app/rag/scope.py`); длинный перечень баллов печатает КОД (`pipeline.points_table`); заведены
метрика полноты (`scripts/eval_completeness.py`) и среда замера из снапшотов
(`scripts/eval_env.py`); ответ больше не упоминает использование кейса эксперта.
Открытые долги — `EV1`–`EV4` (issues #78–#81).

⚠ **Три урока о методике замеров** (подробно — в `EVAL_GUIDE` и отчёте прогона):
диагностический инструмент может врать (сравнение обрезанных якорей показало «ретрив стабилен»,
хотя нестабилен был именно он); **правило трёх прогонов относится и к детерминизму** — четыре
замера на неизменном коде дали 0.80 / 0.60 / 0.90 / 0.60; **доступность нельзя мерить через
рваный канал** — снаружи `/ping` уходил в таймаут, изнутри VM отвечал за 1.4 мс.

Июльская волна дала приёмку **33 % при гейте ≥70 %**. Причина диагностирована и устранена:
потолок задавала **полнота данных**, а не пайплайн — позиций без требований было 26 % корпуса
→ **4 %**, сверка баллов по записи 7 расхождений → **0** (`D9`: пороги узлов и вторых шкал
вернулись в корпус из первоисточника).

**Режим работ:** правки → `dev` → PR в `main` → тег → выкатка → `prod` перематывается на
выкаченный коммит (точка отката обязана указывать ровно на то, что крутится). Чек-лист выкатки —
[docs/REVIEW_TZ.md §1а](docs/REVIEW_TZ.md): там же ⚠ про файлы данных, которые обязаны попасть в
архив, и про переиндексацию — правка корпуса меняет payload и без неё до ответа не доедет.

_История:_ демо заказчику 2026-07-02; база актуализирована под ред. ПП 719 от 01.07.2026;
_Движок:_ в Qdrant 1381 точка (29 разделов приложения; **актуализация 2026-07-05 под ред. 01.07.2026** — VII медизделия +21 позиция + детектор 26.60.11.130, VIII фарма → балльная модель N 1392 с порогом ≥50), гибрид-поиск + буст по ОКПД2 + LLM-реранкер,
навигатор, `POST /navigate`, петля кейсов (`verified_cases`); ретрив измерен (recall@1 0.98,
MRR 0.988), ответ измерен (`eval_answers.py`: faithfulness ~1.0 + рантайм-постпроверка чисел;
тест-батарея P0/P1/P2 закрыта).
_Продукт мини-1.0_ (dev=`3cc9309`, 2026-07-01): **FastAPI + Jinja2 + ванильный JS** — вход/роли,
окно чата (ChatGPT-стиль: история диалогов, экспорт JSON/md/txt, поиск, сворачиваемый сайдбар),
лог диалогов, фидбек, `/admin` (дашборд KPI токены/₽/активность + логи); БД `app/db/`
(users/messages/feedback), auth bcrypt + сессии + RBAC (`user`/`expert`/`admin`) + «Запомнить меня»,
процедурный дефер-предохранитель; **юнит-тесты офлайн, зелёные**
(`.venv\Scripts\python -m unittest discover -s tests`)
⚠ **Запускать ИМЕННО так, без `PYTHONUTF8=1`.** С форсированным UTF-8 батарея зеленеет там,
где документированная среда даёт падения: скрипты печатают `⚠`/`✅`/`→`, которых нет в cp1251
(issue #107). CI на ubuntu этот класс не видит по той же причине — там UTF-8 по умолчанию.
⚠ **Пароли:** `seed_users.py` генерирует случайные и печатает в консоль. Дефолтного
`admin/admin` НЕТ — прежняя запись здесь была устаревшей и опасной.
_Сделано после демо (было «отложено»):_ стриминг ответа (SSE + фолбэк), history-aware мультитёрн
(окно 12 реплик + якорь-код диалога), корпус **Правил реестра** — `pp719_rules` = 242 пункта
(Правила + тело ПП №719 + Приказ ТПП РФ №52); дефер остался честным ФОЛБЭКОМ на случай сбоя.
**Цель `1.0` (решение 2026-06-29, уточнено 2026-06-30): мультиюзер веб-сервис «чат-ассистент
эксперта ТПП по 719»** — окно диалога с LLM (как ChatGPT). Основной юзер — **эксперт ТПП**; роли
`user`/`expert`/`admin` (до 6 человек, разные устройства). Ответ строго по базе (**приложение 719 +
Правила реестра**) с источниками и пометкой эксперта. UI — **FastAPI (auth + роли + сервинг) +
лёгкий веб-фронт** (Streamlit-форма вытеснена). Этапы **1.0.0–1.0.5**: фундамент/гигиена → Правила
реестра → ядро чата → RBAC/БД → качество + приёмка (≥70%) → деплой. Всё остальное
(валидатор/калькулятор, акты, масштаб) — `2.0`+.
`APP_VERSION` держим `0.5.0` до деплоя + приёмки эксперта ≥70% (это гейт `1.0`).
**СЛЕД. ШАГ — доделать доработки локально (см. [docs/REVIEW_TZ.md](docs/REVIEW_TZ.md)) → пакетный
редеплой с переиндексацией → контрольная волна тестирования для повторного замера приёмки.**
Запуск: [docs/LAUNCH.md](docs/LAUNCH.md). Детальный план по этапам и концепция — в **ROADMAP.md** (§1.0).

---

## Ветки и рабочий процесс

**Модель:** `prod` (развёрнуто на сервере — FF из main/dev при деплое) · `main` (стабильная, её
тестирует эксперт — теги `vX.Y`) · `dev` (интеграция, рабочая ветка по умолчанию) · `exp/<тема>`
(выбрасываемые эксперименты с моделями/UI/функционалом).
Поток: `exp/* → dev` (прямой merge) → `dev → main` (**через PR** + `/code-review`) → `main → prod`
(FF при деплое). Эксперименты изолируем через **git worktree** (своя папка, свой `.env`).
_(2026-07-02: main перемотан на dev, prod создан из dev — первый релиз-синк; см. docs/BRANCHING.md.)_

⚠️ В эксперименте задавай своё `QDRANT_COLLECTION` (напр. `pp719_exp_*`), иначе `load_kb.py`
затрёт рабочую `pp719`. Автопуш-хук пушит текущую ветку (`HEAD`), но **`exp/*` не пушит**.
Полностью — в **[docs/BRANCHING.md](docs/BRANCHING.md)**.

---

## Как продолжить работу на новом устройстве
```bash
git clone https://github.com/pan3v1n/navigator-719.git
cd navigator-719
py -3 -m venv .venv
.venv\Scripts\pip install -r requirements.txt
copy .env.example .env
# Заполнить .env секретами
# Запустить Qdrant: docker run -p 6333:6333 qdrant/qdrant
.venv\Scripts\python scripts/load_kb.py   # загрузить базу знаний
.venv\Scripts\python main.py              # запустить сервис
```

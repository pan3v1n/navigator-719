# Зависимости, которые НЕ нужны сервису в рантайме (R22).
# Ставить поверх основных:  pip install -r requirements.txt -r requirements-dev.txt
#
# Почему отдельным файлом. Прод-образ собирается из requirements.txt, и всё лишнее в нём — это
# вес, время сборки и лишняя поверхность зависимостей. Здесь то, что нужно только разработчику.
#
# Тесты сюда НЕ входят: они на стандартном `unittest` и запускаются без единой доп. зависимости —
#   .venv\Scripts\python -m unittest discover -s tests

# Ранний MVP-интерфейс (форма) — frontend/streamlit_app.py. Вытеснен веб-чатом 1.0, но оставлен
# как рабочий запасной путь и демонстрация движка без auth/БД.
#   .venv\Scripts\streamlit run frontend/streamlit_app.py
streamlit==1.39.0

# Профиль релиза v0.5.0-test13 (21.08.2026) — заход 3.
#
# Вошли: A1 #56 (классификация непокрытых порогов), K2 #47 (четыре формы примечаний),
# K3 #42 (измерена и ОТВЕРГНУТА — в коде ничего), K6 #43 (хвост кандидатов свёрнут),
# EV16 #102 (кейс о чужой продукции отсечён), EV10 #91 (метрика детерминизма разделена),
# плюс гигиена замера: EV14 #98, EV15 #99, O2 #100.
#
# ⚠ ВЫКАТКА РАНТАЙМОВАЯ. Корпус и индексатор НЕ тронуты (`git diff` по knowledge_base/ и
# scripts/load_kb.py пуст), поэтому SNAPSHOT пуст и счётчики коллекций обязаны ОСТАТЬСЯ
# прежними. Если они изменятся — это ошибка, а не ожидаемый результат.

TAG="v0.5.0-test13"

EXPECT=([pp719]=1377 [pp719_rules]=311 [verified_cases]=12)

SNAPSHOT=""
SNAPSHOT_COLLECTION="pp719"

# Маркеры распакованного кода: файл|шаблон|описание. Шаблон ловит ОПРЕДЕЛЕНИЕ, а не упоминание.
MARKERS=(
  "app/rag/thresholds.py|^def _intro_codes|K2: коды во вводной примечания (прим. 18, 19, 71, 72)"
  "app/rag/thresholds.py|^def _intro_name|K2: имя продукции во вводной вместо кода (прим. 67-69)"
  "app/rag/thresholds.py|^def _qualified_list_rows|K2: форма прим. 27 с подблоками «для …»"
  "app/rag/thresholds.py|^def _named_block_rows|K2: подтаблицы по узлам (прим. 37, 74)"
  "app/rag/thresholds.py|^_NOTE_START_RE|K2: подпункт «26.1» — самостоятельное примечание"
  "app/rag/pipeline.py|ПРОЧИЕ КАНДИДАТЫ ОКНА|K6: хвост кандидатов свёрнут в один блок"
  "app/core/prompts.py|ПРОЧИЕ КАНДИДАТЫ ОКНА|K6: правило 3в цитирует новый заголовок"
  "app/rag/retriever.py|^def case_fits_scope|EV16: кейс о чужой продукции отсекается"
)

# Анти-маркеры: код, которого в пакете быть НЕ должно.
ANTI_MARKERS=(
  "app/rag/pipeline.py|Требования этой позиции НЕ ПОКАЗАНЫ|старый блок кандидата (до K6)"
)

"""EV13 (#97): у порога есть ВЕЛИЧИНА, а не предлог «от».

Перенос ячейки ЗНАЧЕНИЯ давал «Порог: … — от [прим. 39]» без числа (5 позиций).
"""
from app.rag.thresholds import lookup_threshold

v = lookup_threshold(["28.30.59.114"], "Подборщики для зерновых", None)
print("   ", (v or "")[:100])
assert v and "40 процентов" in v and "60 процентов" in v, "значение снова обрезано до «от»"
print("   OK: перенесённые ячейки значений собраны")

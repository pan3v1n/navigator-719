# ROADMAP — Навигатор ПП РФ №719

> Обновляй этот файл при старте и завершении каждой задачи.
> Статусы: `[ ]` — не начато · `[~]` — в работе · `[x]` — готово
>
> 💡 Сырые идеи и гипотезы по доработкам (ещё не принятые в фазы) — в
> [docs/IDEAS.md](docs/IDEAS.md). Созревшую идею переносим оттуда сюда.

---

## V0 — Фундамент ✅ _завершено_
**Цель**: репозиторий настроен, контекст задокументирован, среда запускается  
**Срок**: 1–2 дня  

- [x] Создать репозиторий на GitHub ([pan3v1n/navigator-719](https://github.com/pan3v1n/navigator-719))
- [x] CLAUDE.md — полный контекст проекта
- [x] ROADMAP.md — план фаз
- [x] `.env.example`, `.gitignore`, `requirements.txt` (скелет)
- [x] Базовая структура папок (`app/`, `knowledge_base/`, `scripts/`)
- [x] `main.py` — заглушка FastAPI с healthcheck `/ping`
- [x] Установить зависимости, проверить `GET /ping` локально
- [x] Загрузить и распарсить ПП №719 в `knowledge_base/pp719/` (16 секций, 2.96 МБ текста)
- [x] Документация: README.md, SETUP.md, WORK_ON_OTHER_PC.md, docs/V1_SPEC.md, docs/BUDGET.md
- [x] Автопуш-хук (`post-commit`) настроен на текущем ПК
- [x] Проверить: клонировать репо на втором устройстве и запустить _(2026-06-15: развёрнуто на 2-м ПК, `GET /ping` → ok)_

**Критерий готовности**: `GET /ping` → `{"status": "ok"}` ✅, репо на GitHub ✅

---

## V1 — RAG-навигатор ✅ _MVP готов (код 1.0–1.5), ожидает приёмочный тест с экспертом_
**Цель**: пользователь описывает товар → система находит применимую группу по 719,
показывает минимальный балл и ключевые требования  
**Срок**: 4–6 недель  
**📋 Полная спецификация**: [docs/V1_SPEC.md](docs/V1_SPEC.md)  
**Статус**: пункты 1.0–1.4 готовы; 1.5 — инфраструктура петли кейсов готова, осталось
провести тест с экспертом ТПП (5–10 кейсов, критерий ≥70%). Запуск — [docs/LAUNCH.md](docs/LAUNCH.md).  

### 1.0 Инфраструктура (Спринт 1) ✅
- [x] Поднять Qdrant в Docker (:6333, данные в `D:/qdrant_storage`, autorestart)
- [x] `app/rag/embeddings.py` — обёртка над `multilingual-e5-large` (e5, префиксы query/passage, L2-норма)
- [x] Проверка: Qdrant отвечает (v1.18.2), e5 даёт векторы (dim 1024), сквозной поиск e5→Qdrant ранжирует верно

### 1.1 База знаний — Спринт 2 (структуризация) + Спринт 3 (индексация) ✅
- [x] `app/core/prompts.py` — системный промпт парсера записей (роль, схема JSON, правила)
- [x] `scripts/structure_kb.py` — пайплайн: чанк раздела → регулярки режут на «кандидатов-записей» → DeepSeek нормализует в JSON (есть `--dry-run`, `--limit`, отчёт стоимости)
- [x] Пилот: раздел II → `structured/II_avtomobilestroenie.json` (4 продукта, 206 блоков, 698 операций). Стоимость **¥0.57** (21 вызов). Качество ОК; баллы, зависящие от категории ТС, хранятся как `points:null` + полный текст в `text` (доразбор в V2). Артефакт таблицы: общая «менюшка» компонентов автопрома прицеплена к продукту-автобусы — для навигации V1 не критично.
- [x] Батч всех разделов завершён (I, III, IV, VI, VII, VIII, IX, X). **Вся база структурирована: 9 разделов, 1601 продукт.** Факт. стоимость ≈ ¥11.5.
- [x] Контроль качества: `scripts/verify_structured.py` (сверка баллов JSON ↔ исходник, ловит галлюцинации и потери). Нашёл: галлюцинации баллов на пустых блоках (модель дофантазировала операции). Чисто сразу: I, II, VI, VIII, IX.
- [x] Фиксы: (1) промпт ужесточён (дословность, запрет округлять баллы, null вместо догадки, пороги → min_threshold); (2) **в `structure_kb.py` — пропуск LLM для продуктов с пустым блоком требований** (главный фикс); (3) санитизация `points<=0 → null`; (4) verify понимает дробные баллы «0,5». VII перепрогнан — **чисто ✅** (было 47 галлюцинаций → 0).
- [x] Перепрогнаны III, IV (галлюцинации → 0). X: вскрыта структурная потеря баллов в парсере (брал только `fields[2]`, терял столбцы пороговых баллов по датам). **Фикс парсера**: распознавание заголовков таблиц-порогов + захват всех колонок + склейка в `min_threshold` с привязкой к датам. Прозовые пороги раздела (потолки НИОКР, ступенчатые минимумы) — в отдельной записи-методичке (`record_type: section_methodology`, 393 строки дословно). **`verify_structured.py` по всем 9 → ✅** (галлюцинаций 0, потерь по таблицам/операциям 0).
- [x] Ускорение: `--workers N` (параллельная отправка в DeepSeek, ~7× быстрее) + прогресс-бар `tqdm`.
- [x] **Спринт 3 — индексация.** `scripts/load_kb.py`: JSON → Qdrant (1602 точки = 1601 продукт + методичка X). Гибрид: dense e5-large (1024d, косинус) + sparse BM25. `app/rag/sparse.py` — локальный BM25 (mmh3 + Snowball-стемминг, без скачивания моделей) с TF-сатурацией и нормировкой по длине (k1=1.2, b=0.75; IDF считает Qdrant через `Modifier.IDF`). Payload = вся структура + индексы по `okpd2_codes`/`section_roman`.
- [x] **Smoke-тест гибридного поиска (RRF)** ✅: сварка кузова → [II] Средства транспортные (1.0); чиллеры → [X] ККБ (1.0); фарма → [VIII] Препараты; станки → [I] Станки токарные. Нормировка BM25 по длине убрала доминирование мега-записей («Автобусы», методичка).
- [x] Производительность (CPU, без GPU): все 18 потоков + батч энкодера 64; диск-кэш эмбеддингов (`.emb_cache/`) — повторная индексация считает только изменённые чанки (проход из кэша 0.02с vs 2.5с).

### 1.2 RAG-пайплайн ✅
- [x] `app/rag/retriever.py` — гибрид-поиск (dense+sparse, RRF) + иерархический буст/фильтр по ОКПД2 (`okpd2_match`, подстраховка MatchAny по префиксам кода). Возвращает `Hit` с payload.
- [x] `app/rag/pipeline.py` — запрос → гибрид-поиск → контекст (с усечением операций) → промпт → ответ DeepSeek. CLI-смоук: `python -m app.rag.pipeline "вопрос" [ОКПД2]`.
- [x] `app/core/prompts.py` — `NAVIGATOR_SYSTEM_PROMPT` («помощник эксперта Курской ТПП», строго по контексту, не выдумывать баллы).
- [x] Обязательная пометка в каждом ответе (`EXPERT_DISCLAIMER`, гарантируется `_ensure_disclaimer`). Сквозной тест: прицепы/29.20.23 → раздел III поз.62, ответ обоснован контекстом.

### 1.3 Навигатор ✅
- [x] `app/tools/navigator.py` — извлечение ОКПД2 из текста (`extract_okpd2`) + вызов RAG-пайплайна; возвращает `Navigation` (ответ, источники, чек-лист, пометка).
- [x] API endpoint `POST /navigate` (`app/api/routes.py` + `schemas.py`, подключён к `main.py`; sync-обработчик → пул потоков). Тест TestClient: 200, источники, чек-лист; пустой query → 422.
- [x] Базовый чек-лист документов (`build_checklist`): база ТПП + условные пункты (расчёт баллов при `points/mixed`, подтверждение порога при `min_threshold`, права на КД/ТД).

### 1.4 Интерфейс MVP ✅
- [x] `frontend/streamlit_app.py` — Streamlit UI: форма (описание + код ОКПД2 + число позиций) → анализ, источники, чек-лист, пометка эксперта. Вызывает `navigate()` in-process (API-сервер не нужен), кэш `st.cache_data`. Запуск: `.venv/Scripts/streamlit run frontend/streamlit_app.py`. Headless-проверка: HTTP 200, health ok.
- [x] Отображение источника: раздел, наименование, коды ОКПД2, порог, ключевые операции с баллами, `source_anchor`; бейдж «совпадение по коду».

### 1.5 Тестирование с экспертом
- [x] **Петля кейсов (инфраструктура готова):** формат кейса + `knowledge_base/cases/README.md` + `_example.json`; коллекция `verified_cases` (гибрид); `scripts/seed_cases.py` (загрузка/переиндексация, файлы `_*` пропускаются); `retriever.search_cases` + интеграция в pipeline — подтверждённые кейсы идут в контекст с **высшим приоритетом** (правило 1а промпта). Сквозной тест: кейс эксперта учитывается в ответе. _Реальные кейсы — за экспертом ТПП._
- [ ] 5–10 реальных кейсов с экспертом ТПП → файлы в `knowledge_base/cases/` → `seed_cases.py`
- [ ] Замер точности на проверенных кейсах

**Критерий готовности**: эксперт ТПП проверил 10 кейсов, точность ≥ 70%

---

## V2 — Валидатор документов и калькулятор баллов
**Цель**: предприятие загружает документы → система проверяет комплектность,
считает набранные баллы и указывает на пробелы  
**Срок**: 6–10 недель после V1  

### 2.1 Загрузка документов
- [ ] Поддержка PDF, DOCX, изображений (сканов)
- [ ] Извлечение текста (pdfplumber, python-docx)
- [ ] OCR для сканов (easyocr — работает локально, без внешних API)

### 2.2 Валидатор
- [ ] `app/tools/validator.py` — сравнение загруженных документов с чек-листом группы
- [ ] Вывод: ✓ есть / ✗ отсутствует / ⚠ требует уточнения
- [ ] API endpoint `POST /validate`

### 2.3 Калькулятор баллов
- [ ] Парсинг балльных таблиц из приложений к 719
- [ ] Расчёт набранных баллов по указанным операциям
- [ ] Прогноз: хватает ли баллов для признания российским

### 2.4 База кейсов — петля обратной связи
- [ ] Форма для эксперта: «исправить вердикт AI» → запись в `knowledge_base/cases/`
- [x] Скрипт переиндексации `scripts/seed_cases.py` (+ коллекция `verified_cases`, приоритет кейсов в pipeline) — сделано авансом в V1
- [ ] Дашборд: частые ошибки AI по типам товаров

**Критерий готовности**: валидатор проверен на 20+ реальных пакетах документов

---

## V3 — Генератор актов экспертизы
**Цель**: система формирует черновик акта экспертизы ТПП на основе введённых данных,
эксперт редактирует и подписывает  
**Срок**: 6–8 недель после V2  

### 3.1 Шаблоны актов
- [ ] Загрузить типовые шаблоны актов ТПП в `knowledge_base/templates/`
- [ ] Параметризация: поля для подстановки данных предприятия

### 3.2 Генератор
- [ ] `app/tools/act_gen.py` — генерация черновика акта по данным из навигатора/валидатора
- [ ] Явная пометка «ЧЕРНОВИК — требует проверки и подписи уполномоченного эксперта»
- [ ] Экспорт в DOCX (python-docx)

### 3.3 Кабинет эксперта
- [ ] Список заявок на экспертизу
- [ ] Просмотр и редактирование черновика акта
- [ ] Фиксация итогового вердикта (одобрен / отклонён / доработка)

**Критерий готовности**: 5 реальных актов подготовлено через систему и подписано экспертами

---

## V4 — Масштабирование (будущее)
**Цель**: расширение на другие нормативные базы и сценарии  
**Срок**: по результатам V3  

- [ ] Добавить форс-мажорные обстоятельства (статья 1 ГК, процедура ТПП)
- [ ] Добавить Статью 1 / процедуры подтверждения обстоятельств непреодолимой силы
- [ ] Мобильное приложение или Telegram-бот для предприятий
- [ ] Интеграция с ГИСП (Государственная информационная система промышленности)
- [ ] Многопользовательский режим: предприятие + эксперт + администратор ТПП
- [ ] Аналитика: отчёты для руководства ТПП

---

## Лог изменений

| Дата | Версия | Что сделано |
|---|---|---|
| 2026-06-15 | V0 | Инициализация репозитория, CLAUDE.md, ROADMAP.md |
| 2026-06-15 | V0 | Парсинг ПП №719 (16 секций), репо на GitHub, автопуш |
| 2026-06-15 | V0 | Документация: README, SETUP, гайд по 2-му ПК, спец V1, бюджет |
| 2026-06-15 | V0 | ✅ Фаза V0 закрыта. Следующее — V1, Спринт 1 (Qdrant + эмбеддинги) |
| 2026-06-15 | V0 | Развёртывание на 2-м ПК: Python 3.12, venv, зависимости, `GET /ping` → ok |
| 2026-06-15 | V1 | Старт Спринта 1: Qdrant в Docker + `app/rag/embeddings.py` (e5-large) |
| 2026-06-15 | V1 | ✅ Спринт 1 закрыт: WSL2 + Docker, Qdrant на :6333, e5-large (1024d), сквозной тест OK. Следующее — Спринт 2 (структуризация базы знаний) |
| 2026-06-15 | V1 | План на завтра уточнён: пилот `structure_kb.py` на разделе II → батч → `load_kb.py` → smoke retrieval |
| 2026-06-16 | V1 | Спринт 2 старт: `prompts.py` + `structure_kb.py` (регулярки + DeepSeek JSON). Пилот раздела II ОК — 4 продукта, 206 блоков, ¥0.57. Полный парсинг базы оценён ≈¥10. Дальше — батч разделов I, III, IV, VI–X |
| 2026-06-16 | V1 | Спринт 2: вся база структурирована (9 разделов, 1601 продукт) в `structured/*.json`. Факт. ≈¥11.5. Добавлен `verify_structured.py` (контроль галлюцинаций) |
| 2026-06-16 | V1 | Контроль качества вскрыл галлюцинации баллов (пустые блоки → модель фантазировала). Фиксы: строгий промпт + пропуск LLM на пустых блоках + 0→null. VII перепрогнан → чисто. **ОСТАЛОСЬ перепрогнать III, IV, X + финальная verify, потом `load_kb.py`** (пауза для смены сессии) |
| 2026-06-16 | V1 | III, IV перепрогнаны → чисто. X: найдена структурная потеря в парсере (многоколоночные таблицы пороговых баллов по датам — брался лишь 1 столбец). Фикс парсера (заголовки этапов + все колонки + склейка в `min_threshold`). Прозовые пороги раздела (393) — в записи-методичке `section_methodology`. Добавлены `--workers` (параллель ~7×) и прогресс-бар. **verify по всем 9 → ✅ 9/9.** Дальше — `load_kb.py` |
| 2026-06-16 | V1 | **Спринт 3 — индексация ✅.** `load_kb.py` загрузил 1602 точки в Qdrant (гибрид dense e5 + sparse BM25). `app/rag/sparse.py` — локальный BM25 (mmh3+Snowball, без скачивания), нормировка по длине убрала доминирование мега-записей. Smoke-тест RRF релевантен (сварка→II, чиллеры→ККБ, фарма→VIII). Оптимизация CPU: 18 потоков + кэш эмбеддингов. Дальше — 1.2 RAG-пайплайн (retriever + фильтр ОКПД2) |
| 2026-06-18 | V1 | **Eval-харнес (стадия 0)** + **out-of-scope guard (стадия 1)** + юнит-тесты (`tests/`). База: recall@1=0.77, recall@3=1.0, MRR=0.878. Guard двухслойный (порог dense top-1 + правило 1б промпта). |
| 2026-06-18 | V1 | **🔴→✅ Исправлен дефект нарезки приложения.** Регексп заголовков в `parse_rtf.py` обрывался на X (`I..X`) + лимит длины заголовка → разделы **XI–XXIX склеивались в «X»** (851 чужая запись с ярлыком «стройматериалы»), раздел **V терялся**. Новый `scripts/rechunk_appendix.py` (из `pp719_full.txt`, римские любой длины, граница последнего раздела) → нарезал все 29 разделов. Перепарс V/X/XI–XXIX (`structure_kb`, ≈¥5), `verify` 20/21 чисто (XXIX ⚠ — пороги вариантов кранов, V2). Реиндекс: **1681 точка, 29 разделов, 1672 продукта**. **recall@1 0.77→0.86, MRR 0.878→0.925**, атрибуция разделов корректна (локомотивы→XII, не «X»). Golden set 30→46. Остаток промахов recall@1 — пересечение оборудования с XXIX (нефтегаз) → реранкер (стадия 2). |
| 2026-06-18 | V1 | **🔴→✅ Глобальная сверка чанков с исходником — ещё 3 дефекта данных.** (1) **XXIX вбирал примечания приложения** (пункты 1–80 после `Примечания:`) → 296 фантомных «продуктов» (краны, медизделия, кресла-коляски) вместо **9** реальных сейсмо/геофиз; граница раздела теперь на `Примечания:`, блок вынесен в `chunks/130_PRIMECHANIYA` (→ doc_type appendix_notes, отдельно). (2) **`05_IV` содержал ВЕСЬ раздел V** (+67 задвоенных) → IV перерезан из исходника, **37** чистых. (3) **Потеря 1-го продукта** при склейке заголовка без `|` (XXIX «Морской геофизкомплекс», V «Электр.машина») → фикс `iter_rows` (`ROW_START_RE`, не рвёт многострочные коды). Сверка: 28/29 чанков == исходник. Перепарс IV/V/XXIX (≈¥0.85), verify ✅. Реиндекс **1681→1325 точек** (−356 фантомов/дублей), **1315 продуктов**. **recall@1 0.86→0.95, MRR 0.925→0.976** (recall@3=1.0). Остаток 2 промаха — реальная семантика (экскаваторы→VI, стреловые краны→XVIII) → реранкер. |

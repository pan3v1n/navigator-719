# ROADMAP — Навигатор ПП РФ №719

> Обновляй этот файл при старте и завершении каждой задачи.
> Статусы: `[ ]` — не начато · `[~]` — в работе · `[x]` — готово
>
> 💡 Сырые идеи и гипотезы по доработкам (ещё не принятые в фазы) — в
> [docs/IDEAS.md](docs/IDEAS.md). Созревшую идею переносим оттуда сюда.

---

## Версионирование продукта

Архитектурное решение (2026-06-29, уточнено 2026-06-30): продукт идёт к одной чёткой цели —
**рабочему мультиюзер веб-сервису «чат-ассистент эксперта ТПП по ПП №719»** в формате окна диалога
с LLM (как ChatGPT и аналоги).

- **Текущая стадия — `0.5.0`.** Движок навигатора (гибрид-RAG по приложению 719) построен и измерен
  (recall@1 0.98, MRR 0.988 с реранкером; тест-батарея качества ответа P0/P1/P2 закрыта), а поверх
  него **собран и отполирован продукт мини-1.0** — веб-чат (FastAPI + Jinja2 + JS: роли, история
  диалогов, экспорт, логи/фидбек, админ-дашборд; 42 теста + e2e). До полного `1.0` осталось:
  **развернуть** (docker-compose на РФ-облаке) + **приёмка экспертом ≥70%**; отложены стриминг,
  history-aware мультитёрн, полный корпус Правил реестра. → стадия `0.5.0`.
- **`1.0` — рабочий продукт: мультиюзер чат-ассистент эксперта ТПП по 719.** Многоходовой веб-диалог,
  ответ строго по базе (приложение 719 + Правила реестра) с источниками и пометкой эксперта;
  до 6 юзеров с ролями `user`/`expert`/`admin`, развёрнут и принят экспертом (≥70%).
- **`2.0` и далее — всё остальное:** валидатор документов + калькулятор баллов и вердикт (`2.0`),
  генератор актов экспертизы (`3.0`), масштабирование на другие нормбазы / ГИСП-интеграция (`4.0`).
- **Принцип нумерации:** всё, что не входит в «работающий чат-ассистент по 719», — это `2.0+`.

Путь версий: `0.1` фундамент → `0.2–0.5` движок навигатора → **`1.0` чат-ассистент (цель этапа)**
→ `2.0` валидатор/калькулятор → `3.0` акты → `4.0` масштаб. `APP_VERSION` в `app/core/config.py`.

---

## 0.1 — Фундамент ✅ _завершено_
**Цель**: репозиторий настроен, контекст задокументирован, среда запускается
**Срок**: 1–2 дня

- [x] Создать репозиторий на GitHub ([pan3v1n/navigator-719](https://github.com/pan3v1n/navigator-719))
- [x] CLAUDE.md — полный контекст проекта
- [x] ROADMAP.md — план фаз
- [x] `.env.example`, `.gitignore`, `requirements.txt` (скелет)
- [x] Базовая структура папок (`app/`, `knowledge_base/`, `scripts/`)
- [x] `main.py` — заглушка FastAPI с healthcheck `/ping`
- [x] Установить зависимости, проверить `GET /ping` локально
- [x] Загрузить и распарсить ПП №719 в `knowledge_base/pp719/` (16 секций, 2.96 МБ текста)
- [x] Документация: README.md, SETUP.md, WORK_ON_OTHER_PC.md, docs/V1_SPEC.md, docs/BUDGET.md
- [x] Автопуш-хук (`post-commit`) настроен на текущем ПК
- [x] Проверить: клонировать репо на втором устройстве и запустить _(2026-06-15: развёрнуто на 2-м ПК, `GET /ping` → ok)_

**Критерий готовности**: `GET /ping` → `{"status": "ok"}` ✅, репо на GitHub ✅

---

## 0.2–0.5 — Движок навигатора (гибрид-RAG) ✅ _готов, ожидает упаковки в продукт 1.0_
**Цель**: пользователь описывает товар → система находит применимую группу по 719,
показывает минимальный балл и ключевые требования
**📋 Полная спецификация**: [docs/V1_SPEC.md](docs/V1_SPEC.md)
**Статус**: ретрив-движок готов и измерен (recall@1 0.95). Это **ядро будущего продукта 1.0** —
поверх него строится чат-интерфейс и упаковка в рабочий сервис (см. раздел 1.0).
Приёмочный тест с экспертом перенесён в `1.0` (это критерий готовности продукта, а не движка).

### Спринт 1 — Инфраструктура ✅
- [x] Поднять Qdrant в Docker (:6333, данные в `D:/qdrant_storage`, autorestart)
- [x] `app/rag/embeddings.py` — обёртка над `multilingual-e5-large` (e5, префиксы query/passage, L2-норма)
- [x] Проверка: Qdrant отвечает (v1.18.2), e5 даёт векторы (dim 1024), сквозной поиск e5→Qdrant ранжирует верно

### Спринт 2–3 — База знаний (структуризация + индексация) ✅
- [x] `app/core/prompts.py` — системный промпт парсера записей (роль, схема JSON, правила)
- [x] `scripts/structure_kb.py` — пайплайн: чанк раздела → регулярки режут на «кандидатов-записей» → DeepSeek нормализует в JSON (есть `--dry-run`, `--limit`, отчёт стоимости)
- [x] Вся база структурирована: 29 разделов приложения. Контроль качества: `scripts/verify_structured.py` (сверка баллов JSON ↔ исходник, ловит галлюцинации и потери).
- [x] Фиксы качества: строгий промпт (дословность, null вместо догадки), пропуск LLM на пустых блоках требований, санитизация `points<=0 → null`, многоколоночные таблицы пороговых баллов по датам (раздел X), запись-методичка (`section_methodology`). Ускорение: `--workers N` + прогресс-бар.
- [x] **Индексация.** `scripts/load_kb.py`: JSON → Qdrant. Гибрид: dense e5-large (1024d, косинус) + sparse BM25. `app/rag/sparse.py` — локальный BM25 (mmh3 + Snowball, без скачивания моделей; k1=1.2, b=0.75; IDF — Qdrant через `Modifier.IDF`). Нормировка BM25 по длине убрала доминирование мега-записей.
- [x] Производительность (CPU): 18 потоков + батч энкодера 64; диск-кэш эмбеддингов (`.emb_cache/`).
- [x] **Глобальная сверка чанков с исходником** вскрыла и устранила серьёзные дефекты нарезки приложения (XI–XXIX склеивались в X; V терялся; XXIX вбирал примечания → 296 фантомных «продуктов»; IV содержал весь V; потеря 1-го продукта раздела). **recall@1 0.77 → 0.86 → 0.95, MRR → 0.976.** Текущее состояние: 1285 точек / 1275 продуктов / 29 разделов (после чистки 68 исключённых позиций приложения, см. 1.0.0).

### Спринт 4 — RAG-пайплайн ✅
- [x] `app/rag/retriever.py` — гибрид-поиск (dense+sparse, RRF) + иерархический буст/фильтр по ОКПД2 (`okpd2_match`, подстраховка MatchAny по префиксам). Возвращает `Hit` с payload.
- [x] `app/rag/pipeline.py` — запрос → гибрид-поиск → контекст (с усечением операций) → промпт → ответ DeepSeek. CLI-смоук.
- [x] `NAVIGATOR_SYSTEM_PROMPT` («помощник эксперта Курской ТПП», строго по контексту, не выдумывать баллы) + обязательная пометка (`EXPERT_DISCLAIMER`, гарантируется `_ensure_disclaimer`).
- [x] **Eval-харнес** (`scripts/eval_retrieval.py`, golden set 46 кейсов) + **out-of-scope guard** (порог dense top-1 + правило 1б промпта) + юнит-тесты (`tests/`).

### Спринт 5 — Навигатор и интерфейс (форма) ✅
- [x] `app/tools/navigator.py` — извлечение ОКПД2 из текста + вызов RAG-пайплайна; возвращает `Navigation` (ответ, источники, чек-лист, пометка). Базовый чек-лист (`build_checklist`).
- [x] API endpoint `POST /navigate` (`app/api/`, подключён к `main.py`; sync → пул потоков). Тест TestClient.
- [x] `frontend/streamlit_app.py` — **форма** (описание + код ОКПД2 + число позиций) → анализ, источники, чек-лист, пометка. _Это MVP-интерфейс; в 1.0 заменяется на диалоговое окно (см. 1.0.A)._

### Спринт 6 — Петля кейсов (инфраструктура) ✅
- [x] Формат кейса + `knowledge_base/cases/README.md` + `_example.json`; коллекция `verified_cases` (гибрид); `scripts/seed_cases.py`; `retriever.search_cases` + интеграция в pipeline (приоритет кейсов, правило 1а промпта). _Наполнение реальными кейсами — за экспертом ТПП._

**Критерий движка**: гибрид-поиск находит верный раздел (recall@1 ≥ 0.9 на golden set) ✅

---

## 1.0 — Чат-ассистент эксперта ТПП по ПП №719 (мультиюзер, веб) 🎯 _цель текущего этапа_
**Цель**: рабочий внутренний веб-сервис Курской ТПП — **окно диалога с LLM** (как ChatGPT):
сотрудники ведут многоходовую беседу про 719, система отвечает строго по базе знаний (приложение
719 + Правила реестра) с источниками и обязательной пометкой эксперта. Поверх готового движка
навигатора (раздел 0.2–0.5).

### Концепция (архитектурное решение 2026-06-30)
- **Основной юзер — эксперт ТПП** (силовой пользователь с петлёй обратной связи); также `user`
  (только чат) и `admin` (полный доступ). Мультиюзер: **до 6 человек, разные устройства**.
- **3 роли (RBAC):** `user` — диалог + источники/чек-лист/дисклеймер; `expert` — + «сохранить
  верный кейс» / «исправить ответ» → `knowledge_base/cases/` → `verified_cases`; `admin` — +
  управление юзерами, переиндексация, логи/аналитика, кейсы.
- **UI:** FastAPI (auth + роли + хранение + сервинг модели) + лёгкий веб-фронт (HTML/JS, без
  React-сборки). Streamlit-форма (`frontend/streamlit_app.py`) вытесняется.
- **Охват базы:** приложение 719 (готово) + **Правила формирования реестра и тело постановления**
  (процедурные вопросы: внесение в реестр, состав сведений, ГИСП).
- **Хостинг:** РФ-облако (ФЗ-152) или on-prem; на этапе приёмки — локально (~₽2.5–5k/мес, см.
  [docs/BUDGET.md](docs/BUDGET.md)).
- **Ниша vs рынок** (ИСКРА/Нейроюрист/КонсультантПлюс — общеправовые, облако): **глубина по 719 +
  встроенная петля эксперта + RF-first self-hosted**.

Этапы идут по зависимостям; каждый заканчивается демонстрируемым результатом. `APP_VERSION` бампим
по мере закрытия этапов (сейчас `0.5.0`).

### 1.0.0 Фундамент и гигиена (разблокировка)
- [ ] **Сервинг в backend:** вынести e5 + RAG-пайплайн в долгоживущий FastAPI (`main.py` есть),
      модель грузится **один раз**; UI ходит по HTTP, а не in-process. _(было 1.0.D / IDEAS P1)_
- [x] Не отдавать пользователю текст исключения (`routes.py` 500) — логировать, показывать generic.
      _(из ревью; сделано 2026-06-30)_
- [x] **Вычистить 68 записей с пустым `product_name`.** Расследование (diag по `pp719_full.txt`):
      это НЕ осиротевшие подкоды, а **исключённые из приложения позиции** («Позиция исключена»
      поправками N 610/N 911) и пустые строки-коды — ни имени, ни требований. Парсер исправлен
      (фильтр пустых строк-кодов ПОСЛЕ привязки компонентов — реальный 29.10.52.190 сохранён),
      JSON детерминированно почищены (`scripts/drop_excluded_positions.py`, без DeepSeek), реиндекс.
      База **1353→1285 точек / 1343→1275 продуктов**; recall@1 0.95 / MRR 0.976 без регресса.
      _(сделано 2026-06-30; см. memory empty-product-names)_
- [x] **Восстановить 63 потерянных продукта-варианта** (находка через A/B-разбор XIX, Opus vs
      DeepSeek). Регекс-сегментатор распознавал продукт только по коду в начале строки → бескодовые
      строки-варианты под общим «Из N»-кодом (катализаторы XIV, кислоты/мономеры XXI, оптика IV,
      насосы питательные XIX…) молча поглощались родителем. Дефект СЕГМЕНТАЦИИ, не LLM. Фикс
      `structure_kb.py` (`variant_product`: наследует код родителя; guard от шума — отсев имён на «;»
      и с цифры). Охват доказан: ровно 7 разделов (IV/V/XIV/XVI/XIX/XXI/XXVII), перепарс этих 7
      (DeepSeek ≈¥2-3). База **1285→1357 точек / 1275→1347 продуктов**; verify 29/29 ✅; recall@1
      0.95 / MRR 0.976 без регресса. _(сделано 2026-06-30)_
- [ ] Репо-гигиена: убрать дамп сессии из корня + в `.gitignore`; убрать неиспользуемый
      `llama-index-*` из `requirements.txt` + синк стек-таблиц (доки↔код); закоммитить
      `scripts/eval_experiments.py`. _(из ревью)_

### 1.0.1 Расширение базы знаний (Правила реестра + тело постановления)
- [ ] **Прозовый чанкер** для Правил/тела (НЕ `structure_kb` — он для product-таблиц): нарезка по
      пунктам/абзацам, отдельный `doc_type` (`rules` vs `product`) в payload. Источник
      `knowledge_base/pp719/pp719_full.txt` (стр. 22856+). ⚠️ Старые «Правила выдачи заключения»
      (стр. 22850) **не индексировать** — утратили силу (ред. 29.06.2024 N 894).
- [ ] Ретрив несёт `doc_type`; промпт различает «позиция приложения» vs «процедура по Правилам»;
      ослабить out-of-scope guard для процедурных запросов.
- [ ] Добавить процедурные кейсы в `scripts/eval_golden.json`, перемерить.
- [x] **Процедурный дефер-предохранитель** _(2026-07-01)_ — до полного корпуса Правил: детерминированный
      детектор процедурного интента (`app/rag/procedural.py`: STRONG-маркеры регистра 719 + GENERIC-глаголы
      с 719-якорем, тумблер `PROCEDURAL_DEFLECT_ENABLED`). Чистый процедурный вопрос (реестр/ГИСП/подача/
      сроки/обжалование) короткозамыкается честным дефером с указателем на первоисточники, БЕЗ вызова LLM
      (0 галлюцинаций, 0 стоимости). Свип: **0 ложных на 104 реальных запросах** (golden/negative/confusable),
      процедурные пробы 7/7. +правило 1в промпта для смешанных вопросов. +4 юнит-теста.

### 1.0.2 Ядро чата (определяющая фича)
- [~] **Чат-эндпоинт** _(2026-07-01)_: `POST /api/chat` (`app/api/chat.py`) — вопрос → движок →
      лог диалога в БД по `session_id`, 503 при падении движка. Группировка беседы есть.
      **History-aware мультитёрн ОТЛОЖЕН** (fast-follow): сейчас каждый вопрос самостоятелен
      (`pipeline.answer()` одноходовой) — follow-up «а какой порог?» пока не переиспользует контекст.
- [ ] Стриминг ответа DeepSeek (`stream=True`) + индикатор «думает». _(fast-follow — эндпоинт синхронный)_
- [x] **Веб-фронт чата** _(2026-07-01)_: `app/api/web.py` + `app/web/templates|static` — поток
      сообщений, ChatGPT-стиль, история диалогов в сайдбаре (появляется при отправке), markdown-рендер
      (жирные заголовки/списки), кликабельные источники → normativ.kontur.ru, экспорт (per-chat «⋮» +
      окно настроек JSON/md/txt/даты/источники) + удаление, поиск по чатам, сворачиваемый сайдбар,
      meta-ответы (`app/rag/meta.py`), футер-дисклеймер + рантайм-strip эмодзи/дисклеймеров.
- [ ] **Мобильная адаптивность** _(fast-follow, запрошено 2026-07-02)_: довести вёрстку под телефоны/планшеты —
      сайдбар как off-canvas «шторка» (сейчас просто сворачивается), инпут на всю ширину (сейчас 50vw — на
      мобиле узко), ширина сообщений и карточек источников, дашборд `/admin` (CSS-графики + таблицы) под узкий
      экран, `viewport`-мета, размеры шрифтов и тач-таргеты. **Только CSS media-queries + пара JS-правок для
      «шторки», без бэкенда и новых зависимостей.** Оценка: chat-страница ~0.5 дня, полный полиш с /admin ~1–1.5 дня.
- [ ] Фикс протечки CJK-токенов + усилить «отвечай только на русском». _(из ревью / IDEAS; в
      `eval_answers` CJK 1.00, но отдельный явный фикс промпта не делали)_
- [~] **Чистый контекст модели** _(P0; частично, 2026-06-30)_: ✅ `format_context` помечает точный хит
      (`okpd2_match` + «✓ СОВПАДЕНИЕ ПО КОДУ»), ✅ усечение операций релевантно запросу (`_rank_operations`
      по стем-токенам, не первые 12) + `MAX_OPS_PER_HIT` 12→15→25. Остаток: обрезать слабый хвост хитов,
      порог релевантности score для кейсов (`search_cases`).
- [x] **INTERIM-фиксы P2 (мега-продукты)** _(2026-07-01)_: `MAX_OPS_PER_HIT` 15→25 + `temperature` 0.1→0 +
      пометка «⚠ СПИСОК ОПЕРАЦИЙ НЕПОЛНЫЙ» в `format_context` и правило 2а промпта («не выдавать за полный
      список, не суммировать, направить к первоисточнику»). Замер до/после: покрытие балльных операций
      (242 мега) median 0.62→**1.00** / все влезли 0.05→**0.66** (мега 264→83); детерминизм чисел 0.60→**0.80**
      (раздел 1.00); faithfulness-стресс 0.973→**1.000** (0 выдумок из 1125). Экстремальный хвост (676/220
      опер) порогом не лечится → parent/child auto-merge, отложено в 2.0.
- [x] **Промпт генерации (P1):** инлайн-цитаты `[N]` на позицию (правило 3б), эксперт-first регистр
      (читатель = эксперт ТПП, проверяющий заявку), формат-шаблон + few-shot отказа «вне сферы» (правило 4).
      Замер `eval_answers`: **цитаты 0→1.00**, без регресса (атрибуция/дисклеймер/CJK/out-of-scope 1.00,
      faithfulness 0.98). _(сделано 2026-06-30)_ _(остаток P1 — «не гасить guard на слабом совпадении кода» —
      требует различения strong/weak okpd2_match в retriever, отдельно)_
- [x] **Заземление ответа — faithfulness-постпроверка (P0):** в `pipeline.answer()` числа баллов/%
      из ответа сверяются с контекстом (`unverified_numbers`); незаземлённые НЕ переписываются (верное
      значение неизвестно), а ВИДИМО помечаются эксперту + структурное поле `Answer.unverified_numbers`.
      Та же логика в `eval_answers.py` (рантайм и замер синхронны). Замер: guard метит 2/2 ответов с
      выдумками (golden), сырая faithfulness 0.95 (выдумки спорадичны → ловит при каждом вызове).
      +5 юнит-тестов. _(сделано 2026-06-30)_
- [ ] _(остаток P2)_ нормализовать `_ensure_disclaimer` + timeout/retry на DeepSeek + опц. регенерация
      при незаземлённых числах.

### 1.0.3 Мультиюзер, роли, персистентность (RBAC)
- [x] **БД-слой** (`app/db/{models,engine,queries}.py`) _(2026-07-01)_: users, messages (лог диалогов
      — запрос, ОКПД2/источники, ответ, low_relevance, unverified, prompt/completion_tokens, время),
      feedback. Sync SQLite (`APP_DB_URL` → `navigator_app.db`, в .gitignore); ALTER-миграция токенов в `init_db`.
- [x] **Аутентификация** _(2026-07-01)_: `app/api/auth.py` — bcrypt + сессии-куки (`SessionMiddleware`)
      + «Запомнить меня» (подписанный cookie 30 дн); `scripts/seed_users.py` (`--admin-password`); **3 роли**.
- [~] Гейтинг по ролям: `require_user`/`require_admin`; `user`/`expert` — чат + фидбек; `admin` — +
      логи/аналитика (`/admin` дашборд). _(остаток: expert «сохранить/исправить кейс → `seed_cases.py`»
      и admin-управление юзерами/переиндексация из UI — не подключены)_

### 1.0.4 Доверие, качество, приёмка эксперта
- [x] **Eval КАЧЕСТВА ГЕНЕРАЦИИ** (не только ретрива): `scripts/eval_answers.py` — прогоняет
      полный пайплайн (DeepSeek) по golden set и ДЕТЕРМИНИРОВАННО (без LLM-судьи, без эталонов
      эксперта) мерит: **faithsfulness** (числа баллов/%/порогов из ответа обязаны быть в контексте),
      атрибуцию раздела, дисклеймер, отказ на out-of-scope, CJK. Отчёт `docs/eval_answers_report.md`.
      **БАЗА (2026-06-30): faithfulness 0.95 (2/249 чисел выдуманы, спорадически), атрибуция 1.00,
      дисклеймер 1.00, без CJK 1.00, отказ out-of-scope 4/4.** Слой ответа крепкий; единственная
      слабость — спорадическая выдумка числа → лечится P0-постпроверкой (1.0.2), не данными.
      _(сделано 2026-06-30; эталоны эксперта/LLM-judge — позже, когда появятся кейсы)_
- [x] Замерить эффект правок генерации из `1.0.2` (чистый контекст / промпт / пост-проверка чисел)
      на `eval_answers` — до/после, а не «на глаз». _(2026-07-01)_ После интерим-фиксов P2 регресс чист:
      атрибуция/цитаты/дисклеймер/CJK/отказ-outscope **1.00**, faithfulness 0.98 (1 спорадич., guard-flagged);
      ретрив без изменений (recall@1 0.98/MRR 0.988). Ось качества ответа: faithfulness-стресс 0.973→**1.000**.
- [ ] **Приёмочный тест с экспертом ТПП**: 5–10 реальных кейсов через встроенную форму →
      `knowledge_base/cases/` → `seed_cases.py`; критерий **≥70%**.
- [ ] Петля: правки эксперта → `verified_cases` → перемерить прирост.

### 1.0.5 Деплой и релиз
- [ ] `docker-compose` (app + Qdrant + БД) + `Dockerfile` + детерминированный провижнинг e5
      (зеркало / локальный путь) + TLS/домен — воспроизводимый запуск на РФ-сети. _(из ревью)_
      _(варианты хостинга РФ и удешевление — исследование 2026-07-01 в `docs/IDEAS.md` § Инфра: фикс-VPS
      Timeweb 4vCPU/8ГБ ≈₽1782/мес vs Yandex/MWS ~₽4k; managed-RAG РФ не берём — заменяет наш движок)_
- [ ] Минимальный CI (GitHub Actions): юнит-тесты на push. _(из ревью)_
- [ ] Релиз: PR `dev → main` + `/code-review`; тег `v1.0`; **переиндексация на `main`** (закрыть
      расхождение — `main` держит старое битое состояние базы).

**Критерий готовности 1.0**: многоходовой веб-чат (стриминг, источники, пометка эксперта), заземлён
на приложение 719 + Правила реестра; мультиюзер ≤6 с ролями `user`/`expert`/`admin` с разных
устройств; развёрнут через Docker; качество ответа под метрикой; приёмка эксперта ≥70%.

---

## 2.0 — Валидатор документов и калькулятор баллов
**Цель**: предприятие загружает документы → система проверяет комплектность,
считает набранные баллы и указывает на пробелы

### 2.1 Загрузка документов
- [ ] Поддержка PDF, DOCX, изображений (сканов); извлечение текста (pdfplumber, python-docx); OCR (easyocr, локально)

### 2.2 Валидатор
- [ ] `app/tools/validator.py` — сравнение загруженных документов с чек-листом группы (✓ есть / ✗ нет / ⚠ уточнить); `POST /validate`

### 2.3 Калькулятор баллов и вердикт
- [ ] Дозахват порогов разделов X и XXIX (многоколоночные/вариантные пороги) — **предусловие точного расчёта** _(из IDEAS / ревью)_
- [ ] `app/tools/scorer.py` — **детерминированный** счёт баллов/долей под предприятие (баллы/пороги НЕ доверять LLM); прогноз «хватает ли баллов»
- [ ] Предварительный вердикт «вероятно соответствует / не хватает N баллов / не соответствует» — формулирует LLM поверх детерминированного числа _(из IDEAS)_

### 2.4 Аналитика и расширенная петля (поверх 1.0)
- [ ] Дашборд частых промахов ИИ по типам товаров — поверх логов диалогов из `1.0.3`.
      _(базовое логирование/БД, индексация Правил реестра и форма обратной связи эксперта подняты в `1.0`)_

**Критерий готовности**: валидатор проверен на 20+ реальных пакетах документов

---

## 3.0 — Генератор актов экспертизы
**Цель**: система формирует черновик акта экспертизы ТПП, эксперт редактирует и подписывает

### 3.1 Шаблоны актов
- [ ] Типовые шаблоны актов ТПП в `knowledge_base/templates/`; параметризация полей предприятия

### 3.2 Генератор
- [ ] `app/tools/act_gen.py` — черновик акта по данным навигатора/валидатора; явная пометка «ЧЕРНОВИК»; экспорт в DOCX

### 3.3 Кабинет эксперта
- [ ] Список заявок; просмотр/редактирование черновика; фиксация вердикта (одобрен / отклонён / доработка)

**Критерий готовности**: 5 реальных актов подготовлено через систему и подписано экспертами

---

## 4.0 — Масштабирование (будущее)
**Цель**: расширение на другие нормативные базы и сценарии

- [ ] Форс-мажор (статья 1 ГК, процедура ТПП подтверждения обстоятельств непреодолимой силы)
- [ ] Мобильное приложение или Telegram-бот для предприятий
- [ ] Интеграция с ГИСП
- [ ] Многопользовательский режим: предприятие + эксперт + администратор ТПП
- [ ] Аналитика: отчёты для руководства ТПП

### 4.x Масштабирование обслуживания (высокая нагрузка, 100+ одновременных) _(разбор 2026-07-02)_
Триггер: если сервис выходит за внутренний контур ТПП (цель `1.0` — **≤6 экспертов**) в массовый/публичный режим.
**Ядро RAG (Qdrant + e5 + DeepSeek) переписывать НЕ нужно — оно масштабируется горизонтально.** Меняется
**слой обслуживания (serving)**, эволюционно, а не редизайном. Профиль запроса: доминирует ожидание DeepSeek
(I/O 5–20 с), CPU VM при этом простаивает → одновременность ограничивают не вычисления, а пул потоков и квоты API.
- [ ] **`/api/chat` sync → async** + async-клиент DeepSeek — снять потолок threadpool Starlette (~40 sync-потоков).
- [ ] **SQLite → PostgreSQL** для лога диалогов: у SQLite один писатель, при 100 параллельных записях — сериализация
      (уже заложено: `DATABASE_URL` prod=Postgres, `asyncpg` в зависимостях закомментирован).
- [ ] **e5 в общий инференс-сервис** (или больше RAM): иначе каждый uvicorn-воркер грузит свою копию модели ~2.5 ГБ.
      Опционально GPU — ускорит энкод и снимет CPU-контеншн на всплесках.
- [ ] **Больше vCPU + 2–4 воркера**, либо **2-я VM за балансировщиком** (+ отказоустойчивость).
- [ ] **Лимиты DeepSeek — главный внешний потолок** при 100 параллельных вызовах (429/очередь): проверить/поднять
      квоты аккаунта + очередь с backpressure на стороне сервиса.
- [ ] Qdrant — single-node хватает (база крошечная); при дальнейшем росте — реплики/шардирование.

**Оценка ресурсов:** «100 активных, реалистично ~15–30 в полёте» → **8 vCPU / 16 ГБ** + async + 2–4 воркера +
Postgres (лимиты DeepSeek подтвердить). «100 строго одновременно в полёте» → async + общий e5 + поднятые лимиты
DeepSeek, **~8–16 vCPU / 32 ГБ** или 2 VM + LB. Узкое место при таком наплыве — **квоты DeepSeek, не наш код**.

---

## Сквозной бэклог RAG (подтягиваем по мере нужды)

Улучшения поиска поверх готового движка — берём по метрике на харнесе из Спринта 4, а не «на глаз».
Подробно — [docs/IDEAS.md](docs/IDEAS.md) (раздел RAG, стадии 2–5) и [docs/RAG_EXPLAINED.md](docs/RAG_EXPLAINED.md).

- [x] Реранкер — СДЕЛАН как **LLM-rerank через DeepSeek** (`app/rag/reranker.py`), а НЕ cross-encoder
  (RF-friendly, без скачивания модели). Гейт: только code-less выдача (код авторитетнее); graceful
  fallback на исходный порядок при ошибке/таймауте. Замер на харнесе: **recall@1 0.95→0.98, MRR
  0.976→0.988 без регресса** (починил #14 стреловые краны XVIII→VI). Остаток #9 экскаваторы→VI —
  спорный ярлык golden (одноковшовые экскаваторы правдоподобно и VI «тяжмаш») → сверить с экспертом.
  Включён в пайплайн (`RERANK_ENABLED=True`, +1 вызов DeepSeek на code-less запрос). _(2026-06-30)_
- Тюнинг гибрида (веса dense/sparse, RRF `k`), query expansion / HyDE для коротких запросов.
- Гранулярность parent/child (мега-продукты усекаются до `MAX_OPS_PER_HIT=15` операций) — **подтверждён данными (P2 #7, `eval_truncation.py`): 264 мега-продукта = 20% базы, лишь 13/242 показывают все балльные операции, авто 10/627.** Экстремальный хвост (676/220 опер) порогом не лечится → нужна архитектура. Ближе к 2.0; interim для 1.0 — поднять порог 15→25 + пометка неполноты.
- Эволюция эмбеддера (BGE-m3 dense+sparse+ColBERT) / адаптация e5 на кейсах эксперта.

---

## Лог изменений

| Дата | Версия | Что сделано |
|---|---|---|
| 2026-06-15 | V0 | Инициализация репозитория, CLAUDE.md, ROADMAP.md |
| 2026-06-15 | V0 | Парсинг ПП №719 (16 секций), репо на GitHub, автопуш |
| 2026-06-15 | V0 | Документация: README, SETUP, гайд по 2-му ПК, спец V1, бюджет |
| 2026-06-15 | V0 | ✅ Фаза V0 закрыта. Следующее — V1, Спринт 1 (Qdrant + эмбеддинги) |
| 2026-06-15 | V0 | Развёртывание на 2-м ПК: Python 3.12, venv, зависимости, `GET /ping` → ok |
| 2026-06-15 | V1 | Старт Спринта 1: Qdrant в Docker + `app/rag/embeddings.py` (e5-large) |
| 2026-06-15 | V1 | ✅ Спринт 1 закрыт: WSL2 + Docker, Qdrant на :6333, e5-large (1024d), сквозной тест OK. Следующее — Спринт 2 (структуризация базы знаний) |
| 2026-06-15 | V1 | План на завтра уточнён: пилот `structure_kb.py` на разделе II → батч → `load_kb.py` → smoke retrieval |
| 2026-06-16 | V1 | Спринт 2 старт: `prompts.py` + `structure_kb.py` (регулярки + DeepSeek JSON). Пилот раздела II ОК — 4 продукта, 206 блоков, ¥0.57. Полный парсинг базы оценён ≈¥10. Дальше — батч разделов I, III, IV, VI–X |
| 2026-06-16 | V1 | Спринт 2: вся база структурирована (9 разделов, 1601 продукт) в `structured/*.json`. Факт. ≈¥11.5. Добавлен `verify_structured.py` (контроль галлюцинаций) |
| 2026-06-16 | V1 | Контроль качества вскрыл галлюцинации баллов (пустые блоки → модель фантазировала). Фиксы: строгий промпт + пропуск LLM на пустых блоках + 0→null. VII перепрогнан → чисто. **ОСТАЛОСЬ перепрогнать III, IV, X + финальная verify, потом `load_kb.py`** (пауза для смены сессии) |
| 2026-06-16 | V1 | III, IV перепрогнаны → чисто. X: найдена структурная потеря в парсере (многоколоночные таблицы пороговых баллов по датам — брался лишь 1 столбец). Фикс парсера (заголовки этапов + все колонки + склейка в `min_threshold`). Прозовые пороги раздела (393) — в записи-методичке `section_methodology`. Добавлены `--workers` (параллель ~7×) и прогресс-бар. **verify по всем 9 → ✅ 9/9.** Дальше — `load_kb.py` |
| 2026-06-16 | V1 | **Спринт 3 — индексация ✅.** `load_kb.py` загрузил 1602 точки в Qdrant (гибрид dense e5 + sparse BM25). `app/rag/sparse.py` — локальный BM25 (mmh3+Snowball, без скачивания), нормировка по длине убрала доминирование мега-записей. Smoke-тест RRF релевантен (сварка→II, чиллеры→ККБ, фарма→VIII). Оптимизация CPU: 18 потоков + кэш эмбеддингов. Дальше — 1.2 RAG-пайплайн (retriever + фильтр ОКПД2) |
| 2026-06-18 | V1 | **Eval-харнес (стадия 0)** + **out-of-scope guard (стадия 1)** + юнит-тесты (`tests/`). База: recall@1=0.77, recall@3=1.0, MRR=0.878. Guard двухслойный (порог dense top-1 + правило 1б промпта). |
| 2026-06-18 | V1 | **🔴→✅ Исправлен дефект нарезки приложения.** Разделы **XI–XXIX склеивались в «X»**, раздел **V терялся**. Новый `scripts/rechunk_appendix.py` → нарезал все 29 разделов. Реиндекс: **1681 точка, 29 разделов**. **recall@1 0.77→0.86, MRR 0.878→0.925**. Golden set 30→46. |
| 2026-06-18 | V1 | **🔴→✅ Глобальная сверка чанков — ещё 3 дефекта.** XXIX вбирал примечания (296 фантомных «продуктов»→9 реальных); `05_IV` содержал ВЕСЬ раздел V; потеря 1-го продукта при склейке заголовка. Реиндекс **1681→1325 точек**, **1315 продуктов**. **recall@1 0.86→0.95, MRR 0.925→0.976** (recall@3=1.0). |
| 2026-06-18 | V1 | Фикс «Из» (заглавное) и коротких кодов ОКПД2 — 28 потерянных продуктов. Счётчики 1315→1343 продукта / 1325→1353 точки. |
| 2026-06-29 | 0.5.0 | **Ре-версионирование продукта.** Цель `1.0` переопределена: рабочий **чат-ассистент по 719** (диалоговое окно как ChatGPT); прежние фазы V2/V3/V4 → `2.0`/`3.0`/`4.0`. Текущая стадия — `0.5.0` (движок навигатора готов, продукт — нет). `APP_VERSION` 0.1.0→0.5.0. В план внесены улучшения из ревью репозитория: eval качества ответа, чистка 68 пустых `product_name`, backend-сервинг модели, Docker/CI, репо-гигиена, индексация Правил реестра, логирование/БД. |
| 2026-06-30 | 0.5.0 | **Концепция 1.0 расширена и актуализирована** (после изучения рынка РФ). 1.0 = **мультиюзер веб-чат-ассистент эксперта ТПП** (роли `user`/`expert`/`admin`, ≤6 юзеров); охват базы += **Правила реестра/тело постановления**; UI = **FastAPI + лёгкий веб-фронт** (Streamlit вытесняется). Блоки 1.0.A–E → этапы **1.0.0–1.0.5** (по зависимостям). RBAC/auth/БД-персистентность подняты из `2.0` в `1.0`. Ниша подтверждена vs ИСКРА/Нейроюрист/КонсультантПлюс. |
| 2026-06-30 | 0.5.0 | **Анализ RAG-движка (генерация).** Прочитан слой генерации (`pipeline`/`prompts`/`retriever`): поиск крепкий, но генерация без проверки заземления и без метрики. В `docs/IDEAS.md` («Качество») — разбор P0/P1/P2 (faithfulness-постпроверка, релевантное усечение операций, пометка точного хита, порог кейсов, цитаты/эксперт-first/few-shot в промпте, timeout/seed). Конкретика подвязана в ROADMAP 1.0.2 (чистый контекст + промпт + заземление) и 1.0.4 (замер до/после). |
| 2026-06-30 | 0.5.0 | **Движок «до идеала», часть 1 (1.0.0 + P0 генерации).** (1) **Чистый контекст** (P0): `format_context` помечает точный хит `✓ совпадение по ОКПД2` + правило 3а промпта; релевантное усечение операций по стем-токенам запроса (`MAX_OPS_PER_HIT` 12→15). (2) `routes.py` не отдаёт текст исключения наружу (generic 500 + лог). (3) **68 пустых `product_name`** оказались исключёнными позициями приложения («Позиция исключена» N 610/N 911) — парсер исправлен, JSON почищены (`scripts/drop_excluded_positions.py`), реиндекс: **1353→1285 точек / 1343→1275 продуктов**, recall@1 0.95 / MRR 0.976 (без регресса), verify ✅, 29 юнит-тестов OK. |
| 2026-06-30 | 0.5.0 | **Движок «до идеала», часть 2 — 63 потерянных продукта-варианта.** A/B-разбор раздела XIX (Opus claude code vs DeepSeek по источнику): DeepSeek в основном верен (минусы — обрезка дат в %-блоках, кривой последний блок), но **главный рычаг — не LLM, а регекс-сегментатор**: распознавал продукт только по коду в начале строки → бескодовые строки-варианты под общим «Из N»-кодом (катализаторы XIV, кислоты/мономеры XXI, оптика IV, насосы питательные XIX…) молча поглощались родителем. Скан корпуса: 63 отсутствовали в базе. Фикс `structure_kb.py` (`variant_product` наследует код родителя + guard от шума: отсев имён на «;» и с цифры), перепарс ровно 7 затронутых разделов (IV/V/XIV/XVI/XIX/XXI/XXVII, DeepSeek ≈¥2-3), реиндекс **1285→1357 точек / 1275→1347 продуктов**, verify 29/29 ✅, recall@1 0.95 / MRR 0.976 без регресса. Вывод по Opus: полный Opus-парс оправдан как точечный приём для структурно-сложных МАЛЫХ разделов, не как замена пайплайна (контекст на гигантах + воспроизводимость + проверяемость). |
| 2026-06-30 | 0.5.0 | **Первая метрика КАЧЕСТВА ОТВЕТА (1.0.4).** Сделан `scripts/eval_answers.py` — детерминированная сверка сгенерированного ответа (полный пайплайн+DeepSeek по golden set): faithfulness чисел (баллы/%/пороги из ответа обязаны быть в контексте), атрибуция, дисклеймер, out-of-scope-отказ, CJK. БАЗА: **faithfulness 0.95** (2/249 чисел выдуманы, спорадически — temp 0.1, на повторе #4/#12 чисто), атрибуция 1.00, дисклеймер 1.00, без CJK 1.00, отказ out-of-scope 4/4. Вывод: слой ответа крепкий; разрыв — спорадическая выдумка числа (→ P0-постпроверка, не данные) + 2 семантических промаха recall (→ реранкер). Подтверждает: Opus-парс данных НЕ на оси качества ответа. Отчёт `docs/eval_answers_report.md`. |
| 2026-06-30 | 0.5.0 | **P0 faithfulness-постпроверка (1.0.2).** В `pipeline.answer()` числа баллов/% из ответа сверяются с контекстом (`unverified_numbers`); незаземлённые НЕ переписываем (верное значение неизвестно — выдумать «правильное» хуже), а ВИДИМО помечаем эксперту («⚠ Проверьте показатели…») + поле `Answer.unverified_numbers`. Логика каноническая в pipeline, переиспользована в `eval_answers.py` (рантайм=замер). Замер: guard метит **2/2** ответов с выдумками; сырая faithfulness 0.95 (выдумки спорадичны: #21/#33 в этом прогоне vs #4/#12 в прошлом → рантайм-постпроверка ловит при каждом вызове, чего разовый фикс не дал бы). +5 юнит-тестов (всего 34 OK). Принцип «ИИ — черновик»: выявить и показать, не подменить. |
| 2026-06-30 | 0.5.0 | **Реранкер (стадия 2) — recall@1 0.95→0.98.** `app/rag/reranker.py`: LLM-rerank через DeepSeek (НЕ cross-encoder — RF-friendly, без скачивания), переупорядочивает top-k по смыслу запроса. Гейт: только code-less выдача (совпадение по ОКПД2 авторитетнее); graceful fallback при ошибке/таймауте (не роняет выдачу). Замер на харнесе (`eval_retrieval.py --rerank`): **recall@1 0.95→0.98, MRR 0.976→0.988, БЕЗ регресса** (починил #14 стреловые краны XVIII→VI). Остаток #9 экскаваторы→VI — спорный ярлык golden (→ сверка экспертом). Включён (`config.RERANK_ENABLED=True`, +1 вызов DeepSeek на code-less запрос; toggleable). +флаг `--rerank` в харнесе. |
| 2026-06-30 | 0.5.0 | **Промпт P1.** В `NAVIGATOR_SYSTEM_PROMPT`: инлайн-цитаты `[N]` на позицию-источник (правило 3б), эксперт-first регистр (читатель = эксперт ТПП), формат-шаблон ответа + few-shot отказа «вне сферы» (правило 4). В `eval_answers.py` добавлена метрика цитат. Замер: **инлайн-цитаты 0→1.00** (каждый ответ ссылается на позицию), БЕЗ регресса — атрибуция/дисклеймер/CJK/отказ-outscope 1.00, faithfulness 0.98 (выдумки спорадичны, guard метит 1/1). Эксперт мгновенно сверяет источник по `[N]`. |
| 2026-06-30 | 0.5.0 | **Глобальные тесты движка P0/P1 + F1 identity-эмбеддинг (промоучен).** P0 `scripts/eval_coverage.py` — product-level recall по ВСЕЙ базе (1357, self-retrieval по `source_anchor`): вскрыл, что раздельный recall@1 0.98 мерил РАЗДЕЛ, а позиционный = 0.886 (реранкер→0.987). P1 `scripts/eval_paraphrase.py` — просадка на DeepSeek-перефразе (raw exact r@1 0.877→0.533; реранкер вытягивает→0.859 — **доказал, что реранкер несущий, не опция**); `scripts/eval_guard.py` + наборы `eval_golden_negative.json`(36 вне-719)/`eval_confusable.json`(22 кросс-пары) — out-of-scope отказ 36/36, различение 0.95. Аномалия «Машины самоходные для добычи»→top-1 «Присадки к топливу» вскрыла **бойлерплейт-дилюцию**: `build_text` эмбеддил весь рецепт локализации (generic операции + повсеместный шаблон ЕАЭС), топя идентичность продукта. **F1** (`build_embedding_text`, теперь ДЕФОЛТ `load_kb`; `--full-text` — откат): эмбеддить только идентичность (имя/раздел/код/порог), операции остаются в payload (ответы/генерация не тронуты). A/B (pp719_exp→прод): coverage recall@1 **0.886→0.972**, recall@3 0.973→0.999, невидимки 3→0, коллизии 150→38; golden raw 0.95→0.98; перефраз raw exact 0.533→0.663, дыры 9→6; guard отказ 36/36 без изменений. Реиндекс прода с identity. Label-вопросы эксперту: краны/экскаваторы III↔VI, буровые VI↔XX. |
| 2026-06-30 | 0.5.0 | **Тест-батарея P2 (качество ответа в масштабе) — замерено.** `eval_truncation.py` (детерм.), `eval_faithfulness_stress.py`, `eval_determinism.py` (+отчёты `docs/eval_*`). Все три оси СЦЕПЛЕНЫ вокруг операционно-тяжёлых мега-продуктов (264 = 20% базы, max 676 опер): **#6 faithfulness-стресс** 112 числовых запросов = 0.973 (выдумано 4/672 = 0.6%, все помечены рантайм-guard; провал [II] Автобусы 676 опер «1500» — усечение спровоцировало); **#7 truncation** `MAX_OPS_PER_HIT=15` КУСАЕТ — лишь 13/242 мега показывают все балльные операции (median покрытия 0.62, авто 10/627), подъём порога частичен → **parent/child (2.0) подтверждён данными**; **#8 детерминизм** атрибуция раздела 1.00, но числа стабильны лишь 0.60 (temp=0.1 → модель перечисляет разные подмножества операций на много-опер. продуктах). INTERIM-фиксы для 1.0 (НЕ внедрены, на след. сессию): temp 0.1→0; MAX_OPS 15→25 + пометка неполноты мега-продукта в промпте; parent/child → 2.0. ИТОГ батареи: P0✅ P1✅ P2✅ — движок «до идеала» на ~80% базы, остаточный узел = мега-продукты. |
| 2026-07-01 | 0.5.0 | **Интерим-фиксы P2 + процедурный дефер + замер до/после.** (1) Внедрены интерим-фиксы мега-продуктов (`MAX_OPS_PER_HIT` 15→25, `temperature` 0.1→0, пометка «⚠ СПИСОК ОПЕРАЦИЙ НЕПОЛНЫЙ» + правило 2а промпта): faithfulness-стресс 0.973→**1.000** (0 выдумок из 1125), покрытие балльных операций median 0.62→**1.00** (мега 264→83), детерминизм чисел 0.60→**0.80**; регресс чист (eval_answers атрибуция/цитаты/дисклеймер/CJK/outscope 1.00, faithfulness 0.98; ретрив 0.98/0.988). Экстремальный хвост (676/220 опер) → parent/child в 2.0. (2) **Процедурный дефер-предохранитель** (`app/rag/procedural.py`) — до полного корпуса Правил: детектор процедурного интента (STRONG-маркеры регистра 719 + GENERIC-глаголы с 719-якорем, тумблер `PROCEDURAL_DEFLECT_ENABLED`) короткозамыкает чистый процедурный вопрос честным дефером БЕЗ LLM; свип 0 ложных на 104. +правило 1в. +4 юнит-теста (всего 42). (3) GitHub Projects доска #1 (28 issues). |
| 2026-07-01 | 0.5.0 | **Продукт мини-1.0 собран и отполирован (dev=`3cc9309`).** Решение юзера: приёмку отложить, сначала собрать демо-продукт (≈полный 1.0.2+1.0.3+фидбек). Стек **FastAPI + Jinja2 + ванильный JS** (Streamlit не тянет мультиюзер-auth). Шаги 2–6: БД-слой `app/db/` (users/messages/feedback, sync SQLite); auth `app/api/auth.py` (bcrypt + сессии + «Запомнить меня» + 3 роли) + `scripts/seed_users.py`; чат-эндпоинт `app/api/chat.py` (`POST /api/chat`, лог по session_id); веб-фронт `app/api/web.py` + `app/web/` (ChatGPT-стиль, история диалогов, markdown, кликабельные источники→kontur, экспорт JSON/md/txt + окно настроек, удаление, поиск, сворачиваемый сайдбар, meta-ответы, рантайм-strip эмодзи/дисклеймеров); `/admin` дашборд (KPI токены/₽/активность CSS-графики + сворачиваемые логи с датами). Учёт токенов (`Message.prompt/completion_tokens`, `app/core/costs.py`). **42 юнит-теста + e2e с живым движком OK**; admin=admin/admin. Отложено: стриминг, history-aware мультитёрн, корпус Правил реестра. Демо-хостинг = Yandex Cloud (грант). **СЛЕД. ШАГ — деплой docker-compose на Yandex (1.0.5).** |

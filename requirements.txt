# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# LLM & RAG
openai==1.54.0          # DeepSeek совместим с OpenAI SDK
qdrant-client==1.12.0
# RAG рукописный (qdrant_client + sentence-transformers напрямую) — LlamaIndex НЕ используется

# Embeddings (локально, без внешних API)
sentence-transformers==3.3.1
hf_xet==1.5.1              # чанковая докачка весов с HuggingFace (устойчиво к обрывам сети/DPI)
mmh3==5.2.1                 # хэш термов для sparse BM25 (app/rag/sparse.py)
py-rust-stemmers==0.1.8     # Snowball-стемминг (русский) для sparse BM25

# Database
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
# asyncpg==0.30.0       # раскомментировать для PostgreSQL

# Document parsing
pdfplumber==0.11.4
python-docx==1.1.2
striprtf==0.0.32
# easyocr==1.7.1        # OCR для сканов — добавить в V2 (требует PyTorch)

# Corpus manifest (K8): состав корпуса задаётся knowledge_base/manifest.yaml.
# ⚠ Указан ЯВНО, хотя приходил транзитивно (через sentence-transformers): полагаться на чужую
# зависимость — значит потерять её при первом же обновлении соседа, и загрузчик корпуса упадёт
# на боевой машине, а не в CI.
PyYAML==6.0.3

# Settings & validation
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Frontend: ранний Streamlit-MVP (frontend/streamlit_app.py) вытеснен веб-чатом 1.0 и в рантайме
# сервиса НЕ используется — .dockerignore исключает frontend/ из образа, а зависимость всё равно
# ставилась. Вынесена в requirements-dev.txt (R22).

# Web UI 1.0 (чат + auth + роли: FastAPI + Jinja2 + сессии)
jinja2==3.1.4              # server-rendered страницы (login/чат/админ)
bcrypt==4.2.0              # хэш паролей (auth)
itsdangerous==2.2.0        # подпись сессионных кук (Starlette SessionMiddleware)

# Utilities
httpx==0.27.2
python-multipart==0.0.12   # для загрузки файлов в FastAPI
loguru==0.7.2
tqdm==4.68.2               # прогресс-бар для структуризации/загрузки базы

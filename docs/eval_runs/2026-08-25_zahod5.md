# Замер перед выкаткой — заход 5 (`K13`, `K15`) + гигиена

> **Дата:** 25.08.2026. **Коммит:** `75e4867` (`dev`). **Версия:** `0.5.0`. **Тег релиза:** `v0.5.0-test15`.
> **База сравнения:** [eval_runs/2026-08-24_zahod4.md](2026-08-24_zahod4.md)
> (прогон 24.08 на `048249d`, релиз `v0.5.0-test14` = `d119e58`).
> **Предусловия `EVAL_GUIDE §1.1` исполнены:** дерево чистое, кейсы выключены
> (`QDRANT_CASES_COLLECTION=__eval_disabled__`), `EXACT_SEARCH=1` для ретрива.

## Что вошло в заход

| Задача | Итог |
|---|---|
| `K13` #46 | Юридическая сила источника в шапке пункта контекста + правило 1а промпта. Значение из МАНИФЕСТА по `doc_type`, не из payload |
| `K15` #36 полов. А | Закрытый справочник подтверждающих документов ДАННЫМИ + правило 3б + рантайм-гард `phantom_documents` |
| `K15` #36 полов. Б | Методрекомендации ТПП вер. 1.18.3 в корпусе: `pp719_rules` **311 → 328** |
| `EV14` #98 | База сравнения обновлена до `v0.5.0-test14`; ссылки отчётов переведены на `eval_runs/` |
| `O4` #107 | Скрипты замера и проверки выкатки не падают на консоли Windows: долг **36 → 25** |
| выкатка | Шаг `REINDEX_FULL` + профиль `v0.5.0-test15` + релизная проверка |

⚠⚠ **ВЫКАТКА С ПОЛНОЙ ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`, А НЕ ЧАСТИЧНОЙ.** `avgdl` считается по
всему корпусу и **запекается в sparse-вектор каждой точки**; новый документ уводит в чужой масштаб
BM25 не свои точки, а точки **соседей** (замерено **95.84 → 96.72**). Замер снят на коллекции,
переиндексированной ПОЛНОСТЬЮ, — то есть меряется ровно то состояние, которое поедет.

⚠ Товарный `pp719` не тронут: `K13` берёт `legal_force` из манифеста, а не из payload, — именно
затем, чтобы правка осталась рантаймовой для корпусов без паспорта в точках.

---

## Уровень 0 — целостность ✅

| Метрика | Порог | База 24.08 | **25.08** |
|---|---|---|---|
| Юнит-тесты | все зелёные | 666 | **725 / OK** |
| `verify_structured --records` | 0 расхождений | 0 | **0 на 1366 позиций** |
| `reconcile_kb` | 29/29 | 29/29 | **29/29** |
| Непокрытые порогом (`points`+`mixed`) | наблюдение | 50.8 % | **50.8 %** (447 из 880) |

⚠ **Батарея прогонялась КОМАНДОЙ ИЗ `CLAUDE.md`, без `PYTHONUTF8=1`.**

⚠⚠ **И именно на уровне 0 замер едва не остановился.** `eval_threshold_coverage.py` — команда
этого уровня — падала `UnicodeEncodeError`'ом на символе «←» до печати первой строки. Следом
выяснилось, что в списке долга `#107` лежат ПОЧТИ ВСЕ скрипты уровней 1–3. Обход через
`PYTHONUTF8=1` сознательно не выбран (замер в подстроенной среде — не замер); одиннадцать скриптов
защищены, долг 36 → 25.

## Уровень 1 — ретрив ✅

### Товарная ось (`pp719` не тронут — ожидалось равенство, оно и получено)

| Метрика | Порог | База 24.08 | **25.08** | |
|---|---|---|---|---|
| `recall@1` (NAME sweep, 3 прогона) | ≥0.93 | 0.944 ± 0.000 | **0.944 ± 0.000** | = |
| `MRR` | — | 0.971 | **0.971** | = |
| `recall@3` / `recall@10` | ≥0.99 / 1.00 | 0.999 / 1.000 | **0.999 / 1.000** | = |
| Невидимки (нет в top-10) | 0 · блокирующая | 0 | **0** | = |
| Кросс-секционные коллизии | ≤12, невозрастание | 9 | **9** | = |
| Код разрешается в свою ветку | ≥0.99 | 1.000 | **1.000** (1366/1366) | = |

**Гейт out-of-scope.** Негативы: min 0.734 · median 0.811 · max 0.852. Пограничные in-scope:
min 0.804 · median 0.830 · max 0.895. **Полоса не сдвинулась ни на сотую** — вывод `EV2` в силе.

### Процедурная ось (сюда добавлен пятый документ)

| Метрика | Порог | База 24.08 | **25.08** |
|---|---|---|---|
| Атрибуция@1 (`gs_rules`, 25 кейсов) | ≥0.95 | 0.96 | **0.96** |
| Представленность нужного документа | 1.00 | 1.00 | **1.00** |
| Точность темы | — | 0.84 | **0.84** |
| Доля окна: Приказ №52 | — | 74 % | **70 %** |
| Доля окна: Правила реестра | — | 61 % | **61 %** |

⚠ **Пятый документ не стоил норме ни одного места.** Методрекомендации лежат в
`RULES_QUOTA_ON_DEMAND` — гарантированного места в окне не резервируют. Четыре процентных пункта
отдал Приказ №52 из своего избытка (тот самый, который сокращала `K10`); Правила реестра держат
9/9 представленности и прежнюю долю окна.

---

# Раунд 8 ревью PR #137 — 15 находок, 6 HIGH. Правило остановки не выполнено

**Дата:** 01.09.2026 · **Мишень:** `2f67c19..2912f9d` (дифф раунда 7 и только он) · **Уровень:** max
**Правило остановки,** записанное коммитом `d3facaa` **ДО запуска ревью:** нет HIGH → выкатываем
`v0.5.0-test23`, независимо от числа LOW/MED.

## Вердикт: 6 HIGH — правило НЕ выполнено, выкатки нет

| Раунд | 1 | 2 | 3 | 4 | 5 | 6 | 7 | **8** |
|---|---|---|---|---|---|---|---|---|
| находок | 7 | 5 | 7 | 5 | 6 | 8 | 5 | **15** |
| HIGH | 2 | 2 | 3 | 2 | 2 | 0 | 2 | **6** |

**Ряд HIGH `2·2·3·2·2·0·2·6` не убывает ни по числу, ни по тяжести — восьмой раунд самый тяжёлый.**
Правки самого ревью давали HIGH в раундах **2, 5, 7 и 8 — четырёх из восьми, половине.**
Батарея была зелёной на всех пятидесяти восьми находках цикла.

⚠ Свип отработал полностью: 10 углов, 3 упали на транзиентных ошибках API и были **дособраны**.
Предусловие к правилу («ноль на неполном свипе — это "не считаю", а не "чисто"») соблюдено.

---

## Шесть HIGH

### 1. Число-сумма снова принимается за код ТН ВЭД — и ПОБЕЖДАЕТ настоящий код

`app/rag/okpd2_ref.py:272`. Раунд 7 сузил безусловный отказ раунда 6 до «нет признака ТН ВЭД между
словом о деньгах и числом», а `re.search` берёт **самый ЛЕВЫЙ** триггер, не ближайший.

```
"по контракту ТН ВЭД 250000 за товарную позицию 8703"   '8703'  → '250000'
"оборот по ТН ВЭД 3500000 и код 8703"                   '8703'  → '3500000'
"стоимость по ТН ВЭД 350000, товарная позиция 8703"     '8703'  → '350000'
"сумма по ТН ВЭД 250000, код 8703"                       None   → '250000'
```

Прослежено до промпта: `st1_ref.format_for_context` печатает дословно
`Код ТН ВЭД 250000: в Перечень условий (приложение 1) НЕ включён — применяется общее правило`
под инструкцией «использовать ТОЛЬКО это значение», а настоящий 8703 пользователя выброшен.
**HIGH раунда 6 восстановлен плюс новый подслучай.** Отрицательный контроль
`test_negative_control_money_alone_is_not_a_code` покрывает только входы БЕЗ признака между —
ровно ту форму, которую правка и вырезала.

### 2. Унификация словарей РАСШИРИЛА тему — новая утечка вне-сферы, созданная правкой утечки

`app/rag/topics.py:71`. `сертификат` и `перечн` были в копии `procedural._ANCHOR_PATTERNS`, но
**не были** в копии `topics`; слитый `ST1_COMPANION` кормит теперь обе.

```
"где получить фитосанитарный сертификат ТН ВЭД 0810"    False/None → True/st1_origin
"нужен ли ветеринарный сертификат на ТН ВЭД 0406"       False/None → True/st1_origin
"какой сертификат требуется таможне для ТН ВЭД 8703"    False/None → True/st1_origin
"нужен ли сертификат на ТН ВЭД 8544"                    False/None → True/st1_origin
```

Каждый уходит на процедурную ветку, где **гейта вне-сферы нет**, и получает ответ из Правил СНГ
и Приказа №14. Урок раунда 7 («унификация обязана перечислить расхождения») поймал асимметрию
`требован` в ОДНУ сторону и вернул её явно; **обратную сторону усреднил молча.**

### 3. Закрытый список дисквалификаторов сверяется только со СЛЕДУЮЩИМ словом

`app/rag/topics.py:79`. Дисквалификатор ищется вплотную за стволом (плюс один из пяти предлогов),
а окно якоря, которое он охраняет, — 40 символов. Любое прилагательное, местоимение или
родительный падеж между ними обходит **весь список**.

**14 из 16 реалистичных таможенных вопросов по-прежнему уходят процедурной веткой**, включая
`«какие условия ВРЕМЕННОГО ввоза по ТН ВЭД 8703»` — это кейс №2 самого набора плюс одно
прилагательное, при том что `ввоз` в списке ЕСТЬ. Также: таможенного оформления · декларирования ·
растаможки · уплаты пошлины · маркировки · квотирования · сертификации.

⚠⚠ **Набор был построен в форме самого правила:** все 12 кейсов `eval_offdomain_tnved.json` ставят
дисквалификатор вплотную. Значит «утечка 0/12» — свойство двенадцати строк, а не класса.
Это ровно тот дефект, который раунд 7 записал как урок про себя — и повторил в измерительном наборе.
Тот же класс, что `K15-1` #120 («одно слово между "внесение" и "в реестр"»).

### 4. Дисквалифицирована СОБСТВЕННАЯ лексика домена — потерян главный вопрос релиза

`app/rag/topics.py:65`. `ST1_DISQUALIFIER` содержит `ввоз|вывоз|импорт|экспорт|поставк|перевозк|
достав|транзит`. **СТ-1 — сертификат происхождения, выдаваемый ПОД ЭКСПОРТ в зону свободной
торговли СНГ.** Правка запретила словарь той самой сделки, ради которой документ существует.

```
"какие условия экспорта в Казахстан по ТН ВЭД 8403"   True/st1_origin → False/None
"какие условия при вывозе товара ТН ВЭД 8403"         True/st1_origin → False/None
"какие условия для экспорта по ТН ВЭД 8403"           True/st1_origin → False/None
```

Падают на ТОВАРНУЮ ветку, где нет ни Соглашения, ни Приказа №14, ни таблицы Перечня —
экспортёр получает требования локализации по ОКПД2. Частота запрещённых стволов в документах,
которые этот же релиз и индексирует: `prikaz14_tpp_full.txt` вывоз **54**, экспорт **111**;
`sng_origin_rules.txt` вывоз **25**, экспорт **12**.

⚠⚠ Записанная цена правки («потеряно СТ-1 — 1») этого видеть не могла: **ни в одном наборе
`scripts/` нет вопроса про СТ-1 в экспортной формулировке.** Ноль означал «не считаю».

### 5. Слово «позиция» попало в список «это НЕ код» — и роняет голый четырёхзначный код

`app/rag/okpd2_ref.py:212`. Добавление `позиц\w*|единиц\w*` в `_TNVED_NOT_A_CODE_RE` роняет код
целиком: `_trim_to_code` уходит в ветку `pos <= 0` (пробела для среза нет) и возвращает `None`.

```
"ТН ВЭД 1902 позиций"                  '1902'   → None    (1902 — настоящая товарная позиция)
"ТН ВЭД 8429 единиц техники"           '8429'   → None
"какие условия для ТН ВЭД 8403 позиция" '8403'  → None
"код ТН ВЭД 840310 позиций"            '840310' → None
```

⚠ `позиция` — **половина собственного признака модуля** `_TNVED_POSITION_CUE_RE`
(`товарн\w*\s+позици`): токен, означающий «это ЕСТЬ код», положили в список «это НЕ код».
Промах здесь не даёт пустоты: Перечень — список ИСКЛЮЧЕНИЙ, и ответ строится по общему правилу,
то есть **чужому и правдоподобному**. Та же фраза через запятую («мой код ТН ВЭД 8403, позиция
Перечня какая») по-прежнему даёт 8403 — асимметрия, не покрытая ни одним тестом.

### 6. Сплайс словаря в `_ANCHOR_RE` сделал регулярку квадратичной, а у `/navigate` нет предела длины

`app/rag/procedural.py:128`. Замерено, `2f67c19` против нового, один `re.search` по `'услови'*n`:

| длина | до | после |
|---|---|---|
| 1.8 КБ | 0.9 мс | 5.9 мс |
| 7.2 КБ | 2.2 мс | 94.9 мс |
| 28.8 КБ | 9.0 мс | **1512 мс** |

Длина ×4 → время ×16 (до правки — линейно). Сквозь `procedural.is_procedural()`: 30 КБ — 0.43 с,
60 КБ — 1.73 с, 99 КБ — **4.83 с чистого CPU** до всякого поиска и вызова модели.
`app/api/schemas.py:9` объявляет `query: str = Field(..., min_length=2)` **без верхнего предела**,
`POST /navigate` доходит до `is_procedural` через `navigator.navigate → pipeline.answer:1265`.
⚠ Смягчает: `/navigate` за `require_user` (учёток ~28), путь чата ограничен 2000 символами.
Лечится `max_length` на `NavigateRequest.query`.

---

## Шесть MED

1. **`topics.py:83` — комментарий, который служит ЕДИНСТВЕННЫМ обоснованием асимметрии, ложен.**
   «`classify` спрашивают ТОЛЬКО на процедурной ветке — то есть УЖЕ ЗА гейтом»: на деле
   `procedural._has_routing_topic` (`procedural.py:198`) зовёт `topics.classify` и является одним
   из ДВУХ дизъюнктов самого гейта (`procedural.py:290`), а `pipeline.py:1396` зовёт его на
   ТОВАРНОЙ ветке. ⚠ Сторож `test_wider_word_does_not_widen_the_route` проходит только потому,
   что его три строки не содержат попадания в `_ANCHOR_RE` вовсе — короткое замыкание на якоре,
   а не на теме: **он не проверяет механизм, который называет.** Цена — лицензия: следующее
   расширение будет обосновано этим же ложным утверждением.
2. **`topics.py:41` — заголовок «одно определение на весь проект» не выполнен, а сторож выбран на
   единственном фрагменте, на котором он может пройти.** Пять из шести фрагментов
   (`достаточн\w*\s+(?:обработк|переработк)`, `стран\w*\s+происхожден`, `кумулятивн`, `адвалорн` …)
   по-прежнему лежат отдельными записями в `procedural._ANCHOR_PATTERNS`. Сторож полицирует
   `перечн\w*\s+услови` — фрагмент, которого в `procedural.py` не было НИКОГДА, то есть
   `count == 0` утверждается о строке, которой там и не могло быть. ⚠ Хуже: эта же строка
   побайтно лежит ТРЕТИЙ раз в `app/rag/retriever.py:420`, файле, который тест не открывает —
   **инвариант ложен на единственной строке, которую он охраняет.**
3. **`scripts/load_rules_kb.py:727` — механизм `head_buf` заведён в `parse_sng_origin._close_section`
   и НЕ заведён в структурно идентичном `parse_prikaz14._close_section`,** который даёт 287 записей
   из 681 — вчетверо больше. Замерено: 0 записей «(вводная)», **8 значений `section_title`
   обрезаны по запятой**, напр. «…Перечень и порядок предоставления документов,» — хвост
   «необходимых для получения сертификата формы СТ-1» потерян. Девять приложений Приказа №14
   называют разделы одинаково до этой запятой, поэтому печатаемый якорь больше не различает
   СТ-1 / СТ-2 / EAV. **Это класс #105 / `R29`** — верный текст, привязанный не к тому месту, —
   который комментарий этого же файла называет самым дорогим в проекте.
   ⚠ И это записанный урок «урок, выученный в одном модуле, не переезжает в соседний сам»,
   повторившийся **внутри одной пары функций**.
4. **`scripts/load_rules_kb.py:596` — вводная запись берёт номер из пространства НАСТОЯЩИХ пунктов.**
   Воспроизведено на синтетическом корпусе: два разных `point='3.1'` в разделе 3. `by_key`
   (`{(doc_type, section_roman, point)}`) молча оставляет последнюю — подпункт индексируется с
   преамбулой раздела в качестве `parent_intro`. На сегодняшнем корпусе не срабатывает (латентно),
   выстрелит на следующей редакции Соглашения. `test_anchors_stay_unique` поймать не может:
   якоря различаются суффиксом «(вводная)», номера — нет.
5. **`okpd2_ref.py:259` — `_trim_to_code` режет по `rfind(" ")` (ASCII-пробел), а `_TNVED_POSITION_RE`
   склеивает группы через `\s?`,** то есть допускает TAB, перевод строки и **NBSP**. При любом
   не-пробеле `pos == -1` → ветка `pos <= 0` → `None`. `'ТН ВЭД 8403\xa012 шт'` → `None` против
   `'8403'` с обычным пробелом. NBSP — норма для кодов, скопированных из Word, выгрузок ГИСП и
   деклараций. ⚠ **Это класс «предохранитель, который не может сработать никогда»,** найденный
   самим раундом 7, воспроизведённый одной функцией дальше.
6. **`okpd2_ref.py:263` — токен с ДВУМЯ склеенными счётчиками возвращается целиком как
   искажённый ключ:** `'ТН ВЭД 8403 10 20 шт'` → `None` → `'8403 10 20'`; `'ТН ВЭД 8403 10 1 позиций'`
   → `'8403 10 1'` — семь цифр, длины, которой у ТН ВЭД не бывает. Ключ доезжает до
   `st1_ref.conditions_for`, и тот отвечает про несуществующую позицию. Прежнее поведение
   (`None`, блока нет) было строго безопаснее.

## Три LOW

7. **`eval_offdomain_tnved.json:5` — положительный контроль набора не воспроизводится, и в одном
   коммите живут ТРИ разных числа.** `_meta.positive_control` говорит «6 утечек из 8», в файле
   **12** кейсов, измерено на `2f67c19` — **9 из 12** (число из докстринга теста). Арифметика
   «0.87 → 0.89», которой обосновано отдельное хранение набора, посчитана для восьми.
   ⚠ Плюс **3 из 12 кейсов инертны** — они были `route=False` и ДО правки, то есть не могут ни
   упасть, ни впервые пройти. Лечится исполняемостью: `expected_leaks_before: 9` + утверждение.
8. **`procedural.py:196` — мёртвый отложенный импорт с ложным комментарием,** затеняющий новую
   привязку модульного уровня (строка 29). Проверено: `topics` не тянет ни `retriever`, ни
   `embeddings` — лень никогда ничего не предотвращала и предотвратить не может, поскольку
   `_ANCHOR_PATTERNS` строится из `topics.ST1_COMPANION` на импорте. Локальная привязка ещё и
   затеняет модульную: тест, патчащий `procedural.topics`, молча не подействует.
9. **`okpd2_ref.py:254` — `_trim_to_code` возвращает кортеж, второй элемент которого не читает
   никто,** а оба `strip`/`rstrip` доказуемо холостые (группа 1 начинается и кончается на `\d`).
   Цена — сигнал: «регулярка умеет отдавать пробельные хвосты» приглашает искать фантомный баг
   здесь, тогда как настоящий пробельный дефект — строкой ниже, в `rfind(" ")` (находка 5).

---

## ⚠⚠⚠ Два структурных вывода координатора — важнее любой отдельной находки

**Первый: у трёх HIGH ОДИН корень.** Все найденные утечки прошли через `_has_routing_topic`,
то есть через расширенный `ST1_TOPIC_COMPANION`, кормящий гейт. **Дальнейшее сужение
`ST1_COMPANION` будет гонкой за симптомами.**

**Второй: названная причина не тронута вовсе.** Раунд 7 записал диагноз — «у процедурной ветки
НЕТ гейта вне-сферы» — и лечил его словарём. Гейта по-прежнему нет: `search_rules` не имеет порога
релевантности, `_answer_procedural` держит `low_relevance=False` жёстко, а **остальные восемь
якорей** (`реестр`, `заявлени`, `заявк`, `ТПП`, `минпромторг` …) пускают вне-сферные вопросы,
и за ними не стоит ничего.

### Что из этого следует для метода

⚠⚠⚠ **ПРАВКА, ЛЕЧАЩАЯ ПРИЧИНУ СЛОВАРЁМ, — ЭТО ПРАВКА СИМПТОМА, И ОНА ПОРОЖДАЕТ РЕГРЕССИИ
ПАРАМИ.** Два словарных дефекта раунда 7 — **зеркальные**: унификация РАСШИРИЛА (`сертификат`,
`перечн` → новые утечки), а список дисквалификаторов СУЗИЛ (`экспорт`, `вывоз` → потерян главный
вопрос релиза). Оба вне популяции всех наборов репозитория — потому `eval_routing.py` и
отчитался, что 220 прежних вопросов побайтно те же.

⚠⚠⚠ **ЗАМЕР, ВЫБРАВШИЙ МЕХАНИЗМ РАУНДА 7, БЫЛ НЕДЕЙСТВИТЕЛЕН.** Три состояния сравнивались на
наборе, построенном в форме одного из кандидатов (дисквалификатор вплотную). Победитель
«стволы + закрытый список» выиграл у популяции, которая не умела его опровергнуть.
**Выбор между вариантами замером стоит ровно столько, сколько стоит независимость популяции
от вариантов.**

## Что снято с подозрения (повторных раундов не требует)

Управляющих символов в изменённых файлах нет (класс «`\b` → backspace» здесь чист) · сплайсы
f-строк корректно обёрнуты в `(?:…)`, нумерация групп не едет · циклов импорта нет ·
`IGNORECASE`/`[а-яё]`/Ё обработаны верно · `point_id` уникальны на текущем корпусе (681/681) ·
маркеры профиля выкатки 7 + 1 анти грепаются верно, `EXPECT=681` совпадает с выводом парсеров.

**Рабочее дерево не изменено, временный worktree удалён. Батарея зелена на всех находках:
`Ran 1173 tests — OK`.**

# Раунд 10 ревью — остаток раунда 8. Правило остановки, записанное ДО прогона

**Дата:** 07.09.2026 · **Статус на момент записи: ревью НЕ ЗАПУЩЕНО, находок НОЛЬ.**

Шапка коммитится ОТДЕЛЬНО и ЗАРАНЕЕ — по той же причине, что в раундах 8 и 9: критерий,
сформулированный после результата, подгоняется под него (так родился неверный вывод раунда 6
о сходимости).

## Мишень

**`f0c19d8..6d41582` — ровно один коммит, остаток раунда 8**, закрытый 03.09 ПОСЛЕ выкатки
`v0.5.0-test23` и потому не прошедший ни одного раунда ревью. Шесть файлов, 124 вставки /
24 удаления:

| файл | что менялось |
|---|---|
| `app/rag/okpd2_ref.py` | MED-6: счётчик после кода ТН ВЭД («8403 10 1 позиций») больше не съедается в ключ — десятый знак приписывается только к девятизначной основе; LOW-9: `_trim_to_code` возвращает одно значение |
| `app/rag/topics.py` | MED-1: ложное обоснование асимметрии `ST1_TOPIC_COMPANION` заменено замером (166 вопросов пяти наборов: разницы нет ни на одном); MED-2: заголовок «одно определение на весь проект» сужен до «на маршрут» |
| `app/rag/procedural.py` | LOW-8: мёртвый отложенный импорт `topics` убран, привязка модульная |
| `scripts/load_rules_kb.py` | MED-4: запись раздела Приказа №14 нумеруется через `§`, чтобы не совпадать с настоящим пунктом `{прил}.{раздел}.1` — 13 записей сменили `point_id`, текст не изменился ни у одной |
| `scripts/eval_offdomain_tnved.json` | LOW-7: `_meta.positive_control` стал проверяемым числом |
| `tests/test_k14_review_pr137_round7.py` | сторожа к перечисленному + отрицательный контроль к сторожу MED-2 (счёт по строкам БЕЗ комментариев) |

⚠ Не в мишени: слияние ветки железа (`0882cd7`, только `docs/`) и пакет документации (`e20c015`).
Коммиты самого PR #137 прошли по девять раундов и повторно не ревьюятся (решение раундов 8–9:
повторный проход размывает мишень).

## ПРАВИЛО ОСТАНОВКИ

**1. Нет HIGH → PR `dev` → `main`.** Без выкатки: MED-4 корпусная и действует только после
переиндексации, а окно переиндексации — `K16` #48. LOW/MED чинятся в том же PR, если правка не
меняет поведения; меняющая поведение — отдельной issue со своим замером.

**2. Есть HIGH → чиним, и решение о раунде 11 принимает ВЛАДЕЛЕЦ.** Автоматического следующего
раунда нет. Ряд HIGH по девяти раундам PR #137: **2·2·3·2·2·0·2·6·1** — не убывает ничем, «ещё
один раунд» не является измеренным способом приблизиться к цели.

**3. К PR допускается только состояние, где НЕ ПРОСЕЛИ измеряемые оси:**

| ось | значение на момент записи |
|---|---|
| батарея (документированная среда, без `PYTHONUTF8`) | **1217, OK** — 07.09, перепрогнано без пайпа |
| `K9` атрибуция@1 · представленность · нужный пункт | **24/25 · 25/25 · 5/5** — 03.09, после раунда 9 |
| корпус `pp719_rules` локально | **681**; коллекции 1377 / 681 / 12 сходятся с профилем `test23` |
| свип маршрута | вне сферы 53 · процедурных **0**; ложных / пропусков 1 / 15 — 03.09 |

⚠ Оси `K9` и свипа ПОСЛЕ `6d41582` не перемерялись — коммит их не трогал по заявлению автора
(«ось K9 и маршрут не трогались»). **Это заявление — тоже объект раунда:** перемерить свипом
`scripts/eval_routing.py` после ревью и ДО PR.

## Что уже известно и НЕ должно всплывать как находка

Записано заранее, чтобы отличить новую находку от уже принятого решения:

* **порога релевантности у процедурной ветки нет НАМЕРЕННО** — заведён и откачен 03.09 по замеру
  трёх конструкций ([2026-09-03_rules_relevance_gate.md](2026-09-03_rules_relevance_gate.md)),
  отсутствие закреплено тестами; предложение «завести гейт» — повторение отвергнутого;
* **асимметрия `ST1_TOPIC_COMPANION` (`требован` только в теме) ИНЕРТНА и оставлена сознательно** —
  измерено на 166 вопросах, разницы нет ни на одном; хранит записанное намерение эксперта;
  расширять без замера нельзя, потому что кормит гейт;
* **две копии `перечн\w*\s+услови` в `topics.py` и `retriever.py` объединять НЕЛЬЗЯ** — разные
  задачи (тема кормит гейт; квота решает место документа в окне); слияние механизмов с разными
  задачами дало HIGH-2 раунда 8;
* **MED-4 не действует на бою до переиндексации** — записано в трёх документах, не дефект;
* **сторож MED-2 считает строки БЕЗ комментариев** — намеренно: класс «grep поймал имя удалённой
  константы в комментарии, объясняющем удаление» куплен 18.08 остановкой верной выкатки.

## Уровень и метод

`/code-review` уровня **max** по мишени. Каждая находка проверяется на первоисточнике и, где есть
число, замером ДО правки — рекомендация ревью проходит ту же проверку, что правка кода (урок
03.09: за день отменены три предписания раунда 8). Правки по находкам — отдельным коммитом с
полным списком находок и вердиктом по каждой; отклонённая находка получает обоснование здесь же.

---

# РЕЗУЛЬТАТ: 17 находок, **1 HIGH**. Правило остановки НЕ выполнено

**Прогон 07.09.2026, уровень max, 10 углов поиска + сводный проход.** Батарея была ЗЕЛЁНОЙ на
всех семнадцати — коммит `6d41582` прошёл `1217 тестов, OK` и уехал в `dev` непроверенным.

⚠⚠⚠ **HIGH — РЕГРЕССИЯ САМОЙ ПРОВЕРЯЕМОЙ ПРАВКИ.** Раунд 10 ловил остаток раунда 8, и главное,
что он поймал, — что правка MED-6 из этого остатка сломала то, что чинила. Ряд HIGH по десяти
раундам PR #137: **2·2·3·2·2·0·2·6·1·1**.

## Находки и вердикты

| № | тяжесть | где | суть | вердикт |
|---|---|---|---|---|
| 1 | **HIGH** | `okpd2_ref._trim_to_code` | MED-6 усекала НАСТОЯЩИЕ девятизначные коды записи 4+2+2+1 («0710 40 00 0» → «0710 40 00») | **исправлено**, признак заменён |
| 2 | MED | `okpd2_ref._trim_to_code` | длина ключа не проверялась на ОБЫЧНОМ пути: «ТН ВЭД 8403 10 1» → семизначный ключ | **исправлено** тем же признаком |
| 3 | MED | `okpd2_ref._trim_to_code` | двух- и трёхзначный счётчик приклеивался, терялась оговорка `narrower` | **исправлено** тем же признаком |
| 4 | MED | `test_..._round7.py` | сторож, заведённый в этом же коммите против слепоты, был удовлетворён ДОКСТРИНГОМ цели | **исправлено**, счёт по код-объекту |
| 5 | MED | `load_rules_kb.py` | у MED-4 нулевое мутационное покрытие: возврат схемы `§` не краснит батарею | **исправлено**, сторож + мутация |
| 6 | MED | `okpd2_ref._trim_to_code` | довод «подъём к родителю стоит более общего условия» ложен у 73 кодов из 79 | **исправлено** (текст + цена закреплена тестом), поведение оставлено |
| 7 | MED | `load_rules_kb.add_index_text` | ДЕВЯТЬ живых коллизий `by_key`, сторож MED-4 слеп к ним по построению | **исправлено**, неоднозначный родитель не прикрепляется |
| 8 | LOW | `test_k14_round8_section_titles.py` | докстринг сторожа описывает нумерацию, которую заменила MED-4 | **исправлено** |
| 9 | LOW | `test_..._round7.py` | обоснование, признанное ложным в раунде 8, уцелело в докстринге теста, который его опровергает | **исправлено** |
| 10 | MED | `topics.py` | «она ИНЕРТНА» — вывод шире инструмента: числа мерили МАРШРУТ, а тема меняется на 5 пробниках из 5 | **исправлено** (текст + сторож на тему) |
| 11 | LOW | `eval_offdomain_tnved.json` | «Проверяется тестом» — не читал НИКТО; «первых 12 кейсах» ≠ `cases[:12]` | **исправлено**, популяция поимённо + сторож |
| 12 | LOW | `procedural.py` | «`topics` не тянет ни того, ни другого» верно только для ВРЕМЕНИ ИМПОРТА | **исправлено** |
| 13 | LOW | `test_..._round7.py` | «отрицательный контроль» приколот к ПРОЗЕ, добавленной тем же коммитом; на родителе был бы КРАСНЫМ | **исправлено**, контроль на синтетике |
| 14 | MED | `test_..._round7.py` | `OFF_DOMAIN` — замороженная копия 12 строк, а в наборе 14: у двух пробников не было сторожа маршрута | **исправлено**, читается из набора |
| 15 | MED | `okpd2_ref.extract_tnved_position` | `gap` — расстояние в ТЕКСТЕ, но считался от УСЕЧЁННОГО ключа; код внутри окна выпадал | **исправлено** |
| 16 | LOW | `topics.py` | комментарий MED-2 называет таблицу `_DOC_HINTS`, которой в проекте НЕТ, и 1 общий фрагмент из 6 | **исправлено**, перечисление держит тест |
| 17 | LOW | `load_rules_kb.py` | довод отказа от `head_buf` ссылается на MED-4, которую тот же коммит и снял | **исправлено** |

**Отклонённых находок нет.** Все семнадцать подтверждены исполнением кода, не чтением.

## Замеры радиуса

**Находка 1 (HIGH).** Три состояния разбора на ВСЕЙ таблице Перечня, 1895 запросов
(241 код × формы записи × слова-счётчики):

| состояние | отдаёт не тот код | 9-значные 4+2+2+1 со счётчиком (236 запросов, 59 кодов) |
|---|---|---|
| `f0c19d8` (до MED-6) | 1040 | усечено **0** |
| `6d41582` (проверяемый) | 1276 | усечено **236 из 236**, все 59 кодов |
| после правки | 1040 | усечено **0** |

⚠ **Числа называют свою величину:** КЛЮЧ усекался у **59 из 59** девятизначных кодов, а ВЕРДИКТ
«включён в Перечень» терялся у **53 из 59** — шесть уцелели совпадением по ПРЕФИКСУ, а не потому,
что разбор их не тронул. На основе `code ∪ code_to` (61 код) вердиктов теряется 54; отчёт ревью
считал по ней, здесь считается по `code`. Прежняя запись «53 строки Перечня из 59» смешивала две
величины и исправлена.

Относительно проверяемого коммита: **починено 236, сломано 0.** Относительно состояния ДО MED-6:
**регрессий 0, улучшений 0** — то есть разбор вернулся к прежнему поведению на этом свипе
ПОБАЙТНО (1895 из 1895), а новые случаи закрыты находками 2–3. Положительный контроль: канонная
запись 4+2+3 не страдала ни в одном состоянии (0 из 295).

**Находка 15.** 9600 запросов, 6 формулировок с маркером ПОСЛЕ кода: **240 расхождений, ВСЕ 240 —
«было None → стал код»**, потерь 0, подмен одного кода другим 0. Все 240 возвращённых ключей
осмысленны (код или его товарная позиция). Копия рантайма сверена с `extract_tnved_position`.

**Находка 7.** `index_text` и `parent_intro` всех 681 записи сверены до/после:
**расхождений 0, sha256 совпал** (`a6a95cba02e2bf43`), записей с `parent_intro` 87 → 87. Радиус
сегодня нулевой — предохранитель заведён ДО того, как корпус родит пострадавшего. ⚠ Отсюда
следствие для выкатки: **находка 7 переиндексации НЕ требует** (payload не меняется), в отличие
от MED-4.

**Находка 6.** Из 241 кода Перечня 79 длиннее четырёх знаков; у **73** родитель условия не несёт
вовсе. Но подъём в коде достижим только при четырёхзначной ОСНОВЕ, то есть у шестизначного кода
записи 4+2 со словом-счётчиком: таких кодов **19, и у всех 19** подъём теряет вердикт. Поведение
оставлено прежним (см. `_trim_to_code`), цена закреплена `TestParentClimbCostIsRecorded`.

**Находка 10.** Мутация `ST1_TOPIC_COMPANION = ST1_COMPANION` **в исходнике**: тема меняется у
**5 пробников из 5**, маршрут — у **0 из 19**. ⚠ Первый мой замер дал «0 из 5» и был пустым:
константа интерполируется в скомпилированный регексп на загрузке модуля, поэтому переприсваивание
атрибута не мутация вовсе. Инструмент, не видящий популяцию, печатает честный ноль.

## Мутационная проверка сторожей

Каждая правка ОТМЕНЕНА в исходнике, названный тест обязан покраснеть, файл возвращён:

| мутация | тест | итог |
|---|---|---|
| `gap` снова от длины ключа | `TestGapIsATextDistance` | краснеет |
| вернуть признак «десятый знак при девятизначной основе» | `TestNineDigitCodeSurvivesEveryGrouping` | краснеет |
| снять проверку длины на обычном пути | `TestNoImpossibleKeyEverReachesTheLookup` | краснеет |
| множество длин → «любая длина» | `TestKeyLengthsComeFromTheSource` | краснеет |
| вернуть словарь-компрехеншен | `TestParentIntroNeverTakesAnAmbiguousParent` | краснеет |
| вернуть старую схему нумерации | `TestSectionRecordNumberingIsPinnedExecutably` | краснеет |
| убрать общий фрагмент из ретривера | `TestSharedFragmentsAreEnumerated` | краснеет |
| гейт перестал звать тему | `TestTopicVocabularyIsWiderByConstruction` | краснеет |
| популяция набора переехала | `TestOffDomainSetPopulationIsPinned` | краснеет |

Все девять на восстановленном дереве — зелёные.

⚠⚠⚠ **ЭТА ЖЕ ПРОВЕРКА НАШЛА ДЕФЕКТ В МОЕЙ СОБСТВЕННОЙ ПРАВКЕ, И ОН ИЗ ТОГО ЖЕ КЛАССА, ЧТО
НАХОДКА 13.** Сторож находки 4 во второй редакции считал `topics.classify` по ТЕЛУ функции без
докстринга — и **прожил один час**: мой же комментарий к находке 12 назвал `topics.classify` в
теле `_has_routing_topic`, мутационная половина сторожа покраснела **на верном коде**. То есть
починка «считать тело вместо всего исходника» воспроизвела ровно ту болезнь, от которой лечила
находка 13 («предохранитель, бьющий по верному коду, дороже отсутствующего», куплено 18.08
остановкой ВЕРНОЙ выкатки). Вывод шире одного сторожа: **считать ТЕКСТ функции нельзя вообще** —
любой текст вокруг кода прозаический, а инвариант про КОД. Сторож переписан на `co_names`
код-объекта: комментарии и докстринг туда не попадают ПО ПОСТРОЕНИЮ.

## Оси — перемерены ПОСЛЕ правок

Правило остановки требовало перемерить заявление автора «ось `K9` и маршрут не трогались».
Перемерено, и оно **подтвердилось**:

| ось | до раунда | после правок |
|---|---|---|
| батарея (документированная среда, без `PYTHONUTF8`) | 1217, OK | **1246, OK** |
| `K9` атрибуция@1 · представленность · нужный пункт | 24/25 · 25/25 · 5/5 | **24/25 · 25/25 · 5/5** |
| свип маршрута (237 вопросов, 7 наборов) | вне сферы 53 · процедурных 0 · ложных 1 · пропусков 15 | **то же, побайтно** |
| коллекции Qdrant | 1377 / 681 / 12 | **1377 / 681 / 12** |

Ось `K9` не сдвинулась за десять раундов подряд.

## Что это меняет для выкатки

**Ничего не требует переиндексации сверх уже известного.** Находка 7 payload не меняет (sha256
`index_text` совпал), находки 1–3, 6, 15 живут в разборе запроса — рантайм. Единственная
корпусная правка пакета по-прежнему MED-4 из `6d41582`, и она едет окном `K16` #48.

## Вердикт по правилу остановки

**Пункт 2: HIGH был — значит «нет HIGH → PR» не выполнено, и решение о раунде 11 принимает
ВЛАДЕЛЕЦ.** Автоматического следующего раунда нет по правилу, записанному ДО прогона.

⚠ Довод в пользу раунда 11: HIGH десятого раунда сидела в правке ВОСЬМОГО, найденной ревью, —
то есть пакет правок по ревью снова оказался объектом ревью (четвёртый случай в проекте).
Правки этого раунда никем не проверены.

⚠ Довод против: ряд HIGH **2·2·3·2·2·0·2·6·1·1** не убывает ничем, «ещё один раунд» не является
измеренным способом приблизиться к цели, а `K16` #48 — основная работа — не начата.

# База сравнения замеров (`EV14` #98)

> Заведён 21.08.2026. До этого каталога не существовало, хотя `EVAL_GUIDE §1.1` требовал его
> с первого прогона: из двух заявленных механизмов работал только один — **абсолютные пороги
> ловили провалы, но дельта не считалась ни разу**, то есть метрика, медленно ползущая вниз и
> остающаяся выше порога, не была бы замечена вовсе. Ровно тот класс, который гайд называет
> «тихой деградацией».
>
> Что именно так и произошло: 19.08 метрика «назвал источник требований» просела 3/3 → 2/3 = 0.67
> при пороге 1.00 и была **удалена из обеих сводок**, пока строка показывала ✅. Число выжило
> только в сыром отчёте. База не спасла бы от удаления, но сделала бы просадку видимой без
> человека.

## Что здесь лежит

| Файл | Что это |
|---|---|
| `baseline.json` | Числа базы в машиночитаемом виде — то, с чем сравнивается следующий прогон |
| `2026-08-25_test17_full.md` | Замороженная копия отчёта, из которого взяты числа. **Не редактируется** |

## Правило обновления — база живёт по релизам, а не по прогонам

**База обновляется ТОЛЬКО на релизном теге**, то есть после выкатки. Если обновлять её на каждом
прогоне, дельта всегда будет нулевой: каждый следующий замер сравнивается с предыдущим, и
медленный спуск на 0.02 за прогон окажется «без изменений» шесть раз подряд, а суммарные 0.12
не увидит никто. Именно от этого база и защищает.

Как обновлять:

1. Прогон уровней 0–3 идёт на коммите, который выкатывается (не «примерно на нём» — см. ⚠ ниже).
2. Отчёт прогона кладётся в `docs/eval_runs/` как обычно.
3. Его копия кладётся сюда с пометкой «снимок, не редактируется», числа переносятся
   в `baseline.json`, прежние файлы базы удаляются (база одна, история — в `eval_runs/`).
4. В шапке `baseline.json` обязательны `commit`, `release_tag` и состав коллекций: без них
   нельзя сказать, что именно было базой.
5. **Ссылка «База сравнения» в шапке отчёта ведёт в `eval_runs/`, а не сюда.** Каталог базы
   ротируется — файл, на который сослался отчёт, исчезнет при следующем релизе, и ссылка станет
   висячей. Так и произошло: отчёты 21.08 и 24.08 ссылались на снимки в `eval_baseline/`, и обе
   ссылки сломались ротацией (исправлено 25.08). В `eval_runs/` копия лежит вечно.
6. **У каждого числа в `baseline.json` обязана быть КОМАНДА, которой оно считается** — поле
   `_command` рядом с метрикой, вместе с параметрами, если они на число влияют.
   ⚠ Заведено 25.08.2026, и повод хуже, чем кажется. Блок `context_size` лежал в базе с 21.08
   («6 890 → 5 034, хвост 0.50 → 0.31»), был помечен «перемерить в следующем прогоне» — и в
   следующем прогоне выяснилось, что мерить его НЕЧЕМ: скрипта не существовало. Число, которое
   нечем пересчитать, — не критерий, а цитата; проект уже отзывал такие однажды (`K2`, «24.6 % →
   18.4 %»). Пометка «перемерить» проблему не решает — откладывает.
   ⚠ Параметры входят в команду не для красоты: перефраз при другом `--per-section` или `--seed` даёт
   другую выборку, и сравнивать его с базой нельзя вовсе.

## Что сравнивается и что считать регрессией

Сравниваются **все метрики из `baseline.json`**. Решение принимается по двум правилам гайда
одновременно:

| Правило | Откуда | Как применяется к базе |
|---|---|---|
| Абсолютный порог | колонка `threshold` | Ниже порога — блокирует выкатку независимо от дельты |
| Значимость дельты | `EVAL_GUIDE §1.2` | Изменение значимо, если **дельта средних больше удвоенного разброса**. Иначе метрика «не изменилась», и её не надо ни праздновать, ни расследовать |
| Направление | `direction` | `невозрастание` — рост числа коллизий или падение recall расследуется, даже оставаясь в пороге |

⚠ **Метрики с известным разбросом на неизменном коде** (`stable_number_set`: полоса 0.60–0.90)
сравнивать по одиночному прогону нельзя вовсе — только среднее трёх и разброс. Это записано
в самой метрике полем `note`, чтобы дельта не читалась как эффект правки.

⚠ **Метрика, исчезнувшая из сводки, — это регрессия, а не «нет данных».** Если в новом прогоне
нет числа, которое есть в базе, прогон неполон; сводка не имеет права показывать ✅.

## Текущая база — `v0.5.0-test18` (26.08.2026)

Числа сняты на `32cbab0`. Петля кейсов выключена (§1.1).
Корпус: `pp719` 1377 · `pp719_rules` 328 · `verified_cases` 12.

### ⚠⚠ Эта база обновлена ДО выкатки — условие её действительности

Правило 1 требует, чтобы числа были сняты на ВЫКАЧЕННОМ коммите. Здесь порядок обратный по
решению владельца: база обновлена сразу после прогона, выкатка следом.

**База действительна, только если выкачен РОВНО `32cbab0` тегом `v0.5.0-test18`.** Выкатили
другой коммит — правка после прогона, откат, hotfix — база недействительна, и её надо
**переснять**, а не досогласовывать задним числом. Это записано и в `_meta` самого `baseline.json`,
чтобы проверять можно было машинно.

### Что вошло в релиз

`EV21` #119 (документные кейсы в наборе, 46 → 50, общий оракул) · `K15-1` #120 (асимметрия
разрыва в маркерах маршрута + согласование гейта блока справочника с гейтом его содержимого) ·
`P3` #121 (намерение вопроса стало сигналом маршрута, пропуски 17 → 11) · пакет из восьми находок
`/code-review`, две из которых — в правках #120 и #121.

### Что появилось в этой базе впервые

| Что | Почему раньше не было |
|---|---|
| **`level2_documents`** | Кластер жалоб №1 не имел в наборе НИ ОДНОГО кейса (`EV21` #119) |
| **`whole_prompt`** в `context_size` | Мерилось только заземление, а блок документов в него осознанно не входит — правки этого блока по прежнему числу не видны в принципе |
| `measured_at` у непеременных блоков | Прежде «перенесено» и «снято заново» выглядели в базе одинаково |
| Пометка «эта метрика не видит маршрут» у `rules_axis` | `P3` #121: `eval_rules` ходит в корпус мимо гейта |

### ⚠ Что в этом прогоне НЕ перемерялось

`recall@1` и весь товарный свип · уровень 3 целиком · `corpus_metrics`. У каждого блока стоит
`measured_at` с коммитом и причиной. Обоснование одно и **проверяемое командой**:
`git diff main...dev -- app/` меняет только `prompts.py`, `documents_ref.py`, `pipeline.py`,
`procedural.py`; `retriever`, `embeddings`, `sparse`, `reranker`, корпус и загрузчик не тронуты.

⚠ Уровень 3 перемерить **обязательно** на первом же релизе, который тронет поиск, корпус или
ранжирование: именно на нём `EV19` #111 поймала два релиза подряд с частичным исполнением.

### ⚠⚠ Чего эта база НЕ покрывает

**Эффект `P3` #121 на уровне ОТВЕТА.** Девять переехавших вопросов лежат в
`eval_golden_rules.json` и `eval_topics.json`, а `eval_answers` читает `eval_golden.json` — их там
нет. Улучшение подтверждено свипом `scripts/eval_routing.py` (детерминированный, офлайн) и живыми
прогонами, но в постоянную батарею оно не попало. Ровно класс, который вскрыла `EV21`: **чего нет
в наборе, того для замера не существует.**

**Latency и нагрузка (уровень 4) не измерены** — за пять заходов ни разу. Мерить на VM: пороги
гайда описывают 2 vCPU, а не машину разработчика.

### ⚠ Две метрики сдвинулись популяцией, а не кодом

Полоса out-of-scope: негативов 36 → 39 (ревью потребовало завести класс «членство в ТПП»),
медиана 0.811 → 0.809 при неизменных границах 0.734 / 0.852. Уровень 2 и `context_size` считаются
срезом `--kind product` — без него набор даёт 46 кейсов вместо 42.

**Это третий за день случай одного класса.** Правило 6 («у каждого числа обязана быть команда»)
ломается не только удалением скрипта, но и **расширением набора**: команда прежняя, популяция под
ней другая.

### ⚠ Судьба предсказания прошлой базы

В базе `v0.5.0-test15` стояло: «следующий прогон законно покажет меньший `context_size`». Прогон
25.08 дал те же числа — мерить было не на чем. 26.08 предсказание наконец проверено: на срезе
`--kind product` числа не двинулись (4934 / 3852), а вот на документной популяции промпт виден и
равен 13 293 / 11 794. Предсказание было верным по сути и непроверяемым по инструменту.

_История баз: 20.08 (`zahod2`) → 21.08 (`zahod3` = `v0.5.0-test13`) → 24.08 (`zahod4` =
`v0.5.0-test14`) → 25.08 (`zahod5` = `v0.5.0-test15`) → 25.08 (`test17_full` =
`v0.5.0-test17`) → 26.08 (`level12` = `v0.5.0-test18`, текущая).
Отчёты всех прогонов — в `docs/eval_runs/`._

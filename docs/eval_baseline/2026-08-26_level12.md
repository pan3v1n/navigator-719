# Прогон уровней 0–2 — пакет 26.08.2026 (`EV21` + `K15-1` #120 + `P3` #121 + ревью)

> **Коммит:** `32cbab0` на `dev`. **На бою** по-прежнему `v0.5.0-test17` (`d02702f`) — пакет НЕ выкачен.
> **База сравнения:** [eval_runs/2026-08-25_test17_full.md](2026-08-25_test17_full.md).
> **Предусловия §1.1:** дерево чистое, кейсы выключены (`QDRANT_CASES_COLLECTION=__eval_disabled__`),
> `EXACT_SEARCH=1`. Коллекции — `pp719` 1377 · `pp719_rules` 328 · `verified_cases` 12.

## Что мерили и почему именно так

Пакет дня меняет ТРИ вещи в поведении: маршрут вопроса (`P3`), гейт закрытого справочника
(`K15-1`) и шапку блока документов в промпте (ревью). Товарный ретрив не тронут — поэтому свип
recall@1 пропущен **обоснованно**, а не забыт (см. ниже).

⚠⚠ **Популяции разведены.** Числа базы сняты на 42 ТОВАРНЫХ кейсах; `EV21` добавила четыре
документных. Считать их вместе значит смешать эффект правки со сменой знаменателя, поэтому у
каждого замера свой срез `--kind`.

---

## Уровень 0 — целостность ✅

| Метрика | Порог | База | **26.08** |
|---|---|---|---|
| Юнит-тесты | все зелёные | 767 | **828 / OK** |
| `verify_structured --records` | 0 | 0 | **0 на 1366 позиций** |
| `reconcile_kb` | 29/29 | 29/29 | **29/29** |

## Уровень 1 — ретрив ✅

### Процедурная ось — совпала с базой ПОБАЙТНО

| Метрика | Порог | База | **26.08** |
|---|---|---|---|
| Атрибуция@1 | ≥0.95 | 0.96 | **0.96** |
| Представленность | 1.00 | 1.00 | **1.00** |
| Точность темы | — | 0.84 | **0.84** |
| Доля окна: Приказ №52 / Правила / ПП №719 | — | 0.70 / 0.61 / 0.38 | **0.70 / 0.61 / 0.38** |

Детерминизм `M2` держится.

⚠ **И эти числа НЕ реагируют на `P3` — по построению.** `eval_rules` дёргает `search_rules`
напрямую, минуя гейт маршрута. Ровно то, что `P3` #121 и вскрыла: метрика описывает качество
поиска В корпусе, а не попадание вопроса в корпус. Эффект `P3` меряет свип маршрута.

### Гейт out-of-scope

| Величина | База (36 негативов) | **26.08 (39)** |
|---|---|---|
| min / max по негативам | 0.734 / 0.852 | **0.734 / 0.852** |
| медиана | 0.811 | 0.809 |

⚠ **Медиана сдвинулась популяцией, а не кодом:** ревью потребовало завести класс «членство в
ТПП», негативов стало 39. Границы полосы не двинулись. Оговорка внесена в `baseline.json`.

### Товарный свип recall@1 — ПРОПУЩЕН ОБОСНОВАННО

`git diff main...dev -- app/` меняет ровно четыре файла: `prompts.py`, `documents_ref.py`,
`pipeline.py`, `procedural.py`. `retriever.py`, `embeddings.py`, `sparse.py`, `reranker.py`,
`okpd2_ref.py`, корпус и загрузчик — **не тронуты ни строкой**, товарный ретрив измениться не мог.
Тот же приём, что 25.08; проверка выполнена командой, а не памятью.

---

## Уровень 2 — ответ, ТОВАРНАЯ популяция (42 кейса, 3 прогона) ✅

| Метрика | Порог | База | **26.08** |
|---|---|---|---|
| Faithfulness | ≥0.95 | 1.00 ×3 | **1.00 ×3** (440 / 423 / 442 числа, выдумано **0**) |
| Порог доехал до ответа | ≥0.90 | 1.00 ×3 | **1.00 ×3** (19 / 19 / 18) |
| Баллы операций доехали | ≥0.90 | 1.00 ×3 | **1.00 ×3** (134 / 138 / 129 чисел) |
| Предупредил о неполноте | 1.00 | 1.00 ×3 | **1.00 ×3** (5/5) |
| Назвал источник требований | 1.00 | 1.00 ×3 | **1.00 ×3** |
| Инлайн-цитаты `[N]` | — | 1.00 ×3 | **1.00 ×3** (42/42) |
| Без CJK | — | — | **1.00 ×3** |
| Отказ вне сферы | 1.00 | 4/4 ×3 | **4/4** |
| **Все восемь запретов §2.2** | 1.00 | 1.00 ×3 | **1.00 ×3** |
| Атрибуция раздела (СТРОГО) | нет (`EV20`) | 0.90 / 1.00 / 0.95 | 0.95 / 0.90 / 0.93 |
| _справочно_ лексика раздела | нет | 0.98 / 1.00 / 1.00 | 0.98 / 0.98 / 1.00 |

### Атрибуция: дельта НЕ значима, и это считается, а не оценивается

Среднее 0.950 → **0.927**, дельта **0.023**. Разброс базы 0.050, текущий 0.025; правило `§1.2`
требует дельту больше УДВОЕННОГО разброса — то есть больше 0.050. **0.023 < 0.050 → метрика не
изменилась.**

⚠ Прогон снова подтверждает `EV20` #117: падает не фиксированный набор, а **плавающий** —
#43 (XXV) в двух прогонах, #23 (IX) в двух, #46 (XI) в двух. **Исключение — #45 (XXIX)
«сейсмические виброисточники»: падает во ВСЕХ ТРЁХ.** Это единственный кандидат на воспроизводимый
дефект атрибуции, остальное — нестабильность следования правилу.

### Контекст — воспроизвёл базу ТОЧНО

| Метрика | База | **26.08 (`--kind product`)** |
|---|---|---|
| Заземление: средняя / медиана | 4934 / 3852 | **4934 / 3852** |
| Доля хвоста | 0.33 | **0.33** |

_(новое, в базе пока нет: весь промпт 5477 / 4206.)_

---

## Уровень 2 — ДОКУМЕНТНАЯ популяция (4 кейса, 3 прогона) — впервые в штатном прогоне

| Метрика | ДО правок дня | **26.08** |
|---|---|---|
| Разъяснение по условию (`K15`) | 0.75 ×3 | **1.00 ×3** |
| Несуществующий документ не утверждён | 0.75 ×3 | **1.00 ×3** |
| Faithfulness | 1.00 ×3 | **1.00 ×3** |
| Закрытый перечень доехал | 1.00 ×3 | 0.75 / 0.75 / 1.00 |
| Источник назван | 0.67 / 1.00 / 0.67 | 1.00 / 0.67 / 0.67 |

**Две метрики, ради которых делались #120 и #121, вышли на 1.00 и держатся все три прогона.**
Кейсы #48, #49, #50 чисты во всех трёх.

⚠ **Вся нестабильность сидит на одном кейсе — #47** («какие документы подготовить для
подтверждения производства»), каноническом вопросе кластера: в двух прогонах из трёх он не
называет «Сведения о реестровой записи», в двух из трёх не ссылается на Методрекомендации. Тот же
класс, что `EV20`: соблюдение правила плавает. ⚠ Промпт кейса #47 правками дня НЕ ЗАТРОНУТ —
проверено побайтно (заземление 9978, промпт 11161, справочник 727 до и после), то есть это
разброс генерации, а не радиус.

### Контекст документных кейсов

| Метрика | Утром (до #120/#121) | **26.08** |
|---|---|---|
| Заземление: средняя / медиана | 8209 / 8330 | **6581 / 6493** |
| Весь промпт: средняя / медиана | 15 275 / 14 102 | **13 293 / 11 794** |

Просадка законна и объяснима: кейс #50 переехал на процедурную ветку, где контекст — нормы Правил
(6304), а не 12 817 символов требований к охранной сигнализации.

---

## Свип маршрута (`P3`, офлайн, 191 вопрос)

| Величина | До пакета | **26.08** |
|---|---|---|
| Пропуски (процедурный вопрос → товарная ветка) | 17 | **11** |
| Ложные срабатывания на товарных | 1 | **1** (прежнее, `topics#45`) |
| Вне сферы 719 на процедурной ветке | — не считалось | **0** |
| Документные кейсы не по своему маршруту | — не считалось | **0** |

---

## Вывод

**Регрессий нет. Все метрики с порогом выполнены, ни одна не просела значимо.**
Две метрики документного кластера выросли с 0.75 ×3 до 1.00 ×3 — это и есть цель пакета.

**База сравнения НЕ обновляется:** прогон не на релизном теге (правило 1 базы). Обновить — после
выкатки, и тогда же внести документную популяцию и числа свипа маршрута.

## Что этот прогон НЕ покрывает

* **Эффект `P3` на уровне ответа.** Девять переехавших вопросов лежат в `eval_golden_rules.json` и
  `eval_topics.json`, а `eval_answers` читает `eval_golden.json` — их там нет. Улучшение
  подтверждено свипом (детерминированный, офлайн) и живыми прогонами; чтобы оно попало в
  постоянную батарею, вопросам `K9` нужен ответный уровень — той же машинерией, что сделала `EV21`.
  ⚠ Это ровно класс, который `EV21` и вскрыла: чего нет в наборе, того для замера не существует.
* **Уровень 3** (детерминизм, перефраз, возмущения) — не гонялся: правки не задевают ретрив.
* **Уровень 4** (latency, нагрузка) — по-прежнему ни разу, нужен VM.

## Очередь после прогона

1. Выкатка пакета — **рантаймовая**, корпус не тронут (коллекции те же 1377 / 328 / 12).
   ⚠ После — живая проверка ПОВТОРНАЯ, 3–5 прогонов.
2. Заход 6, `K14`.
3. Долги: `K12-1` #122 · `EV20` #117 (кейс #45 XXIX — единственный воспроизводимый) · `O4` #107.

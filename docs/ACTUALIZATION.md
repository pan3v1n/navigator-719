# Актуализация базы RAG под новую редакцию ПП №719

> ПП 719 правится 6+ раз в год. База RAG — **снимок** редакции; отставание даже на одну
> редакцию = формально неверный ответ (баллы/пороги/позиции). Это приоритет №1 по концепции
> продукта («актуализация раньше функций»). Здесь — переиспользуемый процесс + **конкретный
> план на ближайшую сессию** (редакция от 01.07.2026, аудит сделан 2026-07-03).

---

## ✅ ВЫПОЛНЕНО 2026-07-05 (редакция 01.07.2026)

Актуализация проведена по плану ниже. Итог:
- **VII (медизделия)** перепарсен: +21 позиция (протезы `32.50.23.131–171`, тест-полоски
  `21.20.23.110`, экзопротез, кало/моче/уроприёмники, воск `20.59.52.120`; детектор `26.60.11.130`
  перепрошит, N 794). 108→129 продуктов + запись-методичка (7 прозовых порогов).
- **VIII (фарма)** перепарсен: модель бинарная→**балльная** (50 баллов/процесс, N 1392); коды
  `21.10`→`из 21.10 (за искл. 21.10.51)`, добавлен `21.20.23`. **Порог п.63 (≥50 баллов) внесён
  вручную в `min_threshold` обоих продуктов** — порог живёт в примечаниях, не в таблице раздела,
  поэтому при повторном `structure_kb --section VIII` его надо ПРОСТАВИТЬ СНОВА (иначе теряется).
- `pp719_full.txt` + чанк примечаний (`130_PRIMECHANIYA`) обновлены под полную редакцию 01.07.2026;
  **VII/VIII добавлены в `rechunk_appendix.TARGETS`** (при смене исходника их чанки надо регенерить —
  раньше rechunk их не трогал как «уже корректные»).
- Проверка: **verify 29/29 ✅, reconcile 2/2 ✅**; реиндекс **1357→1379 точек / 1347→1368 продуктов**
  (F1 identity, :6533); **recall@1 0.98 / MRR 0.988 без регресса**; живой фарма-ответ стал балльным
  с порогом ≥50. DeepSeek-перепарс ≈¥0.97.
- Opus-сверка краёв: VIII-модель верна; таблица **п.81** (протезы 2026–2030) — материал×год,
  корректно лежит в чанке примечаний.

**ОСТАЛОСЬ ДЕФЕРОМ (1.5 — процедурный/примечания-корпус, сейчас НЕ индексируется):**
- Пороги-примечания **п.18** (28.22.16.111), **п.33** (медпром-таблица), **п.70** (подшипники),
  **п.81** (протезы 2026–2030, материал×год) — живут в `130_PRIMECHANIYA`, но не структурированы
  и не грузятся в Qdrant. Их структуризация = будущая работа по корпусу примечаний.
- **Правила формирования реестра** + тело постановления (критерий «д», фарма→Минпромторг, N 1392) —
  процедурные, дефер к 1.5.
- Пред-существующий артефакт нарезки: 1 «мусорный» продукт VII (`14.12.30.160`, исключённая позиция
  с именем-фрагментом «или наличие…») — был и в старой базе, reconcile его пропускает; на сверку эксперту.

**СЛЕД:** редеплой актуализированной базы на ту же VM (юзер стартует её в консоли → новый IP →
`git archive dev | scp` + `docker compose up -d --build` + `load_kb` на VM, т.к. база изменилась).

---

## Переиспользуемый процесс (для любой новой редакции)

1. **Положить новый RTF** в корень (тот же источник, что и текущий — КонсультантПлюс, cp1251).
2. **Сверка (не мутирует базу):**
   ```bash
   PYTHONUTF8=1 .venv/Scripts/python.exe scripts/diff_edition.py --new-rtf "<новый>.rtf"
   ```
   Даёт: новые постановления, добавленные/удалённые позиции, -/+ по разделам, `full_diff.txt`.
3. **Понять дельту** по `full_diff.txt`: отделить **реальные правки** (новые позиции, изменённые
   баллы/пороги/операции) от **шума реэкспорта** (переносы `|`, «стран-членов»→«стран - членов»).
4. **Обновить исходник:** заменить `knowledge_base/pp719/pp719_full.txt` новым текстом
   (`scratchpad/edition_diff/pp719_new_full.txt`) → `python scripts/rechunk_appendix.py`
   (регенерит `chunks/*.txt`).
5. **Перепарсить ТОЛЬКО затронутые разделы** (остальные не трогать — DeepSeek недетерминирован,
   лишний перепарс = churn):
   ```bash
   PYTHONUTF8=1 .venv/Scripts/python.exe scripts/structure_kb.py --section <ROMAN>
   ```
6. **Проверить:** `verify_structured.py` (потери состава/баллов) + `reconcile_kb.py` (сверка по
   кодам исходник→JSON). ⚠ пайп-таблицы порогов в примечаниях парсер может принять за продукты.
7. **Реиндексировать:** поднять Qdrant (порт **6533** локально) → `python scripts/load_kb.py`
   (реиндекс дефолтом = **F1 identity-эмбеддинг**, НЕ `--full-text`).
8. **Замер без регресса:** `eval_retrieval.py` (recall@1 ~0.972) + `eval_answers.py` (faithfulness).
9. Обновить дату/редакцию в доках; на VM приедет при следующем редеплое (тома данных
   пересобираются из свежих JSON; юзер стартует VM в консоли, IP сменится).

---

## КОНКРЕТНЫЙ ПЛАН на следующую сессию — редакция от 01.07.2026

**Файл:** `Постановление Правительства РФ ... от 01.07.2026.rtf` (в корне, 18.7 МБ).
**Аудит сделан 2026-07-03** (перепарс НЕ запускался — только сверка).

**Наша база отстаёт на 4 постановления** (в новом есть, у нас нет):
| N | Дата | Что задело |
|---|---|---|
| **1392** | 10.09.2025 | VIII фарма → балльная модель; критерий «д»; фарма-заявки в Минпромторг (Правила) |
| **1789** | 13.11.2025 | подшипники 28.15.10 (примечания п.16, п.70) |
| **746** | 15.06.2026 | п.18 (28.22.16.111) порог |
| **794** | 27.06.2026 | детектор 26.60.11.130 + таблица п.33 (медпром) |
| _(351 от 31.03.2026 — был в шапке, но контент протезов приехал только сейчас)_ | | ~20 новых позиций VII + таблица п.81 |

**Реальные правки — ТОЛЬКО 2 из 29 товарных разделов + пороги (остальные 27 идентичны):**

- 🔴 **VIII (Фармацевтика)** — модель переписана бинарная→**балльная** (N 1392). Коды:
  `21.10`→`из 21.10 (за искл. 21.10.51)`, добавлен `из 21.20.23`. **Текущие ответы по фарме
  формально неверны.** → перепарс `structure_kb --section VIII`.
- 🔴 **VII (Медизделия)** — ~20 новых позиций-протезов (N 351: узлы кисти 32.50.23.161/162/163,
  локтевой .171, стопы .131/.132/.139, коленные .141–.149, тазобедренный .151, тест-полоски,
  экзопротез, кало/моче/уроприёмники, воск) + переработка детектора 26.60.11.130 (N 794) +
  исключённые позиции. → перепарс `structure_kb --section VII`.
- 🟠 **Пороги-примечания:** п.18 (28.22.16.111 → до 2026 ≥120 / с 2027 ≥135, N 746); п.33
  (таблица медпром, колонки 2023/24/25 → **2026/27**, значения ↑, N 794); п.63 (фарма ≥50);
  **п.81 (новая таблица протезов 2026–2030, N 351)**; п.16/п.70 (подшипники). Пороги хранятся
  как **свободный текст** (`min_threshold` per-product + `methodology_thresholds`), разбросанный
  по разделам — не структура. → перепарс раздела-носителя + регенерация `section_methodology`.
- ⚪ **Тело постановления + Правила реестра** (критерий «д», п.1(7), фарма→Минпромторг) —
  **процедурные, НЕ индексируются** (дефер до 1.5). В актуализацию базы RAG сейчас НЕ входят.

**⚠ Ловушки при перепарсе:**
- п.33 и п.81 — это **пайп-таблицы внутри примечаний** (`26.60.13.120|...|2026 год|...`), парсер
  может принять их за продукты → после `structure_kb` проверить `verify`/`reconcile` отдельно.
- Возможно, стоит подстраховать VIII (фарма) и таблицу п.81 сверкой Opus'ом офлайн (см.
  `docs/IDEAS.md` → «Качество парсинга»): это как раз структурно-сложные края.

**Оценка:** ~¥5–8 DeepSeek, ~40 мин (перепарс + verify + реиндекс). Локально на `dev`
(VM погашена, Qdrant :6533). После — редеплой на VM.

**Порядок шагов следующей сессии:** diff_edition.py (пересверить) → обновить pp719_full.txt +
rechunk → structure_kb VII, VIII (+ разделы-носители п.18/п.33/п.81) → verify + reconcile →
load_kb реиндекс → eval_retrieval + eval_answers (без регресса) → обновить дату редакции в доках.

# План на следующую сессию — Навигатор ПП №719

> Обновлён 2026-07-20 (сессия «все доработки локально, деплой в последнюю очередь»).
> Полный план задач — `docs/POST_TEST1_TZ.md`. Состояние движка — память проекта.

## Состояние

**`dev = 06933d5` = origin/dev.** `main = prod = e4bf3ef` — **VM у экспертов на СТАРОМ коде**;
редеплой отложен по решению юзера (накапливаем доработки, деплой одним заходом).

Сделано (все локально-буилдабельные доработки к тесту №1):
- ✅ **Волна 0** — T1 (СТ-1, не «невозможно»), T7 (терминология), T3 (обязательные/балльные), T6 (якорь
  диалога), петля кейсов (5 verified_cases).
- ✅ **Волна 1** — тело ПП 719 + Приказ ТПП №52 в `pp719_rules` (58→242); простые пороги «не менее N
  баллов» (`thresholds.py`); T3 закрыт Волной 0; T11 системный → 2.0; боль авто → verified_case.
- ✅ **Волна 2 / T5** — поиск по частичному коду ОКПД2 (класс-код → дочерние; `okpd2_prefixes`); recall@1=1.00.
- ✅ **Волна 3 / T17** — табличный вывод (Markdown-таблицы).
- ✅ **T18 стриминг** — SSE `/api/chat/stream` + фолбэк на `/api/chat`; `_plan_answer`/`answer_stream`;
  live e2e (687 delta, usage захвачен, delta==финал, unverified=[]).
- ✅ **T2-хвост** — многострочные пороги прим.9 (`код "имя": / - не менее N`) + правило прим.55 навигатора
  (белорусские/союзные компоненты и СТ-1 приравниваются; условное, честно помечены исключения).
- ✅ **T16** — навигация по вопросам внутри чата (плавающая кнопка → список вопросов → скролл).
- ⓘ **Петля кейсов** — 26 экспертных исправлений разобраны: доминируют темы, УЖЕ закрытые Волной 0 (СТ-1/
  терминология/вне-приложения) + 5 кейсами + T3/T11; чистой additive-жилы под новые кейсы нет. Дальнейшее
  обогащение ждёт сверки эксперта Курской ТПП (кейсы — черновики) и свежих исправлений следующего раунда.

**83 юнит-теста зелёные.** Локальный Qdrant живой (том `D:/navigator-719/qdrant_storage`, порт 6533):
`pp719=1379` (+okpd2_prefixes), `pp719_rules=242`, `verified_cases=5`.

## Приоритет 1 — РЕДЕПЛОЙ на VM (финальный шаг, требует старта юзером + явного согласия на prod)

Всё в `dev` (12+ коммитов), экспертам не видно. VM: юзер стартует/подтверждает (IP менялся — сверить).
- `git archive dev | ssh tar-x` на VM → `sudo docker compose up -d --build app` (пересобрать код-слой;
  torch/e5 из кэша; T18/T16/таблицы — в слое `COPY . .`).
- `sudo docker compose run -T --rm app python scripts/load_rules_kb.py` — **корпус 58→242** (тело 719 +
  Приказ 52) — ОБЯЗАТЕЛЬНО.
- `sudo docker compose run -T --rm app python scripts/seed_cases.py` — **verified_cases 5** — ОБЯЗАТЕЛЬНО.
- `sudo docker compose run -T --rm app python scripts/backfill_okpd2_prefixes.py` — поле для T5 (частичный
  код), БЕЗ 20-мин `load_kb` (структура pp719 не менялась, только payload-поле).
- e2e снаружи: стрим (`/api/chat/stream` — delta+done), СТ-1/пороги/таблица/частичный код/прим.9/прим.55 →
  FF `prod`/`main` на `dev` → следующий раунд экспертов меряет улучшения (гейт ≥70%).
- ⚠ `run` только с `-T` (иначе SSH рвётся); docker через `sudo` (yc-user не в группе); длинные шаги —
  fire-and-poll (`nohup … &` + короткие опросы лога).

## Остаток бэклога (внешне-блокированное / 2.0)

- **T2-хвост descriptive** (прим. 8(1) ВЭУ/лопасти — «не менее N для <описание>», без кода): матч по имени
  фрагилен (без стемминга), ниша — отложено; частично покрыто verified_case.
- **T9 / T5-хвост** (нужен внешний датасет): справочник ОКПД2 (подсказка кода по имени, глубокий recall
  сверла→25.73.4) + переходные ключи ОКПД2↔ТН ВЭД (alta.ru/ЕЭК/Росстат).
- **T12** (нужны исходники от заказчика): Методические рекомендации, Методика расчёта стоимости экспертизы.
- **T13** редакции во времени · **T14** меры поддержки (ПП 1875) · **T15** генерация документов/чек-листов
  (+инфобезопасность) · **T10** загрузка ТУ (фото/PDF).
- **T11 системный** (2.0): parent/child auto-merge для авто разд. II / судов XVIII (матрица категория×
  компонент — риск неверных баллов, только осторожно).

## Среда / как продолжить

- Локальный Qdrant: `Start-Process 'C:\Program Files\Docker\Docker\Docker Desktop.exe'` → контейнер
  `qdrant` поднимется сам (restart-policy), ~1-2 мин. Проверка: `curl http://localhost:6533/collections`.
- Прогон движка: `.venv/Scripts/python.exe -m app.rag.pipeline "вопрос" [код]` (нужен Qdrant + ключ в .env).
- Стрим локально: `answer_stream("вопрос", okpd2=...)` (генератор delta/done); SSE — `POST /api/chat/stream`.
- Тесты: `.venv/Scripts/python.exe -m unittest discover -s tests` (83, офлайн). JS: `node --check app/web/static/chat.js`.
- ⚠ Сеть рваная (DPI); длинные коннекты к DeepSeek/SSH роняются — ретраи 3-5 попыток.

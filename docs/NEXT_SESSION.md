# План на следующую сессию — Навигатор ПП №719

> Обновлён **2026-08-10** по итогам сессии глобального ревью.
> Главный рабочий документ — [REVIEW_TZ.md](REVIEW_TZ.md) (задачи `R1…R29` со статусами,
> чек-лист редеплоя §1а, связь с `T1…T18`). Аудит-снимок — [REVIEW_2026-08-10.md](REVIEW_2026-08-10.md).

## Состояние на конец сессии

**`dev` = `origin/dev` = `6b10341`**, всё запушено, **174 теста зелёные**, CI на GitHub Actions
зелёный. На VM — код `9d7c630` (тег `v0.5.0-test2`, версия июльского теста); **в `dev` 21 коммит
поверх неё, не выкачено** по решению владельца: копим доработки, деплоим одним заходом.

**Закрыто 25 из 29 задач ревью.** Всё, что можно было сделать локально, сделано.

| Метрика | Начало сессии | Сейчас |
|---|---:|---:|
| Позиций без требований в ответе | 349 (26 %) | **56 (4 %)** |
| Определённость по порогу | 47 % | **63 %** |
| Скрытых требований в `component`-блоках | 56 449 симв. | 0 |
| Словарь sparse-индекса | 3 079 термов | 5 718 (после переиндексации) |
| Юнит-тестов | 105 | **174** |
| Узлов в графе знаний | 231 | 1586 (625 мостов doc→code) |

---

## Бэклог: что осталось

### Требует доступа к VM (нельзя сделать локально)

| # | Задача | Приоритет |
|---|---|---|
| **R11** | TLS + домен + статический IP. Сейчас ПДн (ФИО/регион/Telegram) и пароли идут по HTTP, при этом сервис собирает согласие по 152-ФЗ. **Комплаенс-блокер публичного этапа** | **P0**\* |
| **R14** | `https_only=True` + `secure` на куках — только ПОСЛЕ R11, иначе сломается вход | P2 |
| — | cron бэкапа + вынос off-VM (код и инструкция готовы, см. `DEPLOY.md`) | P1 |
| — | `X-Forwarded-For` для лимита входа (R12 ключует по IP; за прокси попадёт адрес прокси) | вместе с R11 |

\* P0 к моменту расширения аудитории за пределы контролируемой группы тестировщиков.

### Требует сверки эксперта ТПП (нельзя решить кодом)

- **R6 шаг 4** — 9 позиций: 5 NO_MATCH, 3 PARSER_LOSS, 1 EXCLUDED. Список в
  `scripts/diag_orphan_requirements.py --json`.
- **R29** — фактический состав требований 4 групп (Аддитивные установки, Светодиоды ×2, Хладоны).
  Сейчас они помечены как неполные; после сверки данные можно восстановить точно.
- **10 контрольных позиций наследования** R6 (вручную сверены две — совпадают).
- Прежний список из §7 отчёта за июль (Методические рекомендации, ПП №1875 и др.) — без изменений.

### Отложено осознанно

- **R17** — сокращение цепочки LLM-вызовов. Понижен до P3: на скорость **0 жалоб из 143**, а
  реранкер (68 % запросов) даёт recall@1 0.95→0.98. Вернуться только после прохождения гейта.
- **R24 (пагинация логов админки)** — порог возврата зафиксирован: **>5000 реплик или >1 с** на
  `build_admin_view` (сейчас 52 мс).

---

## План на завтра

### ✅ 1. Перед деплоем — прогнать то, что не прогонялось — **ВЫПОЛНЕНО 12.08.2026**

> Переиндексация и все три замера прогнаны локально. **Регресса нет ни на одной метрике — везде рост.**
>
> | Метрика | Было | Стало |
> |---|---:|---:|
> | Позиционный recall@1 (вся база) | 0.887 | **0.940** |
> | Позиционный recall@3 | 0.973 | **0.999** |
> | Позиции-невидимки | — | **0** |
> | Кросс-секционные коллизии | 24 | **11–12** |
> | Секционный recall@1 (golden 46) | 0.98 | **1.00** (потолок набора) |
> | Faithfulness ответа | 0.98 | **1.00** (0 выдуманных из 390 чисел) |
> | Отказ на out-of-scope | 4/4 | **4/4** |
>
> Риск R9 («длинные документы перетянут выдачу») **не подтвердился**. Попутно проверено, что
> реранкер по-прежнему нужен: на кросс-секционных коллизиях он даёт 1/12 → 9/12 (см. R17).
>
> ⚠ **Это локальный прогон. На VM переиндексацию надо повторить** — она в чек-листе §1а.

<details>
<summary>Исходная инструкция шага (для повтора на VM)</summary>

### 1. Перед деплоем — прогнать то, что не прогонялось (нужен Qdrant, ~1 ч)

```bash
# поднять Docker Desktop → контейнер qdrant стартует сам (~1-2 мин)
curl http://localhost:6533/collections
.venv/Scripts/python.exe scripts/load_kb.py            # переиндексация: R28 (section_title) + R9 (sparse)
.venv/Scripts/python.exe scripts/eval_retrieval.py     # ГЛАВНОЕ: R9 — единственная правка, способная сдвинуть ретрив
.venv/Scripts/python.exe scripts/eval_answers.py       # faithfulness после R6/R7/R8
```
⚠ **R9 меняет sparse-индекс на полный текст** — документы до 13 882 токенов теперь совпадают с
бо́льшим числом запросов. Нормировка BM25 по длине (`b=0.75`) для этого и нужна, но подтвердить
должен замер, а не рассуждение. Если recall@1 просел — откат флагом `--full-text` не поможет,
нужно вернуть `sparse_text_fn=build_embedding_text` в `index_all`.

⚠ Добавить в прогон **`eval_coverage.py`** — на 12.08 выяснилось, что `eval_retrieval` насыщен
(recall@1 = 1.00 и с реранкером, и без) и судить по нему о ретриве больше нельзя. Позиционный
замер по всей базе — единственный, который ещё что-то различает.

Докерная деталь, стоившая времени: контейнер `qdrant` **не стартует сам** после запуска Docker
Desktop, если был остановлен (`docker start qdrant`).

</details>

### ✅ 2. Живой прогон движка на контрольных вопросах — **ВЫПОЛНЕНО 12.08.2026**

Прогнано через боевой пайплайн (живой DeepSeek), пять пунктов из пяти:

| Проверка | Итог |
|---|---|
| **«Автокраны» (29.10.51)** — наследование + атрибуция | ✅ Требования группы показаны, атрибуция явная: «приведены у позиции "Краны грузоподъемные стрелкового типа" (ОКПД2 28.22.14.125)». Незаземлённых чисел 0 |
| **«Хладон-218»** — пометка неполноты (R29) | ✅ «Список операций неполный… показанное — лишь часть общего перечня». Вывода «порог набирается» модель НЕ делает. ⚠ Но 4 операции по 100 баллов стоят рядом с порогом «не менее 100» — соседство остаётся подсказывающим, работу делает именно предупреждение |
| **«Гидротермокостюмы»** — «порог не предусмотрен» | ⚠ Не как ожидалось, но корректно: позиция получила **групповой порог 22.19 = 120 баллов** с оговоркой «применимость к вашей позиции проверьте по первоисточнику». Формулировка «порог не предусмотрен» появилась у альтернативного кандидата («Спасательные жилеты»). Старого дефекта «Порог: в контексте не указан» **нет**. R7 отработал по правилу: молчит, когда не уверен |
| **Требования из `component`** (R6 шаг 2) | ✅ Видны в обоих релевантных ответах: права на КД/ТД на срок не менее 5 лет, наличие сервисного центра |
| **Экспорт md/txt/json** | ✅ Дисклеймер в шапке всех форматов + подпись каждой реплики ассистента; 7 тестов `test_export` зелёные |

**Побочное наблюдение — R30 виден в проде.** Во всех четырёх прогонах подмешивалось ровно
**3 кейса независимо от релевантности запроса**. В ответе про спасательные круги виден маркер
`[Кейс 1]` — сработал кейс про заполнение карточки каталога ГИСП. Здесь это оказалось уместно
(и заодно доказывает, что петля обучения замкнулась end-to-end), но механизм именно тот,
что описан в R30: порога отсечки нет.

> ✅ **R30 закрыт 12.08.2026, до контрольной волны.** Ложных срабатываний **22/22 → 1/22**,
> отказ на out-of-scope держится 4/4, faithfulness 1.00 — регресса нет. Отсечка идёт по чистому
> dense-косинусу (`CASE_RELEVANCE_MIN=0.84`) плюс отдельный путь по коду ОКПД2. Полная история,
> включая цену порога на реальном трафике, — в [REVIEW_TZ R30](REVIEW_TZ.md).

<details>
<summary>Что именно проверялось (для повтора после деплоя)</summary>

- позиция с наследованием — «Автокраны» (29.10.51): требования группы + **атрибуция** к
  «Краны грузоподъемные стрелкового типа»;
- позиция R29 — «Хладон-218»: пометка «СПИСОК ОПЕРАЦИЙ НЕПОЛНЫЙ», модель НЕ делает вывод о пороге;
- модель «перечень» — «Гидротермокостюмы»: «Порог: не предусмотрен», а не «в контексте не указан»;
- обязательные требования из `component` (права на КД/ТД, сервисный центр) видны в ответе;
- экспорт диалога в md/txt/json — пометка «ИИ — черновик» на месте.

</details>

### 3. Пакетный редеплой (когда владелец даст добро; ~1 ч)

**Сверять по чек-листу [REVIEW_TZ §1а](REVIEW_TZ.md)**, там же статусы. Ключевое:
- ⚠ **новые файлы данных ОБЯЗАНЫ попасть в архив деплоя**: `knowledge_base/pp719/
  fragmented_requirements.json` и `inherited_requirements.json`. Без них R29 и наследование R6
  **молча выключатся** — модули отдают пусто при отсутствии файла. Это ровно та ошибка, что уже
  была с `classifiers/*.tsv` в T9;
- ⚠ **тег ДО деплоя** (правило R18);
- переиндексация на VM (`load_kb.py`) — ради R28/R9;
- `load_rules_kb.py` / `seed_cases.py` / `backfill_okpd2_prefixes.py` — **не нужны**, не менялись;
- после деплоя: e2e снаружи (стрим + фолбэк, лимиты 429), затем FF `main`/`prod`.

### 4. Контрольная волна тестирования — главный шаг к гейту

Всё, что делалось в этой сессии, меряется одним: **повторным замером приёмки**. Целевой объём —
не менее 60–80 оценённых ответов. Волна R-A ревью (целостность метрики, дисклеймер, честный дефер)
закрыта именно ради того, чтобы этот замер нельзя было оспорить.

---

## Среда / как продолжить

- Локальный Qdrant: `Start-Process 'C:\Program Files\Docker\Docker\Docker Desktop.exe'` → контейнер
  `qdrant` поднимется сам. Проверка: `curl http://localhost:6533/collections`.
- Тесты (офлайн, без Qdrant и ключа): `.venv/Scripts/python.exe -m unittest discover -s tests`
- Диагностика данных: `.venv/Scripts/python.exe scripts/diag_orphan_requirements.py`
  (`--fragments` / `--inheritance` перегенерируют файлы данных детерминированно).
- Бэкап с проверкой: `python scripts/backup_db.py --verify <файл>`
- ⚠ Сеть рваная (DPI): длинные коннекты к DeepSeek/GitHub/SSH рвутся — ретраи 3–5 попыток.
- ⚠ Windows + кириллица: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8` для скриптов.
- ⚠ После `/graphify .` проверять `graphify-out/.graphify_root` — скилл пишет туда POSIX-путь,
  который ломает автообновление графа (см. `CLAUDE.md`).

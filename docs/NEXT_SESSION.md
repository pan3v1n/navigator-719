# План на следующую сессию — Навигатор ПП №719

> Обновлён **07.09.2026**. **Порядок работ — [BACKLOG.md § План заходов](BACKLOG.md), идём по нему
> сверху вниз**; там же вся фактура по задачам. Рабочая очередь — [BACKLOG.md](BACKLOG.md); протокол замера —
> [WAVE3_PROTOCOL.md](WAVE3_PROTOCOL.md); планы по волнам — [REVIEW_TZ.md](REVIEW_TZ.md) (R1–R30),
> [KB_EXPANSION_TZ.md](KB_EXPANSION_TZ.md) (K1–K17), [AGENTS_PLAN.md](AGENTS_PLAN.md) (A1–A6, B1).

## Состояние — 08.09.2026 · `K16` #48 ЗАКРЫТА И ЗАМЕРЕНА, РЕЛИЗ СОБРАН, НА БОЮ НЕ ВЫКАЧЕН

**Бой не тронут: `prod` = `main` = `f0c19d8` = тег `v0.5.0-test23`.**
`dev` = **`503ad96`**, впереди `main` на 10 коммитов. **Батарея 1284, OK** документированной
командой (без `PYTHONUTF8`). Локальный Qdrant переиндексирован полностью: **1377 / 752 / 12**.

| ось | значение | комментарий |
|---|---|---|
| батарея | **1284 OK** | было 1246 на начало сессии |
| `K9` атрибуция@1 · представленность · нужный пункт | **24/25 · 25/25 · 5/5** | не сдвинулась ни за раунд 11, ни за `K16`, ни за правку квоты |
| свип маршрута (237 вопросов) | **0 расхождений**, вне сферы 53, ложных 1 | |
| коллекции | 1377 / **752** / 12 | `pp719_rules` 681 → 752 |

### Что сделано

**1. Раунд 11 ревью — 15 находок, 1 HIGH, все закрыты** (`86d19bd`, `a2f8bb5`, отчёт
[2026-09-07_review_round11.md](eval_runs/2026-09-07_review_round11.md)).
HIGH — снова регрессия проверяемого пакета: гейт длины из раунда 10 поставили на ОБЫЧНЫЙ путь,
где матч никем не опровергнут. Посылка «ключ недопустимой длины не обозначает ничего» ложна на
лукапе — `conditions_for` матчит по ПРЕФИКСУ. Замер: **147 потерь вердикта из 308, приобретений 0**;
после правки 0 расхождений с состоянием до раунда 10.
⚠⚠⚠ **Рекомендация ревью (находка 1) замерена и ОТВЕРГНУТА:** отмена «окна разрыва» теряет
**453 верных вердикта из 906**, не убирая ни одного ложного. Третий случай в проекте, когда
предписание отчёта ревью не выдерживает собственной проверки.
⚠⚠⚠ **Признак формы заменён ФАКТОМ.** Раунды 7–11 разводили «подъём к родителю» пятью признаками
формы, и каждый закрывал свой случай и открывал соседний — классы неразличимы лексически.
`_climb_resolves` спрашивает Перечень: есть запись у родителя — поднимаемся, нет — отказываемся.
Итог: 38 ложных «НЕ включён» убрано, потерь 0, все 453 верных вердикта сохранены.

**2. `K16` #48 закрыта** (`5ff0386`, `22c9e04`, `503ad96`). Корпус `pp719_rules` **681 → 752**:
* **Положение об экспертизе происхождения** (приложение к приказу ТПП РФ **№49** от 14.07.2023),
  62 записи. ⚠⚠⚠ **Первоисточник публикует УСТАРЕВШИЙ файл:** на tpprf.ru лежит PDF редакции
  2023 г., а документ действует **в ред. приказа №59 от 16.07.2026** — того же, которым изменён
  Приказ №14. Сверка: похожесть тела 0.983, +371 знак, ОДИН абзац в п. 6.13. Абзац дописывается
  конвертером дословно, с контролем на единственность якоря и на отсутствие дубля.
* **FAQ ГИСП**, 9 записей, `legal_force: 5`, `topic: ГИСП`. Критерий приёмки #48 («источник и
  пометка не норма») обеспечен паспортом и подписью якоря; проверено на живом поиске.
* Квота окна: у Положения своя строка в `_RULES_TOPIC`, у №52 сужен ствол «экспертиз». Радиус на
  21 вопросе четырёх наборов: **одно** целевое изменение, у №52 7/7 своих на месте.
* Релизная проверка `checks/v0.5.0-test24/` — шесть блоков, **провалов 0** на живом рантайме.
* Профиль `releases/v0.5.0-test24.env` — сухой прогон: **маркеры 7 + 1 анти сошлись**, релизных
  проверок 1. Каждый маркер сверен `grep -cE` против нового кода И против `f0c19d8`.

### ⚠⚠⚠ ОТКРЫТЫЕ ВОПРОСЫ — РЕШЕНИЕ ВЛАДЕЛЬЦА

1. **РАУНД 12 НЕ ОТРАБОТАЛ.** Он был назначен, правило остановки записано ДО прогона
   (`a8a0362`), ревью запущено — и **прервано лимитом сессии, находок ноль, вердикта нет**.
   По правилу раунд 12 — последний перед PR. **Рекомендую: перезапустить его на мишени
   `1e918ec..ae0a0ae`, это один прогон**, и уже по его итогу открывать PR. Мержить `dev` → `main`
   без объявленного ревью я не стал: правило записано заранее именно чтобы его не обходили.
2. **Выкатка `v0.5.0-test24` на VM — за владельцем.** Всё готово (профиль, релизная проверка,
   переиндексация проверена локально). ⚠ Окно недоступности процедурной ветки ~15–35 мин: полная
   переиндексация обязательна (загрузчик не делает `--doc` для нового документа). ⚠ Прежнее обоснование «`avgdl` 95.8 → 82.3» ОТОЗВАНО: пересчёт даёт **81.73 → 82.35**, сдвиг +0.76 %, а 95.8 — величина эпохи 311 записей. Перед сборкой пакета читать `deploy.sh`:
   в каталоге пакета нужны **и `SHA256SUMS.txt`, и тарбол**; подключения объединять, кулдаун
   канала 7–8 мин.
3. **Платный прогон уровней 2–3 на новом корпусе НЕ снят** — денег я не тратил. Ядро приёмки
   `K16` снято офлайн и живым поиском; сам ответ LLM на вопросах ГИСП не мерился.
4. **Страница «Сервис ПП 719 в ГИСП»** — владелец обещал прислать; конвейер под неё готов
   (`convert_gisp_faq.py` принимает любой PDF страницы портала).
5. **Записанный предел:** «какие документы подтверждают критерий полностью полученного товара» —
   тема назначается верно (№49 = 2 против №52 = 1) и документ берёт 3 слота из 6, но главу окна
   держит №52: `primary` даёт КВОТУ, а не первое место. Правка ранжирования имеет более широкий
   радиус — отдельной задачей.
6. **Долг:** `_AMEND_RE` в `load_rules_kb.py` ловит любое «от ДД.ММ.ГГГГ N NNN», хотя комментарий
   обещает пометку «(в ред. …)». Строгий вариант замерен и отвергнут — он чинит FAQ, но телу
   ПП №719 даёт «ред. от 10.09.2025 N 1392» вместо «22.07.2026 N 923». Симптом закрыт там, где
   вреден (`edition_expected: null` → редакция не штампуется), тест не даёт забыть про долг.

---

## _(история)_ Состояние — 07.09.2026 · БОЙ `v0.5.0-test23`, `K14` ЗАКРЫТА, БЛОК 0 СЕССИИ СДЕЛАН

**`prod` = `main` = `f0c19d8` = тег `v0.5.0-test23`** (выкачен 03.09 с полной переиндексацией).
`dev` впереди `main` на остаток раунда 8 (`6d41582`: семь находок, HIGH 0, **НЕ ревьюён**), на
слитую ветку железа и на пакет документации этой сессии. **1217 тестов, OK** документированной
командой. Локальный Qdrant поднят: **1377 / 681 / 12**, сходится с профилем выкатки. Открытых
issues **29**, PR нет. CI зелёный на `dev` и `main`.

### Что случилось 03.09 и не было записано здесь

| шаг | итог |
|---|---|
| Развилка раунда 8 | закрыта владельцем вариантом (а) — структурный гейт. Блок 1 (HIGH-1/5/6б, MED-5) и MED-3 закрыты; MED-3 — сборкой заголовка, а не `head_buf` (предписание отчёта проверено на первоисточнике и отвергнуто) |
| Гейт вне-сферы | **заведён и ОТКАЧЕН тем же днём** (`208b379`): замер трёх конструкций на 164 вопросах пяти популяций — выигрыш 2 из 36 (шум) ценой девяти отказов на флагманах, включая вопрос про самого заказчика; порога, разделяющего классы, нет (полосы 0.8316–0.8836 и 0.8297–0.8622 перекрываются). Закреплено тестами, разбор [eval_runs/2026-09-03_rules_relevance_gate.md](eval_runs/2026-09-03_rules_relevance_gate.md) |
| Раунд 9 (правило записано ДО прогона, `cf69311`) | 8 находок, **1 HIGH**: правка HIGH-5 раунда 8 вернула «уверенный неверный код» — отменена, выбор сделан направлением ошибки. Правило «нет HIGH → выкатываем» НЕ выполнено; раунд 10 по правилу — решение владельца, не проводился; выкачено после закрытия HIGH. Разбор [eval_runs/2026-09-03_k14_review_round9.md](eval_runs/2026-09-03_k14_review_round9.md) |
| Выкатка `v0.5.0-test23` | PR #137 слит (`f0c19d8`), «все проверки прошли», провалов 0. Окно недоступности процедурной ветки ~34 мин, бэкап снят до всего (28 / 1262 / 297). Маркеры 8 + 1 анти. Релизная проверка СТ-1 на бою: Соглашение СНГ и Приказ №14 первыми на своих вопросах. ⚠⚠ **Три захода остановлены предохранителями — все три ошибки сборки пакета** (нет `SHA256SUMS.txt` · нет тарбола · нет соединения): скрипт ждёт в каталоге пакета манифест сумм И тарбол |
| Остаток раунда 8 | закрыт ПОСЛЕ выкатки в `dev` (`6d41582`): MED-1, MED-2, MED-4, MED-6, LOW-7…9. Батарея 1215 → 1217. ⚠ MED-4 — корпусная (нумерация записей раздела Приказа №14), действует только после следующей переиндексации |

Ряд девяти раундов ревью PR #137: находок 7·5·7·5·6·8·5·15·8, HIGH 2·2·3·2·2·0·2·6·1. Ось `K9`
не сдвинулась ни разу: 24/25 · 25/25 · 5/5. ⚠⚠ За 03.09 отменены ТРИ предписания отчёта раунда 8
(гейт, фикс MED-3, HIGH-5) — **рекомендация ревью требует той же проверки, что правка кода.**

### Блок 0 сессии 07.09 — сделано

* Docker Desktop поднят, коллекции локального Qdrant сверены с профилем выкатки: 1377 / 681 / 12.
* Слита `worktree-docs-hardware-onprem` (13.08, три коммита, только документация):
  [HARDWARE_ONPREM.md](HARDWARE_ONPREM.md) — сборка on-prem сервера из железа кладовой ТПП. Ветка
  три недели жила несли́той и нигде не была записана; worktree и ветка удалены.
* `CLAUDE.md`, этот файл и `BACKLOG.md` доведены до состояния 03.09 и 07.09.
* ⚠ Неотслеживаемый `AGENTS.md` (копия `CLAUDE.md` для Codex, семь строк отличий) — **решение
  владельца**: коммитить копию или заменить указателем на `CLAUDE.md`. Не тронут.

### Блок 1 сессии 07.09 — СДЕЛАН: раунд 10, 17 находок, **1 HIGH**

`dev` = **`fa74b85`**, батарея **1217 → 1246, OK**, CI зелёный. Бой не тронут:
`prod` = `main` = `f0c19d8` = `v0.5.0-test23`.

**HIGH — регрессия САМОЙ проверяемой правки.** MED-6 из `6d41582` усекала настоящие
девятизначные коды записи 4+2+2+1 («0710 40 00 0» → «0710 40 00»): признак «десятый знак
приписывается к девятизначной ОСНОВЕ» знал ровно одну форму записи. Ключ усекался у **59 из 59**
девятизначных кодов Перечня, вердикт «включён» терялся у **53 из 59**. Признак ЗАМЕНЁН на
проверяемый (`_TNVED_KEY_LENGTHS`, состав сверяется тестом ПРОТИВ таблицы), а не подкручен:
раунды 7, 8 и 9 отвечали на один вопрос тремя разными признаками, каждый закрывал свой случай и
открывал соседний.

Ещё по коду: **находка 15** — `gap` считался от УСЕЧЁННОГО ключа, хотя это расстояние в тексте
(9600 запросов: 240 «было None → стал код», потерь 0); **находка 7** — ДЕВЯТЬ живых коллизий
`by_key` у Приказа №52 и сносок, сторож MED-4 слеп к ним по построению. Восемь находок — про
ложные или пережившие свой факт ОБОСНОВАНИЯ (тот же класс, что чинила MED-1 в проверяемом
коммите). Отклонённых находок нет. Разбор — [eval_runs/2026-09-07_review_round10.md](eval_runs/2026-09-07_review_round10.md).

⚠⚠ **Мутационная проверка нашла дефект в МОЕЙ ЖЕ правке, из класса находки 13.** Сторож находки 4
во второй редакции считал `topics.classify` по ТЕЛУ функции и прожил час: мой же комментарий к
находке 12 назвал это имя в теле, и сторож покраснел НА ВЕРНОМ КОДЕ. Считать ТЕКСТ функции нельзя
вообще — переписан на `co_names` код-объекта, куда проза не попадает по построению.

**Оси перемерены ПОСЛЕ правок** (правило требовало перепроверить заявление «`K9` и маршрут не
трогались» — подтвердилось): `K9` **24/25 · 25/25 · 5/5** не сдвинулась за десять раундов; свип
маршрута 237 вопросов — вне сферы 53 · процедурных 0 · ложных 1 · пропусков 15, побайтно то же;
коллекции **1377 / 681 / 12**. ⚠ Находка 7 переиндексации НЕ требует: `index_text` всех 681
записи побайтно тот же (sha256 совпал). Корпусная в пакете по-прежнему только MED-4 — едет окном
`K16`.

⚠⚠ **РЕШЕНИЕ ВЛАДЕЛЬЦА — ПЕРВОЕ ПРИ ВОЗОБНОВЛЕНИИ.** По правилу, записанному ДО прогона
(`7b7b3c3`), пункт 2: HIGH был, значит «нет HIGH → PR» не выполнено, и **раунд 11 назначает
владелец**. Автоматического следующего раунда нет.
*За раунд 11:* HIGH десятого раунда сидела в правке ВОСЬМОГО, найденной ревью, — пакет правок по
ревью снова оказался объектом ревью (четвёртый случай в проекте); правки этого раунда никем не
проверены.
*Против:* ряд HIGH **2·2·3·2·2·0·2·6·1·1** не убывает ничем, «ещё один раунд» не измеренный способ
приблизиться к цели, а `K16` #48 — основная работа — не начата.

### План сессии 07.09 — блоки 1–4, в порядке исполнения

1. ✅ **СДЕЛАН — раунд 10 ревью по диффу `6d41582`** (см. блок выше): 17 находок, **1 HIGH**,
   `fa74b85`. Правило остановки НЕ выполнено по пункту 2 — **раунд 11 назначает владелец**,
   PR `dev` → `main` до этого решения не открывается.
2. **`K16` #48** — основная работа, своим окном полной переиндексации. ⚠ Документов на диске НЕТ:
   Положение о порядке проведения экспертизы по определению страны происхождения товара от
   14.07.2023 (tpprf.ru, раздел certificates) и FAQ ГИСП «Как попасть в реестр»
   (gisp.gov.ru/mainpage/faq/24920). Шаги: добыть → конвертер с чисткой обвязки сайта и
   положительным контролем (как Приказ №14 из MHTML) → сверить ПРОЦЕДУРУ с действующими нормами,
   не только реквизиты (Методрекомендации были действующими и описывали отменённый шаг; устаревшее —
   `exclude_fragments` с контролем на совпадение) → манифест (Положение `topic: СТ-1`; ГИСП
   `legal_force: 5`, «не норма») → полная переиндексация локально (`avgdl` запекается в sparse
   соседей), квота окна ЗАМЕРОМ как в заходе 5 → документные кейсы (`kind`) + живой прогон; ось
   `K9` 24/25 · 25/25 · 5/5 не сдвигается, свип маршрута побайтно → критерий issue: ответы по
   процедуре ГИСП с источником и пометкой «не норма».
3. **Запасной, если `K16` упрётся в документы:** полный прогон `EVAL_GUIDE` на `test23` и сведение
   базы сравнения на один тег (уровни 0–3 сняты на `test18`, уровень 4 — на `test21`). К нему
   цепляются `EV20` #117 (правка промпта + перезамер уровня 2) и `EV11` #92.
4. **Выкатка `v0.5.0-test24`** (`K16` + остаток раунда 8) с полной переиндексацией — только если
   блоки 1–2 закрыты и ревью `K16` без HIGH; иначе следующая сессия. Перед сборкой пакета читать
   `deploy.sh`: манифест сумм И тарбол в каталоге пакета; подключения к VM объединять, кулдаун
   канала 7–8 минут.

**Не в этой сессии:** `B1` #61 (после `K16`, отдельный дизайн), `K7` #44, `K17` #49, `O4` #107 фоном.

### Решения владельца

* ⚠⚠ **РАУНД 11 ИЛИ PR** — первое при возобновлении. Правило `7b7b3c3`, пункт 2: HIGH был, значит
  автоматического следующего раунда нет и решение за владельцем. Доводы — в блоке 1 выше.
* **Письмо эксперту** — владелец отправляет сам (сказано 07.09). Держит #53, #95, #69, #52, #65 и
  путь к `1.0` через #13.
* **Документы `K16`** — если tpprf.ru / gisp.gov.ru не отдадут файлы с этого ПК, нужны от владельца.
* **`EV22-1` #134** — механизм поломки для перепроверки на бою (фаервол / переменная + рестарт /
  холодный пул) и окно.
* **`AGENTS.md`** — копия или указатель на `CLAUDE.md`.

---

## _(история)_ Состояние — 01.09.2026 · РАУНД 8: 6 HIGH, ПРАВИЛО ОСТАНОВКИ НЕ ВЫПОЛНЕНО, ВЫКАТКИ НЕТ

**Бой не тронут: `prod` = `main` = `722ac8e` = тег `v0.5.0-test22`.**
`dev` впереди `main` на **32 коммита**, PR #137 открыт, CI зелёный.
**1173 теста** — зелено и с живым Qdrant, и с недоступным. Открытых issues **30**.

**Что в пакете:** `K14` #35 целиком (второй ключ ТН ВЭД, корпус СТ-1, лукап Перечня, маршрут,
приёмочные кейсы) + **восемь раундов ревью PR #137** (58 находок, 19 HIGH).
Корпус `pp719_rules` **328 → 681**, таблица условий **245** записей.
Локальный Qdrant переиндексирован полностью (681 точка), релизная проверка — **0 провалов**.

### ⚠⚠⚠ РАУНД 8 ОТРАБОТАН: 15 находок, 6 HIGH — ВЫКАТКИ ПО ОБЪЯВЛЕННОМУ ПРАВИЛУ НЕТ

Правило было записано коммитом `d3facaa` **ДО запуска ревью**: «нет HIGH → выкатываем». Получено
**шесть HIGH**, из них **четыре — регрессии, замеренные "до/после" против `2f67c19`**, то есть
внесённые самим пакетом правок раунда 7. Полный разбор —
[eval_runs/2026-09-01_k14_review_round8.md](eval_runs/2026-09-01_k14_review_round8.md).

| раунд | 1 | 2 | 3 | 4 | 5 | 6 | 7 | **8** |
|---|---|---|---|---|---|---|---|---|
| находок | 7 | 5 | 7 | 5 | 6 | 8 | 5 | **15** |
| HIGH | 2 | 2 | 3 | 2 | 2 | 0 | 2 | **6** |

**Ряд `2·2·3·2·2·0·2·6` не убывает ничем; восьмой раунд — самый тяжёлый.** Правки самого ревью
дали HIGH в раундах **2, 5, 7 и 8 — четырёх из восьми.** ⚠ Свип отработал ПОЛНОСТЬЮ (10 углов,
3 упали на транзиентных ошибках API и дособраны) — предусловие «ноль на неполном свипе означает
"не считаю"» соблюдено.

**Шесть HIGH:** сумма денег снова принимается за код и ПОБЕЖДАЕТ настоящий (`8703` → `250000`,
доезжает в промпт дословно) · унификация словарей РАСШИРИЛА тему — новые утечки вне-сферы,
созданные правкой утечки · закрытый список сверяется только со СЛЕДУЮЩИМ словом, 14 из 16
таможенных вопросов текут по-прежнему · **дисквалифицирована СОБСТВЕННАЯ лексика домена**
(`экспорт`, `вывоз` — СТ-1 и выдаётся под экспорт; потерян главный вопрос релиза) · слово
«позиция» в списке «это НЕ код» роняет голый четырёхзначный код · сплайс словаря сделал
`_ANCHOR_RE` квадратичной, а у `/navigate` нет предела длины (28.8 КБ → 1512 мс).

#### ⚠⚠⚠ Два структурных вывода — важнее любой отдельной находки

**У трёх HIGH ОДИН корень:** все утечки прошли через `_has_routing_topic`, то есть через
расширенный `ST1_TOPIC_COMPANION`, кормящий гейт. Дальнейшее сужение словаря — гонка за симптомами.

**Названная причина не тронута вовсе.** Раунд 7 записал диагноз «у процедурной ветки НЕТ гейта
вне-сферы» и лечил его СЛОВАРЁМ. Гейта нет до сих пор: `search_rules` без порога релевантности,
`_answer_procedural` держит `low_relevance=False` жёстко, остальные восемь якорей (`реестр`,
`заявлени`, `ТПП`, `минпромторг` …) пускают вне-сферные вопросы, и за ними не стоит ничего.

⚠⚠⚠ **Два словарных дефекта раунда 7 ЗЕРКАЛЬНЫ:** унификация расширила, список дисквалификаторов
сузил. Оба вне популяции ВСЕХ наборов репозитория — потому свип и отчитался, что 220 прежних
вопросов побайтно те же.
⚠⚠⚠ **ЗАМЕР, ВЫБРАВШИЙ МЕХАНИЗМ РАУНДА 7, БЫЛ НЕДЕЙСТВИТЕЛЕН:** три состояния сравнивались на
наборе, построенном В ФОРМЕ одного из кандидатов (дисквалификатор вплотную). Победитель выиграл у
популяции, которая не умела его опровергнуть.

#### ⚠ РАЗВИЛКА, КОТОРАЯ ЖДЁТ ВЛАДЕЛЬЦА

**(а)** Откатить рантайм-дифф раунда 7 и закрыть причину СТРУКТУРНО — гейт вне-сферы на
процедурной ветке. Снимает 5 HIGH из 6 разом (четыре регрессии + квадратичность), лечит названную
причину, но это правка поведения со своим замером и своей популяцией.
**(б)** Чинить пятнадцать находок поштучно, оставив механизм словаря. Ровно то, что раунды 6→7→8
уже делали трижды, каждый раз порождая регрессии парами.
**Рекомендация — (а).** Механизм, дающий находку каждый раунд, заменяют, а не крутят.

**⚠⚠ Выкатка `v0.5.0-test23` — С ПОЛНОЙ ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`** (не частичной): в
профиле `REINDEX_FULL="pp719_rules"`. Причина — `K11`: `avgdl` запекается в sparse-вектор
каждой точки, и новый документ уводит в чужой масштаб BM25 точки СОСЕДЕЙ. Значит будет окно
недоступности процедурной ветки; `K16` #48 владелец решил делать **отдельным окном**.

---

## _(история)_ Состояние — 30.08.2026 · ✅ ВЫКАЧЕНА `v0.5.0-test22`, PR #135 СЛИТ, `#136` ЗАКРЫТ

**`prod` = `main` = `722ac8e` = тег `v0.5.0-test22`.** `dev` = `497f853`, впереди `main` на
**1 коммит** (правки `#136` — инструменты замера, в рантайм не идут). **967 тестов**,
открытых issues **31**.

### Что сделано

1. **Слит PR #135** — регрессия гарда снята с `main`. Проверено не на глаз:
   `git branch -r --contains aae28c8` теперь называет `origin/main`.
2. **Выкачена `v0.5.0-test22`**, код возврата **0**, «все проверки прошли». Рантаймовая:
   Qdrant не перезапускался (Up 2 days), коллекции 1377 / 328 / 12 не тронуты.
3. **Живая проверка ×5: здоровых 5 из 5**, источников **6** в каждом ответе, 5.6–11.0 с.
   Каждый ответ прямо говорит, что «заключения ТПП» как такового не существует.
   **`K15` в журнале после выкатки — 0 срабатываний**: цель `b890f69` (гард молчит на верном
   ответе) сохранена, а возвращённая половина закреплена тестами и мутацией локально.
4. **`#136` закрыт** — 7 находок раунда 6, у каждой сторож, каждый проверен мутацией.
   Тестов 955 → **967**.

### ⚠⚠ Главное за сессию — про метод

* **⚠⚠⚠ ПРОБНИК ЗДОРОВЬЯ ОБЪЯВИЛ НЕЗДОРОВЫМ ИСПРАВНЫЙ БОЙ И ОСТАНОВИЛ ВЫКАТКУ.** Временная
  учётка заводилась ролью `user`, а роль `user` — это региональный тестировщик, обязанный до
  чата заполнить профиль и согласие (`require_user_profiled`). Все пробы возвращали **403
  `profile_required`**, пробник печатал «!! НЕ ЗДОРОВ», гейт остановился.
  **Тот же класс, что чинился в `#136` в ту же сессию:** инструмент упёрся в СВОЁ состояние и
  обвинил в этом сервис. Исправлено двумя способами сразу: учётка заводится ролью `expert`
  (внутренняя, гейт профиля её не касается), а пробник теперь различает ТРИ исхода —
  здоров / нездоров / **НЕ ИЗМЕРЕНО** (401, 403, 429 — это порог, до движка запрос не дошёл).
* **⚠⚠ НАПРАВЛЕНИЕ ОШИБКИ РЕШАЕТ ВСЁ.** Дефект пробника был ложно ОТРИЦАТЕЛЬНЫМ: он остановил
  выкатку вместо того, чтобы пропустить плохую. Именно поэтому предохранитель и стоял НА ПУТИ
  действия, а не рядом с ним. Ложно положительный на этом же месте выкатил бы вслепую.
* **⚠⚠ РАСПОЗНАВАТЕЛЬ, ПОЗВАННЫЙ В ОДНОМ МЕСТЕ ИЗ ТРЁХ, — ЭТО НЕ ПОЧИНКА** (`#136`, находки
  1–4 одного корня). Правка, заводящая распознаватель, обязана позвать его ВЕЗДЕ, где
  распознаваемое возможно.
* **⚠⚠ ОРАКУЛ, ПОВТОРЯЮЩИЙ МОДЕЛЬ КОДА, ЛОВИТ ОПЕЧАТКУ, НО НЕ ОШИБКУ МОДЕЛИ.** Тест пола опроса
  брал те же `requests_per_probe = 2` и потому не видел, что потребителей окна больше. Заменён
  на независимую симуляцию + отрицательный контроль «прежнее число обязано краснеть».
* **⚠ ДЕРЕВО ТЕГА СВЕРЕНО С ДЕРЕВОМ, НА КОТОРОМ ЗЕЛЕНЕЛ CI.** На merge-коммите прогонов ещё не
  было, и гейт сказал бы «прогонов нет» — но `git diff 7e09c9a 722ac8e` пуст, то есть едет
  ровно проверенный код. Это сильнее, чем наличие прогона.
* **⚠ HEREDOC СНОВА СЪЕЛ ЭКРАНИРОВАНИЕ** — правка файла через `<<'EOF'` не совпала по строке.
  Записанный урок подтверждён: сложные текстовые правки — специальным инструментом, не heredoc.

### `EV22-1` #134 — прогнан на бою: восстановление РАБОТАЕТ, поломка НЕ ВОСПРОИЗВЕЛАСЬ

**Класс инцидента 28.08 закрыт.** Репетиция механизма (впервые сделана, `scripts/rehearse_chaos_llm.sh`)
на боевой машине в одноразовом контейнере: **старая команда упала** с той самой ошибкой
`sed: cannot rename … Device or resource busy` (положительный контроль — среда воспроизводит
боевое условие), новая сняла **реально поставленную** запись, **inode не изменился**.
Боевой прогон: `чиню: код 0`, `/etc/hosts` чист, возврат **17.1 с**, живая проверка **3 из 3**
с шестью источниками, сторож не понадобился, БД вернулась к 28 / 1262 / 297.

⚠⚠ **НО ПОЛОМКА НЕ СЛОМАЛА, и issue оставлена ОТКРЫТОЙ.** Запись легла в файл (код 0), а
приложение продолжило ходить в DeepSeek и отдало настоящий ответ. `/etc/hosts` влияет только на
**НОВЫЕ разрешения имени**, а у приложения, поднятого 44 минуты назад и только что обслужившего
пробы, тёплый пул соединений и уже выполненный резолв. 28.08 сценарий СРАБОТАЛ — значит
воспроизводимость зависит от состояния пула, а не от команды.
**Сценарий, срабатывающий через раз, измеряет состояние пула, а не устойчивость сервиса**, и его
числа нельзя класть в базу сравнения. Сценарий Qdrant не затронут: остановка контейнера рвёт
соединения. Три варианта замены механизма — в issue и
[отчёте](eval_runs/2026-08-30_ev22-1_chaos_llm.md); **выбор за владельцем**.

⚠⚠ **Инструмент при этом НЕ СОВРАЛ** — вернул третий исход «НЕ ВОСПРОИЗВЕДЕНО», а не «ПРОЙДЕН».
Предохранитель ранних раундов ревью PR #135 **впервые сработал на бою**.

### `K14` #35 — ГОТОВА, СЕМЬ РАУНДОВ РЕВЬЮ ОТРАБОТАНЫ · на бою НЕ выкачено

**`dev` = `2912f9d`, впереди `main` на 30 коммитов, PR #137. 1173 теста** — зелено И с живым
Qdrant, И с недоступным. CI зелёный на последнем коммите (push и pull_request).

**Приёмка:** условие Перечня доехало **1.00 ×4** · faithfulness **1.00 ×4** — ✅ критерий ТЗ
выполнен. ⚠ Две метрики уровня формулировки ПЛАВАЮТ на неизменном коде (общее правило
1.00/0.50/0.50/1.00, источник 0.57/0.57/0.57/0.71) и порогом служить не могут — класс `EV20` #117.
**Ось `K9` не сдвинулась ни за один из семи раундов:** атрибуция@1 **24/25** · представленность
**25/25** · нужный пункт **5/5** (перепроверено после полной локальной переиндексации).
Корпус `pp719_rules` 328 → **681**, таблица условий **245** записей.
Свип маршрута (188 вопросов прежних наборов) — **1/0/0/15/5, побайтно как до правок раунда 7**.
Выкатка готова: профиль `v0.5.0-test23` (маркеры **7 + 1 анти**, сверены реальным `grep`
31.08 против нового кода и против `722ac8e`), релизная проверка **0 провалов** (5 блоков на
переиндексированном корпусе), сухой прогон чист.

#### ⚠⚠⚠ Семь раундов ревью: 43 находки, 13 HIGH, СХОДИМОСТИ НЕТ

| раунд | находок | HIGH | что характерно |
|---|---|---|---|
| 1 | 7 | 2 | сломан инвариант `K12`; год как код |
| 2 | 5 | 2 | **обе HIGH — в правках раунда 1** |
| 3 | 7 | 3 | потеря текста первоисточника |
| 4 | 5 | 2 | загрязнение таблицы фактов чужими правилами |
| 5 | 6 | 2 | **обе HIGH — в правке раунда 4** |
| 6 | 8 | 0 | первый раунд без HIGH |
| 7 | 5 | 2 | утечка вне-сферы по общей таможенной лексике |

**Батарея была зелёной на всех сорока трёх.** Дефекты жили в зазорах между компонентами, у
каждого из которых были свои зелёные тесты.

⚠⚠ **ВЫВОД РАУНДА 6 О СХОДИМОСТИ ОКАЗАЛСЯ НЕВЕРНЫМ.** Там было записано: находок не убывает, но
тяжесть падает (2·2·3·2·2·**0**) — вот признак. Раунд 7 дал 2 HIGH; ряд целиком
**2·2·3·2·2·0·2** не убывает ни по числу, ни по тяжести. Один нулевой раунд был точкой ряда с
неизмеренной дисперсией, а не сигналом. **Правило трёх прогонов относится и к «признакам
сходимости» цикла ревью.** Правки самого ревью давали HIGH в раундах **2, 5 и 7** — три из семи.

**Крупное, что чинилось:** инвариант `K12` · усечение десятизначного кода до шести знаков —
уверенный НЕВЕРНЫЙ вердикт про позицию, названную полностью · год «2026» как код ТН ВЭД ·
заземление гарда без блока фактов · диапазон УЖЕ запроса, выданный за покрывающий · молчание про
более узкий код · потеря разделов 9-1 и 9-2 Соглашения · таможенные вопросы в корпусе 719 ·
недостижимость пути `T9` · загрязнение условий чужими правилами (29 записей из 244; у позиции 3507
предел 20 % из ЧУЖОЙ главы) · «включён в Перечень» с ПУСТЫМ условием · документ, пришедший добором
и режущийся первым · **вопрос про СТ-1, возглавляемый документом про акт экспертизы** (раунд 6) ·
**таможенный вопрос с ответом, синтезированным из Правил СНГ** (раунд 7) · **вводная приложения 4
Соглашения, не попадавшая в индекс вовсе** (раунд 7).

⚠ **Одна находка ПРИНЯТА, а не исправлена** (MED-HIGH раунда 5): круговая раздача квоты меняет
состав окна на оси документов — Приказ №52 6 мест → 5, одно уходит Методрекомендациям (законный
второй документ темы по `K15`). Ни один критерий не просел; факт замерен на настоящем корпусе,
записан и закреплён сторожем на НАМЕРЕНИЕ. Откат вернул бы потерю `decree_body` на главном вопросе.

#### Что дал раунд 7 (последний)

* **Утечка вне-сферы 11 → 0.** Слова-спутники ТН ВЭД были общей таможенной лексикой, а у
  процедурной ветки НЕТ гейта вне-сферы. ⚠⚠ **Первая редакция правки была замерена на наборе,
  который я под неё же и подобрал** — выглядела чистой, батарея уронила ШЕСТЬ тестов прежних
  раундов. Решено ЗАМЕРОМ трёх состояний: стволы 11/1 · только фразы 0/3 · **стволы + закрытый
  список дисквалификаторов 0/1** (доминирует оба). Форма правила — «список ИСКЛЮЧЕНИЙ», приём
  самого Перечня условий.
* **Предохранитель, который не мог сработать никогда:** проверка «есть ли признак позиции МЕЖДУ
  словом о деньгах и числом» смотрела в ПУСТУЮ строку (`m.end()` = начало числа).
* **Своя регрессия, пойманная проверкой краёв, а не ревью:** отсечение числа-счётчика усекало
  настоящие полные коды («8544 70 000 0 позиции» → «8544 70 000») — класс HIGH раунда 1.
  Разрешено только у четырёхзначной основы: подъём к РОДИТЕЛЮ, а не переход к ЧУЖОЙ позиции.
* **Словарь жил в ТРЁХ копиях**, сведён в одну; ⚠ унификация ТИХО выбросила ствол, стоявший
  только в `topics`. Асимметрия обоснована (`classify` зовут только ЗА гейтом) и возвращена ЯВНО.
* **Класс «таможня + ТН ВЭД» заведён в свип отдельным файлом** `scripts/eval_offdomain_tnved.json`
  (12 вопросов), не в `eval_golden_negative`: ⚠⚠ тот кормит ПОРОГОВУЮ метрику отказа, и 12 легко
  проходящих кейсов подняли бы её **0.87 → 0.89 без единой правки кода**. Положительный контроль
  набора: на коде до правки 9 утечек из 12, после — 0.

⚠⚠ **CI был КРАСНЫМ на моём же коммите:** два новых класса звали живой Qdrant — ровно `O3` #104,
про который я сам писал в отчётах. Заметил по статусу CI, а не из локального прогона. Механизм
переехал на заглушки, заведена проверка «батарея с НЕДОСТУПНЫМ Qdrant».
⚠⚠ **Heredoc семь раз съел экранирование**, дважды превратив `\b` в РЕАЛЬНЫЙ backspace (0x08)
внутри регулярки: grep его не видит, шаблон молча мёртв, тест зелёный. Заведён сторож на класс.

**Сторожа `K14` суммарно: 134 утверждения в одиннадцати файлах, слепых 0** (шесть слепых найдено
мутацией по ходу и переписано). Раунд 7: 24 утверждения, **11 мутаций, слепых 0**.

Отчёт о цикле — [eval_runs/2026-08-31_k14_review_rounds.md](eval_runs/2026-08-31_k14_review_rounds.md).

#### ✅ РЕШЕНИЕ ВЛАДЕЛЬЦА (01.09.2026): РАУНД 8 ПО ДИФФУ, ПРАВИЛО ОСТАНОВКИ ОБЪЯВЛЕНО

Цикл не заканчивается по построению — у него нет измеренного критерия остановки, а «ревью всё ещё
что-то находит» им не является. Владелец выбрал **раунд 8 ТОЛЬКО по диффу раунда 7** (правки ревью
дали HIGH в трёх раундах из семи — самая урожайная мишень).

**Правило остановки объявлено ЗАРАНЕЕ, до запуска ревью и до первой находки:**

> **Нет HIGH в раунде 8 → выкатываем `v0.5.0-test23`**, независимо от числа находок LOW/MED.
> Есть HIGH → чиним, и это само по себе ответ на вопрос о сходимости цикла.

⚠ Почему запись сделана коммитом ДО прогона: критерий, сформулированный после результата,
подгоняется под него. Именно так и получился неверный вывод раунда 6 о сходимости — он был
объявлен по ОДНОЙ точке ряда с неизмеренной дисперсией и опровергнут раундом 7.
⚠ Мишень раунда 8 — коммит `2912f9d` и только он: `app/rag/okpd2_ref.py` +80,
`app/rag/topics.py` +59, `app/rag/procedural.py` +9. Прочие 30 коммитов пакета в мишень НЕ входят
— они прошли по семь раундов каждый, и повторный проход по ним размывает мишень.

**Осталось:** ① раунд 8 по диффу → выкатка по объявленному правилу · ② `K16` #48 своим окном ·
③ `B1` #61 · ④ три решения владельца (механизм `#134`, порог первого токена, база сравнения на
один тег).

---

### _(история)_ `K14` — шаг 6 локально

✅ **Переиндексация:** `pp719_rules` **328 → 673** (+58 Соглашение СНГ, +287 Приказ ТПП РФ №14),
полная, не `--doc`. **Тестов 1053 → 1069.** Отчёт —
[eval_runs/2026-08-31_k14_step6_reindex.md](eval_runs/2026-08-31_k14_step6_reindex.md).

✅ **Квота окна выбрана ЗАМЕРОМ трёх вариантов.** Головные числа у всех совпали (СТ-1 8/8,
атрибуция@1 24/25, представленность 25/25) — различие в СОСТАВЕ окна на ЧУЖИХ вопросах: с
гарантированным местом новые документы занимали слот на **44 %** вопросов оси `K9`, сноски
вытеснялись **полностью**. Выбран `ON_DEMAND` + документы темы: **8 %**, соседи на месте.
**Радиус оси `K9` к базе не сдвинулся** (0.96 / 1.00 / 0.84).

⚠⚠ **ЛУКАП ПЕРЕЧНЯ НЕ БЫЛ ПОЗВАН НИ ИЗ ОДНОГО МЕСТА РАНТАЙМА.** Модуль жил с 30.08 и не
использовался, а Перечень в вектор не идёт намеренно — вопрос про условия по ТН ВЭД отвечать было
НЕЧЕМ. Подключён под двойным гейтом: тема И явный код.

⚠⚠ **Гранулярность второго ключа была невидима извлекателю:** `extract_tnved` требует шести цифр
(писался под HS6), а Перечень ключуется на ЧЕТЫРЁХЗНАЧНОЙ позиции — ни один вопрос набора он не
разбирал. Заведена отдельная функция.

⚠⚠ **Платный прогон нашёл рантайм-дефект, невидимый офлайну:** faithfulness 0.62 — гард объявлял
выдумкой «50» из условия Перечня, то есть бил по ВЕРНОМУ ответу числами, заземлёнными сильнее
всего в системе. Заземление расширено до всего фактического, что отдано модели: **0.62 → 1.00**.

⚠⚠ **Распознаватель источника не знал СОБСТВЕННОГО названия документа** («Правила определения
страны происхождения…»): 0.43 → 0.71. ⚠ Контроль усилен, но **проверено — этот класс он всё равно
не ловит**; записано как граница, а не гарантия.

**Приёмка (3 платных прогона):** условие доехало **6/6** · faithfulness **8/8** · источник назван
5/7 · общее правило 1/2. ✅ **Критерий ТЗ выполнен.**
⚠ Два содержательных остатка (#55 не называет Приказ №14, #58 не даёт операционное правило) —
лечатся промптом темы, требуют СВОЕГО замера, в пакет не взяты.

⚠ **Не сделано шагом 6:** `K16` #48 (отдельная задача, ~1 сессия) и **выкатка на бой**.

---

### _(история)_ `K14` — шаги 4 и 5

✅ **Шаг 5 — восемь приёмочных кейсов СТ-1/ТН ВЭД и их оракул** (`07b65bf`). Тестов **1035 →
1053**. Вторая по частоте претензия июля (**29 упоминаний**) впервые проверяется стандартным
замером, а не адресными живыми прогонами руками. Машинерия `EV21` переиспользована целиком:
`kind` в кейсе, срез `--kind st1`, отдельная популяция в сводке.

| # | что ловит |
|---|---|
| 51 · 8403 | адвалорное условие Перечня (50 % цены) |
| 52 · 8501 | **две** величины — 50 % и подпредел 10 % на материалы позиции 8503: усечение меняет заявителю расчёт |
| **53 · 8402** | ⚠⚠ **ключевой кейс оси:** позиции в Перечне НЕТ, а сосед 8403 есть. Перечень — список ИСКЛЮЧЕНИЙ, промах даёт не пустоту, а ЧУЖОЕ правдоподобное условие |
| 54 · 8704 | запись ЧАСТИЧНАЯ (тип «форвардер») — кроме предела нужны технологические операции |
| 55–58 | процедура выдачи (Приказ №14) · кумулятивный принцип · путь для продукции вне 719 · критерий в форме САМОГО документа |

⚠⚠ **Оракул независим от рантайма** — не зовёт `app.rag.st1_ref`: позови, и снятие лукапа сделало
бы метрику зелёной (класс `O3` #104). Таблица фактов читается **только как положительный
контроль**: число или слово, которого нет в условии Перечня, **останавливает замер** до первого
платного вызова. Проверено четырьмя мутациями.

⚠⚠ **Новый документ создал дефект в СУЩЕСТВУЮЩЕЙ метрике.** Распознаватель источника
`tpp_order_52` был голым `приказ\w*\s+ТПП` и срабатывал на «Приказ ТПП РФ **№14**»; вторая его
альтернатива («положение о порядке выдачи») совпадала с заголовком №14 — коллизия по обеим
половинам сразу. **Направление худшее — ложно ПОЛОЖИТЕЛЬНОЕ:** метрика зеленела бы на ответе,
сославшемся не на тот документ. Заведён контроль «распознаватель обязан узнать СВОЙ ярлык и **не
узнать ни один чужой**» — прежняя проверка требовала только первой половины.

⚠⚠ **Чужой предохранитель сработал верно ВТОРОЙ раз за день:** кейсы лежат в ТОВАРНОМ наборе
`eval_golden.json`, и без своего класса все восемь считались ложными срабатываниями — главный
сторож правки маркеров покраснел на ВЕРНОМ поведении. Свип **212 → 220**, ложные снова 1,
новых пропусков 0.

⚠ **Найден слепой сторож в моих же тестах:** `test_percent_spacing_does_not_decide` пережил
мутацию, снимавшую нормализацию, — величины записаны голыми числами, и пробел на них не влияет.
Тест приписывал заслугу не тому механизму. Переписан; настоящий сторож нормализации заведён
отдельно.

⚠ **Кейсы НЕ ПРОГНАНЫ:** `eval_answers` зовёт DeepSeek и требует Qdrant с новыми документами.
Сводка проверена офлайн на синтетических ответах — **в обе стороны** (1.00 на верных, 0.00 на
сломанных), иначе её числа впервые читались бы в платном прогоне.

---

### `K14` #35 — шаг 4: ПЕРВАЯ правка поведения, выбрана замером

✅ **Шаг 4 — маршрут второго ключа** (`5050a18` замер + `6bd7b24` правка). Тестов **1023 → 1035**,
зелено в документированной среде.

⚠⚠ **Свип маршрута по второму ключу не мерил НИЧЕГО:** во всех пяти прежних наборах (191 вопрос)
было **ноль** вопросов по ТН ВЭД. Пять вопросов с темой `st1_origin` проходили процедурной веткой
по **случайным** причинам — ГИСП, Минпромторг, ТПП, «страна происхождения», «подпункт г», — и это
создавало видимость покрытия: голое «как получить СТ-1» уходило товарной веткой. Замер до правки:
**19 из 19 товарной веткой**.

**Выбор сделан замером четырёх вариантов, а не рассуждением** (метод `P3` #121):

| вариант | пробник 19 | радиус на 191 |
|---|---|---|
| база | 19/19 | — |
| а) словарь гейта | 11/19 | нулевой |
| б) а + уступка дисквалификатора | 9/19 | нулевой |
| в) гейт не трогаем | 19/19 | нулевой (по построению) |
| **б+) б + словарь темы** | **3/19** | **нулевой** |

⚠⚠ **Нули доказаны ТРЕМЯ отрицательными контролями.** Первые два не двигали ось «ложные на
контролях пробника» — её блокировала уцелевшая половина гейта; без третьего утверждение «товарные
вопросы не перехватываются» осталось бы недоказанным.

⚠⚠ **Третий случай класса `#120`:** `procedural.py` требовал предлог («сертификат **о**
происхождении»), а `topics.py` давно сделал его необязательным с комментарием «так пишут в
половине реальных вопросов». Урок снова не переехал через модуль. ⚠ Вторая находка того же рода:
регулярка темы не знала формы **самого документа** — «достаточной **обработки/**переработки»,
13 вхождений в корпусном тексте.

⚠⚠ **Пробник встроен в свип постоянным набором** (`scripts/eval_st1_route.json`, 21 кейс): свип
**191/5 → 212/6**. Это и был дефект инструмента, прятавший находку. Разметка **по маршруту, а не
по теме** — тему выдаёт проверяемый код.

⚠⚠ **Чужой предохранитель сработал на этой правке и сработал ВЕРНО:** храповик «пропусков ≤ 11»
ударил на **11 → 14**. Порог **не поднят** — это утопило бы остаток `P3` в чужой популяции; область
счёта сужена явно, а к сужению добавлен положительный контроль «набор обязан быть в свипе».

**Сторожей 11, каждый проверен мутацией — слепых 0 из 11.**

⚠ **Остаток 3 из 19 записан в данных набора и объяснён поимённо**, а не замолчан.

⚠⚠ **ЧТО ШАГ 4 НЕ ЗАКРЫЛ и почему:**
* **квота окна** (`_DOC_TYPES["st1_origin"]` по-прежнему называет `decree_body` и `tpp_order_52`,
  новых документов там нет) — решается составом выдачи, а свип маршрута состава окна не видит;
* **`key_type` в рантайме** — поле есть в паспорте манифеста и проверяется на загрузке, но
  роутер его пока не читает;
* оба упираются в живой Qdrant с новыми документами, то есть в шаг 6.

---

### _(история)_ `K14` — шаги 1–3

✅ **Шаг 3 — корпусный текст обоих документов и записи манифеста** (`7b777b0`).
Соглашение СНГ **58 записей**, Приказ ТПП РФ №14 **287**; конвертер `scripts/convert_st1_docs.py`,
парсеры `parse_sng_origin` / `parse_prikaz14`, паспорта в манифесте (`key_type: ТН ВЭД` у
Соглашения — второй ключ объявлен).

⚠⚠ **Перечень в корпус НЕ пошёл** — 91 769 знаков вырезаны на конвертации: они уже таблица фактов
с лукапом, а два источника одного факта расходятся молча. Бланки и обвязка `alta.ru` вырезаны там
же, у каждого выреза положительный контроль.

⚠⚠ **Четыре дефекта в собственном разборе, найденные числами:**

| Дефект | Радиус |
|---|---|
| Пункты Приказа пишутся **без точки**, шаблон точку требовал — ловил подписи граф бланков вместо пунктов (**инверсия**) | **12 → 287** |
| «Термины и понятия» не нумерованы вовсе — терялся раздел в ОБОИХ документах | 54 → 58 |
| Девять Положений в Приказе, «Раздел 1» встречается **9 раз** — без владельца якорь указывал бы не на тот документ | повторов **0** |
| Редакция договора — Протокол **без номера**; распознаватель выдавал за неё ссылку на посторонний акт `ред. от 18.06.2010 N 324` | штампы **5** прежних документов не изменились |

⚠ Правка распознавателя внесла свой дефект (брала первую дату перечня протоколов вместо
последней — штамп на 12 лет старше), пойман замером радиуса по всем семи документам.
⚠ Два моих теста оказались неверны и исправлены ПО ФАКТУ: якорь ненумерованного раздела не
содержит «п. » по построению; названия граф законно стоят в п. 7.4 (норма о заполнении) —
проверять надо разграфку бланка в ЗАПИСЯХ, а не строку в файле.

**Тестов 1000 → 1023**, четыре предохранителя проверены мутацией.

---

### _(история)_ `K14` — шаги 1–2

✅ **Шаг 1 — Перечень условий СНГ → таблица фактов по ТН ВЭД** (`fc66a4d`): **244 записи**,
`knowledge_base/classifiers/tnved_st1_conditions.json`, парсер `scripts/convert_st1_perechen.py`.
Приложение 1 — 65 % документа; **промышленных позиций 62**, из них раздел XVI (машины,
оборудование, электротехника) — 39 строк. Шесть форм строки найдены на исходнике, а не угаданы.

✅ **Шаг 2 — детерминированный лукап** (`91f50c2`): `app/rag/st1_ref.py` — по коду отдаёт условия
из Перечня либо общее правило, всегда говоря, что именно из двух.

**Тестов 967 → 1000.**

⚠⚠ **Обоснование «таблица, а не вектор» оказалось сильнее записанного в ТЗ.** Перечень — СПИСОК
ИСКЛЮЧЕНИЙ: общее правило действует для ВСЕХ товаров, кроме включённых. Значит промах поиска
**не даёт пустого ответа** — он даёт ДРУГОЕ, правдоподобное и неверное условие, снаружи
неотличимое от верного. Любая ошибка на этой оси молчалива по построению.

⚠⚠ **Три дефекта в собственных правках найдены сверкой и прогоном, а не рассуждением:**
список кодов через запятую терял второй код молча (**33 записи из 244**) · «8803 - Исключена.»
приклеивалась к условию соседа (8704) · лукап печатал «позиция 8702» для строки-диапазона
«из 8702 - 8704», то есть на запрос про 8704 показывал условие ЧУЖОЙ позиции.

### Чем начать следующую сессию

1. ✅ **`K14`, шаг 4 — маршрут: СДЕЛАН 31.08** (вариант `б+`, выбран замером). Осталось от шага:
   **квота окна** и **`key_type` в рантайме** — оба требуют живого Qdrant с новыми документами,
   то есть делаются вместе с шагом 6.
2. ✅ **`K14`, шаг 5 — приёмочные кейсы: СДЕЛАН 31.08** (8 кейсов + оракул). Не прогнаны: нужен
   Qdrant с новыми документами и платный вызов — это шаг 6.
3. **`K14`, шаг 6 — ЕДИНСТВЕННОЕ, ЧТО ОСТАЛОСЬ** — полная переиндексация одним окном с `K16` #48, и в том же окне —
   замер квоты (`RULES_QUOTA_ON_DEMAND` против второго документа темы, по очереди, как в заходе 5)
   и живой прогон. ⚠ Урок `K15`: юнит-тесты были зелёными при невыполненном критерии, потому что
   документ не проходил в окно.
4. **Решить по механизму поломки `#134`** (фаервол / переменная + рестарт / холодный пул) —
   решение, а не код.
5. **Пересмотр порога первого токена** (2.614 при назначенном ≤2 с) — тоже решение.
6. **Свести базу сравнения на один тег**: уровни 0–3 сняты на `test18`, уровень 4 — на `test21`.

⚠⚠ **Письмо эксперту готово с 24.08 и НЕ ОТПРАВЛЕНО.** Оно держит пять issues (`#53` P0 по
риску, `#95`, `#69`, `#52`, `#65`) и весь критический путь к `1.0`: письмо → волна → `#13`
(приёмка ≥70 %) → `#25`. От нас тут не зависит ничего — это самый дорогой простой в проекте.

---

## _(история)_ Состояние — 28.08.2026, конец сессии · ⚠⚠ PR #135 ОТКРЫТ И НЕ СЛИТ

**Бой не тронут и здоров:** `prod` = `bac288d` (`v0.5.0-test21`), `/ping` 200 ×3.
`dev` = `511eb27`, **впереди `main` на 17 коммитов**. **955 тестов**, открытых issues **32**.
⚠ Число сверено `gh issue list --limit 100`: у `--json` ТИХИЙ ЛИМИТ 30 по умолчанию, и он уже дал сегодня ложную «поправку» 31 → 30. Инструмент с молчаливым усечением — не счётчик.

### ⚠⚠ СОСТОЯНИЕ БОЯ НА МОМЕНТ ЗАКРЫТИЯ — ПОДТВЕРЖДЕНО НЕ ДО КОНЦА

**Последнее ПОДТВЕРЖДЁННОЕ здоровье:** `/ping` 200 ×3 (0.14–0.16 с) плюс два настоящих ответа с
`источников=6` и чистым `/etc/hosts` — снято ПОСЛЕ восстановления от инцидента и ПОСЛЕ уборки
тестовых аккаунтов. **После этого на бою не менялось НИЧЕГО.**

⚠ **Но перед самым закрытием канал перестал проходить совсем:** 5 попыток `/ping` и SSH — все в
таймаут. По записанной асимметрии таймаут ОТСЮДА не доказывает ничего (сегодня тот же `/ping`
давал 0.147 с и таймаут 20 с в пределах минуты), и признаков проблемы нет. Но **живого
подтверждения на момент закрытия тоже нет.**

⚠⚠ **ПОЭТОМУ ПЕРВОЕ ДЕЙСТВИЕ СЛЕДУЮЩЕЙ СЕССИИ — ПРОВЕРИТЬ БОЙ, а не читать планы.** Основание не
паранойя: сегодня хаос-проверка **уже роняла сервис и не могла его поднять**, и хотя
восстановление подтверждено запросами, закрывать сессию после такого без финального
подтверждения нельзя. Проверять: `/ping`, затем настоящий вопрос — здоровый ответ несёт
**источников > 0**, деградированный отвечает кодом 200 с нулём источников.

### ⚠⚠⚠ ПЕРВЫМ ДЕЙСТВИЕМ СЛЕДУЮЩЕЙ СЕССИИ

```
1. Дочитать ревью PR #135, раунд 6 (запускался в конце сессии — результат НЕ сохранён)
2. Слить PR #135  — main СЕЙЧАС НЕСЁТ РЕГРЕССИЮ ГАРДА
3. Выкатить v0.5.0-test22 (профиль и проверки готовы) + EV22-1 #134 тем же окном
```

⚠⚠ **`main` НЕСЁТ РЕГРЕССИЮ ГАРДА И БУДЕТ НЕСТИ, ПОКА PR НЕ СЛИТ.** На нём `b890f69` без
исправления: гард молчит на четырёх классах настоящей выдумки. **На бою этого нет** — `prod`
старше `b890f69`, — но `main` тестирует эксперт. Проверено командой: `git log main --grep`
исправления не находит.

⚠⚠ **РАУНД 6 РЕВЬЮ ВЕРНУЛСЯ — 7 НАХОДОК, СОХРАНЕНЫ В [#136](https://github.com/pan3v1n/navigator-719/issues/136), НЕ ИСПРАВЛЕНЫ.**
Все — в инструментах замера; **хаос-часть в выкатку `v0.5.0-test22` не входит**, поэтому релиз
они не блокируют. Блокируют **`EV22-1` #134** (перепроверка хаос-сценария LLM).
⚠⚠ **Первая — HIGH и ложно ПОЛОЖИТЕЛЬНАЯ:** 429 в пробе `during` проходит через `judge` как
внятный фолбэк и даёт **«ПРОЙДЕН»** — инструмент не наблюдал поломку вовсе (упёрся в собственный
лимит), а отчитался, что фолбэк проверен.
⚠ Общий корень первых четырёх: `rate_limited()` заведён правкой раунда 5, но **позван в одном
месте из трёх**, где проба может вернуть 429.

### Что сделано за сессию

1. **`EV22` #130 — уровень 4 снят впервые за шесть заходов**, числами вместо строки «частично».
2. **Цикл ревью PR #135: пять раундов, 29 находок** (9 → 5 → 8 → 4 → 3).
3. Заведена **`EV22-1` #134**.
4. Инцидент на бою (хаос уронил сервис) — разобран, устранён, бой восстановлен и проверен
   запросами; БД возвращена к 28 пользователям и 1262 репликам.

### ⚠⚠ Что готово к выкатке и НЕ выкачено

Профиль `releases/v0.5.0-test22.env` — рантаймовая, коллекции 1377 / 328 / 12 не трогаем.
Сухой прогон чист: маркеры **5 + 1 анти**, проверок рантайма **9 общих**, CI зелёный.
⚠ В релизе РОВНО ОДНА правка поведения — гард `K15`; остальное инструменты замера, из рантайма
не вызываемые (проверено `git diff prod..dev` по `app/`: один файл).
⚠ Живая проверка ×5 после выкатки ОБЯЗАТЕЛЬНА: метрики идут с ВЫКЛЮЧЕННЫМИ кейсами, на бою они
включены — именно так был найден дефект, с которого начался весь цикл.

### Числа уровня 4 (бой, 2 vCPU, против 127.0.0.1)

| Режим | первый токен p50 / p95 | полный ответ p50 / p95 | отказы |
|---|---|---|---|
| одиночный (n=30) | **2.614 / 7.328 с** | **6.423 / 9.228 с** | 0 |
| 10 одновременных | 5.594 / 12.721 с | 8.586 / 12.747 с | 0 |
| 25 одновременных | 11.563 / 18.758 с | 16.162 / 18.992 с | 0 |
| 50 одновременных | 19.839 / 27.556 с | 31.354 / 34.404 с | 0 |

**Предел параллельности НЕ НАЙДЕН** — при 50 дошли все 50. Волна (17 ТПП) внутри с тройным
запасом: **единственный незакрытый технический риск волны снят**.
Стоимость **0.2546 ₽ и 8271 токен на ответ** (оценка 0.34 ₽ в `CLAUDE.md` завышена).
⚠ Порог первого токена ≤2 с назначен ДО измерения — вынесен на ПЕРЕСМОТР, не на подгонку системы.

### ⚠⚠ Главное за сессию — про метод

* **ДВА КОНТУРА НА ОДНОМ МЕХАНИЗМЕ — ЭТО ОДИН КОНТУР.** Хаос-проверка уронила бой: снятие
  делалось `sed -i` по `/etc/hosts` контейнера (bind-mount, rename запрещён), а сторож выполнял
  ТУ ЖЕ команду и упал так же. Резервирование считается по РАЗЛИЧИЮ способов.
* **МЕХАНИЗМ, ДАЮЩИЙ НАХОДКУ КАЖДЫЙ РАУНД, ЗАМЕНЯЮТ.** Гард колебался три раунда между
  «ложные срабатывания» и «слепота»; заменён на инвариант «определители — открытый класс,
  отменители отрицания — перечислимый», и раунд 5 прошёл чисто.
* **ЗДОРОВЬЕ СЕРВИСА НЕЛЬЗЯ МЕРИТЬ КОДОМ ОТВЕТА** — при лежащем Qdrant он отвечает 200; различает
  только число ИСТОЧНИКОВ (6 против 0).
* **ТЕСТ, НАПИСАННЫЙ ПОД УДОБНОЕ ЧИСЛО, СЕРТИФИЦИРУЕТ НЕСУЩЕСТВУЮЩУЮ ПОЧИНКУ** — порог
  `tool_error` проверялся на n=100, которого драйвер не производит: зелёный тест при
  неизменившемся поведении.
* **ПРОВЕРКА, СТЕРЕГУЩАЯ ТРИЖДЫ СЛОМАННЫЙ МЕХАНИЗМ, ЛЕЖАЛА В КАТАЛОГЕ ОДНОГО РЕЛИЗА** — так уже
  терялась `D12`. Перенесена в `checks/common`.
* **ИНСТРУМЕНТ, КОТОРЫЙ БЛОКИРУЕТ САМ СЕБЯ, ОБВИНЯЕТ В ЭТОМ СЕРВИС** — опрос хаоса давал 40
  запросов/мин при лимите 20 и печатал «сервис не вернулся» про здоровый сервис.

### Оперативное

* Канал до VM открывается со 2-й попытки после паузы ~150 с. Работу вести **отвязанно**
  (`setsid nohup` + файлы): за заход канал рвался 4 раза, ни один прогон не пострадал.
* ⚠ **`docker compose exec -T` ЧИТАЕТ STDIN** и молча съедает остаток скрипта из
  `ssh 'bash -s' <<EOF` — лечится `< /dev/null` на КАЖДОМ вызове.
* ⚠ **Регулярки не писать через heredoc:** экранирование съедается, в файл попал настоящий
  символ backspace вместо ``, и гард срабатывал на всём подряд.
* Локально остался поднятым Docker с контейнером `qdrant` (порт 6533) — при необходимости
  остановить вручную.

---

## _(история)_ Состояние — 28.08.2026 · ✅ УРОВЕНЬ 4 СНЯТ ВПЕРВЫЕ ЗА ШЕСТЬ ЗАХОДОВ

**На бою `v0.5.0-test21`** (`prod` = `bac288d`). **`dev` = `0ea24f7` — впереди `main` на
10 коммитов, ничего из сделанного сегодня НЕ ВЫКАЧЕНО.** **942 теста**, открытых issues **30**.

### ⚠⚠ ЧЕМ НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

```
1. PR dev → main  (минуты)   — main СЕЙЧАС несёт регрессию гарда
2. v0.5.0-test22  (сессия)   — выкатка исправления + EV22-1 #134 тем же окном на VM
3. Заход 6: K14 #35 + K16 #48 (своя сессия, полная переиндексация)
```

⚠⚠ **`main` НЕСЁТ РЕГРЕССИЮ.** На нём `b890f69` без исправления `aae28c8`: гард молчит на
четырёх классах настоящей выдумки («…заключение ТПП…, а не «сертификат СТ-1»»). На бою этого
нет — `prod` старше `b890f69`, — но `main` тестирует эксперт, и она не должна быть хуже прода.

### Что сделано 28.08

* **`EV22` #130 закрыт по критерию.** Уровень 4 в базе сравнения числами, а не строкой
  «частично». Отчёт — [eval_runs/2026-08-28_level4.md](eval_runs/2026-08-28_level4.md).
* **Ревью `b890f69` — 6 находок**, главная остановила регрессию до боя.
* **Заведена `EV22-1` #134** — перепроверить хаос-сценарий LLM.

### Числа уровня 4 (бой, 2 vCPU, против 127.0.0.1)

| Режим | первый токен p50 / p95 | полный ответ p50 / p95 | отказы |
|---|---|---|---|
| одиночный (n=30) | **2.614 / 7.328 с** | **6.423 / 9.228 с** | 0 |
| 10 одновременных | 5.594 / 12.721 с | 8.586 / 12.747 с | 0 |
| 25 одновременных | 11.563 / 18.758 с | 16.162 / 18.992 с | 0 |
| 50 одновременных | 19.839 / 27.556 с | 31.354 / 34.404 с | 0 |

**Предел параллельности НЕ НАЙДЕН:** при 50 одновременных дошли все 50. Волна (17 ТПП) внутри
с тройным запасом — **единственный незакрытый технический риск волны снят**.
Стоимость: **0.2546 ₽ и 8271 токен на ответ** (оценка 0.34 ₽ в `CLAUDE.md` завышена).

⚠⚠ **Порог первого токена вынесен на ПЕРЕСМОТР, а не «починен»:** 2.614 при ≤2 с. Назначался
до единого замера, а перед первым токеном стоят эмбеддинг e5 на CPU, поиск и реранк.

### ⚠⚠⚠ Инцидент: хаос-проверка уронила бой и не смогла поднять

`sed -i` по `/etc/hosts` контейнера — это **bind-mount отдельного файла**, rename поверх которого
ядро запрещает. Поломка работала, снятие не работало **никогда**; бой отдавал 5xx до ручной
починки. Доказано структурно: старая команда даёт код 4 **даже на файле, где строки нет**.

⚠⚠ **Сторож не спас — он выполнял ТУ ЖЕ команду.** ДВА КОНТУРА НА ОДНОМ МЕХАНИЗМЕ — ЭТО ОДИН
КОНТУР. Исправлено: снятие без rename + **холостая проверка восстановления ПЕРЕД поломкой**
(команды идемпотентны, исполнимость доказывается, ничего не ломая).

### Оперативные правила, подтверждённые сегодня

* **Канал открывается со 2-й попытки после паузы ~150 с** — сегодня подтвердилось трижды.
  Работу на VM вести **отвязанно** (`setsid nohup` + файлы), тогда обрыв не отменяет работу:
  за заход канал рвался 4 раза, ни один прогон не пострадал.
* ⚠ **`docker compose exec -T` ЧИТАЕТ STDIN** и съедает остаток скрипта, переданного через
  `ssh 'bash -s' <<EOF`. Шаг молча не выполняется, код возврата 0. Лечится `< /dev/null`.
* **Замер только НА машине:** снаружи один и тот же `/ping` дал 0.147 с и два таймаута по 20 с.

---

## _(история)_ Состояние — 27.08.2026, ночь · ✅ ВЫКАЧЕНА `v0.5.0-test21`

**`prod` = `bac288d` = тег.** Вердикт скрипта «✅ выкатка завершена, все проверки прошли»,
рантаймовая, коллекции 1377 / 328 / 12. **909 тестов**, открытых issues 30.
⚠ `main` = `dev` = `b890f69` — на один коммит впереди: послевыкаточная правка `K15-2`, НЕ выкачена.

### ⚠⚠ ЧЕМ НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

```
EV22 #130 — уровень 4 на VM: latency, нагрузка 10/25/50, хаос, стоимость
```

Единственный уровень протокола, **не снятый ни разу за пять заходов**: в базе сравнения на его
месте строка «частично». Нужен **до** волны — волна и есть нагрузочное событие, а p50, доля
таймаутов и предел параллельности неизвестны. ⚠ Диск на бою занят на 80 % (5.9 ГБ свободно) —
мерить и это.

⚠ **Работу на VM батчить одним заходом.** Канал рвался семь раз за выкатку `v0.5.0-test21`,
каждый шаг проходил со 2–3 попытки с паузой 150 с. Три отдельных визита дороже одного.

⚠ **Гейт CI проверяет коммит только на машине разработчика.** На VM нет ни `.git`, ни `gh`
(доставка тарболом), там он честно говорит «CI НЕ ПРОВЕРЕН». Сухой прогон гнать **локально и
ПОСЛЕ тегирования** — тогда он резолвит релизный коммит по тегу.

⚠ **Инструментарий выкатки едет ОТДЕЛЬНЫМ полным архивом**, не дельтой: в дельте `checks/common`
содержит только изменённые файлы, и запуск из неё прогнал бы 2 проверки вместо 8.

### Что уехало

`K2-2` #128 и `K12-1` #122 плюс два замера с отрицательным результатом (`K15-3` #132,
`K15-2` #129). **Два раунда ревью PR #133 — 12 находок.**

### ⚠⚠ Ревью заставило заменить механизм, и это оказалось верно

Первая редакция `K2-2` сравнивала НОМИНАЛЫ. Пять находок подряд показали, что путь не сходится:
«6 кВ **и выше**» читалось точным значением и роняло порог у «110 кВ» · «110 - 500 кВ» теряло
нижнюю границу (**такие позиции в корпусе есть**) · «массой свыше **0,25** кг» давало 25 кг ·
предлог «в» и союз «а» становились вольтами и амперами · и **инвариант «вето умеет только
снимать порог», которым я оправдывал вольный разбор, структурно неверен**.

Заменено правилом «у строк `strict_name` имя и есть ключ» — совпадение по множеству значимых
слов, с исключением для имён-классов («… и выше»). **Радиус обоих правил измерен и совпал:**
одна позиция, 0 появившихся, 0 изменённых. При равном результате выбрано правило без разбора
чисел.

⚠ Настоящий различитель двух позиций 27.12 — слово «(**воздушный**)», а не ампераж. Номинал
давал верный ответ по неверной причине.

### Живая проверка ×5 — и она снова нашла то, чего не видела ни одна метрика

| Вопрос | Итог |
|---|---|
| «как получить выписку из реестра» | **5/5** начинают с выписки, гард 0/5 — `K12-1` подтверждена |
| «нужно ли заключение ТПП…» (контроль) | ⚠ гард сработал **1 из 5** |
| «какой порядок внесения» (контроль) | 5/5 чисто |
| Данные `K2-2` на боевом корпусе | «до 2000 А» → порога нет ✅, «до 6300 А» → свой график ✅ |

⚠⚠ **Срабатывание 1/5 разобрано, а не списано на шум.** Воспроизведено локально — 1 из 10, и
текст оказался **верным ответом**: «Именно эти документы, **а не «заключение ТПП»**, являются
подтверждением производства». Гард не знал формы отрицания «а не «X»» и писал эксперту WARNING —
вред, ради устранения которого делалась `ddd3e16`.
⚠ Гард в релизе **не менялся** (правки `documents_ref` были только в комментариях) — это не
регрессия, а проявление предела `K15-2` #129 на новой формулировке.
⚠ **Ни одна проверка до выкатки этого не показала:** документная популяция мерялась с
ВЫКЛЮЧЕННЫМИ кейсами, а на бою они включены. Живая проверка после выкатки — единственное место,
где это видно.

### ⚠ Проверка сама попросила убрать закрывшийся долг

`checks/common/70` напечатала на бою «**склейка исчезла** — проверить, что порог не потерян»:
сработала обратная половина, заведённая специально. Пара выключателей ушла из `KNOWN_COLLAPSED`,
историческая справка оставлена рядом.

### Порядок дальше

1. **Уровень 4 `EV22` #130** — не снимался ни разу за пять заходов. ⚠ До волны.
2. **Заход 6, `K14` #35** — полная переиндексация, окно недоступности; `K16` #48 тем же окном.
3. Послевыкаточная правка `K15-2` поедет следующим релизом.

⚠ Критический путь от нас не зависит: **письмо эксперту → волна → `#13` → `1.0`**.

---

## _(история дня)_ Состояние — 27.08.2026, поздний вечер · тир 1 выполнен

**На бою `v0.5.0-test20`** (`prod` = `main` = `c34bcab`). **`dev` впереди на 5 коммитов —
правки тира 1 НЕ выкачены.** **904 теста**, открытых issues **30**.

### Заведены три недостающие задачи

Три долга жили только в документах и потому пять заходов подряд выпадали из планирования:
`EV22` #130 (уровень 4), `P3-1` #131 (остаток гейта маршрута), `K15-3` #132 (механизм смыслов).
⚠ Плюс метка `ждёт-эксперта` на `#69` — задача требовала эксперта в теле, но выпадала из фильтра.

### Тир 1: две задачи закрыты, у двух записан предел

| Задача | Итог |
|---|---|
| `K2-2` #128 | **Постановка была неверна.** Вводная прим. 27 называет ОДНУ позицию, подблоки делят ЕЁ по току. Настоящий дефект: порог получала ВТОРАЯ позиция, которую примечание не называет («2000 А» нет в примечаниях ни разу). Вето по номиналу, **радиус 1/0/0** |
| `K15-3` #132 ✅ | Сужение **отвергнуто замером**: 0 расхождений с текущим на 39 вопросах. Заведён свип `eval_conclusion_gate` (офлайн, 0 ₽) — то, чего не хватало все пять раундов |
| `K15-2` #129 | Вариант **отвергнут числом**: 0 пропущенных против **5 ложных срабатываний из 5** на верных ответах. Предел записан и виден |
| `K12-1` #122 ✅ | Два источника; второй — **пример в правиле стиля**, который модель копировала как шаблон |

### ⚠⚠ Три урока дня, купленные дорого

1. **Первый замер радиуса `#128` был пуст** — старый модуль лежал вне репозитория и не нашёл
   корпус, показав «536 появившихся порогов». Теперь в замере стоит контроль на путь к данным.
2. **Длина фрагмента промпта — это ЦЕНА.** Развёрнутый фрагмент темы (300 → 747 знаков) уронил
   кейс #50 до «утверждает несуществующий документ» 2 прогона из 3, **при том что справочник
   оставался в контексте**. Ужат до 414 — метрика вернулась к 1.00 ×4.
3. **Мои «верные» примеры снова оказались добрыми.** Вариант `#129` давал 10/10 на шести моих
   случаях и 5 ложных из 5 на формах, написанных против него. Четвёртый раз за сутки.

### Что едет в `v0.5.0-test21`

Рантаймовая, корпус не тронут. Две правки ПОВЕДЕНИЯ: `#128` меняет числа, которые видит эксперт
(снимает чужой порог с одной позиции), `#122` меняет промпт.
⚠ Платных замеров не требуется, и это ПРОВЕРЕНО: кейсов набора, задевающих выключатели или код
27.12, — **ноль**, значит товарная популяция сдвинуться не может; документная перемерена ×4.

### Порядок дальше

1. **Выкатка `v0.5.0-test21`** — живая проверка ×5.
2. **Уровень 4 `EV22` #130** — в той же сессии на VM: канал рвётся, работу на бою выгодно
   батчить. ⚠ До волны.
3. **Заход 6, `K14` #35** — своя сессия: полная переиндексация, окно недоступности. `K16` #48
   тем же окном.

⚠ Критический путь от нас не зависит: **письмо эксперту → волна → `#13` → `1.0`**. Держит
5 issues, лежит готовым с 24.08.

---

## _(история дня)_ Состояние — 27.08.2026, вечер · ✅ ВЫКАЧЕНА `v0.5.0-test20`

**`prod` = `main` = `c34bcab` = тег.** Вердикт скрипта «✅ выкатка завершена, все проверки
прошли». Выкатка **рантаймовая**: коллекции 1377 / 328 / 12, окна недоступности не было.
**885 тестов**, открытых issues **29** _(заведены `K2-2` #128, `K15-2` #129)_.

### Что уехало

Исправления по ревью **захода 5** (PR #112, 6 находок) и **захода 5.5** (PR #116, 6 находок)
плюс **пять раундов ревью самого пакета** — **30 находок суммарно**. Две новые общие проверки
на дефекты `test19` (контакторы, мочеприемники) — у него релизного каталога проверок не было
вовсе.

### Живая проверка ПОСЛЕ выкатки — ×5, четыре вопроса, 20 прогонов

| Вопрос | Гард | Итог |
|---|---|---|
| Какие документы, чтобы внести в реестр | 0/5 | акт экспертизы + СТ-1 названы |
| Нужна ли копия устава | 0/5 | реальный документ **не отрицается** |
| Контроль: нужно ли заключение ТПП | 0/5 | термин назван + «не существует» |
| Правка раунда 5: заключение ТПП **по договору поставки** | 0/5 | справочник доехал, документ отрицается |

Незаземлённых чисел **0** во всех двадцати.

⚠⚠ **ПЕРВАЯ РЕДАКЦИЯ ЖИВОЙ ПРОВЕРКИ МЕРИЛА НЕ ТО.** Она брала `str(a)` — repr ВСЕГО объекта
`Answer` вместе с `cases=[...]` и контекстом (75 000 знаков вместо 1 400) — и «нашла» два
фантома в тексте КЕЙСОВ ЭКСПЕРТА, а не в ответе. Верные поля: `a.text` — то, что видит
пользователь, `a.phantom_documents` — вывод самого рантайм-гарда, то есть то, что уходит в
админ-журнал. **Инструмент проверки — сам объект проверки; это третий такой случай за день.**

### ⚠ Что выкатка сказала о себе

* **Гейт CI на VM честно молчит** — «репозитория рядом нет (пакет распакован без .git)».
  Это и есть исправленное поведение: до правки он молчал НЕЗАМЕТНО. По-настоящему коммит
  проверяет **локальный** сухой прогон — он и дал «CI на `c34bcab`: зелёный».
* **Инструментарий выкатки едет ОТДЕЛЬНО от дельты.** `checks/common` в дельте содержит только
  ДВА новых файла из восьми: запусти скрипт из распакованной дельты — прошло бы 2 проверки
  вместо 8. Раскладка `~/deployNN` (плоско: `deploy.sh` + `checks/` + `releases/` + тарбол)
  это и решает — сверено с `~/deploy19` ДО запуска.
* **Канал рвался семь раз**, каждый раз проходил со второй–третьей попытки (пауза 150–180 с).
  Сама выкатка не пострадала — шла `setsid nohup`.

### Долги, заведённые этой выкаткой

| Что | Issue |
|---|---|
| `K2-2` выключатели: ведущий «Порог:» из чужого блока прим. 27 | [#128](https://github.com/pan3v1n/navigator-719/issues/128) |
| `K15-2` сильное отрицание про другое подлежащее через границу предложения | [#129](https://github.com/pan3v1n/navigator-719/issues/129) |
| Пересмотр механизма различения смыслов «заключения» — регулярки на пределе | — |

---

## _(история дня)_ Состояние — 27.08.2026, до выкатки

**На бою `v0.5.0-test19`** (`prod` = `main` = `452057d`). **`dev` = `8d65605`**, PR
[#127](https://github.com/pan3v1n/navigator-719/pull/127) открыт, **875 тестов**, открытых
issues 27.

### ✅ Заход 5.5 отревьюирован — последний из трёх непроверенных пакетов закрыт

**Шесть находок PR #116, все исправлены.** Заходы 3, 5 и 5.5 больше не числятся непроверяемыми.

⚠⚠ **Главная — про сам предохранитель выкатки.** Гейт CI в сухом прогоне был **недостижим** в
том сценарии, который предписывает usage самого скрипта («ЗАПУСК на VM, из каталога пакета»):
ветка «пакет рядом» ставит `MARKER_ROOTS`, но не `SRC`, а вызов стоял под `if [ -n "$SRC" ]`
без `else`. Гейт не звался и **не печатал ни строки** — лог выглядел полным. Заведён он был
после `O3` #104 (красный CI шесть прогонов подряд, и в этой красноте PR #103 уехал в `main`, а
`v0.5.0-test13` — на бой), то есть **три выкатки подряд шли без него**, если запускались с VM.
Воспроизведено прогоном скрипта из `HEAD`: 0 строк про CI.

⚠ **Гейт БЛОКА и гейт СОДЕРЖИМОГО разошлись в ТРЕТИЙ раз** (`dbfc622` → `K15-1` #120 → эта
находка): разъяснение решалось голой регуляркой, не знающей «заключение как ДЕЙСТВИЕ», и на
вопросе «какие документы готовить при заключении договора поставки» термин уезжал пользователю,
спросившему про договор. Исключение теперь в одном месте.

⚠ **13 тестов предохранителей выкатки не выполнялись** при прямом запуске файла — `unittest.main()`
стоял посреди `test_deploy_script.py`. Замерено: 32 теста против 45.

### ⚠⚠ Что нашлось ПРОВЕРКОЙ, а не чтением — три случая за одну сессию

1. **Пример из отчёта ревью не воспроизводился.** «При **отсутствии** продукции…» — регулярка
   искала основу `отсутству`, формы «отсутств**ии**» не покрывающую: гард срабатывал и до
   правки. Тест на этом тексте был бы **фиктивным контролем**. Формы подобраны прогоном модуля
   из `HEAD`. **Отчёт ревью — гипотеза, а не факт; воспроизводить обязательно.**
2. **Первая редакция моей же правки дала ложное срабатывание** на верном ответе в два
   предложения («…нужно ли заключение ТПП. Такого документа не существует.»). Поймано сверкой
   вердиктов старого и нового кода на **65 абзацах** документации — отсюда два тира отрицаний,
   а не сплошное сужение.
3. **Мой тест на BuildKit бил по верному коду** — запрещал строку, которой выглядит законная
   ветка «`$DOCKER` без sudo».

⚠ **Побочно: профиль релиза читают ДВА парсера, и они расходились молча.** Анти-маркер с
кавычками внутри bash читал верно, а текстовый парсер тестов резал по первой кавычке и
**пропускал** запись — 17 из 18. Предупреждение об этом лежит в профиле `test19` с 26.08;
теперь оно проверяется тестом.

### Замеры

* **Документная популяция ×3** — распределение базы `test18` воспроизведено **точно**
  (`{0.75, 0.75, 1.00}` и `{1.00, 0.67, 0.67}`), регрессий нет. Вся нестабильность на кейсе
  #47, класс `EV20` #117. Отчёт — [eval_runs/2026-08-27_test20_docs.md](eval_runs/2026-08-27_test20_docs.md).
* **Радиус** по 213 вопросам: узкий гейт 0, разъяснение 0. ⚠ Ноль означает, что класс
  «заключение как действие» приёмочными наборами **не покрыт** — дефект латентный.

### ⚠⚠ Новый долг: тот же дефект жив на выключателях

Корпусная половина новой общей проверки нашла **третью склейку**: «Выключатель автоматический
… до 2000 А» и «… до 6300 А» отдают **побайтно одну строку**, ведущую ЧУЖИМ блоком прим. 27
(147/187 баллов «до 4000 А» вместо 105/167 «от 4000 до 6300 А»). Это дефект того же класса,
что контакторы, — **в числах, которые видит эксперт, на бою**.

⚠ **Не регрессия:** прогон модуля из `2af38a3~1` даёт то же самое. Правка `test19` исключила
числа из сопоставления намеренно («идентичность несут слова»), а эти два блока различаются
**только числами** — имя позиции несёт границу диапазона, и сопоставлять надо числом. У задачи
свой радиус, отдельным заходом. ~~Держится явно в `KNOWN_COLLAPSED`, чтобы не забылась.~~
_(поправка 28.08.2026, ревью `b890f69`: долг закрыт `K2-2` #128 и выкачен в `v0.5.0-test21`,
пара убрана из `KNOWN_COLLAPSED` — запись выше с этого момента говорила о корпусе неправду.
Историю не переписываю, но и врущую строку не оставляю: по правилу самого коммита `K2-2`,
«запись о склейке, которая распалась, начинает врать про корпус».)_


### ⚠⚠ РЕВЬЮ САМОГО ПАКЕТА (PR #127) — ЕЩЁ СЕМЬ НАХОДОК, И ПЕРВАЯ ЛОМАЛА ТО, ЧТО ЧИНИЛА

Правило «пакет правок по ревью — сам объект ревью» подтверждено **в пятый раз**.

⚠⚠ **ВЫСОКАЯ.** `_denial_scope` не находил границу предложения слева от термина, когда она —
**ТОЧКА**. `finditer(text, lo, start)` обрезает строку по `start`, а альтернативе с точкой
нужен lookahead **на символ в позиции `start`** — первую букву термина. Работали только «;» и
перевод строки, у которых lookahead нет. **То есть правка чинила самый частый разделитель хуже
всего — а мой тест брал пример с «;» и был ЗЕЛЁНЫМ на сломанном коде.** Тест теперь несёт три
формы, две через точку.

⚠⚠ **Регрессия покрытия на кластере жалоб №1, которую замер увидеть не мог.** Исключение
«заключение как ДЕЙСТВИЕ» применялось ко ВСЕМУ вопросу, и «нужно ли **заключение ТПП** при
**заключении СПИК**» теряло разъяснение — а СПИК и госконтракт это самая частая рамка, в
которой про «заключение ТПП» и спрашивают. Радиус по 213 вопросам показал **ноль**: класс
смешанных вопросов наборами **не покрыт вовсе**. Смысл «документ» теперь побеждает смысл
«действие».

⚠⚠ **Фолбэк гейта CI брал `$APP` по одному наличию `.git`** — а на VM это ПРЕДЫДУЩИЙ релиз,
и нового тега туда не приносит ничто (доставка тарболом). Гейт доложил бы «CI зелёный» про
коммит, который НЕ едет. Теперь `$APP` принимается, только если тег там резолвится.

⚠ **У корпусной половины новой проверки не было положительного контроля** — на пустом корпусе
печатала «OK: склеек 0» с кодом возврата 0. Воспроизведено. Заведены полы (1141 / 16).

Ещё три: `sudo -n env` может быть запрещён command-restricted sudoers (фолбэк сборки) ·
ослабленное мной утверждение теста · гард `exclude_fragments` до пропуска по статусу.

⚠ **Замер ×3 не устарел:** контекст всех четырёх документных кейсов побайтно совпадает с
коммитом прогона, а оракул замера рантайм-гард не зовёт (условие проверки, класс `O3` #104).

**878 тестов.**

### Порядок дальше

1. **Merge PR #127 → тег `v0.5.0-test20` → выкатка.** Рантаймовая, коллекции 1377 / 328 / 12.
   ⚠ Живая проверка **×5**, не ×1.
3. **Заход 6, `K14`** #35 — кластер жалоб №2, машинерия документных кейсов готова.
4. **Уровень 4 на VM** — не снимался ни разу за пять заходов. ⚠ До волны.

⚠ **Гейт CI проверяется только на машине разработчика:** на VM нет ни `.git`, ни `gh`, поэтому
сухой прогон там честно скажет «CI НЕ ПРОВЕРЕН». Прогонять сухой прогон **локально** — там он
верифицирует коммит по-настоящему.

---

## _(история)_ Состояние — 26.08.2026, конец дня

**На бою `v0.5.0-test19`** (`prod` = `main` = `452057d`) — заход 3 отработан и подтверждён живой
проверкой. **`dev` = `45e2ef3`, впереди на два коммита: исправления по ревью захода 5 НЕ ВЫКАЧЕНЫ.**
**859 тестов**, открытых issues 27.

### ⚠⚠ ЧЕМ НАЧАТЬ СЛЕДУЮЩУЮ СЕССИЮ

```
/code-review high 116        — заход 5.5, последний непроверенный пакет (330 строк кода)
```

Это последний из трёх заходов, месяц числившихся непроверяемыми. Два уже сделаны и дали
**12 находок**, из них две были НА БОЮ и в числах, которые видит эксперт.

⚠ `ultra` пока не запускается: приватный репозиторий не добавлен в **Installed GitHub Apps**
(`github.com/settings/installations` → Claude → Configure → Repository access). «Authorized OAuth
Apps» — не то место. За владельцем.

### Порядок после ревью 116

1. **Выкатка исправлений захода 5** — `v0.5.0-test20`. Рантаймовая; корпус не тронут.
   ⚠ Правка промпта (правило 3б) — документную популяцию перемерить ×3, она уже 1.00 ×1.
2. **Заход 6, `K14`** #35 — СТ-1 и ТН ВЭД, кластер жалоб №2 (29 упоминаний). Документы на руках,
   машинерия документных кейсов готова.
3. **Уровень 4 на VM** — единственный уровень, не снятый ни разу за пять заходов. ⚠ До волны, не
   после: волна это нагрузочное событие, а p50 и поведение при 10/25/50 параллельных неизвестны.

### Долги

| Что | Issue | Кто |
|---|---|---|
| `K12-1` фрагмент темы уже самой темы | #122 | нас, P2 |
| `EV20` устойчивость правила про раздел | #117 | нас, P2. Есть первый ВОСПРОИЗВОДИМЫЙ кейс — #45 (XXIX), падает 3 из 3 |
| Остаток `P3`: 11 вопросов | — | нас. ⚠ Проверено: отвечают ЧЕСТНО («нет уверенного совпадения»), а не неверно. Стоит полезности, не корректности |
| `EV11` #92 · `O4` #107 | | нас, фон |
| Письмо эксперту — держит 5 из 27 issues, `R29-4` #53 P0 по риску | — | **владельца** |

⚠ **Подключение `documents_table`** — решение владельца: перечень в ответе сейчас воспроизводит
МОДЕЛЬ, детерминированной таблицы нет. Правка поведения со своим радиусом.

---

## _(история дня)_ Состояние — 26.08.2026 · ✅ ВЫКАЧЕНА `v0.5.0-test19` — ревью захода 3 отработано

**`prod` = `main` = `452057d` = тег.** Вердикт «все проверки прошли», маркеры 7 + 0 анти.
Выкатка **рантаймовая**, окна недоступности не было. **851 тест.**

### Первое ревью захода 3 — того самого, что месяц числился непроверяемым

Запись «`/code-review` — команда пользовательская» оказалась **неверной**: агент запускает её сам,
форком в фоне. Из-за этой записи три пакета на бою числились непроверяемыми не «руки не дошли», а
«нельзя». Исправлено.

**7 находок: 6 исправлено, 1 отклонена.**

⚠⚠ **Главная была НА БОЮ, в числах для эксперта.** «Контакторы и пускатели электромагнитные
низковольтные» получали главной строкой график **вакуумных контакторов** со ступенью 51 балл, к
ним не относящейся; собственные 41 балл — в сноске. Обе позиции возвращали **побайтно одну
строку**: `blocks[0]` был захардкожен, и ответ не зависел от вопроса.

⚠⚠ **Порог, не доезжавший до эксперта.** У «Мочеприемников» в прим. 81 есть строка, названная
ДОСЛОВНО так же, с порогом 135 баллов, — рантайм отдавал `None`. Тай-брейк требовал пересечения
≥2 слов, а однословное наименование даёт максимум 1: **непроходимо по построению**. Прятался
месяц, потому что классификатор его тоже не видел (`covering = covering or row`), а
`attachment_defects` гейтится порогом 0 — гейт показывал ноль при живом дефекте.

⚠ **Замер радиуса нашёл дефект в моей же правке:** совпадение токена «6300» вытаскивало более
узкий диапазон. Числа исключены — идентичность несут слова, числа в квалификаторе это границы.

⚠ **Одна находка отклонена.** `case_fits_scope`: поведение при пустом окне закреплено тестом с
обоснованием. Правка написана и **откачена** — менять осознанное решение по находке ревью нельзя.

**Радиус правок разбора:** 1374 позиции, **+1 порог, потеряно 0**. Покрытие 50.8 % → 50.7 %.
Кейсов набора на изменённых позициях **ноль** — платный перезамер не нужен, проверено командой.

### ⚠⚠ Что выкатка сказала о себе самой

Сухой прогон поймал **две мои ошибки в профиле**, и вторая важнее: анти-маркер был наведён на
файл, где проверяемой строки **никогда не было** — он проходил только потому, что не мог
сработать. А навести на верный файл нельзя: там строка теперь в комментарии, объясняющем правку,
и анти-маркер ударил бы по верному коду. **Нет анти-маркера лучше, чем ложный или пустой.**

⚠ Канал рвался и до, и после выкатки; сама выкатка не пострадала — шла `setsid nohup`.

---

## _(история дня)_ Состояние — 26.08.2026 · ✅ ВЫКАЧЕНА `v0.5.0-test18`

**`prod` = `main` = `a8d6c21` = тег.** Вердикт скрипта «все проверки прошли». Выкатка
**рантаймовая**: коллекции те же 1377 / 328 / 12, окна недоступности не было.

**Живая проверка на бою — ПОВТОРНАЯ.** Ключевой вопрос («нужно ли заключение ТПП для внесения
продукции в реестр») прогнан **5 раз**: фантомных документов 0, незаземлённых чисел 0, и каждый
ответ прямо говорит, что такого документа не существует. **До правки тот же вопрос УТВЕРЖДАЛ
несуществующий документ 3 прогона из 3.** Контроли ×2: загранпаспорт отказан, выписка идёт
процедурной веткой, этикетировщики остались товарными с актом экспертизы.

⚠ Скрипт выкатки сам назвал пробел — «каталога проверок (релизные) нет». Заведена
`checks/v0.5.0-test18/10_routing_and_reference.py`: держит ОБЕ стороны (5 переехавших + 4
контрольных + гард), офлайновая, прогнана на бою с кодом возврата 0.

⚠ Канал до VM рвался ЧЕТЫРЕ раза за выкатку — каждый раз проходил со второй попытки по рецепту
(пауза ~200 с, одно соединение на файл). ⚠ Предохранитель остановил первый запуск: в пакете не
было `SHA256SUMS.txt`. Сумму считать ЛОКАЛЬНО и передавать отдельно — иначе проверка
сертифицирует сама себя.

---

## _(история дня)_ ЗАКРЫТЫ `EV21`, `K15-1` и `P3`; заведена `K12-1` #122

**На бою `v0.5.0-test17`** (`prod` = `main` = `d02702f`), правки дня **НЕ выкачены**.
**816 тестов** зелёные в документированной среде. Открытых issues **27** _(закрыты `#110`, `#111`,
`#113`, `#114` — были сделаны и на бою с 25.08, но не закрыты на доске; закрыты `#119` и `#120`;
заведена `#121`)_.

| Что | Итог |
|---|---|
| `EV21` #119 ✅ | Кейсы 46 → **50**, четыре документных, покрывают обе ветки. Оракул — общий модуль `scripts/eval_documents.py`. Срез `--kind` в трёх скриптах |
| Товарная популяция | Не сдвинулась: `--kind product` воспроизводит базу **точно** (4934 / 3852, хвост 0.33) |
| Отчёт | [eval_runs/2026-08-26_ev21.md](eval_runs/2026-08-26_ev21.md). База сравнения **не обновлялась** — не релизный тег |

## ✅ `K15-1` #120 сделана — и вскрыла `P3` #121, которая крупнее

Чинилась **асимметрия**, а не число: три соседние строки списка маркеров задавали разные правила
разрыва. Плюс согласованы гейт БЛОКА справочника и гейт его СОДЕРЖИМОГО. Радиус замерен новым
свипом `scripts/eval_routing.py`: **+3 / −0 / ложных 0**, два из трёх — из набора `K9`.
Живая проверка ×3: «несуществующий документ не утверждён» и «разъяснение по условию»
**0.75 ×3 → 1.00 ×3**, кейс #50 проходит во всех трёх. Тестов **807**.

## ⚠⚠ Что следующее — и почему именно в таком порядке

### ✅ `P3` #121 СДЕЛАНА 26.08 — пропуски 17 → 11, утечек 0

Решение выбрано **замером**: сигнал темы победил расширение словаря и «якорь без товарного
сигнала» (тот давал лишнее ложное срабатывание). ⚠⚠ Две регрессии чуть не уехали, и обе прятал
мой же свип: смешанный вопрос уходил с товарной ветки, вне-719 вопрос уходил на процедурную.
Оба случая теперь считаются свипом отдельными строками. Попутно: **ТПП не было в списке
719-якорей** — список называл ГИСП и Минпромторг.

⚠ **Критерий «ноль пропусков», записанный мной же в постановку, оказался недостижим выбранным
классом средств** — оставшиеся 11 вопросов утвердительные, без глагола и без темы. Структурный
ответ другой: добор процедурной ветки при НИЗКОЙ релевантности товарной выдачи — правка
`_plan_answer`, со своей латентностью и своим радиусом. Отдельной задачей.

### 1. Полный прогон уровней 1–2 на 50 кейсах, три прогона · платный

⚠ **Идёт ПОСЛЕ правок намеренно.** `EV21` поведения продукта не меняет — только код замера плюс
no-op выделение `plan_procedural`. Поведенческие правки в пакете — #120 и #121, и ОДИН оплаченный
прогон покрывает всё. Прогони мы его между ними — платить пришлось бы дважды.

Популяции разводить: `--kind product` (42, сравнимо с базой) и `--kind documents` (4, новое).

### 2. Заход 6 — `K14` #35 (СТ-1 и ТН ВЭД, 29 упоминаний)

✅ **Вопрос «принимать вручную или нет» снят.** Он и был причиной паузы перед заходом 6; машинерия
документных кейсов готова и переиспользуема. ⚠ Но #120 добавил к `K14` требование: у `K14` тема
`st1_origin` в роутере уже есть, а #120 показал, что **тема сама по себе ничего не гарантирует,
если вопрос не попал на ветку, где тема применяется**. Проверять оба уровня сразу.

## Прочие долги

| Что | Issue | Кто |
|---|---|---|
| Устойчивость правила про раздел (0.90 / 1.00 / 0.95) | `EV20` #117 | нас, P2. ⚠ Тот же класс виден и на документных: «источник назван» 0.67 / 1.00 / 0.67 |
| Уровень 4: latency и нагрузка — не снимался ни разу | — | нас, нужен VM |
| 25 скриптов на cp1251 | `O4` #107 | нас, попутно |
| Письмо эксперту — держит **5 из 27** issues, `R29-4` #53 P0 по риску | — | **владельца** |
| `/code-review` по заходам 3, 5, 5.5 | — | **владельца** |

## ⚠⚠ Что 26.08 сказало о замерах вообще

* **Запрет «заключения ТПП» три месяца считался там, где термин не мог появиться** — по 42
  товарным вопросам. «1.00 ×3» означало не «не выдумывается», а «неоткуда взять».
* **Четыре дефекта нашлись в самих замерах**, и все — первым же живым прогоном при 794 зелёных
  тестах: порог ссылок `[N]` по пустому `hits` процедурного ответа · распознаватель источника,
  не знавший сокращения «Методрекомендации» · `TypeError` на `expected_section: null` · «0.00»
  вместо «случаев нет».
* **Промпт документного ответа — медиана 14 102 против 4206 у товарного.** Бюджет контекста `K6`
  мерился только на товарной оси.

---

## _(история)_ Состояние — 25.08.2026 (итог) · НА БОЮ `v0.5.0-test17`, ТРИ ВЫКАТКИ ЗА ДЕНЬ

**`prod` = `main` = `d02702f` = тег.** `dev` = `ab91e3b`, **767 тестов**, CI зелёный,
дерево чистое. Коллекции 1377 / **328** / 12. Открытых issues 31.

| Релиз | Что | Цена |
|---|---|---|
| `v0.5.0-test15` | Заход 5: `K13` + `K15`, корпус 311 → 328 | ПОЛНАЯ переиндексация, 16 мин пустой процедурной ветки |
| `v0.5.0-test16` | Заход 5.5 | рантаймовая |
| `v0.5.0-test17` | `M2` #115 + `EV17` #109 | рантаймовая |

**База сравнения — `v0.5.0-test17`, впервые пять уровней и впервые на ВЫКАЧЕННОМ коммите.**
Регрессий нет. Отчёт — [eval_runs/2026-08-25_test17_full.md](eval_runs/2026-08-25_test17_full.md).

## ~~⚠⚠ Что решить ПЕРЕД заходом 6~~ · ✅ РЕШЕНО 26.08.2026

> **Решение принято и исполнено: `EV21` закрыта до старта захода 6.** Оценка «~2 ч» оказалась
> занижена (ушёл день), и не зря: первый же прогон нового набора нашёл `K15-1` #120 и четыре
> дефекта в самих замерах. См. состояние 26.08 выше.

**`EV21` #119: в golden set ноль кейсов с темой `documents`** — кластер жалоб №1 (31 упоминание)
замером не покрыт вовсе. `K12`, `K15`, `EV18` проверялись только адресными живыми прогонами,
руками; оба дефекта `K15` так и нашлись — после выкатки.

`K14` — тоже документная тема. Без кейсов в наборе её придётся принимать тем же ручным способом.
**Вариант:** закрыть `EV21` (~2 ч) до старта захода 6, тогда `K14` мерится штатно.

## Прочие долги

| Что | Issue | Кто |
|---|---|---|
| Устойчивость правила про раздел (0.90 / 1.00 / 0.95) | `EV20` #117 | нас, P2 |
| Уровень 4: latency и нагрузка — не снимался ни разу | — | нас, нужен VM |
| 25 скриптов на cp1251 | `O4` #107 | нас, попутно |
| Письмо эксперту — держит **5 из 31** issues, `R29-4` #53 P0 по риску | — | **владельца** |
| `/code-review` по заходам 3, 5, 5.5 | — | **владельца** |

⚠ **Канал до VM 25.08 рвался шесть раз подряд** на рукопожатии SSH; пауза 400 с не помогала.
TCP до 22 и 80 при этом открывается — рвётся handshake (DPI). Работает: несколько попыток с
паузой ~200 с. **Выкатку запускать только `setsid nohup`** — дважды это её и спасло.
### Дальше — заход 6, `K14` (СТ-1 и ТН ВЭД, 29 упоминаний). Документы на руках.

## _(история)_ Состояние — 25.08.2026 · ЗАХОД 5 ЗАМЕРЕН, РЕШЕНИЕ — ВЫКАТЫВАТЬ

**Замер пройден целиком (уровни 0–3), ни одна метрика с порогом не просела.** Отчёт —
[eval_runs/2026-08-25_zahod5.md](eval_runs/2026-08-25_zahod5.md). Рантайм под замером — `a3a271e`.
Выкатка подготовлена и проверена локально: профиль, шаг полной переиндексации, релизная
проверка. Порядок — ниже. **726 тестов.**


На VM по-прежнему `v0.5.0-test14` (`d119e58`). `dev` впереди на четыре коммита.
**722 теста, батарея зелёная в документированной среде** (без `PYTHONUTF8`).

| Задача | Итог |
|---|---|
| `EV14` #98 | База сравнения отставала на релиз — обновлена до `v0.5.0-test14`. Попутно найден дефект схемы: отчёты ссылались на базу в `eval_baseline/`, а каталог ротируется, и ссылка ломалась при каждом релизе (случилось дважды) |
| **`K13` #46** ✅ | Юридическая иерархия источников в промпте. **Рантаймовая** |
| **`K15` #36** ✅ | Обе половины: закрытый справочник документов + Методрекомендации ТПП в корпусе. **Требует переиндексации** |

## ⚠⚠ ЧТО ОБЯЗАНА ЗНАТЬ СЛЕДУЮЩАЯ ВЫКАТКА

1. **`pp719_rules` 311 → 328 точек.** В профиле релиза обновить `EXPECT[pp719_rules]=328`.
2. ⚠⚠ **ПЕРЕИНДЕКСАЦИЯ ПОЛНАЯ, НЕ ЧАСТИЧНАЯ.** `K11` (`--doc`) для НОВОГО документа не годится:
   `avgdl` считается по всему корпусу и **запекается в sparse-вектор каждой точки**. Новый документ
   меняет среднюю длину для всех — в чужом масштабе BM25 оказываются не его точки, а точки
   **соседей**, загруженных раньше. Замерено: **95.84 → 96.72 (+0.92 %)**. Предохранитель уже
   стоит на пути: `--doc` для отсутствующего в коллекции документа останавливается сам.
   В профиле — `REINDEX_DOCS=""` и полная загрузка (`load_rules_kb.py` без `--doc`).
3. **`K13` переиндексации не требует** — значение берётся из манифеста, не из payload. Это было
   осознанное решение: у товарного `pp719` (1377) и кейсов (12) паспорта в payload нет.
4. **Замер уровней 0–3 обязателен**: заход 5 меняет и корпус, и контекст каждого процедурного
   ответа. ⚠ Блок `context_size` базы **не мерялся в заходе 4** и помечен `measured_at: 2045193` —
   этот прогон обязан замерить его заново.

## ⚠⚠ Находка захода 5 — Методрекомендации описывают отменённый шаг

Документ помечен действующим (вер. 1.18.3), но вводная и схема процесса ведут заявителя
за **«заключением Минпромторга»**, а «Правила выдачи заключения о подтверждении производства»
**утратили силу 29.06.2024** (ПП РФ N 894) и заменены Правилами ведения реестра. Сверено по
корпусу: такого шага в действующих Правилах нет вовсе.

⚠ **`legal_force` от этого не спасает: сила решает КОНФЛИКТ НОРМ, а не УСТАРЕВАНИЕ.** Поля
«действует, но частично устарел» в паспорте нет — и заводить его ради одного случая не стали.
Решение владельца: индексировать, исключив устаревшие фрагменты (932 символа из 19 759, 4.7 %).
Механизм — `exclude_fragments` в манифесте, с причиной и датой отмены, **с положительным
контролем**: несовпавшее правило останавливает загрузку.

## Порядок выкатки `v0.5.0-test15` (подготовлен 25.08.2026)

Всё, что можно было подготовить заранее, подготовлено и **проверено локально**:
профиль, релизная проверка, шаг полной переиндексации в скрипте.

```bash
# 1. PR dev -> main, слить, поставить тег на коммит main
git tag v0.5.0-test15 <sha-main> && git push origin v0.5.0-test15

# 2. Сухой прогон — ОБЯЗАТЕЛЕН. Читать КОД ВОЗВРАТА, а не последнюю строку.
cd scripts/deploy
bash deploy.sh --release releases/v0.5.0-test15.env --dry-run

# 3. Выкатка
bash deploy.sh --release releases/v0.5.0-test15.env

# 4. prod перематывается на выкаченный коммит
```

### ⚠⚠ Три вещи, которые обязан знать оператор

1. **Коллекция `pp719_rules` ПЕРЕСОЗДАЁТСЯ.** Всё время прогона e5 процедурная ветка отвечает
   пустотой (локально: `points_count` = 0 сразу после старта, дальше растёт до 328). Товарная
   ветка при этом работает. Момент выкатки выбирать соответственно — обращений на сервере нет
   с 24.07, но это факт, а не гарантия.
2. **`--doc` (`K11`) здесь НЕ ГОДИТСЯ** и загрузчик его не пропустит: документ новый, а `avgdl`
   запекается в вектора соседей. Флаг `--allow-new-doc` существует, но в этой выкатке он **не
   нужен и ставить его нельзя** — он оставит соседей в прежнем масштабе BM25.
3. **Сухой прогон печатает строку «КОРПУС: …»** — она и говорит, что будет с корпусом. Прежняя
   редакция смотрела только на снапшот и на этом профиле соврала бы «корпус не трогаем».

### ⚠⚠ ПОСЛЕ выкатки — живая проверка, и обязательно ПОВТОРНАЯ

Прогнать на бою 3–5 раз вопрос, задевающий то, что менял релиз, и посмотреть не только на
результат, но и на журнал приложения.

**Куплено выкаткой 25.08.2026.** Единственная живая проверка после релиза поймала дефект,
которого не увидели ни 732 юнит-теста, ни замер уровней 0–3: гард срабатывал на ВЕРНЫХ ответах,
а сам ответ нёс лишнюю текстовку пользователю. Частота — **2 раза из 5**: один прогон дал бы
«чисто» с вероятностью 0.6, и правка ушла бы к пользователям незамеченной.

Правило трёх прогонов относится и к живой проверке, не только к замеру. Одного прогона мало —
он отвечает на вопрос «сломано ли всегда», а не «сломано ли иногда».

### Что уже проверено локально на переиндексированной коллекции (328 точек)

| Проверка | Итог |
|---|---|
| Сухой прогон профиля | ✅ 13 маркеров, 0 анти, 6 общих + 1 релизная проверка, код 0 |
| Релизная проверка `checks/v0.5.0-test15/` | ✅ 17 записей Методрекомендаций, сила 4, отменённый шаг вырезан, алгоритм ОКПД на месте, 5 документов целы |
| Шесть общих проверок `checks/common/` | ✅ все |
| Релизная проверка **прошлого** релиза (`test14`) | ✅ инварианты `K8` и `#105` переиндексацией не задеты |

---

## Что дальше

**Замер по `EVAL_GUIDE` (уровни 0–3) → выкатка `v0.5.0-test15` с ПОЛНОЙ переиндексацией
`pp719_rules`.** Затем заход 6: `K14` #35 (СТ-1 и ТН ВЭД, 29 упоминаний) — **оба его документа
уже на руках**: Соглашение СНГ ред. от 08.06.2023 (RTF, 155 853 символа, «Перечень условий» на
месте) и Приказ ТПП №14 от 01.03.2024 в ред. Приказа №59 от 16.07.2026 (выгрузка alta.ru,
210 642 символа). Реквизиты обоих, помеченные в `T12_SOURCES` как неподтверждённые, теперь
подтверждены, причём редакция Приказа №14 **новее**, чем знало ТЗ.

⚠ **Долг, стоящий выше захода 6:** `/code-review` по заходу 3 так и не запускался — это
единственный пакет на бою, не проходивший ревью (`v0.5.0-test12..v0.5.0-test13`, **40 файлов,
3935 строк**). ⚠⚠ Прежде здесь стояло «команда пользовательская, запустить может только владелец» — НЕВЕРНО, и эта запись держала долг месяц. `/code-review` агент запускает сам (проверено 26.08: 8 находок по дельте дня). Пользовательская только `ultra`.

⏳ **Владельцу:** отправить письмо эксперту (`docs/EXPERT_LETTER_CHECKLIST.md`) — оно держит пять
issues, из них `R29-4` #53 помечена P0 **по риску**.

---

## Состояние на 24.08.2026 _(история)_

На VM тег `v0.5.0-test14` (`d119e58`), `prod` = `main` = тег, вердикт скрипта —
«все проверки прошли». Переиндексация впервые шла средствами `K11` по документам,
прямо на бою: коллекция держалась 311 точек, процедурная ветка не пустела.
Замер — [eval_runs/2026-08-24_zahod4.md](eval_runs/2026-08-24_zahod4.md).

**Дальше — заход 5:** `K15` #36 + `K13` #46. ⏳ Владельцу: письмо эксперту.

---

### Что было сделано за сессию

**`dev` = `5d89a43`, 664 теста, зелено В ДОКУМЕНТИРОВАННОЙ СРЕДЕ** (без `PYTHONUTF8`).
**ЗАХОД 4 ЗАКРЫТ ЦЕЛИКОМ.**
На VM по-прежнему `v0.5.0-test13` (`2045193`): **правки дня НЕ выкачены.**

| Сделано | Итог |
|---|---|
| `O3` #104 | CI был красным **шесть прогонов**, и в этой красноте PR #103 уехал в `main`, а релиз на бой. Тест `EV16` перестал быть офлайновым. Починен + гейт CI в `deploy.sh --dry-run` |
| `R29-4` #53 · `D13` #95 | Письмо эксперту собрано и готово к отправке; лист сверки получил блок **Д** и приложение с полным перечнем 300 позиций |
| `K8` #39 | Манифест корпуса; паспорт у всех 311 пунктов; утративший силу документ не индексируется и отсекается фильтром |
| `K8-1` #105 | 13 пунктов раздела IV Правил были подписаны разделом III — исправлено, радиус сверен |
| `A3` #58 | Закрыт как ненужный (обоснование в issue) |
| ревью #104 | `/code-review high` по дельте захода 4: **7 находок, все закрыты**. Первая — что батарея у меня зеленела только с `PYTHONUTF8=1`; в документированной среде было 2 падения и 5 ошибок |
| `O4` #107 | 37 скриптов печатают символы вне cp1251 и упадут на Windows — долг зарегистрирован, список может только сокращаться |

### ⚠ Что знать перед следующим шагом

1. **Следующая выкатка — С ПЕРЕИНДЕКСАЦИЕЙ `pp719_rules`.** `K8` и #105 меняют payload: без
   переиндексации ни паспорт, ни исправленная привязка раздела до ответа не доедут.
   Товарный корпус (`pp719`) не тронут — снапшот/переиндексация ему не нужны.
2. **Гейт CI в сухом прогоне уже работает** и остановит выкатку на красной батарее.
   Обход — `SKIP_CI_GATE=1`, но это осознанный риск, а не «зелено».
3. **Ревью 24.08 шло по дельте `main...dev`** — то есть проверен заход 4. Заход 3 остаётся
   непроверенным на бою.
4. ⚠ **Батарею гонять КОМАНДОЙ ИЗ `CLAUDE.md`, без своих переменных окружения.** С `PYTHONUTF8=1`
   она зеленеет там, где документированная среда падает (issue #107).
5. **Владельцу — две вещи:** отправить письмо эксперту (`docs/EXPERT_LETTER_CHECKLIST.md`,
   заполнить контакт и срок) и включить branch protection на GitHub: гейт закрывает путь
   «красный CI → бой», но не «красный CI → `main`».

## Что дальше

**Замер по `EVAL_GUIDE` (уровни 0–3) и выкатка с переиндексацией `pp719_rules`.** Заход 4 закрыт,
дальше по плану — заход 5 (`K15` #36 справочник документов + `K13` #46 иерархия источников,
самый частый кластер жалоб — 31 упоминание). `K13` разблокирована: `legal_force` теперь есть в
паспорте каждой записи всех трёх корпусов.

⚠ Переиндексация `pp719` (товарный, 1377) **не срочная**: паспорт там аддитивный и пока никем не
читается, фильтра по нему на товарном пути нет. Обязательной станет к `K13`/`K14`.
⚠ `K11` делает такую переиндексацию частичной: `--doc <doc_type>` грузит один документ, не трогая
остальные.

---

## Состояние на 21.08.2026 _(история)_

✅ **ВЫКАЧЕНА `v0.5.0-test13` (21.08.2026): VM = `prod` = `main` = `2045193`.**
Коллекции **`pp719` 1377** green · `pp719_rules` 311 · `verified_cases` 12 — выкатка
РАНТАЙМОВАЯ, корпус и индексатор не тронуты. **611 тестов.** `dev` = `27553d0`.

**Заход 3 закрыт целиком — шесть задач плюс три долга гигиены:**

| Задача | Итог |
|---|---|
| `EV14` #98 · `EV15` #99 · `O2` #100 | Гигиена замера: база сравнения, предусловие «кейсы выключены» (решено замером), эталонный скрипт выкатки |
| `A1` #56 | Полоса «дефектов разбора порогов» 16.6–31.6 % → **0.0 %**, классификация с уликой |
| `K2` #47 | Четыре формы примечаний из шести: **+28 порогов, 35 верных ссылок, потеряно 0**; покрытие 53.1 % → 50.8 % |
| `K3` #42 | **Измерена и ОТВЕРГНУТА** — не проходит собственный критерий |
| `K6` #43 | Контекст **6 890 → 5 034 (−27 %)**, хвост кандидатов один блок |
| `EV16` #102 | Кейс о чужой продукции отсечён вторым сигналом |
| `EV10` #91 | Метрика детерминизма разделена; порог ≥0.95 вернулся на решающие числа |

**Замер перед выкаткой** — [eval_runs/2026-08-21_zahod3.md](eval_runs/2026-08-21_zahod3.md),
он же **первая полная база** ([eval_baseline/](eval_baseline/README.md)): прогон и релиз на одном
коммите, кейсы выключены. recall@1 **0.943 ± 0.000** · невидимок 0 · кросс-секционных 9 ·
faithfulness **1.00 ×3** (433/439/426 чисел, 0 выдуманных) · отказ 4/4 ×3 · порог 19/19 ·
баллы 134/134 · все восемь запретов §2.2 = 1.00 ×3 · решающие числа **0.98 ± 0.03** (порог ≥0.95)
· гейт «без чужих чисел» **1.00** (0 из 50 при 210 доступных).

⚠ **`/code-review` по PR #103 НЕ запускался** — это пользовательская команда. По уроку проекта
пакет правок сам является объектом ревью, а этот пакет большой (шесть задач). Стоит прогнать.

## Что дальше — заход 4

`K8` #39 паспорт записи + манифест корпуса (+ `A3` #58) → `K11` #41 инкрементальная
переиндексация. Оба разблокируют заходы 5–6 (`K15`, `K13`, `K14`, `K16`).

⚠ Долг, который стоит закрыть раньше: **письмо эксперту** — `R29-4` #53 помечена P0 по риску,
лист сверки пересобран 20.08. Плюс уточнённый список к #95 (215 позиций «порога нет в
первоисточнике» + 85 «модель не балльная»), каждая с уликой.

---

## ⚠⚠ ЧТО ЗНАТЬ ПЕРЕД СЛЕДУЮЩИМ ШАГОМ

**1. Приёмочное число `K2` отозвано — критерий НЕ выполнен.** «24.6 % → 18.4 %» воспроизвести
нечем: кода, считающего долю, в репозитории не было ни одного, ни один тест её не закреплял.
Заведён `scripts/eval_threshold_coverage.py` (в уровне 0 гайда). Одна метрика на обеих ветках:
**58.8 % → 53.1 %** по срезу `points`+`mixed`. Улучшение реально и близко к заявленной дельте
(−6.4 против −6.2 п.п.), но **≤22 % не достигнуто** — остаток разбирать в заходе 3 или выносить
эксперту (там же #95: раздел III, 108 позиций без порога в первоисточнике).

**2. Второй круг ревью ОБОРВАЛСЯ по лимиту сессии — отчитался 1 угол из 10.** Восемь находок
отработаны, но пакет проверен ЧАСТИЧНО. Если что-то всплывёт — искать в `thresholds.py`
(`_split_exception`, `_fmt_flat`), `sync_payloads.py`, `eval_determinism.py`, правиле 2г промпта.

**3. Оба круга ревью нашли дефекты В ПРАВКАХ ПРЕДЫДУЩЕГО круга** (5 и 8, из вторых шесть — в
правках по первому). Урок подтверждён в третий раз: пакет правок по ревью — сам объект ревью.

**4. Детерминизм за сессию: 0.90 → 0.70 → 0.60** при неизменной генерации между первыми двумя.
Историческая полоса 0.60–0.90 — по одиночному прогону судить нельзя. Долг `EV10` #91.

---

## ПЛАН НА СЛЕДУЮЩУЮ СЕССИЮ — заход 3 + долги выкатки 20.08

> Порядок выведен из двух правил, подтверждённых этой сессией: **ревью — до платного замера**
> (находка в данных обнуляет оплаченный уровень 2) и **сначала дешёвое, что разблокирует
> остальное**. Идти сверху вниз.

### 0. ✅ Гигиена замера — _закрыта 21.08.2026_

| # | Что | Зачем | Оценка |
|---|---|---|---|
| ~~0.1~~ | ✅ **`EV13` #97 — СДЕЛАНО 20.08**, выкачено в `v0.5.0-test12` | ⚠ Второй круг ревью 20.08 **оборвался по лимиту сессии: отчитался 1 угол из 10**. Восемь находок отработаны, но пакет проверен ЧАСТИЧНО, и он уже НА БОЮ. Смотреть прежде всего: `thresholds.py` (`_split_exception`, `_fmt_flat`, `_tables`), `sync_payloads.py`, `eval_determinism.py`, правило 2г промпта | ~1 ч |
| ~~0.2~~ | ✅ **`EV14` #98 — СДЕЛАНО 21.08.** `docs/eval_baseline/` заведён: правило обновления по релизным тегам, `baseline.json`, замороженный отчёт. Туда же внесена оговорка про незамеренный тег | — |
| ~~0.3~~ | ✅ **`EV15` #99 — СДЕЛАНО 21.08.** Цена решения замерена (`scripts/eval_cases_influence.py`, 0 ₽): 0 срабатываний на уровнях 0–2 и наборах гейта, 2 из 10 на детерминизме. **Предусловие исполняем, перезамер базы не нужен.** Попутно заведена `EV16` #102 | — |
| ~~3~~ | ✅ **`O2` #100 — СДЕЛАНО 21.08.** `scripts/deploy/deploy.sh` + профиль релиза + `checks/common` + 18 тестов + сухой прогон | — |

⚠ **Гигиена замера закрыта полностью — следующая сессия начинается сразу с захода 3.**

### 1. `K2` #47 — решение принято: метрика РАЗДЕЛЕНА, критерий ≤22 % отменён

⚠⚠ **Решение владельца 20.08.2026.** Прежняя доля складывала два разных явления, и потому не
давала починить ни одно:

| Класс | Что это | Кому |
|---|---|---|
| **Порог ЕСТЬ рядом, привязать не смогли** | наш дефект разбора | нам — это и есть `K2` |
| **Порога НЕТ ни в разделе, ни в примечаниях** | свойство первоисточника | эксперту (#95) |

**Критерий вешается ТОЛЬКО на дефектную половину.** Тот же приём, которым `K2` уже спасли
однажды (разделение общего и закупочного порога), и то же различение, которое `R7` давно провёл
в ПРОДУКТЕ («порог не предусмотрен» против молчания), — метрика его не переняла.

⚠ **Порог назначается ПОСЛЕ классификации.** Замер 20.08 (срез `points`+`mixed`, 880 записей,
непокрыто **467 = 53.1 %** (только пороги в баллах)):

| Детектор | ДЕФЕКТ | НЕ ДЕФЕКТ |
|---|---|---|
| строгий — упоминание порога в тексте раздела | 146 = **16.6 %** | 321 = 36.5 % |
| широкий — плюс код позиции упомянут в примечаниях | 278 = **31.6 %** | 189 = 21.5 % |

**Полоса 16.6 – 31.6 %, прежний ≤22 % лежит внутри неё** — по нему нельзя сказать даже, выполнен ли он.
Назначать порог на неопределённое число значит повторить ошибку, из-за которой отзывали «18.4 %».

⚠ **Раздел III не особенный, исключать его из знаменателя нельзя:** «порога нет» встречается
и в V (13), VI (8), II (4), I (2), а внутри III 21 позиция из 120 — как раз дефект.

**Работа — `A1` #56, поднята в заход 3** (см. ниже): классифицировать каждую непокрытую позицию
С УЛИКОЙ — какое именно примечание или строка должны были нести её порог. Тогда полоса схлопнется
в одно число, и на него назначается критерий `K2`. Вторая половина уходит эксперту вместе с #95
(по широкому детектору ~189 позиций, а не «108 в разделе III»).

### 2. Заход 3 по бэклогу — основная работа

| Задача | Issue | Оценка | Что закрывает |
|---|---|---|---|
| ❌ `K3` контекстный заголовок чанка — _измерено и отвергнуто 21.08_ | #42 | — | Строка «29.10.59.310» без своей группы не самодостаточна; сюда же остаток `K1` — унаследованные требования не попали в поисковый индекс |
| `K6` бюджет контекста вместо капов на хит | #43 | ~0.5 сессии | Половину закрыл `EV7`; остался кап 60 операций, определяющий размер контекста у мега-продуктов |
| **`A1` классификация непокрытых порогов** | **#56** | ~0.5 сессии | **Поднята из «после `K2`».** Разблокирует приёмку `K2` #47: без неё критерий назначать не на что (полоса 15–26 %). Каждая непокрытая позиция получает класс и УЛИКУ — какое примечание/строка должны были нести порог |
| `EV16` кейс о чужой продукции проходит порог релевантности | #102 | ~0.5 сессии | Найдено замером 21.08: «станки с ЧПУ» → кейс про вычислительные машины (0.851 против законных 0.843). Число кейса «заземлено» для faithfulness и «чужое» для гейта — две метрики расходятся. ⚠ Делать ВМЕСТЕ с `EV10`: 2 из 10 запросов детерминизма несут кейс |
| `EV10` разделить метрику детерминизма | #91 | ~0.5 сессии | ⚠ **Делать ПОСЛЕ `K6`:** предполагаемое лекарство от разброса — бюджет контекста, а не формулировки. Считать отдельно стабильность РЕШАЮЩИХ чисел (порог, баллы), а не полноту упоминаний в прозе |

⚠ **Про детерминизм — свежая фактура.** За сессию 20.08 метрика дала **0.90 → 0.70 → 0.60**,
причём между первыми двумя генерация вообще не менялась (правился только оракул). Историческая
полоса 0.60–0.90 замерена 16.08 на НЕИЗМЕННОМ коде. Любой вывод по одиночному прогону здесь
недействителен — это и есть содержание `EV10`.

### 3. ✅ `O2` #100 — инфраструктура выкатки — _сделано 21.08.2026_

`scripts/deploy/deploy.sh` — один скрипт на все релизы; различия живут в профиле
`releases/<тег>.env`. Проверки рантайма разделены: `checks/common/` идут на КАЖДОМ релизе
(ради этого всё и делалось — `D12` проверялась на `test11` и исчезла в `test12`),
`checks/<тег>/` — специфика релиза. Предохранители, купленные инцидентами, закреплены
`tests/test_deploy_script.py`. Перед выкаткой обязателен `--dry-run`: он сверяет маркеры
с кодом, который едет, и падает кодом 2, если профиль остановил бы верную выкатку.

⚠ Первая редакция тестов повторила дефект 18.08 в зеркале: искала `backup_db.py --verify` по
всему файлу и нашла его в комментарии, объясняющем, почему так нельзя.

### 4. Замер и выкатка

Уровни 0–3 по `EVAL_GUIDE` (`K3` и `K6` меняют индекс и контекст каждого ответа) →
переиндексация → выкатка. **Приёмка захода** (из бэклога): `eval_coverage` и `eval_paraphrase`
не хуже базы · детерминизм ≥0.95 **либо** обоснованное решение о недостижимости с записью в
`EVAL_GUIDE` · faithfulness и гейт «без чужих чисел» не просели.

⚠⚠ **ПРОГОН ИДЁТ С ВЫКЛЮЧЕННОЙ ПЕТЛЁЙ КЕЙСОВ** (`QDRANT_CASES_COLLECTION=__eval_disabled__`) —
предусловие §1.1 с 21.08 исполняется. Уровни 0–2 сопоставимы с базой (0 срабатываний), три числа
детерминизма — нет.

⚠ **Выкатка — эталонным скриптом из репозитория:** завести профиль
`scripts/deploy/releases/<новый-тег>.env` копией предыдущего, прогнать `--dry-run` (он сверит
маркеры с кодом, который едет), затем `deploy.sh --release …`. Читать КОД ВОЗВРАТА, а не
последнюю строку. Чек-лист [REVIEW_TZ §1а](REVIEW_TZ.md) теперь несёт только то, что скриптом
не выражается.

⚠ **Заход 3 меняет корпус и контекст** → в профиле релиза заполнить `SNAPSHOT` и обновить
счётчики `EXPECT`, иначе проверка коллекций провалится (и правильно сделает).

---

### Что НЕ в этой сессии и почему

* **Заходы 4–7** (`K8`/`K11` → `K15`/`K13` → `K14`/`K16`/`B1` → `EV11`/`K7`/`K17`) — по плану
  идут после захода 3; `K8` разблокирует `K13` и `K14`, поэтому порядок не меняем.
* **Ждут эксперта:** `R29-4` #53 (**P0 по риску**), `R6-5` #69, `R6-4` #52, `D5` #65, `D13` #95.
* **Ждут старта волны:** `EV3` #80, `A4` #55, `A5` #59.
* **Ждут владельца:** файл Методрекомендаций ТПП (вторая половина `K15`), решение по TLS
  (`R11` #37, `R14` #38), и теперь — **решение по критерию `K2`** (см. п. 1).

## Что сделано 19.08.2026

**Три коммита в `dev` поверх слитого заход 1.**

✅ **`D12` #90 — исключённые позиции ушли из корпуса.** ⚠ Диагноз в постановке ОПРОВЕРГНУТ: не
«записи-обрывки, где парсер отдал требованию статус позиции», а **исключённые позиции**, класс
`D4`. Маркер стоит в ячейке КОДА (`14.12.30.160 - Позиции исключены.|до 1 января 2019 г.: …`), и
наименованием стал текст ячейки требований. Написаний ПЯТЬ, 53 вхождения. ⚠ Две более простые
редакции правила отвергнуты замером: «пустая запись + исключённый код» задевала **20** записей,
17 из них — живая продукция с наследованием; вариант с проверкой наследования верен, но зависит от
ГЕНЕРИРУЕМОГО файла. Признак взят из первоисточника. ⚠ Правка нашла четвёртую заглушку:
`reconcile` сел 29/29 → 28/29 и показал `32.50.23.000` — исключённую позицию с именем-заголовком
раздела и чужими требованиями. Итог: **1377 записей, `reconcile` 29/29**.

⚠ **`K2` #47 — пороги из примечаний. ЧИСЛА «24.6 % → 18.4 %» ОТОЗВАНЫ 20.08.2026** (воспроизвести нечем; воспроизводимый замер — **58.8 % → 53.1 %**, критерий ≤22 % НЕ выполнен, см. `eval_runs/2026-08-20_zahod2.md`). ⚠⚠ Главное — не добранные пороги, а
**исправленные неверные**: все они показывались с подлинной ссылкой на примечание, поэтому
faithfulness-гард молчал (он проверяет заземлённость, а не правильность привязки).

| Что было не так | Масштаб |
|---|---|
| **Закупочный порог выдавался за общий** — примечания «Для целей осуществления закупок …» задают порог другой природы | **57 позиций** |
| **Колонки съезжали**: заголовки «до 31 декабря 2024 г.», «2028 год и далее» не опознавались (у БАС 800 баллов значились 2025 годом вместо 2024, порог 2026-го не показывался) | **104 позиции** |
| **Первая ступень теряла дату**: «до 30 июня 2023 г. — не менее 250» → «не менее 250», прошлый порог читался как действующий | прим. 17 |
| **Вложенные кавычки** ломали остаток строки: из 22 кодов собиралось 6, семь судов брали порог соседней строки — 1950 вместо 2450 | 7 позиций |
| **Пересечение имён ≥2 слов** выбирало соседа: смазка УПИ — 100 вместо 200, сополимеры стирола — 30 вместо 300 | 4 позиции |
| **Коэффициент СЭЗ выдавался за порог** (прим. 80: «Порог: 0,5») | 1 позиция |

**Радиус измерен по эталону до правки** (1366 позиций): +204 порога, 104 изменено (каждое сверено
с первоисточником), потеряно 1 — тот самый коэффициент.

⚠ **Критерий приёмки пересмотрен ≤20 % → ≤22 % (решение владельца, по замеру).** Прежние 24 %
включали 57 неверно привязанных закупочных; честная доля после разделения — 27.7 %; потолок
разрешения примечаний по коду — 21.3 %. ~~Достигнуто **18.4 %**~~ — ⚠ **число отозвано 20.08.2026**: кода, считающего долю, в репозитории не было, и ни одно из четырёх разумных определений его не даёт. Воспроизводимый замер (`scripts/eval_threshold_coverage.py`) — **53.1 %** по срезу points+mixed; улучшение реально (−5.7 п.п. против заявленных −6.2), но критерий **≤22 % НЕ ВЫПОЛНЕН**. ⚠ Первая редакция скрипта сама занижала долю на 13 п.п. (засчитывала проценты и абзацы требований) — числа 39.9/46.3 % отозваны ревью `EV13` #97.
⚠ Семь «приёмочных» примечаний оказались РАЗНОЙ природы: пороги задают только **7** и **11**;
**29, 31, 52, 53** — закупочные; **55** — о документах Союзного государства.

⚠ **Раздел III вынесен эксперту — issue #95.** 108 позиций (подшипники, бульдозеры, экскаваторы,
конвейеры), у которых порога нет НИГДЕ: в чанке раздела ноль упоминаний слова «порог», в
примечаниях и теле постановления тоже. Разбор исчерпан, вопрос к первоисточнику.

✅ **PR #94 СЛИТ 19.08.2026 после ПОВТОРНОГО ревью дельты.** Заход 1 (`EV12` #93, `EV9` #89,
`EV8` #88, `A6` #60) + три пакета правок по первому ревью + пакет по второму. **502 теста.**
Правки рантаймовые: переиндексация и `sync_payloads` не нужны. Выкатки НЕ делали — поедет
вместе с заходом 2, который всё равно требует переиндексации.

⚠⚠ **ПОВТОРНОЕ РЕВЬЮ ДЕЛЬТЫ НАШЛО 15 ДЕФЕКТОВ, И ВСЕ ОНИ ВНЕСЕНЫ ПРАВКАМИ ПО ПЕРВОМУ РЕВЬЮ.**
Ревьюить дельту решили именно потому, что первый пакет уже внёс два дефекта; счёт оказался
15:0 в пользу перепроверки. Все находки воспроизведены на живом коде и корпусе, не вычитаны из
диффа. **Три главных бьют по тем самым правкам, ради которых PR открывался:**

1. **Контекст печатал два взаимоисключающих утверждения о баллах одной позиции** — «баллы за них
   не начисляются» и сразу «баллы НЕ ПРИВЕДЕНЫ (это не значит, что их нет в приложении)», на
   8–11 % корпуса. Условие `and mt` ставили, чтобы убрать ДУБЛЬ, а `elif` пропускал в эту же
   ветку ПРОТИВОПОЛОЖНУЮ фразу. Модель разрешала противоречие сама — от прогона к прогону
   по-разному, то есть коммит, который двигал императив из контекста РАДИ детерминизма, сам же
   подкладывал новый источник разброса. Второй слой того же дефекта — в промпте: переименование
   подписи операции сделало её дословным ТРИГГЕРОМ нового правила 2в.
2. **Фильтр дат поставили в ОДНУ копию регулярки ОКПД2 из семи.** `_anchor_code` по-прежнему
   якорил диалог на дате (`27.12.2023` вытеснял `28.15.10`), а с `EV8` каждый такой «код» делал
   ветку 27.12 целевой и приносил её требования в контекст — утечка НЕВИДИМАЯ для гейта, потому
   что числа в контексте лежат законно. `_HAS_CODE_RE` ошибался в другую сторону: дата в
   уточняющем вопросе молча выключала мультитёрн. Разбор кода теперь ОДИН — `okpd2_ref`.
3. **Обрезка окна выбрасывала записи, которые сама же пометила секунду назад.** На «сравни A и B»
   позиция второго кода вытеснялась добором сиблингов, а семь кандидатов без требований
   оставались: `EV8` отказывала ровно на вопросе, ради которого написана, — при том что
   `Answer.codes` продолжал сообщать метрикам оба кода.

⚠ **Из сводок пропала просевшая метрика.** «Назвал источник требований» упала 3/3 → **2/3 =
0.67 при пороге 1.00** и была удалена из обеих таблиц, пока строка показывала ✅; число выжило
только в сыром `eval_completeness_report.md`. Возвращено в обе сводки, разбор — в заходе 3.

Метрики захода (замер 18.08, до правок этого ревью): гейт «без чужих чисел» **1.00** ×5 (0 утечек
из 250 при 209 доступных), faithfulness **1.00 ×4**, порог 17/17, баллы 0.98, внутренняя лексика
1.00, отказ вне сферы 4/4. Детерминизм 0.70–0.80 — долг `EV10` #91. ⚠ Правки второго ревью
меняют РЕНДЕР контекста и разбор кодов, поэтому замер захода 1 к ним не относится: перемер идёт
вместе с заходом 2, где он и так обязателен.

Дальше по плану — **заход 2** (данные: `D12` #90 + `K2` #47 / `A1` #56), выкатка С ПЕРЕИНДЕКСАЦИЕЙ.

⚠ **`D12`: диагноз в постановке задачи ОПРОВЕРГНУТ разбором первоисточника.** Это не «записи-
обрывки, где парсер отдал требованию статус позиции», а **исключённые позиции** — класс `D4`.
В первоисточнике строка читается `14.12.11, … 14.12.30.160 - Позиции исключены.|до 1 января
2019 г.: …`: маркер стоит в ячейке НАИМЕНОВАНИЯ, а в `product_name` уехал текст ячейки требований.
Фильтр `is_excluded_stub` знает две формы (имя пусто; имя = заголовок раздела) и эту третью не
ловит. В первоисточнике маркер встречается **39 раз в пяти написаниях** (`Позиция исключена` /
`Позиции исключены` / `Код исключен`, с дефисом и без, с префиксом `из`, со списком кодов в пять
строк). Настоящая «Спецодежда» жива в `XVII_legkaya_prom.json` (код `14.12`), поэтому после
удаления заглушек запрос по `14.12.30.131` дойдёт до неё по иерархии — приёмочный критерий
выполним. Код `20.59.52.120` делят заглушка и живой «Воск зуботехнический» (5 блоков) — точь-в-точь
`26.51.20.121` из `D4`. ⚠ Признак брать ИЗ ПЕРВОИСТОЧНИКА, не регуляркой по имени (это ошибка,
которую только что чинила `EV9`), и не перепарсом раздела моделью (VII актуализирован 05.07) —
детерминированным доборщиком, как закрывали `D9`.

Дальше по плану — **заход 2** (данные: `D12` #90 + `K2` #47 / `A1` #56), выкатка С ПЕРЕИНДЕКСАЦИЕЙ.
Приёмочное число к `K2` уже замерено: **229 записей корпуса (17 %)** помечены типом `points`/`mixed`,
но не несут ни одного балла.

✅ **ВЫКАЧЕНА `v0.5.0-test10` (18.08.2026): VM = локально = GitHub = документация.**
На сервере `130.193.36.174` тег `v0.5.0-test10` (`5984839`), `prod` = `main` = тег. Коллекции
`pp719` **1381** green · `pp719_rules` **311** · `verified_cases` **12** — правки рантаймовые,
переиндексация и `sync_payloads` не понадобились. В выкатку вошли `EV6`, `EV7` и пакет правок по
ревью PR #87. Проверено в рантайме: 7 кандидатов из 8 без требований, контекст **7 541** символ
вместо ~16 000; код-группа `28.13` даёт ОДНУ целевую (6 229 символов, 0 чисел); живой ответ по
центробежным насосам — верная позиция, незаземлённых чисел 0; страницы и `/ping` отвечают снаружи.

⚠ **Находка выкатки: шаг «бэкап боевой БД ДО всего» в прежних скриптах НЕ РАБОТАЛ.**
`backup_db.py --verify` требует имя файла — без него печатался usage, копия не создавалась. Спасал
только суточный cron (`O1`). Теперь скрипт зовёт `--dir /data/backups --keep 14` и прерывает
выкатку, если копия не снялась. ⚠ Две проверки-предохранителя ложно сработали на ВЕРНОМ коде
(`grep` поймал имя константы в комментарии об её удалении; `\d` — двойку в «ОКПД2»); обе уточнены.

⚠ **ВОЛНА НЕ СТАРТОВАЛА, СРОКИ СДВИНУТЫ** (решение владельца 17.08.2026). Прежнее окно
14.08 — 28.08 отменено: рассылка в 17 палат не ушла, обращений в окне не было, выкаток под волну
тоже. Сначала закрываем основные задачи, даты тестирования назначаются позже.
**Следствие: заморозка выкаток снята**, движок и корпус свободны для правок, а задачи, отложенные
формулой «меняют контекст каждого ответа перед замером» (`K2`, `K3`, `K6`, `K12`, `K13`), больше
не заблокированы. Шаги, привязанные к «после 28.08», ждут не даты, а **факта старта волны**.
Механика окна дат — [WAVE3_PROTOCOL §2](WAVE3_PROTOCOL.md).

✅ **ВЫКАЧЕНА `v0.5.0-test9` (17.08.2026): VM = локально = GitHub = документация.**
На сервере `130.193.36.174` тег `v0.5.0-test9` (`d3adf2c`), `prod` = `main` = тег. Коллекции
`pp719` **1381** green · `pp719_rules` **311** · `verified_cases` **12**. В выкатку вошли `U5`,
`U6`, `EV1`–`EV5`, `K12`. ⚠ Правка `EV5` меняет ВЕКТОР — на сервере пересчитано
`sync_payloads.py --write` (236 точек, полная переиндексация не понадобилась). Проверено в
рантайме: порога нет в тексте эмбеддинга, окно «городских автобусов» даёт top-1 «Автобусы»
(было — конвейеры и комбайны), роутер `K12` 4/4 включая ноль темы на товарном вопросе.

⚠ **Заморозка выкаток СНЯТА 17.08** — волна не стартовала, обращений палат не было, так что
смешивать замер нечему. ⚠ Уточнение по данным сервера от 17.08: между 14.08 и 17.08 в боевой базе
добавилось **48 реплик**, но все под учёткой `admin` и в дни выкаток — это E2E-проверки, оценок за
период ноль. Реальная активность пользователей — по-прежнему **24.07**. Скоркарту волны считать не
только с окном дат, но и **с фильтром роли**, иначе проверки выкатки попадут в замер.

_Прежние выкатки:_ 13.08 вечером `v0.5.0-test3` → `test4` → `test5` (движок и данные, затем
UX-пакет). Адрес VM менялся: прежних пяти IP среди рабочих нет, в бриф идёт `130.193.36.174`.
`O1` закрыт — cron бэкапа в 03:00 с ротацией 14 копий, бэкап БД снят и вынесен с VM.

**Что добавил `v0.5.0-test4`** (движок и данные не трогал — только интерфейс и защита ввода):
правовые страницы `/terms`, `/privacy` и справочный центр `/help` (20 вопросов, публичные, без
входа) · дисклеймер под вводом со ссылками на них · строка «это ИИ и может ошибаться» над вводом ·
кнопки «Копировать» и «Поделиться» под ответом с пометкой о происхождении · онбординг подсветкой
живого интерфейса вместо модальных карточек · **проверка вопроса перед отправкой во внешнюю
модель**: СНИЛС, ИНН физлица, паспорт, карта и грифы блокируют отправку, телефон и почта
предупреждают. 294 теста.

**E2E на боевой машине:** КОСПАС-САРСАТ → `26.30.11.150` (0.833) · перечень документов в окне
(п. 4.1, 4.2.1) · наследование в ответе · 3 таблицы в процедурном ответе · незаземлённых чисел 0.
Всё совпало с локальными результатами.

## Что сделано 12.08.2026

| Задача | Результат |
|---|---|
| **Актуализация базы** под ред. **22.07.2026** (действует с 24.07) | Отставали на ДВЕ редакции (N 899, N 923); тело постановления — сильнее приложения. Перепарсены IV и VII, +1 позиция «Преформы», пороги 72.2/72.3 проставлены вручную |
| **K10** квота на источник | Приказ №52 в окне: 24/30 → **17/30**; «сроки рассмотрения» 6/6 → 3/3 |
| **K4** вводная фраза требования | Затрагивало **94 % блоков**: операции показывались без условия «на территории РФ» |
| **E1** редакция корпуса | Видна в UI и каждой выгрузке; выводится из текста, не из константы |
| **K9** процедурный golden set | 25 кейсов; атрибуция@1 **0.72 → 0.92**, представленность **1.00** |
| **M1** устойчивость метрики | Замер даёт **0.938 ± 0.003**; `recall@10` снят с заголовка |
| **P1** роутер и структура постановления | Свип 604 вопроса: 5 новых срабатываний, **0 ложных**; ответ по подпункту «г» стал цитатным |
| Протокол волны, агенты на доску, контрольный прогон | — |

**Доска:** 60+ карточек, задачник — `docs/BACKLOG.md`.

---

## Что сделано 13.08.2026

| Задача | Результат |
|---|---|
| **D4** расхождение `reconcile` | В IX найден **живой дефект**: запись исключённой позиции `26.51.20.121` («Продукция радиоэлектроники», ноль требований) делила код с реальной позицией ГЛОНАСС и давала «требований не найдено». Убрана из индекса, правило фильтра расширено. XVIII — не потеря разбора, а отставание JSON от парсера; `reconcile` теперь называет причину. **27/29 → 28/29** |
| **D3** сноски приложения | Диагноз уточнён: чужого текста в JSON XXIX нет, но определения сносок не были доступны поиску **вообще**. Вынесены отдельным чанком, стали четвёртым источником `pp719_rules` (**244 → 311**), место в окне не резервируется. Атрибуция@1 **0.92** без изменений, свип 538 вопросов — 0 ложных |
| Переиндексация | `pp719` 1380 → **1379** (фантом ушёл), `pp719_rules` → **311** |
| **D7** кэш эмбеддингов | Имя файла строилось из `EMBEDDING_MODEL`, и двоеточие диска уводило **17.6 МБ в NTFS-поток** — кэш не пережил бы копирования папки. Имя санитизировано (сегмент + хэш), старое переносится без пересчёта e5. Кэш-хиты после переиндексации: **1381/1381** |
| **D6** перегенерация XVIII | 185 → **187** записей: вернулись две бескодовые строки-варианта (~25 наименований, среди них КОСПАС-САРСАТ, ИРИДИУМ, НАВТЕКС). LLM-разбор недетерминирован, поэтому принят через два правила сохранности («не терять баллы» — 2 записи, «не терять операции» — 12). `reconcile` **28/29 → 29/29**, `verify` ✅, `pp719` **1381** |
| **Фикс ключа R6** (найден в D6) | Ключ карты наследования не снимал висячую « ;» у имени-перечня → родитель «не найден в базе»: **47 отказов**. Стало **3**, наследников **287 → 331**. 46 позиций перестали отвечать «требований не найдено» при живом родителе — на сверку эксперту (`R6-5`) |
| **Карта наследования отставала** | `inherited_requirements.json` был от 10.08, а IV и VII перегенерированы 12.08 — наследники брали требования от устаревших родителей. Перегенерация внесена в чек-лист §1а как обязательный шаг после любой правки корпуса |
| Замер после всех правок | `eval_coverage` (3 прогона): recall@1 **0.940 ± 0.004** против базовых 0.938 ± 0.003 — **регресса нет**; MRR 0.967 → **0.971**, коллизии **87 → 76**, невидимок 0, коды 1370/1370. XVIII — **0.98** |
| **D8** сверка по записи | В `verify_structured` добавлен слой B (`--records`): баллы позиции сверяются с её строкой-исходником, а не с множеством раздела. Калибровка сняла 9 ложных срабатываний (нули, пороги из примечаний, форма «36 (баллов)»), осталось **7 настоящих**. Тестов 225 → **230** |
| **P2** состав документов | Кластер жалоб №1 (31 упоминание). Дефект тройной: подпункт с перечнем не находился по своему вопросу (вводная — в родителе), раздел 4 проигрывал по рангу длинным пунктам, а попавший в окно п. 4.1 резался капом до вводной фразы. Всё три починены; атрибуция@1 **0.92 → 0.96**, «нужный пункт» **5/5**. Ответ теперь называет устав, выписку ЕГРЮЛ, акты на компоненты. ⚠ Требует переиндексации `pp719_rules` |
| **ТН ВЭД не разрешён** | Если кода нет в переходных ключах, механизм молча выключался и позиции подбирались по наименованию — теперь ответ говорит об этом прямо и просит ОКПД2/наименование |
| **T17** табличный вывод | Половина была сделана раньше (рендер в `chat.js`, стили, правило 4б навигатора) — числилась открытой. Добавлено: правило таблиц в процедурный промпт (документы, сроки) и защита стриминга от «палок» недособранной таблицы. Проверено: документы/сроки/баллы — по 3 таблицы, «что делать при отказе» — 0 (проза), незаземлённых чисел нет |
| **D9 — страховка** | Все 7 позиций помечаются в контексте («пороги показаны не полностью, не делай вывод „порог набирается“»); список генерируется `verify_structured --json`. Живой ответ по криогенной АЗС предупреждает явно. Сама задача (порог на блок + перепарс) открыта |
| **Редеплой + два UX-релиза** | `v0.5.0-test3` (движок и данные) → `test4` (интерфейс) → `test5` (версионированный ключ тура). Вторая и третья выкатки заняли минуты: зависимости и корпус не менялись, образ собрался из кэша, снапшоты не понадобились |
| **UX-пакет** `U1–U4` | Правовые страницы и справочный центр · дисклеймеры под и над вводом · кнопки «Копировать»/«Поделиться» · онбординг подсветкой живого интерфейса · **детектор чувствительных данных перед отправкой во внешнюю модель** (блокирует документы и грифы, предупреждает о контактах) |
| **Проверка учёток** | 28 учёток: 1 admin, 8 expert, 19 user; 17 регионов с заполненным профилем. Вход проверен сквозным путём на временной учётке (создана и удалена): 302 → `/`, гейт профиля работает, неверный пароль → 401, тур в разметке, ключ флага новый. ⚠ У `pskov.expert1` и `smolensk.expert1` анкета не заполнена — при входе они попадут на неё |
| Заведены | `D9` (порог узла и второй шкалы теряется — 7 позиций, XXIV/XXV/XXVI/IX), `R6-5` (46 позиций на сверку экспертом) |

**Итог дня:** закрыты `D1`, `D3`, `D4`, `D6`, `D7`, `D8`, `O1`, `P2`, `T17`, `U1`–`U4` + фикс ключа
`R6`; заведены `D9` (#68) и `R6-5` (#69). На доске закрыто 11 issues, открытых осталось 33.
Три выкатки: `v0.5.0-test3` → `test4` → `test5`.

## Что сделано 14.08.2026

| Задача | Результат |
|---|---|
| **Пакет третьей волны** | Окно зафиксировано **14.08 — 28.08**. Лист сверки для эксперта теперь **генерируется** из корпуса (`scripts/build_expert_checklist.py` → `docs/EXPERT_CHECKLIST_WAVE3.md`), а не пишется руками: числа перестали расходиться с данными (`R6-5` числилась как 46 позиций, воспроизводимый дифф даёт **44**). Бриф — `docs/WAVE3_BRIEF.md` |
| **`D9`-страховка** | Условие блока (`note`) доезжает до контекста: 503 блока у 259 позиций не читались рантаймом ВООБЩЕ, а там пороги узлов и правила начисления |
| Промпт | Номера правил инструкции («[2б]») больше не утекают в ответ эксперту как ссылка на несуществующий источник |
| **Гигиена веток** | PR #70 (`dev → main`, 76 коммитов) и #71 слиты; `main` больше не стоит на июльском `9d7c630`. `prod` не трогали — он уже на развёрнутом `v0.5.0-test5` |
| **Догнанное ревью** | Ревью перед merge оборвалось вместе с сессией на второй фазе. Догнано на `v0.5.0-test5..main`: из 7 находок **6 настоящих**, все проверены на корпусе — см. ниже |
| **`D9` закрыта** | Порог получил место в схеме блока, а вернул его туда **детерминированный доборщик из первоисточника** (`scripts/backfill_block_thresholds.py`), а не перепарс моделью: 13 записей в XXIV/XXV/XXVI, 0 откатов. `IX` оказался ложным срабатыванием сверки («Втоп = 30 * К» против «(Втоп = 30 баллов)» в источнике). Сверка по записи **7 → 0 расхождений**, список неполных порогов пуст, ответ по криогенной АЗС печатает поузловые минимумы 100/60/80/40 |
| **Выкатка `v0.5.0-test6`** | VM = локально = GitHub = документация. Образ пересобран **классическим сборщиком**: BuildKit не смог получить метаданные `python:3.12-slim` (до `auth.docker.io` не дошёл TLS), а базовый образ на машине уже есть. Коллекция поднята снапшотом за 7 секунд вместо часа индексации. `prod` перемотан на выкаченный коммит |
| **`scripts/sync_payloads.py`** | Новый инструмент: приводит payload точек к корпусу **без переиндексации** (секунды вместо часа) — на случай, когда меняются поля записи, а не тексты вектора. ⚠ Ключ — `source_anchor`, а не (раздел + имя): 7 пар записей делят название внутри раздела, и по «привычному» ключу 14 точек получили бы payload чужой записи. Поймано пробным прогоном, до записи |

### Находки догнанного ревью (все замерены на корпусе)

| Что нашли | Замер | Как закрыто |
|---|---|---|
| Лист сверки печатал «порог не указан» и врезкой просил эксперта разобраться с этим **в первую очередь** | Порог есть у **26 из 26** позиций блока А1: рантайм добирает его из примечаний (`не менее 300 баллов [прим. 7]`) | Лист считает порог тем же правилом, что рантайм; бриф и протокол исправлены |
| `/navigate` собирал чек-лист без `note` | **12 позиций** теряли п. 4.3.5 / 4.3.6 / 4.3.7 Приказа №52 | `note` читается; потерявших пункт — 0 |
| Условие без имени узла печаталось как «▸ …», а правило 2б читает «▸» как узел изделия | **60 блоков**; у одного (XVIII) — шкала баллов по узлам, баллы могли уехать порогами | Подписывается «Условие блока: …» |
| Рез условия по лимиту резал число пополам | «ГОСТ 3.1129-93» → «…3.1»; постпроверка сверяет ответ с КОНТЕКСТОМ и заземлила бы подделку | Режем по границе слова: 122 усечения, **0** выдуманных чисел |
| f-строка со слэшем внутри выражения | SyntaxError до 3.12, а `SETUP.md` обещает 3.11+ → на 3.11 падала бы **вся** батарея, CI (3.12) не увидел бы | Исправлено + токенайзер-гард на весь репозиторий |
| `n_operations` расходится с рантаймом на блоке только с `note` | 0 случаев в корпусе — дремлет | Описано в коде: разбудит следующая правка парсера |

⚠ **Урок:** находки ревью переживают сессию только в коммитах. Первые две фазы успели отчитаться
и были зафиксированы (`6e74446`, `f0ba541`) — всё, что нашли остальные, умерло вместе с сессией.
Длинное ревью перед merge стоит вести с промежуточной фиксацией, а не копить до финального отчёта.

## Что сделано 16.08.2026

| Задача | Результат |
|---|---|
| **Живой дефект: источники вели на недействующую редакцию** | Клик по источнику открывал текст ПП №719 с плашкой «Не действует». У Контура на каждую редакцию свой `documentId`, он был захардкожен на ред. **27.06.2026**, а корпус ушёл на две редакции вперёд (01.07 → **22.07.2026 N 923**). Действующая — `documentId 507406` |
| Причина | Копий константы было **две** (`app/api/chat.py` и `app/web/static/chat.js`) с комментарием «⚠ при вступлении в силу новой редакции обновить» — тот же ручной шаг, из-за которого редакция корпуса выводится из текста, а не задаётся константой; на ссылку правило не распространили |
| Почему дожил до эксперта | Дефект **невидим**: страница открывается, ссылка рабочая, тесты зелёные. Страницу чата при этом не рендерил **ни один тест** |
| Как закрыто | Адрес переехал в `app/rag/edition.py` — таблица «редакция → `documentId`» + `kontur_719_url()`; копия из `chat.js` убрана (фронт получает адрес из шаблона); нет редакции в таблице → последняя известная + `WARNING` в лог; источник без адреса рисуется подписью, а не пустой ссылкой |
| Проверено вживую | Якорь `#h14240` раздела «Правила» в новой редакции жив (сверен в исходном HTML); Приказ ТПП №52 (`505398`) действует, ред. от 29.10.2025 — не трогали |
| Тесты **335 → 340** | `TestKonturLinks` краснеет, если корпус актуализировали, а ссылку нет; отдельный тест запрещает `documentId=` в коде обоих слоёв; страница чата теперь рендерится в тестах |
| **`docs/PRODUCT_SPEC.md`** | Полная спека работающей версии: принципы с привязкой к коду, ЦА, архитектура, три корпуса, маршрут запроса, анти-галлюцинационный контур, метрики, эксплуатация. Числа пересчитаны из корпуса — полнота требований оказалась лучше записанной в `CONCEPT.md` (**0.8 %** позиций без требований против «4 %»: цифра была до фикса ключа наследования 13.08) |
| **Выкатка `v0.5.0-test7`** | 33 секунды: корпус не менялся, снапшоты не понадобились, образ собрался классическим сборщиком. Проверено в рантайме на VM (редакция корпуса → `507406`), снаружи (`/static/chat.js` без своей копии адреса) и по коллекциям (1381/311/12 green). `prod` перемотан на `36d8cfd` |

⚠ **Урок в копилку к `edition.py`:** правило «величина, зависящая от редакции, не должна быть
константой» было выведено 12.08 и применено к подписи редакции — но не к ссылке, которая от той же
редакции зависит буквально. Стоит перепроверить, нет ли ещё мест, где редакция зашита руками.

## Что сделано 16.08.2026 (вечер) — доработки по итогам прогона `EVAL_GUIDE`

Выкачено тегом **`v0.5.0-test8`** (`4b9fbf6`), `prod` = `main` = тег. Корпус не менялся,
переиндексация не понадобилась, выкатка заняла 39 секунд.

| Что | Зачем | Как проверено |
|---|---|---|
| **Тай-брейк ретрива** (`retriever._order_key`) | RRF выдаёт РАЗНЫМ записям одинаковый score, и на границе окна `limit` они конкурируют за последнее место — кто попадёт, решал порядок ответа Qdrant. На «бульдозерах» из 5 прогонов в 3 в контекст входили «Тракторы гусеничные» (0.200000), в 2 — «Модульная криогенная АЗС» (0.200000) | Контекст 3 варианта из 5 → **1**, числа 2 → **1**. Это же и есть M1, где разброс recall@1 объяснили неустранимым свойством слияния |
| **Числа из вопроса ≠ выдумка** | Гард сверял ответ только с контекстом, и цифру заявителя, процитированную в ответе, помечал как галлюцинацию («набрали 3200 баллов, пройдём?»). Класс «посчитайте мне» — самый частый, ложные флаги копились в приёмочной метрике | Эхо выделено в `Answer.echoed_numbers`, пишется в журнал. `eval_answers.py` обновлён под ту же сигнатуру |
| **Второй сигнал out-of-scope** (`app/rag/scope.py`) | Косинус исчерпан: полоса перекрытия расширилась с 14 тысячных до 44, 5 негативов из 36 проходят порог. Класс ОКПД2 — сигнал другой природы | Ловит сельхоз, отходы, перевозки (3 из 5), не трогает ни один профильный запрос. Флаг только при ЕДИНОГЛАСИИ кандидатов |
| **Таблица баллов печатает КОД** (≥12 балльных операций) | Модель выбирала своё подмножество из шести десятков операций | Незаземлённых чисел 0, таблица — подмножество контекста по построению |
| **Метрика полноты** (`eval_completeness.py`) | Пара к faithfulness была РАЗОМКНУТА: рост 0.98 → 1.00 формально не отличался от «научился молчать» | На 10 кейсах: баллы 1.00 (62 из 62), порог 1.00, неполнота 4/4, все 7 запретов §2.2 = 1.00 |
| **Среда замера из снапшотов** (`eval_env.py`) | Прогон уровня 1 на бою кладёт сервис на 40 минут | — |
| **Ответ не упоминает кейс эксперта** | Решение владельца: откуда взято знание — внутренняя кухня; эксперт читал строку как ссылку на чужое заключение | Промпт-тест + `assert` в скрипте выкатки |

Тесты **340 → 365**.

### Три вывода, которые дороже самих правок

⚠ **Диагностический инструмент врал, и это чуть не увело в другую сторону.** Первая проба
детерминизма сказала «ретрив стабилен» — я сравнивал якоря, обрезанные до 28 символов, а они у
всех начинаются с «Приложение к ПП №719, Раздел». Правду показала полная идентичность хитов.

⚠ **Правило трёх прогонов относится и к самому детерминизму.** Четыре замера на НЕИЗМЕННОМ коде
дали 0.80 / 0.60 / 0.90 / 0.60; три подряд — **0.83 ± 0.05** против исходных 0.80, то есть
«метрика не изменилась». Я успел доложить «0.80 → 0.90» как улучшение — это было чтение шума.

⚠ **Две «находки» оказались артефактами измерения.** Коллизия методичек воспроизводится только
на запросе голым `product_name`, каким живой пользователь не пишет. «Полнота 0.78» штрафовала
уточняющие ответы — то самое поведение, которое правило 1г ПРЕДПИСЫВАЕТ. Оптимизируй по ней — и
система начала бы называть баллы по неподтверждённой позиции.

### Долги

* **Детерминизм 0.83 ± 0.05** при пороге гайда 0.95. Понижать `POINTS_TABLE_MIN` бесполезно:
  у всех нестабильных запросов на целевой позиции 0–6 балльных операций, а плавающие числа
  приходят из **порогов соседних позиций-кандидатов**, которые модель то упоминает, то нет.
  Дальше — либо запретить числа из непрофильных кандидатов (нужен замер), либо признать порог
  недостижимым.
* **Перекалибровка `RELEVANCE_SOFT`** — только вместе с набором пограничных in-scope.
* **`gs_natural`** — собирается из данных волны скриптом, как только она пройдёт.
* **Текстовый faithfulness** — не измеряется; таблица кодом снимает риск лишь там, где печатает.

## Что сделано 17.08.2026

| Задача | Результат |
|---|---|
| **`EV5`** (#82) автобусы → конвейеры | Причина была НЕ в ретриве, а в индексации: в текст dense-вектора попадал `min_threshold`, и у **139 записей из 236** эта строка длиннее наименования. Запрос-пустышка «сколько баллов нужно для производства» без единого товара давал «Конвейеры скребковые» с косинусом **0.858** — выше, чем целевая позиция на своём продукте. Порог убран из вектора, переиндексация 236 записей. Итог: окно автобусов → **top-1 «Автобусы и автомобили скорой медицинской помощи»**, чужие числа **0.90 → 1.00** (0 утечек из 50), recall@1 **0.940 → 0.942** (разброс 0.000), коллизии 11–12 → **9**, полнота 47/47 → **58/58**, уточняющих ответов 2 → 1. ⚠ Разделимость guard'а НЕ изменилась — вывод `EV2` в силе. ⚠ При выкатке нужен `sync_payloads.py --write` (меняется вектор у 236 записей, полная переиндексация не нужна) |
| **`EV1`–`EV4`** (#78–#81) долги замеров | Закрыты все четыре, отчёт — [eval_runs/2026-08-17_EV1-EV4.md](eval_runs/2026-08-17_EV1-EV4.md). **`EV1`:** правило 3в промпта (числа только из целевой позиции) + новая метрика «без чужих чисел» **0.90**; старая (0.80 ± 0.08) складывала шум формулировки с дефектом, порог 0.95 снят как недостижимый. **`EV2`:** собран набор из 20 реальных пограничных in-scope, кривая обмена показала, что **0.830 — уже минимум ошибки**; косинус классы не разделяет, порог не трогаем. **`EV3`:** сборщик `gs_natural` готов, июль даёт 29 кейсов из 120 — добирает волна. **`EV4`:** n-граммная методика гайда **порогом работать не может** — 35 срабатываний, настоящих искажений 0; остаётся диагностикой. Заведена `EV5` (ретрив автобусов). Тестов **389 → 402** |
| **`U6`** (#75) длинная таблица сворачивается | От **8 строк** тела видно **5** + кнопка «Показать полностью (ещё N строк)» ⇄ «Скрыть». Замер по корпусу: автобусы 35 строк, бульдозеры 38, криогенная АЗС 25, подшипники 16 — сворачиваются; позиции без балльной таблицы не затронуты. Состояние «раскрыто» живёт **вне разметки**, иначе пересборка на каждом фрагменте стриминга схлопывала бы таблицу сразу после нажатия; все четыре пути вывода сведены в одну точку `renderAnswer`. **Два молчаливых дефекта по пути:** фолбэк-путь не выставлял `dataset.raw` и не добавлял кнопки — ответ после обрыва SSE нельзя было ни скопировать, ни отправить с пометкой (`R3`); запасной путь копирования читал `innerText`, который не видит скрытых строк. Поведение **исполняется на node** (пороги, плюрализация, полный сценарий приёмки со стримингом), проверено мутацией. Тестов **381 → 389** |
| **`U5`** (#74) подсказка в поле ввода | Подсказку задаёт **ветка, которой отвечено** (`app/rag/followup.py` → `Answer.input_hint` → `/api/chat` и событие `done` стриминга), а не регулярка по тексту ответа. Товарный подтверждённый ответ зовёт к документам, процедурный про документы — к срокам и обратно; приветствие, перевод кода, «позиция не найдена» и сработавший guard оставляют стартовую подсказку; дефер процедурной ветки — пустое поле. Живой прогон **5/5**: вторым ходом контекстуализатор подмешивает наименование («…для внесения **гидравлических насосов** в реестр»), но 719-якорь уцелел и маршрут остался процедурным — это и был главный риск (`K12`). Тестов **365 → 381** |
| **`EV6`** (#84) светодиоды | **Диагноз задачи оказался неверен, и это главное.** Заводилась как «пустая запись-дубль», а сверка с первоисточником показала ОДНУ ячейку требований, разъехавшуюся по трём строкам кода (`R29`): 26.11.22.200 — голова списка документов (3 операции), 26.11.22.210 «Светодиоды (**в части** белого диапазона)» — середина (4), 26.11.22.216 «Светодиоды белого диапазона» — хвост И все балльные операции (15 в 4 блоках). «(в части …)» — не продукция, а область действия строки кода; удалять и схлопывать нечего. Чинилось **ранжирование**: целевой не может быть строка-квалификатор при живом сиблинге той же группы. ⚠ Первая версия правила («берём запись группы с наибольшим числом операций») чинила один запрос и ломала соседний — на «светодиодах красного диапазона» уводила ответ на общую строку «за исключением белого»; вторая меняла обрывок на обрывок. Радиус измерен: из 1381 позиции правило может сработать на **3**. **Вторая половина дефекта, которую метрика не показывала:** «кто целевой» решали ДВА места — `_target_hit` и своё `i == 1` внутри `format_context`; без сведения в одно правка починила бы метрику, но не контекст (содержательная позиция осталась бы усечённой 12 из 15). Замер: гейт «без чужих чисел» **0.80 → 0.90 ± 0.00** (три прогона), запрос светодиодов **0/5** утечек, полнота 1.00 (42 кейса). Остаток утечки вынесен в **`EV7` (#86)**. Тестов **434 → 437** |
| **`EV7`** (#86) насосы | **Правило промпта заменено устройством контекста.** Правило 3в держало пять кандидатов и перестало держать восемь; вместо усиления правила требования НЕЦЕЛЕВЫХ кандидатов убраны из контекста вовсе — приводить чужие баллы стало нечем. Замер, определивший решение: из 635 чисел контекста 42 кейсов **321 (на 37 кейсах)** принадлежит кандидатам и не встречается у целевой. Цена измерена по трафику: из 580 реальных вопросов просьба сравнить позиции ровно ОДНА, и она с кодом (совпавшие по коду остаются целевыми). ⚠ **Эти числа недействительны — метрика измеряла себя (ревью PR #87 18.08); перезамер честным оракулом дал 1.00 ×4 при 209 доступных к утечке.** Исходно отчитано: 1.00 / 1.00 / 1.00, ноль утечек из 50. Faithfulness 1.00 ×3 (417/425/416 чисел — ответы не обеднели), полнота 1.00 (134/134), контекст **16 083 → 7 638** символов. Риск «система молчит» проверен тремя способами, риск правила 1ж — на 6 запросах с кодом, оба не подтвердились. ⚠ Классификатор уточняющих ответов пришлось откалибровать по ФОРМЕ правила 1г: он исключал из полноты ответы, довёзшие все баллы целевой (11 кейсов из 42 против 5) — метрика тихо мерила три четверти выборки. Тестов **437 → 445** |

## Что сделано 18.08.2026 — ревью PR #87 и перезамер

Полный отчёт — [eval_runs/2026-08-18_review_PR87.md](eval_runs/2026-08-18_review_PR87.md).

⚠⚠ **Главное: гейт, которым отчитывалась `EV7`, после самой `EV7` перестал что-либо измерять.**
Эталон «чужих чисел» строился рендером `format_context([target, h])`, а правка из этого рендера
требования кандидатов и убрала — разность пуста ПО ПОСТРОЕНИЮ. Проверено на фикстуре насосов:
кандидат несёт 110/450/750, оракул возвращает пустое множество. «1.00 ×3, ноль утечек из 50» не
могло быть другим на 8 из 10 запросов набора.

✅ **Перезамер честными инструментами подтвердил правку:**

| Метрика | Результат | Порог |
|---|---|---|
| Без чужих чисел | **1.00 ×4**, 0 утечек из 200 прогонов, **при 209 доступных к утечке** | ≥0.95 ✅ |
| Faithfulness | **1.00**, 422 числа, 0 выдуманных | 1.00 ✅ |
| Полнота | **1.00** (порог 16/16, баллы 128/128, неполнота 5/5, источник 3/3, все запреты §2.2) | ≥0.90 ✅ |
| Отказ вне сферы | **4/4** | 1.00 ✅ |
| Детерминизм чисел | 0.90 ± 0.08 — **долг** (мега-продукты: автобусы, бульдозеры) | ≥0.95 ❌ |

**Закрыто из 15 находок ревью — одиннадцать:**

| Что | Замер |
|---|---|
| Оракул гейта выводился из аудируемого артефакта | Переписан на ДАННЫЕ записи; в отчёт добавлен **размер пула** — без него «1.00» не интерпретируется |
| Канал кейсов эксперта метрике не виден | `format_cases` идёт и в промпт, и в заземление с высшим приоритетом (правило 1а) — теперь в метрике |
| **Частичный код обходил `EV7` целиком** | `28.13` подходит 52 записям (531 операция): контекст был 15 799 симв. / 11 чисел / 8 целевых → **6 194 / 0 / 1**. `29.10`: 16 928 → 5 389 |
| Счётчик «(в базе N операц.)» заземлял выдумку | `number_in_context('15', ctx)` → `True`: выдуманное «не менее 15 баллов» проходило гард |
| Наличие требований считалось своим счётчиком | Расхождение с рантаймом на **98 записях**; у 9 позиция С требованиями описывалась как «в базе ничего нет» |
| «Попроси её код ОКПД2» ×7 при известном коде | Против правила 1ж; строка убрана при совпадении по коду |
| `EV7` стирал требования сиблингов СВОЕЙ ячейки | На белых светодиодах терялось **7 операций из 22** |
| «Кто целевой» решалось в ЧЕТЫРЁХ местах | Сведено в `pipeline.target_hits`; чек-лист `/navigate` терял условные пункты 4.3.x Приказа №52 |
| Таблица баллов при нескольких целевых | Строилась по первой, запрещая перечислять операции у всех — не печатается, когда целевая не одна |
| Псевдозапись раздела становилась целевой | 11 записей `section_methodology`; ответ оставался БЕЗ требований вовсе |
| Ключ расколотой ячейки не находил 1 из 15 | Позиция молча теряла пометку `R29`, `EV6` на её группе не работал |

⚠ **Обоснование цены `EV7` опровергнуто.** «Единственная просьба сравнить пришла с кодом, значит обе
записи целевые» — ложно: `extract_okpd2` это `re.search`, из «сравни по 28.13.14 и по 26.30.50»
берётся только **28.13.14**. Заведено issue **#88**; правило 3в переписано.

⚠ **Метрика разошлась с продуктом прямо во время перезамера.** Первый прогон дал faithfulness 0.95 —
два кейса «выдумали» числа, которых рантайм честно держал в промпте, и рантайм-гард не пометил ни
одного (0 из 2). Причина: контекст стал зависеть от кода, а скрипт пересобирал его без кода. Чинилось
в корне — `Answer.grounding` несёт ту самую строку, с которой сверялся гард. ⚠ В том же файле уже
стояло предупреждение ЭТОГО класса (про забытый `question`) — комментарий не защитил.

Заведены **#88** (сравнение по нескольким кодам) и **#89** (роль строки расколотой ячейки должна
лежать в данных, а не в регулярке по прозе: под неё подходят 16 записей, из них 13 — продукция).
Тестов **445 → 455**.

## Что сделано 18.08.2026 — заход 1 (долги ревью)

Закрыты `EV12` #93, `EV9` #89, `EV8` #88, `A6` #60. Тестов 455 → **470**. PR #94, выкатка
рантаймовая. Порядок работ — [BACKLOG § План заходов](BACKLOG.md).

⚠ **Урок захода: правило промпта проиграло словарю САМОГО промпта, и показал это замер.**
`EV12` («ответ говорит „в контексте баллы не указаны“») заводилась как импровизация модели.
Оказалось, шаблон промпта **сам предписывал** «**Порог:** … иначе „в контексте не указан“».
Исправил шаблон, добавил правило 3г, проверил вживую — работало. А метрика дала **35/42 = 0.83**:
слово стояло в промпте **34 раза**, модель брала словарь оттуда. Помогло только переименование
ПОНЯТИЯ («контекст» → «найденные данные»), после него **0.98**.

| Метрика захода | Результат | Порог |
|---|---|---|
| Без чужих чисел | **1.00** ×5, 0 утечек из 250 при **209 доступных** | ≥0.95 ✅ |
| Faithfulness | **1.00 ×4** (429/422/414/421 чисел) | 1.00 ✅ |
| Полнота: порог · баллы · неполнота | 17/17 · 0.98 (131/135) · 5/5 | ≥0.90 / 1.00 ✅ |
| Полнота: назвал источник требований | **2/3 = 0.67** (3 кейса R6 — требования группы) | 1.00 ❌ |
| Нет внутренней лексики | **1.00** (0.83 → 0.98 → 1.00) | 1.00 ✅ |
| Отказ вне сферы | 4/4 ×3 | 1.00 ✅ |
| Детерминизм чисел | 0.70–0.80 — долг `EV10` #91, внутри исторической полосы 0.60–1.00 | ≥0.95 ❌ |

⚠⚠ **Строку «назвал источник требований» вернуло ПОВТОРНОЕ РЕВЬЮ: она просела с 3/3 до 2/3 и
была УДАЛЕНА из обеих сводок, пока строка сводки показывала ✅.** Число живо только в сыром
отчёте `docs/eval_completeness_report.md`, а решение «сливать или ревьюить» читают отсюда.
Это ровно тот класс, на который у проекта есть собственный урок — «замер, сдвинувшийся в
успокаивающую сторону, — повод искать причину», — применённый наоборот: замер убрали из
поля зрения. Разбирается в заходе 3 вместе с `EV10`; знаменатель мал (3 кейса R6), поэтому
одно расхождение даёт 0.67 — но порог этой метрики 1.00, и он не взят.

**Попутно замерено для захода 2:** 229 записей корпуса (17 %) помечены типом `points`/`mixed`, но
не несут ни одного балла — приёмочное число для `K2` (#47).

## План следующей сессии

| Шаг | Что | Оценка |
|---|---|---|
| 1 | **Решить по PR #94**: повторное `/code-review 94` (в нём три пакета правок, которых ревью не видело) → merge → тег `v0.5.0-test11` → рантайм-выкатка | ~1 ч |
| 2 | **Заход 2 по плану** — `D12` #90 (три записи-обрывка из индекса) + `K2` #47 / `A1` #56 (разрешение ссылок на примечания; порог не найден у 24 % корпуса) | 1–1.5 сессии |
| 3 | Выкатка захода 2 — **С ПЕРЕИНДЕКСАЦИЕЙ** (снапшот, см. `REVIEW_TZ §1а`) | ~1 ч |
| 4 | `EV10` #91 — разделить метрику детерминизма (диагноз готов), делать вместе с `K6` | ~0.5 сессии |

⚠ **Что нельзя забыть при выкатке захода 2:** правка корпуса меняет payload И текст вектора, значит
нужны либо переиндексация, либо `sync_payloads.py --write`; перегенерация карты наследования
обязательна после ЛЮБОЙ правки `structured/*.json`.



Волна сдвинута — план строится вокруг **основных задач**, а не вокруг даты замера.

| Шаг | Что | Оценка |
|---|---|---|
| 1 | ✅ **PR #83 `dev` → `main`** слит 17.08 после ревью: 7 находок, все настоящие, все исправлены | сделано |
| 2 | ✅ **`K12` (#45) тематический роутер** — шесть тем, 0.95 на реальных вопросах, `P2`-остаток закрыт | сделано |
| 3 | ✅ **Выкатка накопленного пакета** — `v0.5.0-test9` (`d3adf2c`) ушла на VM 17.08: `sync_payloads.py --write` пересчитал 236 векторов `EV5` за две минуты вместо часовой переиндексации | сделано |
| 4 | **`K15` (#36) Методрекомендации ТПП + справочник документов** — кластер жалоб №1, 31 упоминание. ⚠ Нужен внешний документ от владельца | ~1 сессия |
| 5 | **`K14` (#35) корпус СТ-1 и второй ключ классификации (ТН ВЭД)** — вторая по частоте претензия, 29 упоминаний. Не «ещё один документ», а второй ключ: корпус построен на ОКПД2 | 1–2 сессии |
| 6 | ✅ **`EV6` (#84)** сделан 17.08 вечером — диагноз «пустой дубль» опровергнут, чинилось ранжирование; гейт «без чужих чисел» 0.80 → **0.90 ± 0.00** | сделано |
| 7 | ✅ **`EV7` (#86)** сделан 17.08, **проверен ревью и перезамерен 18.08**: гейт «без чужих чисел» **1.00 ×4 при 209 числах, доступных к утечке** (прежнее «1.00 ×3» мерило само себя), контекст **−53 %**, побочно закрыта половина `K6` | сделано |
| 7а | ✅ **Ревью PR #87 и пакет правок** — 11 находок из 15 закрыто, перезамер честными инструментами, issues #88/#89 на остаток | сделано 18.08 |
| 7б | ✅ **Выкатка `v0.5.0-test10`** — `prod` = `main` = тег = `5984839`, коллекции не трогали | сделано 18.08 |
| 9 | **`EV8` (#88) сравнение по нескольким кодам** и **`EV9` (#89) роль строки расколотой ячейки в данных** | ~0.5 сессии каждая |
| 8 | **`A6` (#60) агент слежения за редакциями** — 12.08 корпус отстал на ДВЕ редакции, и заметили это вручную | ~0.5 сессии |

**Ждёт старта волны** (не даты): скоркарта с окном дат · `gs_natural` до 120+ кейсов (#80) ·
лист сверки эксперту (`R6-5` #69, `R6-4` #52, `R29-4` #53, `D5` #65) · заполнить контакт
организатора в `docs/WAVE3_BRIEF.md` (строки 9 и 96).

⚠ **`EV6` и `EV7` ждут выкатки** — сделаны ПОСЛЕ тега `v0.5.0-test9`, в выкаченном коде их нет.
Оба чисто рантаймовые: переиндексация и `sync_payloads` не нужны. `EV6` меняет выбор опоры,
`EV7` — состав контекста (и заодно вдвое уменьшает вход, то есть стоимость и задержку).

⚠ Прежний шаг «`P2`-остаток» снят: сама `P2` закрыта 13.08, а её остаток — это `K12`
(тематический роутер, ~1 сессия, сознательно после волны), а не получасовая правка.

⚠ `U5`/`U6` — чистый интерфейс, замер волны не сдвигают. Выкатывать либо **до рассылки** (пока в
окне ноль обращений, как ушла `test6`), либо после 28.08 — решает владелец.

⚠ **Выкатка `D9` уже сделана** (14.08, `v0.5.0-test6`): коллекция на сервере поднята снапшотом,
пороги узлов в payload проверены живым ответом. Следующая выкатка — не раньше 29.08.

⚠ **Рецепты, добытые этой выкаткой** (подробности — в `D:\navigator-deploy\2026-08-14\DEPLOY_MANIFEST.md`):
запуск деплой-скрипта на VM обязателен **под `flock`** — ретрай `ssh` после обрыва плодит
параллельные выкатки (сегодня их стало четыре, и они положили SSH на двух ядрах); сборка образа —
`DOCKER_BUILDKIT=0`, иначе BuildKit идёт в реестр за метаданными базового образа и падает на
рваной сети; заливка файлов — только с проверкой `sha256` после каждой попытки, но **сначала
проверка, потом заливка**, иначе мегабайты гоняются впустую.

### Что сделано при выкатке 13.08

1. Бэкап боевой БД снят до всех изменений, вынесен с VM в `D:\navigator-backups\` (28 пользователей,
   1208 сообщений, 297 оценок; sha256 сверен, `integrity ok`).
2. Код доставлен архивом из тега, `.env` не тронут; образ пересобран (2.67 → 2.21 ГБ, ушёл streamlit).
3. Коллекции подняты **из снапшотов** — минуты вместо часовой индексации на 2 vCPU.
4. `O1`: cron бэкапа в 03:00 с `--keep 14`, ручной прогон проверил целостность.
5. За собой прибрано: временные файлы, распакованные снапшоты и старый образ удалены (диск 49 %).

⚠ **Канал до VM рвётся** (DPI): SSH устанавливается через раз, из 12 попыток проходит одна-две.
Рецепт, который сработал: длинные операции запускать на VM через `nohup … > ~/log 2>&1 &` и
опрашивать лог, а скрипты заливать через `ssh 'cat > файл' < локальный` в цикле ретраев — `scp`
рвался чаще. И `docker exec` для heredoc нужен с флагом `-i`, иначе python получает пустой stdin
и молча завершается с кодом 0.

### Пока доступа к VM нет — очередь локальных работ

Ни одна из них не требует сервера и не меняет контекст ответов настолько, чтобы испортить замер
приёмки (кроме `D9` — её эффект надо мерить до выкатки).

| Приоритет | Что | Оценка |
|---|---|---|
| 1 | Снять **снапшоты Qdrant** и собрать архив деплоя заранее — наполнение на 2 vCPU длинный полюс | ~30 мин |
| 2 | Свести списки для эксперта в бриф волны: `R6-5` (46), `R6-4` (11), `R29-4` (4 группы), `D5` | ~30 мин |
| 3 | **`D9`** целиком — порог на блок в схеме + перепарс IX/XXIV/XXV/XXVI (пометка уже стоит) | ~1 сессия |

**Пакет «до редеплоя» закрыт 13.08:** `P2` (состав документов), `T17` (таблицы), сообщение о
неразрешённом ТН ВЭД и пометка неполных порогов `D9`. Дальше — только выкатка.

### Финальный прогон — состав

```bash
.venv/Scripts/python.exe -m unittest discover -s tests          # 295
.venv/Scripts/python.exe scripts/eval_coverage.py --report docs/eval_coverage_report.md
.venv/Scripts/python.exe scripts/eval_rules.py --report docs/eval_rules_report.md
.venv/Scripts/python.exe scripts/eval_answers.py --report docs/eval_answers_report.md
```
Плюс живые прогоны: 5 товарных вопросов (`scratchpad/control_run.py`) и 5 процедурных
(`scratchpad/proc_run.py`), сверка состава архива деплоя.

### Редеплой — чек-лист

Полный — [REVIEW_TZ §1а](REVIEW_TZ.md). Ключевое:
1. **Тег ДО выкатки** (правило R18).
2. ⚠ **Файлы данных обязаны попасть в архив**: `inherited_requirements.json`,
   `fragmented_requirements.json`, 13 файлов `knowledge_base/cases/*.json`. Без них наследование,
   пометки неполноты и петля обучения **молча выключатся**.
3. **Наполнение Qdrant — снапшотами, пакет готов**: `D:\navigator-deploy\2026-08-13\` (коммит
   `34cd546`, 19 МБ вместе с кодом). Восстановление проверено в чистом контейнере — 1381 / 311 / 12
   точек, статус green, живые запросы совпали с рабочей базой. Пошаговая инструкция и проверки —
   `DEPLOY_MANIFEST.md` там же. На VM сверить версию Qdrant: снято на **v1.18.2**, compose тянет
   `latest`. Запасной путь, если восстановление не пойдёт, — `load_kb.py` + `load_rules_kb.py` +
   `seed_cases.py` прямо на VM (десятки минут на 2 vCPU, но ни от чего не зависит).
   ⚠ **Снимать снапшоты с локального рабочего Qdrant нельзя** — он на NTFS-bind-mount, и
   `wal/first-index` уезжает в снапшот нулями (`Wal error: Can't init WAL` при восстановлении,
   checksum при этом сходится). Рецепт — временный Qdrant со storage в docker volume, см. манифест.
4. E2E снаружи: стриминг и фолбэк, лимиты 429, экспорт с дисклеймером и редакцией, «Автокраны»
   с атрибуцией, «подпункт г» цитатным ответом.
5. `O1` — cron бэкапа и вынос копий off-VM.
6. FF `main` и `prod`.

**TLS (R11) и secure-куки (R14) НЕ делаем** — решение владельца 12.08: аудитория не расширяется,
аккаунты те же.

### Сразу после волны

⚠ **Скоркарту считать с окном дат** — `build_admin_view(date_from=, date_to=)`. Аккаунты те же,
июльские 147 оценок с приёмкой 33 % иначе разбавят замер до ≈46 % даже при идеальной волне.

---

## Среда

- Qdrant: `docker start qdrant` (контейнер НЕ поднимается сам после старта Docker Desktop).
  Проверка: `curl http://localhost:6533/collections`.
- Тесты офлайн, без Qdrant и ключа: `.venv/Scripts/python.exe -m unittest discover -s tests`
- ⚠ Windows + кириллица: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8` для скриптов.
- ⚠ Сеть рваная (DPI): длинные коннекты к DeepSeek/GitHub рвутся — ретраи.
- ⚠ После `/graphify .` проверять `graphify-out/.graphify_root` (должен быть `D:/navigator-719`).
